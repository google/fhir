//    Copyright 2018 Google Inc.
//
//    Licensed under the Apache License, Version 2.0 (the "License");
//    you may not use this file except in compliance with the License.
//    You may obtain a copy of the License at
//
//        https://www.apache.org/licenses/LICENSE-2.0
//
//    Unless required by applicable law or agreed to in writing, software
//    distributed under the License is distributed on an "AS IS" BASIS,
//    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//    See the License for the specific language governing permissions and
//    limitations under the License.

// The FHIR specification defines a set of data types that are used for the
// resource elements. There are two categories of data types: simple/primitive
// types, which are single elements, and complex types, which are re-usable
// groups of elements. This file defines proto messages both of these data
// types. They are combined in a single file because of circular dependencies
// involving extensions: primititve types can have extensions, extentions
// can contain complex types, and complex types are built from primitive types.
//
// We've chosen to use protocol messages for all data types, even though
// some can be reasonably represented as primitive proto types. This was done
// for three main reasons:
// - consistency with the primitive types that do benefit from a more
//   sophisticated representation
// - better type safety in code
// - to make sure that each fhir resource element has an existence indicator,
//   a has_X type method, associated with it.
//
// The primitive types in this file were hand-constructed, but all all complex
// types in this file were autogenerated from their corresponding
// StructureDefinitions using j/c/g/research/health/fhir/stu3:ProtoGenerator.

syntax = "proto3";

package google.fhir.stu3.proto;

import "google/fhir/src/stu3/proto/annotations.proto";

option java_multiple_files = true;
option java_package = "com.google.fhir.stu3.proto";

// Primitive Type base64Binary
// See http://hl7.org/fhir/datatypes.html#base64Binary
message Base64Binary {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for base64Binary
  bytes value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type boolean
// See http://hl7.org/fhir/datatypes.html#boolean
message Boolean {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for boolean
  bool value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type code
// See http://hl7.org/fhir/datatypes.html#code
message Code {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for code
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type date
// See http://hl7.org/fhir/datatypes.html#date
message Date {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  enum Precision {
    PRECISION_UNSPECIFIED = 0;
    YEAR = 1;
    MONTH = 2;
    DAY = 3;
  };

  // Primitive value for date
  int64 value_us = 1;
  Precision precision = 2;

  // xml:id (or equivalent in JSON)
  String id = 3;

  // Additional Content defined by implementations
  repeated Extension extension = 4;
}

// Primitive Type dateTime
// See http://hl7.org/fhir/datatypes.html#dateTime
message DateTime {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  enum Precision {
    PRECISION_UNSPECIFIED = 0;
    YEAR = 1;
    MONTH = 2;
    DAY = 3;
    SECOND = 4;
    MILLISECOND = 5;
    MICROSECOND = 6;
  };

  // Primitive value for dateTime
  int64 value_us = 1;
  string timezone = 2;
  Precision precision = 3;

  // xml:id (or equivalent in JSON)
  String id = 4;

  // Additional Content defined by implementations
  repeated Extension extension = 5;
}

// Primitive Type decimal
// See http://hl7.org/fhir/datatypes.html#decimal
message Decimal {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for decimal, stored as a string to preserve precision.
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type id
// See http://hl7.org/fhir/datatypes.html#id
message Id {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for id
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type instant
// See http://hl7.org/fhir/datatypes.html#instant
message Instant {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  enum Precision {
    PRECISION_UNSPECIFIED = 0;
    SECOND = 1;
    MILLISECOND = 2;
    MICROSECOND = 3;
  };

  // Primitive value for instant
  int64 value_us = 1;
  string timezone = 2;
  Precision precision = 3;

  // xml:id (or equivalent in JSON)
  String id = 4;

  // Additional Content defined by implementations
  repeated Extension extension = 5;
}

// Primitive Type integer
// See http://hl7.org/fhir/datatypes.html#integer
message Integer {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for integer
  sint32 value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type markdown
// See http://hl7.org/fhir/datatypes.html#markdown
message Markdown {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for markdown
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type oid
// See http://hl7.org/fhir/datatypes.html#oid
message Oid {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for oid
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type positiveInt
// See http://hl7.org/fhir/datatypes.html#positiveInt
message PositiveInt {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for positiveInt
  uint32 value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type string
// See http://hl7.org/fhir/datatypes.html#string
message String {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for string
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type time
// See http://hl7.org/fhir/datatypes.html#time
message Time {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  enum Precision {
    PRECISION_UNSPECIFIED = 0;
    SECOND = 1;
    MILLISECOND = 2;
    MICROSECOND = 3;
  };

  // Primitive value for time
  int64 value_us = 1;
  Precision precision = 2;

  // xml:id (or equivalent in JSON)
  String id = 3;

  // Additional Content defined by implementations
  repeated Extension extension = 4;
}

// Primitive Type unsignedInt
// See http://hl7.org/fhir/datatypes.html#unsignedInt
message UnsignedInt {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for unsignedInt
  uint32 value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Primitive Type uri
// See http://hl7.org/fhir/datatypes.html#uri
message Uri {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for uri
  string value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// An address expressed using postal conventions (as opposed to GPS or other
// location definition formats) See
// https://www.hl7.org/fhir/datatypes.html#Address
message Address {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // home | work | temp | old - purpose of this address
  AddressUseCode use = 3;

  // postal | physical | both
  AddressTypeCode type = 4;

  // Text representation of the address
  String text = 5;

  // Street name, number, direction & P.O. Box etc.
  repeated String line = 6;

  // Name of city, town etc.
  String city = 7;

  // District name (aka county)
  String district = 8;

  // Sub-unit of country (abbreviations ok)
  String state = 9;

  // Postal code for area
  String postal_code = 10;

  // Country (e.g. can be ISO 3166 2 or 3 letter code)
  String country = 11;

  // Time period when address was/is in use
  Period period = 12;
}

// A duration of time during which an organism (or a process) has existed
// See https://www.hl7.org/fhir/datatypes.html#Age
message Age {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerical value (with implicit precision)
  Decimal value = 3;

  // < | <= | >= | > - how to understand the value
  QuantityComparatorCode comparator = 4;

  // Unit representation
  String unit = 5;

  // System that defines coded unit form
  Uri system = 6;

  // Coded form of the unit
  Code code = 7;
}

// Text node with attribution
// See https://www.hl7.org/fhir/datatypes.html#Annotation
message Annotation {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Individual responsible for the annotation
  oneof author {
    Reference author_reference = 3;
    String author_string = 4;
  }

  // When the annotation was made
  DateTime time = 5;

  // The annotation  - text content
  String text = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Content in a format defined elsewhere
// See https://www.hl7.org/fhir/datatypes.html#Attachment
message Attachment {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Mime type of the content, with charset etc.
  Code content_type = 3 [(fhir_code_type) = CODE_MIME_TYPE];

  // Human language of the content (BCP-47)
  Code language = 4;

  // Data inline, base64ed
  Base64Binary data = 5;

  // Uri where the data can be found
  Uri url = 6;

  // Number of bytes of content (if url provided)
  UnsignedInt size = 7;

  // Hash of the data (sha-1, base64ed)
  Base64Binary hash = 8;

  // Label to display in place of the data
  String title = 9;

  // Date attachment was first created
  DateTime creation = 10;
}

// Base for elements defined inside a resource
// See https://www.hl7.org/fhir/datatypes.html#BackboneElement
message BackboneElement {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 3;
}

// Concept - reference to a terminology or just  text
// See https://www.hl7.org/fhir/datatypes.html#CodeableConcept
message CodeableConcept {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Code defined by a terminology system
  repeated Coding coding = 3;

  // Plain text representation of the concept
  String text = 4;
}

// A reference to a code defined by a terminology system
// See https://www.hl7.org/fhir/datatypes.html#Coding
message Coding {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Identity of the terminology system
  Uri system = 3;

  // Version of the system - if relevant
  String version = 4;

  // Symbol in syntax defined by the system
  Code code = 5;

  // Representation defined by the system
  String display = 6;

  // If this coding was chosen directly by the user
  Boolean user_selected = 7;
}

// Contact information
// See https://www.hl7.org/fhir/datatypes.html#ContactDetail
message ContactDetail {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Name of an individual to contact
  String name = 3;

  // Contact details for individual or organization
  repeated ContactPoint telecom = 4;
}

// Details of a Technology mediated contact point (phone, fax, email, etc.)
// See https://www.hl7.org/fhir/datatypes.html#ContactPoint
message ContactPoint {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // phone | fax | email | pager | url | sms | other
  ContactPointSystemCode system = 3;

  // The actual contact point details
  String value = 4;

  // home | work | temp | old | mobile - purpose of this contact point
  ContactPointUseCode use = 5;

  // Specify preferred order of use (1 = highest)
  PositiveInt rank = 6;

  // Time period when the contact point was/is in use
  Period period = 7;
}

// Contributor information
// See https://www.hl7.org/fhir/datatypes.html#Contributor
message Contributor {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // author | editor | reviewer | endorser
  ContributorTypeCode type = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who contributed the content
  String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Contact details of the contributor
  repeated ContactDetail contact = 5;
}

// A measured or measurable amount
// See https://www.hl7.org/fhir/datatypes.html#Count
message Count {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerical value (with implicit precision)
  Decimal value = 3;

  // < | <= | >= | > - how to understand the value
  QuantityComparatorCode comparator = 4;

  // Unit representation
  String unit = 5;

  // System that defines coded unit form
  Uri system = 6;

  // Coded form of the unit
  Code code = 7;
}

// Describes a required data item
// See https://www.hl7.org/fhir/datatypes.html#DataRequirement
message DataRequirement {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // The type of the required data
  FHIRAllTypesCode type = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The profile of the required data
  repeated Uri profile = 4;

  // Indicates that specific structure elements are referenced by the knowledge
  // module
  repeated String must_support = 5;

  // What codes are expected
  message CodeFilter {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // The code-valued attribute of the filter
    String path = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Valueset for the filter
    oneof value_set {
      String value_set_string = 4;
      Reference value_set_reference = 5;
    }

    // What code is expected
    repeated Code value_code = 6;

    // What Coding is expected
    repeated Coding value_coding = 7;

    // What CodeableConcept is expected
    repeated CodeableConcept value_codeable_concept = 8;
  }
  repeated CodeFilter code_filter = 6;

  // What dates/date ranges are expected
  message DateFilter {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // The date-valued attribute of the filter
    String path = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The value of the filter, as a Period, DateTime, or Duration value
    oneof value {
      DateTime value_date_time = 4;
      Period value_period = 5;
      Duration value_duration = 6;
    }
  }
  repeated DateFilter date_filter = 7;
}

// A length - a value with a unit that is a physical distance
// See https://www.hl7.org/fhir/datatypes.html#Distance
message Distance {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerical value (with implicit precision)
  Decimal value = 3;

  // < | <= | >= | > - how to understand the value
  QuantityComparatorCode comparator = 4;

  // Unit representation
  String unit = 5;

  // System that defines coded unit form
  Uri system = 6;

  // Coded form of the unit
  Code code = 7;
}

// How the medication is/was taken or should be taken
// See https://www.hl7.org/fhir/datatypes.html#Dosage
message Dosage {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // The order of the dosage instructions
  Integer sequence = 3;

  // Free text dosage instructions e.g. SIG
  String text = 4;

  // Supplemental instruction - e.g. "with meals"
  repeated CodeableConcept additional_instruction = 5;

  // Patient or consumer oriented instructions
  String patient_instruction = 6;

  // When medication should be administered
  Timing timing = 7;

  // Take "as needed" (for x)
  oneof as_needed {
    Boolean as_needed_boolean = 8;
    CodeableConcept as_needed_codeable_concept = 9;
  }

  // Body site to administer to
  CodeableConcept site = 10;

  // How drug should enter body
  CodeableConcept route = 11;

  // Technique for administering medication
  CodeableConcept method = 12;

  // Amount of medication per dose
  oneof dose {
    Range dose_range = 13;
    Quantity dose_quantity = 14;
  }

  // Upper limit on medication per unit of time
  Ratio max_dose_per_period = 15;

  // Upper limit on medication per administration
  Quantity max_dose_per_administration = 16;

  // Upper limit on medication per lifetime of the patient
  Quantity max_dose_per_lifetime = 17;

  // Amount of medication per unit of time
  oneof rate {
    Ratio rate_ratio = 18;
    Range rate_range = 19;
    Quantity rate_quantity = 20;
  }
}

// A length of time
// See https://www.hl7.org/fhir/datatypes.html#Duration
message Duration {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerical value (with implicit precision)
  Decimal value = 3;

  // < | <= | >= | > - how to understand the value
  QuantityComparatorCode comparator = 4;

  // Unit representation
  String unit = 5;

  // System that defines coded unit form
  Uri system = 6;

  // Coded form of the unit
  Code code = 7;
}

// Definition of an element in a resource or extension
// See https://www.hl7.org/fhir/datatypes.html#ElementDefinition
message ElementDefinition {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Path of the element in the hierarchy of elements
  String path = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // xmlAttr | xmlText | typeAttr | cdaText | xhtml
  repeated PropertyRepresentationCode representation = 4;

  // Name for this particular element (in a set of slices)
  String slice_name = 5;

  // Name for element to display with or prompt for element
  String label = 6;

  // Corresponding codes in terminologies
  repeated Coding code = 7;

  // This element is sliced - slices follow
  message Slicing {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Element values that are used to distinguish the slices
    message Discriminator {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // value | exists | pattern | type | profile
      DiscriminatorTypeCode type = 3
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Path to element value
      String path = 4 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Discriminator discriminator = 3;

    // Text description of how slicing works (or not)
    String description = 4;

    // If elements must be in same order as slices
    Boolean ordered = 5;

    // closed | open | openAtEnd
    SlicingRulesCode rules = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Slicing slicing = 8;

  // Concise definition for space-constrained presentation
  String short = 9;

  // Full formal definition as narrative text
  Markdown definition = 10;

  // Comments about the use of this element
  Markdown comment = 11;

  // Why this resource has been created
  Markdown requirements = 12;

  // Other names
  repeated String alias = 13;

  // Minimum Cardinality
  UnsignedInt min = 14;

  // Maximum Cardinality (a number or *)
  String max = 15;

  // Base definition information for tools
  message Base {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Path that identifies the base element
    String path = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Min cardinality of the base element
    UnsignedInt min = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Max cardinality of the base element
    String max = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Base base = 16;

  // Reference to definition of content for the element
  Uri content_reference = 17;

  // Data type and Profile for this element
  message TypeRef {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Data type or Resource (reference to definition)
    Uri code = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Profile (StructureDefinition) to apply (or IG)
    Uri profile = 4;

    // Profile (StructureDefinition) to apply to reference target (or IG)
    Uri target_profile = 5;

    // contained | referenced | bundled - how aggregated
    repeated AggregationModeCode aggregation = 6;

    // either | independent | specific
    ReferenceVersionRulesCode versioning = 7;
  }
  repeated TypeRef type = 18;

  // Specified value if missing from instance
  oneof default_value {
    Base64Binary default_value_base64_binary = 19;
    Boolean default_value_boolean = 20;
    Code default_value_code = 21;
    Date default_value_date = 22;
    DateTime default_value_date_time = 23;
    Decimal default_value_decimal = 24;
    Id default_value_id = 25;
    Instant default_value_instant = 26;
    Integer default_value_integer = 27;
    Markdown default_value_markdown = 28;
    Oid default_value_oid = 29;
    PositiveInt default_value_positive_int = 30;
    String default_value_string = 31;
    Time default_value_time = 32;
    UnsignedInt default_value_unsigned_int = 33;
    Uri default_value_uri = 34;
    Address default_value_address = 35;
    Age default_value_age = 36;
    Annotation default_value_annotation = 37;
    Attachment default_value_attachment = 38;
    CodeableConcept default_value_codeable_concept = 39;
    Coding default_value_coding = 40;
    ContactPoint default_value_contact_point = 41;
    Count default_value_count = 42;
    Distance default_value_distance = 43;
    Duration default_value_duration = 44;
    HumanName default_value_human_name = 45;
    Identifier default_value_identifier = 46;
    Money default_value_money = 47;
    Period default_value_period = 48;
    Quantity default_value_quantity = 49;
    Range default_value_range = 50;
    Ratio default_value_ratio = 51;
    Reference default_value_reference = 52;
    SampledData default_value_sampled_data = 53;
    Signature default_value_signature = 54;
    Timing default_value_timing = 55;
    Meta default_value_meta = 56;
  }

  // Implicit meaning when this element is missing
  Markdown meaning_when_missing = 57;

  // What the order of the elements means
  String order_meaning = 58;

  // Value must be exactly this
  oneof fixed {
    Base64Binary fixed_base64_binary = 59;
    Boolean fixed_boolean = 60;
    Code fixed_code = 61;
    Date fixed_date = 62;
    DateTime fixed_date_time = 63;
    Decimal fixed_decimal = 64;
    Id fixed_id = 65;
    Instant fixed_instant = 66;
    Integer fixed_integer = 67;
    Markdown fixed_markdown = 68;
    Oid fixed_oid = 69;
    PositiveInt fixed_positive_int = 70;
    String fixed_string = 71;
    Time fixed_time = 72;
    UnsignedInt fixed_unsigned_int = 73;
    Uri fixed_uri = 74;
    Address fixed_address = 75;
    Age fixed_age = 76;
    Annotation fixed_annotation = 77;
    Attachment fixed_attachment = 78;
    CodeableConcept fixed_codeable_concept = 79;
    Coding fixed_coding = 80;
    ContactPoint fixed_contact_point = 81;
    Count fixed_count = 82;
    Distance fixed_distance = 83;
    Duration fixed_duration = 84;
    HumanName fixed_human_name = 85;
    Identifier fixed_identifier = 86;
    Money fixed_money = 87;
    Period fixed_period = 88;
    Quantity fixed_quantity = 89;
    Range fixed_range = 90;
    Ratio fixed_ratio = 91;
    Reference fixed_reference = 92;
    SampledData fixed_sampled_data = 93;
    Signature fixed_signature = 94;
    Timing fixed_timing = 95;
    Meta fixed_meta = 96;
  }

  // Value must have at least these property values
  oneof pattern {
    Base64Binary pattern_base64_binary = 97;
    Boolean pattern_boolean = 98;
    Code pattern_code = 99;
    Date pattern_date = 100;
    DateTime pattern_date_time = 101;
    Decimal pattern_decimal = 102;
    Id pattern_id = 103;
    Instant pattern_instant = 104;
    Integer pattern_integer = 105;
    Markdown pattern_markdown = 106;
    Oid pattern_oid = 107;
    PositiveInt pattern_positive_int = 108;
    String pattern_string = 109;
    Time pattern_time = 110;
    UnsignedInt pattern_unsigned_int = 111;
    Uri pattern_uri = 112;
    Address pattern_address = 113;
    Age pattern_age = 114;
    Annotation pattern_annotation = 115;
    Attachment pattern_attachment = 116;
    CodeableConcept pattern_codeable_concept = 117;
    Coding pattern_coding = 118;
    ContactPoint pattern_contact_point = 119;
    Count pattern_count = 120;
    Distance pattern_distance = 121;
    Duration pattern_duration = 122;
    HumanName pattern_human_name = 123;
    Identifier pattern_identifier = 124;
    Money pattern_money = 125;
    Period pattern_period = 126;
    Quantity pattern_quantity = 127;
    Range pattern_range = 128;
    Ratio pattern_ratio = 129;
    Reference pattern_reference = 130;
    SampledData pattern_sampled_data = 131;
    Signature pattern_signature = 132;
    Timing pattern_timing = 133;
    Meta pattern_meta = 134;
  }

  // Example value (as defined for type)
  message Example {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Describes the purpose of this example
    String label = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Value of Example (one of allowed types)
    oneof value {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      Base64Binary value_base64_binary = 4;
      Boolean value_boolean = 5;
      Code value_code = 6;
      Date value_date = 7;
      DateTime value_date_time = 8;
      Decimal value_decimal = 9;
      Id value_id = 10;
      Instant value_instant = 11;
      Integer value_integer = 12;
      Markdown value_markdown = 13;
      Oid value_oid = 14;
      PositiveInt value_positive_int = 15;
      String value_string = 16;
      Time value_time = 17;
      UnsignedInt value_unsigned_int = 18;
      Uri value_uri = 19;
      Address value_address = 20;
      Age value_age = 21;
      Annotation value_annotation = 22;
      Attachment value_attachment = 23;
      CodeableConcept value_codeable_concept = 24;
      Coding value_coding = 25;
      ContactPoint value_contact_point = 26;
      Count value_count = 27;
      Distance value_distance = 28;
      Duration value_duration = 29;
      HumanName value_human_name = 30;
      Identifier value_identifier = 31;
      Money value_money = 32;
      Period value_period = 33;
      Quantity value_quantity = 34;
      Range value_range = 35;
      Ratio value_ratio = 36;
      Reference value_reference = 37;
      SampledData value_sampled_data = 38;
      Signature value_signature = 39;
      Timing value_timing = 40;
      Meta value_meta = 41;
    }
  }
  repeated Example example = 135;

  // Minimum Allowed Value (for some types)
  oneof min_value {
    Date min_value_date = 136;
    DateTime min_value_date_time = 137;
    Instant min_value_instant = 138;
    Time min_value_time = 139;
    Decimal min_value_decimal = 140;
    Integer min_value_integer = 141;
    PositiveInt min_value_positive_int = 142;
    UnsignedInt min_value_unsigned_int = 143;
    Quantity min_value_quantity = 144;
  }

  // Maximum Allowed Value (for some types)
  oneof max_value {
    Date max_value_date = 145;
    DateTime max_value_date_time = 146;
    Instant max_value_instant = 147;
    Time max_value_time = 148;
    Decimal max_value_decimal = 149;
    Integer max_value_integer = 150;
    PositiveInt max_value_positive_int = 151;
    UnsignedInt max_value_unsigned_int = 152;
    Quantity max_value_quantity = 153;
  }

  // Max length for strings
  Integer max_length = 154;

  // Reference to invariant about presence
  repeated Id condition = 155;

  // Condition that must evaluate to true
  message Constraint {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Target of 'condition' reference above
    Id key = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Why this constraint is necessary or appropriate
    String requirements = 4;

    // error | warning
    ConstraintSeverityCode severity = 5
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Human description of constraint
    String human = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // FHIRPath expression of constraint
    String expression = 7 [(validation_requirement) = REQUIRED_BY_FHIR];

    // XPath expression of constraint
    String xpath = 8;

    // Reference to original source of constraint
    Uri source = 9;
  }
  repeated Constraint constraint = 156;

  // If the element must supported
  Boolean must_support = 157;

  // If this modifies the meaning of other elements
  Boolean is_modifier = 158;

  // Include when _summary = true?
  Boolean is_summary = 159;

  // ValueSet details if this is coded
  message ElementDefinitionBinding {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // required | extensible | preferred | example
    BindingStrengthCode strength = 3
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Human explanation of the value set
    String description = 4;

    // Source of value set
    oneof value_set {
      Uri value_set_uri = 5;
      Reference value_set_reference = 6;
    }
  }
  ElementDefinitionBinding binding = 160;

  // Map element to another set of definitions
  message Mapping {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Reference to mapping declaration
    Id identity = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Computable language of mapping
    Code language = 4 [(fhir_code_type) = CODE_MIME_TYPE];

    // Details of the mapping
    String map = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Comments about the mapping or its use
    String comment = 6;
  }
  repeated Mapping mapping = 161;
}

// Base for all elements
// See https://www.hl7.org/fhir/datatypes.html#Element
message Element {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;
}

// Optional Extensions Element
// See https://www.hl7.org/fhir/datatypes.html#Extension
message Extension {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // identifies the meaning of the extension
  Uri url = 2 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Value of extension
  oneof value {
    Base64Binary value_base64_binary = 3;
    Boolean value_boolean = 4;
    Code value_code = 5;
    Date value_date = 6;
    DateTime value_date_time = 7;
    Decimal value_decimal = 8;
    Id value_id = 9;
    Instant value_instant = 10;
    Integer value_integer = 11;
    Markdown value_markdown = 12;
    Oid value_oid = 13;
    PositiveInt value_positive_int = 14;
    String value_string = 15;
    Time value_time = 16;
    UnsignedInt value_unsigned_int = 17;
    Uri value_uri = 18;
    Address value_address = 19;
    Age value_age = 20;
    Annotation value_annotation = 21;
    Attachment value_attachment = 22;
    CodeableConcept value_codeable_concept = 23;
    Coding value_coding = 24;
    ContactPoint value_contact_point = 25;
    Count value_count = 26;
    Distance value_distance = 27;
    Duration value_duration = 28;
    HumanName value_human_name = 29;
    Identifier value_identifier = 30;
    Money value_money = 31;
    Period value_period = 32;
    Quantity value_quantity = 33;
    Range value_range = 34;
    Ratio value_ratio = 35;
    Reference value_reference = 36;
    SampledData value_sampled_data = 37;
    Signature value_signature = 38;
    Timing value_timing = 39;
    Meta value_meta = 40;
  }

  // Additional Content defined by implementations
  repeated Extension extension = 41;
}

// Name of a human - parts and usage
// See https://www.hl7.org/fhir/datatypes.html#HumanName
message HumanName {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // usual | official | temp | nickname | anonymous | old | maiden
  NameUseCode use = 3;

  // Text representation of the full name
  String text = 4;

  // Family name (often called 'Surname')
  String family = 5;

  // Given names (not always 'first'). Includes middle names
  repeated String given = 6;

  // Parts that come before the name
  repeated String prefix = 7;

  // Parts that come after the name
  repeated String suffix = 8;

  // Time period when name was/is in use
  Period period = 9;
}

// An identifier intended for computation
// See https://www.hl7.org/fhir/datatypes.html#Identifier
message Identifier {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // usual | official | temp | secondary (If known)
  IdentifierUseCode use = 3;

  // Description of identifier
  CodeableConcept type = 4;

  // The namespace for the identifier value
  Uri system = 5;

  // The value that is unique
  String value = 6;

  // Time period when id is/was valid for use
  Period period = 7;

  // Organization that issued id (may be just text)
  Reference assigner = 8;
}

// Metadata about a resource
// See https://www.hl7.org/fhir/datatypes.html#Meta
message Meta {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Version specific identifier
  Id version_id = 3;

  // When the resource version last changed
  Instant last_updated = 4;

  // Profiles this resource claims to conform to
  repeated Uri profile = 5;

  // Security Labels applied to this resource
  repeated Coding security = 6;

  // Tags applied to this resource
  repeated Coding tag = 7;
}

// An amount of economic utility in some recognized currency
// See https://www.hl7.org/fhir/datatypes.html#Money
message Money {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerical value (with implicit precision)
  Decimal value = 3;

  // < | <= | >= | > - how to understand the value
  QuantityComparatorCode comparator = 4;

  // Unit representation
  String unit = 5;

  // System that defines coded unit form
  Uri system = 6;

  // Coded form of the unit
  Code code = 7;
}

// A human-readable formatted text, including images
// See https://www.hl7.org/fhir/datatypes.html#Narrative
message Narrative {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // generated | extensions | additional | empty
  NarrativeStatusCode status = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Limited xhtml content
  String div = 4 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Definition of a parameter to a module
// See https://www.hl7.org/fhir/datatypes.html#ParameterDefinition
message ParameterDefinition {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Name used to access the parameter value
  Code name = 3;

  // in | out
  OperationParameterUseCode use = 4
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Minimum cardinality
  Integer min = 5;

  // Maximum cardinality (a number of *)
  String max = 6;

  // A brief description of the parameter
  String documentation = 7;

  // What type of value
  FHIRAllTypesCode type = 8 [(validation_requirement) = REQUIRED_BY_FHIR];

  // What profile the value is expected to be
  Reference profile = 9;
}

// Time range defined by start and end date/time
// See https://www.hl7.org/fhir/datatypes.html#Period
message Period {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Starting time with inclusive boundary
  DateTime start = 3;

  // End time with inclusive boundary, if not ongoing
  DateTime end = 4;
}

// A measured or measurable amount
// See https://www.hl7.org/fhir/datatypes.html#Quantity
message Quantity {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerical value (with implicit precision)
  Decimal value = 3;

  // < | <= | >= | > - how to understand the value
  QuantityComparatorCode comparator = 4;

  // Unit representation
  String unit = 5;

  // System that defines coded unit form
  Uri system = 6;

  // Coded form of the unit
  Code code = 7;
}

// Set of values bounded by low and high
// See https://www.hl7.org/fhir/datatypes.html#Range
message Range {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Low limit
  Quantity low = 3;

  // High limit
  Quantity high = 4;
}

// A ratio of two Quantity values - a numerator and a denominator
// See https://www.hl7.org/fhir/datatypes.html#Ratio
message Ratio {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Numerator value
  Quantity numerator = 3;

  // Denominator value
  Quantity denominator = 4;
}

message ReferenceId {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  // Primitive value for id
  string value = 1;

  // History version, if present.
  Id history = 2;

  // xml:id (or equivalent in JSON)
  String id = 3;

  // Additional Content defined by implementations
  repeated Extension extension = 4;
}

// A reference from one resource to another
// See https://www.hl7.org/fhir/datatypes.html#Reference
message Reference {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Literal reference, Relative, internal or absolute URL
  oneof reference {
    // Absolute URIs are untyped.
    String uri = 3 [json_name = "reference"];

    // Fragments (internal references) are untyped, and represented here
    // without the leading '#'.
    String fragment = 4;

    // Typed relative urls are represented here.
    ReferenceId account_id = 5;
    ReferenceId activity_definition_id = 6;
    ReferenceId adverse_event_id = 7;
    ReferenceId allergy_intolerance_id = 8;
    ReferenceId appointment_id = 9;
    ReferenceId appointment_response_id = 10;
    ReferenceId audit_event_id = 11;
    ReferenceId basic_id = 12;
    ReferenceId binary_id = 13;
    ReferenceId body_site_id = 14;
    ReferenceId bundle_id = 15;
    ReferenceId capability_statement_id = 16;
    ReferenceId care_plan_id = 17;
    ReferenceId care_team_id = 18;
    ReferenceId charge_item_id = 19;
    ReferenceId claim_id = 20;
    ReferenceId claim_response_id = 21;
    ReferenceId clinical_impression_id = 22;
    ReferenceId code_system_id = 23;
    ReferenceId communication_id = 24;
    ReferenceId communication_request_id = 25;
    ReferenceId compartment_definition_id = 26;
    ReferenceId composition_id = 27;
    ReferenceId concept_map_id = 28;
    ReferenceId condition_id = 29;
    ReferenceId consent_id = 30;
    ReferenceId contract_id = 31;
    ReferenceId coverage_id = 32;
    ReferenceId data_element_id = 33;
    ReferenceId detected_issue_id = 34;
    ReferenceId device_id = 35;
    ReferenceId device_component_id = 36;
    ReferenceId device_metric_id = 37;
    ReferenceId device_request_id = 38;
    ReferenceId device_usestatement_id = 39;
    ReferenceId diagnostic_report_id = 40;
    ReferenceId document_manifest_id = 41;
    ReferenceId document_reference_id = 42;
    ReferenceId eligibility_request_id = 43;
    ReferenceId eligibility_response_id = 44;
    ReferenceId encounter_id = 45;
    ReferenceId endpoint_id = 46;
    ReferenceId enrollment_request_id = 47;
    ReferenceId enrollment_response_id = 48;
    ReferenceId episode_ofcare_id = 49;
    ReferenceId expansion_profile_id = 50;
    ReferenceId explanation_ofbenefit_id = 51;
    ReferenceId family_memberhistory_id = 52;
    ReferenceId flag_id = 53;
    ReferenceId goal_id = 54;
    ReferenceId graph_definition_id = 55;
    ReferenceId group_id = 56;
    ReferenceId guidance_response_id = 57;
    ReferenceId healthcare_service_id = 58;
    ReferenceId imaging_manifest_id = 59;
    ReferenceId imaging_study_id = 60;
    ReferenceId immunization_id = 61;
    ReferenceId immunization_recommendation_id = 62;
    ReferenceId implementation_guide_id = 63;
    ReferenceId library_id = 64;
    ReferenceId linkage_id = 65;
    ReferenceId list_id = 66;
    ReferenceId location_id = 67;
    ReferenceId measure_id = 68;
    ReferenceId measure_report_id = 69;
    ReferenceId media_id = 70;
    ReferenceId medication_id = 71;
    ReferenceId medication_administration_id = 72;
    ReferenceId medication_dispense_id = 73;
    ReferenceId medication_request_id = 74;
    ReferenceId medication_statement_id = 75;
    ReferenceId message_definition_id = 76;
    ReferenceId message_header_id = 77;
    ReferenceId naming_system_id = 78;
    ReferenceId nutrition_order_id = 79;
    ReferenceId observation_id = 80;
    ReferenceId operation_definition_id = 81;
    ReferenceId operation_outcome_id = 82;
    ReferenceId organization_id = 83;
    ReferenceId parameters_id = 84;
    ReferenceId patient_id = 85;
    ReferenceId payment_notice_id = 86;
    ReferenceId payment_reconciliation_id = 87;
    ReferenceId person_id = 88;
    ReferenceId plan_definition_id = 89;
    ReferenceId practitioner_id = 90;
    ReferenceId practitioner_role_id = 91;
    ReferenceId procedure_id = 92;
    ReferenceId procedure_request_id = 93;
    ReferenceId process_request_id = 94;
    ReferenceId process_response_id = 95;
    ReferenceId provenance_id = 96;
    ReferenceId questionnaire_id = 97;
    ReferenceId questionnaire_response_id = 98;
    ReferenceId referral_request_id = 99;
    ReferenceId related_person_id = 100;
    ReferenceId request_group_id = 101;
    ReferenceId research_study_id = 102;
    ReferenceId research_subject_id = 103;
    ReferenceId risk_assessment_id = 104;
    ReferenceId schedule_id = 105;
    ReferenceId search_parameter_id = 106;
    ReferenceId sequence_id = 107;
    ReferenceId service_definition_id = 108;
    ReferenceId slot_id = 109;
    ReferenceId specimen_id = 110;
    ReferenceId structure_definition_id = 111;
    ReferenceId structure_map_id = 112;
    ReferenceId subscription_id = 113;
    ReferenceId substance_id = 114;
    ReferenceId supply_delivery_id = 115;
    ReferenceId supply_request_id = 116;
    ReferenceId task_id = 117;
    ReferenceId test_report_id = 118;
    ReferenceId test_script_id = 119;
    ReferenceId value_set_id = 120;
    ReferenceId vision_prescription_id = 121;
  }

  // Logical reference, when literal reference is not known
  Identifier identifier = 122;

  // Text alternative for the resource
  String display = 123;
}

// Related artifacts for a knowledge resource
// See https://www.hl7.org/fhir/datatypes.html#RelatedArtifact
message RelatedArtifact {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // documentation | justification | citation | predecessor | successor |
  // derived-from | depends-on | composed-of
  RelatedArtifactTypeCode type = 3
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Brief description of the related artifact
  String display = 4;

  // Bibliographic citation for the artifact
  String citation = 5;

  // Where the artifact can be accessed
  Uri url = 6;

  // What document is being referenced
  Attachment document = 7;

  // What resource is being referenced
  Reference resource = 8;
}

// A series of measurements taken by a device
// See https://www.hl7.org/fhir/datatypes.html#SampledData
message SampledData {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Zero value and units
  Quantity origin = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Number of milliseconds between samples
  Decimal period = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Multiply data by this before adding to origin
  Decimal factor = 5;

  // Lower limit of detection
  Decimal lower_limit = 6;

  // Upper limit of detection
  Decimal upper_limit = 7;

  // Number of sample points at each time point
  PositiveInt dimensions = 8 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Decimal values with spaces, or "E" | "U" | "L"
  String data = 9 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// A digital Signature - XML DigSig, JWT, Graphical image of signature, etc.
// See https://www.hl7.org/fhir/datatypes.html#Signature
message Signature {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Indication of the reason the entity signed the object(s)
  repeated Coding type = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // When the signature was created
  Instant when = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who signed
  oneof who {
    option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
    Uri who_uri = 5;
    Reference who_reference = 6;
  }

  // The party represented
  oneof on_behalf_of {
    Uri on_behalf_of_uri = 7;
    Reference on_behalf_of_reference = 8;
  }

  // The technical format of the signature
  Code content_type = 9 [(fhir_code_type) = CODE_MIME_TYPE];

  // The actual signature content (XML DigSig. JWT, picture, etc.)
  Base64Binary blob = 10;
}

// A timing schedule that specifies an event that may occur multiple times
// See https://www.hl7.org/fhir/datatypes.html#Timing
message Timing {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // When the event occurs
  repeated DateTime event = 3;

  // When the event is to occur
  message Repeat {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Length/Range of lengths, or (Start and/or end) limits
    oneof bounds {
      Duration bounds_duration = 3;
      Range bounds_range = 4;
      Period bounds_period = 5;
    }

    // Number of times to repeat
    Integer count = 6;

    // Maximum number of times to repeat
    Integer count_max = 7;

    // How long when it happens
    Decimal duration = 8;

    // How long when it happens (Max)
    Decimal duration_max = 9;

    // s | min | h | d | wk | mo | a - unit of time (UCUM)
    UnitsOfTimeCode duration_unit = 10;

    // Event occurs frequency times per period
    Integer frequency = 11;

    // Event occurs up to frequencyMax times per period
    Integer frequency_max = 12;

    // Event occurs frequency times per period
    Decimal period = 13;

    // Upper limit of period (3-4 hours)
    Decimal period_max = 14;

    // s | min | h | d | wk | mo | a - unit of time (UCUM)
    UnitsOfTimeCode period_unit = 15;

    // mon | tue | wed | thu | fri | sat | sun
    repeated DaysOfWeekCode day_of_week = 16;

    // Time of day for action
    repeated Time time_of_day = 17;

    // Regular life events the event is tied to
    repeated EventTimingCode when = 18;

    // Minutes from event (before or after)
    UnsignedInt offset = 19;
  }
  Repeat repeat = 4;

  // BID | TID | QID | AM | PM | QD | QOD | Q4H | Q6H +
  CodeableConcept code = 5;
}

// Defines an expected trigger for a module
// See https://www.hl7.org/fhir/datatypes.html#TriggerDefinition
message TriggerDefinition {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // named-event | periodic | data-added | data-modified | data-removed |
  // data-accessed | data-access-ended
  TriggerTypeCode type = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Triggering event name
  String event_name = 4;

  // Timing of the event
  oneof event_timing {
    Timing event_timing_timing = 5;
    Reference event_timing_reference = 6;
    Date event_timing_date = 7;
    DateTime event_timing_date_time = 8;
  }

  // Triggering data of the event
  DataRequirement event_data = 9;
}

// Describes the context of use for a conformance or knowledge resource
// See https://www.hl7.org/fhir/datatypes.html#UsageContext
message UsageContext {
  option (structure_definition_kind) = KIND_COMPLEX_TYPE;
  // xml:id (or equivalent in JSON)
  String id = 1;

  // Additional Content defined by implementations
  repeated Extension extension = 2;

  // Type of context being specified
  Coding code = 3 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Value that defines the context
  oneof value {
    option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
    CodeableConcept value_codeable_concept = 4;
    Quantity value_quantity = 5;
    Range value_range = 6;
  }
}

// Base Resource
// See https://www.hl7.org/fhir/resource.html
message Resource {
  option (structure_definition_kind) = KIND_RESOURCE;
  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  Code language = 4;
};

// Code specializations with required valueset bindings.

// A type defined by FHIR that is an abstract type
// See http://hl7.org/fhir/ValueSet/abstract-types
message AbstractTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/abstract-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TYPE = 1 [(fhir_original_code) = "Type"];
    ANY = 2 [(fhir_original_code) = "Any"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether the account is available to be used.
// See http://hl7.org/fhir/ValueSet/account-status
message AccountStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/account-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines behavior for an action or a group for how many times that item may be
// repeated See http://hl7.org/fhir/ValueSet/action-cardinality-behavior
message ActionCardinalityBehaviorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-cardinality-behavior";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SINGLE = 1;
    MULTIPLE = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines the kinds of conditions that can appear on actions
// See http://hl7.org/fhir/ValueSet/action-condition-kind
message ActionConditionKindCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-condition-kind";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    APPLICABILITY = 1;
    START = 2;
    STOP = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines organization behavior of a group
// See http://hl7.org/fhir/ValueSet/action-grouping-behavior
message ActionGroupingBehaviorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-grouping-behavior";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    VISUAL_GROUP = 1;
    LOGICAL_GROUP = 2;
    SENTENCE_GROUP = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// List of allowable action which this resource can request.
// See http://hl7.org/fhir/ValueSet/actionlist
message ActionListCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/actionlist";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CANCEL = 1;
    POLL = 2;
    REPROCESS = 3;
    STATUS = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of participant for the action
// See http://hl7.org/fhir/ValueSet/action-participant-type
message ActionParticipantTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-participant-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PATIENT = 1;
    PRACTITIONER = 2;
    RELATED_PERSON = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines selection frequency behavior for an action or group
// See http://hl7.org/fhir/ValueSet/action-precheck-behavior
message ActionPrecheckBehaviorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-precheck-behavior";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    YES = 1;
    NO = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines the types of relationships between actions
// See http://hl7.org/fhir/ValueSet/action-relationship-type
message ActionRelationshipTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-relationship-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    BEFORE_START = 1;
    BEFORE = 2;
    BEFORE_END = 3;
    CONCURRENT_WITH_START = 4;
    CONCURRENT = 5;
    CONCURRENT_WITH_END = 6;
    AFTER_START = 7;
    AFTER = 8;
    AFTER_END = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines requiredness behavior for selecting an action or an action group
// See http://hl7.org/fhir/ValueSet/action-required-behavior
message ActionRequiredBehaviorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-required-behavior";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MUST = 1;
    COULD = 2;
    MUST_UNLESS_DOCUMENTED = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines selection behavior of a group
// See http://hl7.org/fhir/ValueSet/action-selection-behavior
message ActionSelectionBehaviorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/action-selection-behavior";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ANY = 1;
    ALL = 2;
    ALL_OR_NONE = 3;
    EXACTLY_ONE = 4;
    AT_MOST_ONE = 5;
    ONE_OR_MORE = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of action to be performed
// See http://hl7.org/fhir/ValueSet/action-type
message ActionTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/action-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CREATE = 1;
    UPDATE = 2;
    REMOVE = 3;
    FIRE_EVENT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// High-level categorization of the type of activity
// See http://hl7.org/fhir/ValueSet/activity-definition-category
message ActivityDefinitionCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/activity-definition-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TREATMENT = 1;
    EDUCATION = 2;
    ASSESSMENT = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of an address (physical / postal)
// See http://hl7.org/fhir/ValueSet/address-type
message AddressTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/address-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    POSTAL = 1;
    PHYSICAL = 2;
    BOTH = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The use of an address
// See http://hl7.org/fhir/ValueSet/address-use
message AddressUseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/address-use";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HOME = 1;
    WORK = 2;
    TEMP = 3;
    OLD = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The gender of a person used for administrative purposes.
// See http://hl7.org/fhir/ValueSet/administrative-gender
message AdministrativeGenderCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/administrative-gender";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MALE = 1;
    FEMALE = 2;
    OTHER = 3;
    UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Overall categorization of the event, e.g. real or potential
// See http://hl7.org/fhir/ValueSet/adverse-event-category
message AdverseEventCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AE = 1 [(fhir_original_code) = "AE"];
    PAE = 2 [(fhir_original_code) = "PAE"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// TODO
// See http://hl7.org/fhir/ValueSet/adverse-event-causality-assess
message AdverseEventCausalityAssessmentCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-causality-assess";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ASSESS1 = 1;
    ASSESS2 = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// TODO
// See http://hl7.org/fhir/ValueSet/adverse-event-causality
message AdverseEventCausalityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-causality";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CAUSALITY1 = 1;
    CAUSALITY2 = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// TODO
// See http://hl7.org/fhir/ValueSet/adverse-event-causality-method
message AdverseEventCausalityMethodCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-causality-method";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    METHOD1 = 1;
    METHOD2 = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// TODO
// See http://hl7.org/fhir/ValueSet/adverse-event-causality-result
message AdverseEventCausalityResultCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-causality-result";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    RESULT1 = 1;
    RESULT2 = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// TODO (and should this be required?)
// See http://hl7.org/fhir/ValueSet/adverse-event-outcome
message AdverseEventOutcomeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-outcome";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    RESOLVED = 1;
    RECOVERING = 2;
    ONGOING = 3;
    RESOLVEDWITHSEQUELAE = 4 [(fhir_original_code) = "resolvedWithSequelae"];
    FATAL = 5;
    UNKNOWN = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Overall seriousness of this event for the patient
// See http://hl7.org/fhir/ValueSet/adverse-event-seriousness
message AdverseEventSeriousnessCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/adverse-event-seriousness";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MILD = 1 [(fhir_original_code) = "Mild"];
    MODERATE = 2 [(fhir_original_code) = "Moderate"];
    SEVERE = 3 [(fhir_original_code) = "Severe"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Either an abstract type, a resource or a data type.
// See http://hl7.org/fhir/ValueSet/all-types
message FHIRAllTypesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/all-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ADDRESS = 1 [(fhir_original_code) = "Address"];
    AGE = 2 [(fhir_original_code) = "Age"];
    ANNOTATION = 3 [(fhir_original_code) = "Annotation"];
    ATTACHMENT = 4 [(fhir_original_code) = "Attachment"];
    BACKBONE_ELEMENT = 5 [(fhir_original_code) = "BackboneElement"];
    CODEABLE_CONCEPT = 6 [(fhir_original_code) = "CodeableConcept"];
    CODING = 7 [(fhir_original_code) = "Coding"];
    CONTACT_DETAIL = 8 [(fhir_original_code) = "ContactDetail"];
    CONTACT_POINT = 9 [(fhir_original_code) = "ContactPoint"];
    CONTRIBUTOR = 10 [(fhir_original_code) = "Contributor"];
    COUNT = 11 [(fhir_original_code) = "Count"];
    DATA_REQUIREMENT = 12 [(fhir_original_code) = "DataRequirement"];
    DISTANCE = 13 [(fhir_original_code) = "Distance"];
    DOSAGE = 14 [(fhir_original_code) = "Dosage"];
    DURATION = 15 [(fhir_original_code) = "Duration"];
    ELEMENT = 16 [(fhir_original_code) = "Element"];
    ELEMENT_DEFINITION = 17 [(fhir_original_code) = "ElementDefinition"];
    EXTENSION = 18 [(fhir_original_code) = "Extension"];
    HUMAN_NAME = 19 [(fhir_original_code) = "HumanName"];
    IDENTIFIER = 20 [(fhir_original_code) = "Identifier"];
    META = 21 [(fhir_original_code) = "Meta"];
    MONEY = 22 [(fhir_original_code) = "Money"];
    NARRATIVE = 23 [(fhir_original_code) = "Narrative"];
    PARAMETER_DEFINITION = 24 [(fhir_original_code) = "ParameterDefinition"];
    PERIOD = 25 [(fhir_original_code) = "Period"];
    QUANTITY = 26 [(fhir_original_code) = "Quantity"];
    RANGE = 27 [(fhir_original_code) = "Range"];
    RATIO = 28 [(fhir_original_code) = "Ratio"];
    REFERENCE = 29 [(fhir_original_code) = "Reference"];
    RELATED_ARTIFACT = 30 [(fhir_original_code) = "RelatedArtifact"];
    SAMPLED_DATA = 31 [(fhir_original_code) = "SampledData"];
    SIGNATURE = 32 [(fhir_original_code) = "Signature"];
    SIMPLE_QUANTITY = 33 [(fhir_original_code) = "SimpleQuantity"];
    TIMING = 34 [(fhir_original_code) = "Timing"];
    TRIGGER_DEFINITION = 35 [(fhir_original_code) = "TriggerDefinition"];
    USAGE_CONTEXT = 36 [(fhir_original_code) = "UsageContext"];
    BASE64_BINARY = 37 [(fhir_original_code) = "base64Binary"];
    BOOLEAN = 38;
    CODE = 39;
    DATE = 40;
    DATE_TIME = 41 [(fhir_original_code) = "dateTime"];
    DECIMAL = 42;
    ID = 43;
    INSTANT = 44;
    INTEGER = 45;
    MARKDOWN = 46;
    OID = 47;
    POSITIVE_INT = 48 [(fhir_original_code) = "positiveInt"];
    STRING = 49;
    TIME = 50;
    UNSIGNED_INT = 51 [(fhir_original_code) = "unsignedInt"];
    URI = 52;
    UUID = 53;
    XHTML = 54;
    ACCOUNT = 55 [(fhir_original_code) = "Account"];
    ACTIVITY_DEFINITION = 56 [(fhir_original_code) = "ActivityDefinition"];
    ADVERSE_EVENT = 57 [(fhir_original_code) = "AdverseEvent"];
    ALLERGY_INTOLERANCE = 58 [(fhir_original_code) = "AllergyIntolerance"];
    APPOINTMENT = 59 [(fhir_original_code) = "Appointment"];
    APPOINTMENT_RESPONSE = 60 [(fhir_original_code) = "AppointmentResponse"];
    AUDIT_EVENT = 61 [(fhir_original_code) = "AuditEvent"];
    BASIC = 62 [(fhir_original_code) = "Basic"];
    BINARY = 63 [(fhir_original_code) = "Binary"];
    BODY_SITE = 64 [(fhir_original_code) = "BodySite"];
    BUNDLE = 65 [(fhir_original_code) = "Bundle"];
    CAPABILITY_STATEMENT = 66 [(fhir_original_code) = "CapabilityStatement"];
    CARE_PLAN = 67 [(fhir_original_code) = "CarePlan"];
    CARE_TEAM = 68 [(fhir_original_code) = "CareTeam"];
    CHARGE_ITEM = 69 [(fhir_original_code) = "ChargeItem"];
    CLAIM = 70 [(fhir_original_code) = "Claim"];
    CLAIM_RESPONSE = 71 [(fhir_original_code) = "ClaimResponse"];
    CLINICAL_IMPRESSION = 72 [(fhir_original_code) = "ClinicalImpression"];
    CODE_SYSTEM = 73 [(fhir_original_code) = "CodeSystem"];
    COMMUNICATION = 74 [(fhir_original_code) = "Communication"];
    COMMUNICATION_REQUEST = 75 [(fhir_original_code) = "CommunicationRequest"];
    COMPARTMENT_DEFINITION = 76
        [(fhir_original_code) = "CompartmentDefinition"];
    COMPOSITION = 77 [(fhir_original_code) = "Composition"];
    CONCEPT_MAP = 78 [(fhir_original_code) = "ConceptMap"];
    CONDITION = 79 [(fhir_original_code) = "Condition"];
    CONSENT = 80 [(fhir_original_code) = "Consent"];
    CONTRACT = 81 [(fhir_original_code) = "Contract"];
    COVERAGE = 82 [(fhir_original_code) = "Coverage"];
    DATA_ELEMENT = 83 [(fhir_original_code) = "DataElement"];
    DETECTED_ISSUE = 84 [(fhir_original_code) = "DetectedIssue"];
    DEVICE = 85 [(fhir_original_code) = "Device"];
    DEVICE_COMPONENT = 86 [(fhir_original_code) = "DeviceComponent"];
    DEVICE_METRIC = 87 [(fhir_original_code) = "DeviceMetric"];
    DEVICE_REQUEST = 88 [(fhir_original_code) = "DeviceRequest"];
    DEVICE_USE_STATEMENT = 89 [(fhir_original_code) = "DeviceUseStatement"];
    DIAGNOSTIC_REPORT = 90 [(fhir_original_code) = "DiagnosticReport"];
    DOCUMENT_MANIFEST = 91 [(fhir_original_code) = "DocumentManifest"];
    DOCUMENT_REFERENCE = 92 [(fhir_original_code) = "DocumentReference"];
    DOMAIN_RESOURCE = 93 [(fhir_original_code) = "DomainResource"];
    ELIGIBILITY_REQUEST = 94 [(fhir_original_code) = "EligibilityRequest"];
    ELIGIBILITY_RESPONSE = 95 [(fhir_original_code) = "EligibilityResponse"];
    ENCOUNTER = 96 [(fhir_original_code) = "Encounter"];
    ENDPOINT = 97 [(fhir_original_code) = "Endpoint"];
    ENROLLMENT_REQUEST = 98 [(fhir_original_code) = "EnrollmentRequest"];
    ENROLLMENT_RESPONSE = 99 [(fhir_original_code) = "EnrollmentResponse"];
    EPISODE_OF_CARE = 100 [(fhir_original_code) = "EpisodeOfCare"];
    EXPANSION_PROFILE = 101 [(fhir_original_code) = "ExpansionProfile"];
    EXPLANATION_OF_BENEFIT = 102
        [(fhir_original_code) = "ExplanationOfBenefit"];
    FAMILY_MEMBER_HISTORY = 103 [(fhir_original_code) = "FamilyMemberHistory"];
    FLAG = 104 [(fhir_original_code) = "Flag"];
    GOAL = 105 [(fhir_original_code) = "Goal"];
    GRAPH_DEFINITION = 106 [(fhir_original_code) = "GraphDefinition"];
    GROUP = 107 [(fhir_original_code) = "Group"];
    GUIDANCE_RESPONSE = 108 [(fhir_original_code) = "GuidanceResponse"];
    HEALTHCARE_SERVICE = 109 [(fhir_original_code) = "HealthcareService"];
    IMAGING_MANIFEST = 110 [(fhir_original_code) = "ImagingManifest"];
    IMAGING_STUDY = 111 [(fhir_original_code) = "ImagingStudy"];
    IMMUNIZATION = 112 [(fhir_original_code) = "Immunization"];
    IMMUNIZATION_RECOMMENDATION = 113
        [(fhir_original_code) = "ImmunizationRecommendation"];
    IMPLEMENTATION_GUIDE = 114 [(fhir_original_code) = "ImplementationGuide"];
    LIBRARY = 115 [(fhir_original_code) = "Library"];
    LINKAGE = 116 [(fhir_original_code) = "Linkage"];
    LIST = 117 [(fhir_original_code) = "List"];
    LOCATION = 118 [(fhir_original_code) = "Location"];
    MEASURE = 119 [(fhir_original_code) = "Measure"];
    MEASURE_REPORT = 120 [(fhir_original_code) = "MeasureReport"];
    MEDIA = 121 [(fhir_original_code) = "Media"];
    MEDICATION = 122 [(fhir_original_code) = "Medication"];
    MEDICATION_ADMINISTRATION = 123
        [(fhir_original_code) = "MedicationAdministration"];
    MEDICATION_DISPENSE = 124 [(fhir_original_code) = "MedicationDispense"];
    MEDICATION_REQUEST = 125 [(fhir_original_code) = "MedicationRequest"];
    MEDICATION_STATEMENT = 126 [(fhir_original_code) = "MedicationStatement"];
    MESSAGE_DEFINITION = 127 [(fhir_original_code) = "MessageDefinition"];
    MESSAGE_HEADER = 128 [(fhir_original_code) = "MessageHeader"];
    NAMING_SYSTEM = 129 [(fhir_original_code) = "NamingSystem"];
    NUTRITION_ORDER = 130 [(fhir_original_code) = "NutritionOrder"];
    OBSERVATION = 131 [(fhir_original_code) = "Observation"];
    OPERATIONDEFINITION = 132 [(fhir_original_code) = "OperationDefinition"];
    OPERATION_OUTCOME = 133 [(fhir_original_code) = "OperationOutcome"];
    ORGANIZATION = 134 [(fhir_original_code) = "Organization"];
    PARAMETERS = 135 [(fhir_original_code) = "Parameters"];
    PATIENT = 136 [(fhir_original_code) = "Patient"];
    PAYMENT_NOTICE = 137 [(fhir_original_code) = "PaymentNotice"];
    PAYMENT_RECONCILIATION = 138
        [(fhir_original_code) = "PaymentReconciliation"];
    PERSON = 139 [(fhir_original_code) = "Person"];
    PLAN_DEFINITION = 140 [(fhir_original_code) = "PlanDefinition"];
    PRACTITIONER = 141 [(fhir_original_code) = "Practitioner"];
    PRACTITIONER_ROLE = 142 [(fhir_original_code) = "PractitionerRole"];
    PROCEDURE = 143 [(fhir_original_code) = "Procedure"];
    PROCEDURE_REQUEST = 144 [(fhir_original_code) = "ProcedureRequest"];
    PROCESS_REQUEST = 145 [(fhir_original_code) = "ProcessRequest"];
    PROCESS_RESPONSE = 146 [(fhir_original_code) = "ProcessResponse"];
    PROVENANCE = 147 [(fhir_original_code) = "Provenance"];
    QUESTIONNAIRE = 148 [(fhir_original_code) = "Questionnaire"];
    QUESTIONNAIRE_RESPONSE = 149
        [(fhir_original_code) = "QuestionnaireResponse"];
    REFERRAL_REQUEST = 150 [(fhir_original_code) = "ReferralRequest"];
    RELATED_PERSON = 151 [(fhir_original_code) = "RelatedPerson"];
    REQUEST_GROUP = 152 [(fhir_original_code) = "RequestGroup"];
    RESEARCH_STUDY = 153 [(fhir_original_code) = "ResearchStudy"];
    RESEARCH_SUBJECT = 154 [(fhir_original_code) = "ResearchSubject"];
    RESOURCE = 155 [(fhir_original_code) = "Resource"];
    RISK_ASSESSMENT = 156 [(fhir_original_code) = "RiskAssessment"];
    SCHEDULE = 157 [(fhir_original_code) = "Schedule"];
    SEARCH_PARAMETER = 158 [(fhir_original_code) = "SearchParameter"];
    SEQUENCE = 159 [(fhir_original_code) = "Sequence"];
    SERVICE_DEFINITION = 160 [(fhir_original_code) = "ServiceDefinition"];
    SLOT = 161 [(fhir_original_code) = "Slot"];
    SPECIMEN = 162 [(fhir_original_code) = "Specimen"];
    STRUCTURE_DEFINITION = 163 [(fhir_original_code) = "StructureDefinition"];
    STRUCTURE_MAP = 164 [(fhir_original_code) = "StructureMap"];
    SUBSCRIPTION = 165 [(fhir_original_code) = "Subscription"];
    SUBSTANCE = 166 [(fhir_original_code) = "Substance"];
    SUPPLY_DELIVERY = 167 [(fhir_original_code) = "SupplyDelivery"];
    SUPPLY_REQUEST = 168 [(fhir_original_code) = "SupplyRequest"];
    TASK = 169 [(fhir_original_code) = "Task"];
    TEST_REPORT = 170 [(fhir_original_code) = "TestReport"];
    TEST_SCRIPT = 171 [(fhir_original_code) = "TestScript"];
    VALUE_SET = 172 [(fhir_original_code) = "ValueSet"];
    VISION_PRESCRIPTION = 173 [(fhir_original_code) = "VisionPrescription"];
    TYPE = 174 [(fhir_original_code) = "Type"];
    ANY = 175 [(fhir_original_code) = "Any"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The risk of an adverse reaction (allergy or intolerance) for this patient
// upon exposure to the substance (including pharmaceutical products). See
// http://hl7.org/fhir/ValueSet/allerg-intol-substance-exp-risk
message AllergyIntoleranceSubstanceExposureRiskCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/allerg-intol-substance-exp-risk";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    KNOWN_REACTION_RISK = 1;
    NO_KNOWN_REACTION_RISK = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The clinical status of the allergy or intolerance.
// See http://hl7.org/fhir/ValueSet/allergy-clinical-status
message AllergyIntoleranceClinicalStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/allergy-clinical-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    RESOLVED = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Category of an identified substance.
// See http://hl7.org/fhir/ValueSet/allergy-intolerance-category
message AllergyIntoleranceCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/allergy-intolerance-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FOOD = 1;
    MEDICATION = 2;
    ENVIRONMENT = 3;
    BIOLOGIC = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Estimate of the potential clinical harm, or seriousness, of a reaction to an
// identified substance. See
// http://hl7.org/fhir/ValueSet/allergy-intolerance-criticality
message AllergyIntoleranceCriticalityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/allergy-intolerance-criticality";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LOW = 1;
    HIGH = 2;
    UNABLE_TO_ASSESS = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identification of the underlying physiological mechanism for a Reaction Risk.
// See http://hl7.org/fhir/ValueSet/allergy-intolerance-type
message AllergyIntoleranceTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/allergy-intolerance-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ALLERGY = 1;
    INTOLERANCE = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Assertion about certainty associated with a propensity, or potential risk, of
// a reaction to the identified substance. See
// http://hl7.org/fhir/ValueSet/allergy-verification-status
message AllergyIntoleranceVerificationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/allergy-verification-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UNCONFIRMED = 1;
    CONFIRMED = 2;
    REFUTED = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate
// the current state of the animal's reproductive organs. See
// http://hl7.org/fhir/ValueSet/animal-genderstatus
message GenderStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/animal-genderstatus";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NEUTERED = 1;
    INTACT = 2;
    UNKNOWN = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The free/busy status of an appointment.
// See http://hl7.org/fhir/ValueSet/appointmentstatus
message AppointmentStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/appointmentstatus";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSED = 1;
    PENDING = 2;
    BOOKED = 3;
    ARRIVED = 4;
    FULFILLED = 5;
    CANCELLED = 6;
    NOSHOW = 7;
    ENTERED_IN_ERROR = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of direction to use for assertion.
// See http://hl7.org/fhir/ValueSet/assert-direction-codes
message AssertionDirectionTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/assert-direction-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    RESPONSE = 1;
    REQUEST = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of operator to use for assertion.
// See http://hl7.org/fhir/ValueSet/assert-operator-codes
message AssertionOperatorTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/assert-operator-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    EQUALS = 1;
    NOTEQUALS = 2 [(fhir_original_code) = "notEquals"];
    IN = 3;
    NOTIN = 4 [(fhir_original_code) = "notIn"];
    GREATERTHAN = 5 [(fhir_original_code) = "greaterThan"];
    LESSTHAN = 6 [(fhir_original_code) = "lessThan"];
    EMPTY = 7;
    NOTEMPTY = 8 [(fhir_original_code) = "notEmpty"];
    CONTAINS = 9;
    NOTCONTAINS = 10 [(fhir_original_code) = "notContains"];
    EVAL = 11;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of response code to use for assertion.
// See http://hl7.org/fhir/ValueSet/assert-response-code-types
message AssertionResponseTypesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/assert-response-code-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OKAY = 1;
    CREATED = 2;
    NOCONTENT = 3 [(fhir_original_code) = "noContent"];
    NOTMODIFIED = 4 [(fhir_original_code) = "notModified"];
    BAD = 5;
    FORBIDDEN = 6;
    NOTFOUND = 7 [(fhir_original_code) = "notFound"];
    METHODNOTALLOWED = 8 [(fhir_original_code) = "methodNotAllowed"];
    CONFLICT = 9;
    GONE = 10;
    PRECONDITIONFAILED = 11 [(fhir_original_code) = "preconditionFailed"];
    UNPROCESSABLE = 12;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicator for type of action performed during the event that generated the
// event See http://hl7.org/fhir/ValueSet/audit-event-action
message AuditEventActionCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/audit-event-action";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    C = 1 [(fhir_original_code) = "C"];
    R = 2 [(fhir_original_code) = "R"];
    U = 3 [(fhir_original_code) = "U"];
    D = 4 [(fhir_original_code) = "D"];
    E = 5 [(fhir_original_code) = "E"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether the event succeeded or failed
// See http://hl7.org/fhir/ValueSet/audit-event-outcome
message AuditEventOutcomeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/audit-event-outcome";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SUCCESS = 1 [(fhir_original_code) = "0"];
    MINOR_FAILURE = 2 [(fhir_original_code) = "4"];
    SERIOUS_FAILURE = 3 [(fhir_original_code) = "8"];
    MAJOR_FAILURE = 4 [(fhir_original_code) = "12"];
  }
  Value value = 1;
  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines codes for resources not yet supported by (or which
// will never be supported by) FHIR.  Many of the codes listed here will
// eventually be turned into official resources.  However, there is no guarantee
// that any particular resource will be created nor that the scope will be
// exactly as defined by the codes presented here.  Codes in this set will be
// deprecated if/when formal resources are defined that encompass these
// concepts. See http://hl7.org/fhir/ValueSet/basic-resource-type
message BasicResourceTypesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/basic-resource-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CONSENT = 1;
    REFERRAL = 2;
    ADVEVENT = 3;
    APTMTREQ = 4;
    TRANSFER = 5;
    DIET = 6;
    ADMINACT = 7;
    EXPOSURE = 8;
    INVESTIGATION = 9;
    ACCOUNT = 10;
    INVOICE = 11;
    ADJUDICAT = 12;
    PREDETREQ = 13;
    PREDETERMINE = 14;
    STUDY = 15;
    PROTOCOL = 16;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indication of the degree of conformance expectations associated with a
// binding. See http://hl7.org/fhir/ValueSet/binding-strength
message BindingStrengthCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/binding-strength";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REQUIRED = 1;
    EXTENSIBLE = 2;
    PREFERRED = 3;
    EXAMPLE = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the purpose of a bundle - how it was intended to be used.
// See http://hl7.org/fhir/ValueSet/bundle-type
message BundleTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/bundle-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DOCUMENT = 1;
    MESSAGE = 2;
    TRANSACTION = 3;
    TRANSACTION_RESPONSE = 4;
    BATCH = 5;
    BATCH_RESPONSE = 6;
    HISTORY = 7;
    SEARCHSET = 8;
    COLLECTION = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a capability statement is intended to be used.
// See http://hl7.org/fhir/ValueSet/capability-statement-kind
message CapabilityStatementKindCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/capability-statement-kind";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INSTANCE = 1;
    CAPABILITY = 2;
    REQUIREMENTS = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// High-level categorization of the type of activity in a care plan.
// See http://hl7.org/fhir/ValueSet/care-plan-activity-category
message CarePlanActivityCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/care-plan-activity-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DIET = 1;
    DRUG = 2;
    ENCOUNTER = 3;
    OBSERVATION = 4;
    PROCEDURE = 5;
    SUPPLY = 6;
    OTHER = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates where the activity is at in its overall life cycle.
// See http://hl7.org/fhir/ValueSet/care-plan-activity-status
message CarePlanActivityStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/care-plan-activity-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NOT_STARTED = 1;
    SCHEDULED = 2;
    IN_PROGRESS = 3;
    ON_HOLD = 4;
    COMPLETED = 5;
    CANCELLED = 6;
    UNKNOWN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes indicating the degree of authority/intentionality associated with a
// care plan See http://hl7.org/fhir/ValueSet/care-plan-intent
message CarePlanIntentCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/care-plan-intent";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSAL = 1;
    PLAN = 2;
    ORDER = 3;
    OPTION = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether the plan is currently being acted upon, represents future
// intentions or is now a historical record. See
// http://hl7.org/fhir/ValueSet/care-plan-status
message CarePlanStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/care-plan-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    ACTIVE = 2;
    SUSPENDED = 3;
    COMPLETED = 4;
    ENTERED_IN_ERROR = 5;
    CANCELLED = 6;
    UNKNOWN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the type of care team.
// See http://hl7.org/fhir/ValueSet/care-team-category
message CareTeamCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/care-team-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    EVENT = 1;
    ENCOUNTER = 2;
    EPISODE = 3;
    LONGITUDINAL = 4;
    CONDITION = 5;
    CLINICAL_RESEARCH = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the status of the care team.
// See http://hl7.org/fhir/ValueSet/care-team-status
message CareTeamStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/care-team-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSED = 1;
    ACTIVE = 2;
    SUSPENDED = 3;
    INACTIVE = 4;
    ENTERED_IN_ERROR = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes identifying the stage lifecycle stage of a ChargeItem
// See http://hl7.org/fhir/ValueSet/chargeitem-status
message ChargeItemStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/chargeitem-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PLANNED = 1;
    BILLABLE = 2;
    NOT_BILLABLE = 3;
    ABORTED = 4;
    BILLED = 5;
    ENTERED_IN_ERROR = 6;
    UNKNOWN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Direction in which lists of question options should be displayed
// See http://hl7.org/fhir/ValueSet/choice-list-orientation
message ChoiceListOrientationCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/choice-list-orientation";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HORIZONTAL = 1;
    VERTICAL = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Complete, proposed, exploratory, other
// See http://hl7.org/fhir/ValueSet/claim-use
message UseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/claim-use";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    COMPLETE = 1;
    PROPOSED = 2;
    EXPLORATORY = 3;
    OTHER = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identifies whether a useContext represents a context or classification for
// the element See http://hl7.org/fhir/ValueSet/classification-or-context
message ClassificationOrContextCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/classification-or-context";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CLASSIFICATION = 1;
    CONTEXT = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The workflow state of a clinical impression.
// See http://hl7.org/fhir/ValueSet/clinical-impression-status
message ClinicalImpressionStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/clinical-impression-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    COMPLETED = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How much of the content of the code system - the concepts and codes it
// defines - are represented in a code system resource See
// http://hl7.org/fhir/ValueSet/codesystem-content-mode
message CodeSystemContentModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/codesystem-content-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NOT_PRESENT = 1;
    EXAMPLE = 2;
    FRAGMENT = 3;
    COMPLETE = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The meaning of the hierarchy of concepts in a code system
// See http://hl7.org/fhir/ValueSet/codesystem-hierarchy-meaning
message CodeSystemHierarchyMeaningCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/codesystem-hierarchy-meaning";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    GROUPED_BY = 1;
    IS_A = 2;
    PART_OF = 3;
    CLASSIFIED_WITH = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Common Tag Codes defined by FHIR project
// See http://hl7.org/fhir/ValueSet/common-tags
message CommonTagsCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/common-tags";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIONABLE = 1;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes for general categories of communications such as alerts, instruction,
// etc. See http://hl7.org/fhir/ValueSet/communication-category
message CommunicationCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/communication-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ALERT = 1;
    NOTIFICATION = 2;
    REMINDER = 3;
    INSTRUCTION = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes for the reason why a communication was not done.
// See http://hl7.org/fhir/ValueSet/communication-not-done-reason
message CommunicationNotDoneReasonCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/communication-not-done-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UNKNOWN = 1;
    SYSTEM_ERROR = 2;
    INVALID_PHONE_NUMBER = 3;
    RECIPIENT_UNAVAILABLE = 4;
    FAMILY_OBJECTION = 5;
    PATIENT_OBJECTION = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Which compartment a compartment definition describes
// See http://hl7.org/fhir/ValueSet/compartment-type
message CompartmentTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/compartment-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PATIENT = 1 [(fhir_original_code) = "Patient"];
    ENCOUNTER = 2 [(fhir_original_code) = "Encounter"];
    RELATEDPERSON = 3 [(fhir_original_code) = "RelatedPerson"];
    PRACTITIONER = 4 [(fhir_original_code) = "Practitioner"];
    DEVICE = 5 [(fhir_original_code) = "Device"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The composite scoring method of the measure
// See http://hl7.org/fhir/ValueSet/composite-measure-scoring
message CompositeMeasureScoringCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/composite-measure-scoring";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OPPORTUNITY = 1;
    ALL_OR_NOTHING = 2;
    LINEAR = 3;
    WEIGHTED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The way in which a person authenticated a composition.
// See http://hl7.org/fhir/ValueSet/composition-attestation-mode
message CompositionAttestationModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/composition-attestation-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PERSONAL = 1;
    PROFESSIONAL = 2;
    LEGAL = 3;
    OFFICIAL = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The workflow/clinical status of the composition.
// See http://hl7.org/fhir/ValueSet/composition-status
message CompositionStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/composition-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PRELIMINARY = 1;
    FINAL = 2;
    AMENDED = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The degree of equivalence between concepts.
// See http://hl7.org/fhir/ValueSet/concept-map-equivalence
message ConceptMapEquivalenceCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/concept-map-equivalence";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    RELATEDTO = 1;
    EQUIVALENT = 2;
    EQUAL = 3;
    WIDER = 4;
    SUBSUMES = 5;
    NARROWER = 6;
    SPECIALIZES = 7;
    INEXACT = 8;
    UNMATCHED = 9;
    DISJOINT = 10;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines which action to take if there is no match in the group.
// See http://hl7.org/fhir/ValueSet/conceptmap-unmapped-mode
message ConceptMapGroupUnmappedModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/conceptmap-unmapped-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROVIDED = 1;
    FIXED = 2;
    OTHER_MAP = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of a property value
// See http://hl7.org/fhir/ValueSet/concept-property-type
message PropertyTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/concept-property-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CODE = 1;
    CODING = 2 [(fhir_original_code) = "Coding"];
    STRING = 3;
    INTEGER = 4;
    BOOLEAN = 5;
    DATETIME = 6 [(fhir_original_code) = "dateTime"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that indicates how the server supports conditional delete.
// See http://hl7.org/fhir/ValueSet/conditional-delete-status
message ConditionalDeleteStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/conditional-delete-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NOT_SUPPORTED = 1;
    SINGLE = 2;
    MULTIPLE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that indicates how the server supports conditional read.
// See http://hl7.org/fhir/ValueSet/conditional-read-status
message ConditionalReadStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/conditional-read-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NOT_SUPPORTED = 1;
    MODIFIED_SINCE = 2;
    NOT_MATCH = 3;
    FULL_SUPPORT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Preferred value set for Condition Categories.
// See http://hl7.org/fhir/ValueSet/condition-category
message ConditionCategoryCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/condition-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROBLEM_LIST_ITEM = 1;
    ENCOUNTER_DIAGNOSIS = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Preferred value set for Condition Clinical Status.
// See http://hl7.org/fhir/ValueSet/condition-clinical
message ConditionClinicalStatusCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/condition-clinical";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    RECURRENCE = 2;
    INACTIVE = 3;
    REMISSION = 4;
    RESOLVED = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Enumeration indicating whether the condition is currently active, inactive,
// or has been resolved. See http://hl7.org/fhir/ValueSet/condition-state
message ConditionStateCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/condition-state";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    RESOLVED = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The verification status to support or decline the clinical status of the
// condition or diagnosis. See http://hl7.org/fhir/ValueSet/condition-ver-status
message ConditionVerificationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/condition-ver-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROVISIONAL = 1;
    DIFFERENTIAL = 2;
    CONFIRMED = 3;
    REFUTED = 4;
    ENTERED_IN_ERROR = 5;
    UNKNOWN = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the degree of adherence to a specified behavior or capability
// expected for a system to be deemed conformant with a specification. See
// http://hl7.org/fhir/ValueSet/conformance-expectation
message ConformanceExpectationCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/conformance-expectation";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SHALL = 1 [(fhir_original_code) = "SHALL"];
    SHOULD = 2 [(fhir_original_code) = "SHOULD"];
    MAY = 3 [(fhir_original_code) = "MAY"];
    SHOULD_NOT = 4 [(fhir_original_code) = "SHOULD-NOT"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a resource reference is interpreted when testing consent restrictions
// See http://hl7.org/fhir/ValueSet/consent-data-meaning
message ConsentDataMeaningCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/consent-data-meaning";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INSTANCE = 1;
    RELATED = 2;
    DEPENDENTS = 3;
    AUTHOREDBY = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How an exception statement is applied, such as adding additional consent or
// removing consent See http://hl7.org/fhir/ValueSet/consent-except-type
message ConsentExceptTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/consent-except-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DENY = 1;
    PERMIT = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the state of the consent
// See http://hl7.org/fhir/ValueSet/consent-state-codes
message ConsentStateCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/consent-state-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    PROPOSED = 2;
    ACTIVE = 3;
    REJECTED = 4;
    INACTIVE = 5;
    ENTERED_IN_ERROR = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// SHALL applications comply with this constraint?
// See http://hl7.org/fhir/ValueSet/constraint-severity
message ConstraintSeverityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/constraint-severity";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ERROR = 1;
    WARNING = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate
// the purpose for which you would contact a contact party. See
// http://hl7.org/fhir/ValueSet/contactentity-type
message ContactEntityTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/contactentity-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    BILL = 1 [(fhir_original_code) = "BILL"];
    ADMIN = 2 [(fhir_original_code) = "ADMIN"];
    HR = 3 [(fhir_original_code) = "HR"];
    PAYOR = 4 [(fhir_original_code) = "PAYOR"];
    PATINF = 5 [(fhir_original_code) = "PATINF"];
    PRESS = 6 [(fhir_original_code) = "PRESS"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Telecommunications form for contact point
// See http://hl7.org/fhir/ValueSet/contact-point-system
message ContactPointSystemCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/contact-point-system";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PHONE = 1;
    FAX = 2;
    EMAIL = 3;
    PAGER = 4;
    URL = 5;
    SMS = 6;
    OTHER = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Use of contact point
// See http://hl7.org/fhir/ValueSet/contact-point-use
message ContactPointUseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/contact-point-use";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HOME = 1;
    WORK = 2;
    TEMP = 3;
    OLD = 4;
    MOBILE = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The content or mime type.
// See http://hl7.org/fhir/ValueSet/content-type
message ContentTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/content-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    XML = 1;
    JSON = 2;
    TTL = 3;
    NONE = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set contract specific codes for status.
// See http://hl7.org/fhir/ValueSet/contract-status
message ContractResourceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/contract-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AMENDED = 1;
    APPENDED = 2;
    CANCELLED = 3;
    DISPUTED = 4;
    ENTERED_IN_ERROR = 5;
    EXECUTABLE = 6;
    EXECUTED = 7;
    NEGOTIABLE = 8;
    OFFERED = 9;
    POLICY = 10;
    REJECTED = 11;
    RENEWED = 12;
    REVOKED = 13;
    RESOLVED = 14;
    TERMINATED = 15;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of contributor
// See http://hl7.org/fhir/ValueSet/contributor-type
message ContributorTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/contributor-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AUTHOR = 1;
    EDITOR = 2;
    REVIEWER = 3;
    ENDORSER = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Copy Number Event
// See http://hl7.org/fhir/ValueSet/copy-number-event
message CopyNumberEventCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/copy-number-event";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AMP = 1;
    DEL = 2;
    LOF = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Used to specify why the normally expected content of the data element is
// missing. See http://hl7.org/fhir/ValueSet/data-absent-reason
message DataAbsentReasonCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/data-absent-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UNKNOWN = 1;
    ASKED = 2;
    TEMP = 3;
    NOT_ASKED = 4;
    MASKED = 5;
    UNSUPPORTED = 6;
    ASTEXT = 7;
    ERROR = 8;
    NOT_A_NUMBER = 9 [(fhir_original_code) = "NaN"];
    NOT_PERFORMED = 10;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the degree of precision of the data element definition.
// See http://hl7.org/fhir/ValueSet/dataelement-stringency
message DataElementStringencyCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/dataelement-stringency";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    COMPARABLE = 1;
    FULLY_SPECIFIED = 2;
    EQUIVALENT = 3;
    CONVERTABLE = 4;
    SCALEABLE = 5;
    FLEXIBLE = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of an element - one of the FHIR data types.
// See http://hl7.org/fhir/ValueSet/data-types
message DataTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/data-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ADDRESS = 1 [(fhir_original_code) = "Address"];
    AGE = 2 [(fhir_original_code) = "Age"];
    ANNOTATION = 3 [(fhir_original_code) = "Annotation"];
    ATTACHMENT = 4 [(fhir_original_code) = "Attachment"];
    BACKBONE_ELEMENT = 5 [(fhir_original_code) = "BackboneElement"];
    CODEABLE_CONCEPT = 6 [(fhir_original_code) = "CodeableConcept"];
    CODING = 7 [(fhir_original_code) = "Coding"];
    CONTACT_DETAIL = 8 [(fhir_original_code) = "ContactDetail"];
    CONTACT_POINT = 9 [(fhir_original_code) = "ContactPoint"];
    CONTRIBUTOR = 10 [(fhir_original_code) = "Contributor"];
    COUNT = 11 [(fhir_original_code) = "Count"];
    DATA_REQUIREMENT = 12 [(fhir_original_code) = "DataRequirement"];
    DISTANCE = 13 [(fhir_original_code) = "Distance"];
    DOSAGE = 14 [(fhir_original_code) = "Dosage"];
    DURATION = 15 [(fhir_original_code) = "Duration"];
    ELEMENT = 16 [(fhir_original_code) = "Element"];
    ELEMENT_DEFINITION = 17 [(fhir_original_code) = "ElementDefinition"];
    EXTENSION = 18 [(fhir_original_code) = "Extension"];
    HUMAN_NAME = 19 [(fhir_original_code) = "HumanName"];
    IDENTIFIER = 20 [(fhir_original_code) = "Identifier"];
    META = 21 [(fhir_original_code) = "Meta"];
    MONEY = 22 [(fhir_original_code) = "Money"];
    NARRATIVE = 23 [(fhir_original_code) = "Narrative"];
    PARAMETER_DEFINITION = 24 [(fhir_original_code) = "ParameterDefinition"];
    PERIOD = 25 [(fhir_original_code) = "Period"];
    QUANTITY = 26 [(fhir_original_code) = "Quantity"];
    RANGE = 27 [(fhir_original_code) = "Range"];
    RATIO = 28 [(fhir_original_code) = "Ratio"];
    REFERENCE = 29 [(fhir_original_code) = "Reference"];
    RELATED_ARTIFACT = 30 [(fhir_original_code) = "RelatedArtifact"];
    SAMPLED_DATA = 31 [(fhir_original_code) = "SampledData"];
    SIGNATURE = 32 [(fhir_original_code) = "Signature"];
    SIMPLE_QUANTITY = 33 [(fhir_original_code) = "SimpleQuantity"];
    TIMING = 34 [(fhir_original_code) = "Timing"];
    TRIGGER_DEFINITION = 35 [(fhir_original_code) = "TriggerDefinition"];
    USAGE_CONTEXT = 36 [(fhir_original_code) = "UsageContext"];
    BASE64_BINARY = 37 [(fhir_original_code) = "base64Binary"];
    BOOLEAN = 38;
    CODE = 39;
    DATE = 40;
    DATE_TIME = 41 [(fhir_original_code) = "dateTime"];
    DECIMAL = 42;
    ID = 43;
    INSTANT = 44;
    INTEGER = 45;
    MARKDOWN = 46;
    OID = 47;
    POSITIVE_INT = 48 [(fhir_original_code) = "positiveInt"];
    STRING = 49;
    TIME = 50;
    UNSIGNED_INT = 51 [(fhir_original_code) = "unsignedInt"];
    URI = 52;
    UUID = 53;
    XHTML = 54;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The days of the week.
// See http://hl7.org/fhir/ValueSet/days-of-week
message DaysOfWeekCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/days-of-week";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MON = 1;
    TUE = 2;
    WED = 3;
    THU = 4;
    FRI = 5;
    SAT = 6;
    SUN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Either a resource or a data type.
// See http://hl7.org/fhir/ValueSet/defined-types
message FHIRDefinedTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/defined-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ADDRESS = 1 [(fhir_original_code) = "Address"];
    AGE = 2 [(fhir_original_code) = "Age"];
    ANNOTATION = 3 [(fhir_original_code) = "Annotation"];
    ATTACHMENT = 4 [(fhir_original_code) = "Attachment"];
    BACKBONE_ELEMENT = 5 [(fhir_original_code) = "BackboneElement"];
    CODEABLE_CONCEPT = 6 [(fhir_original_code) = "CodeableConcept"];
    CODING = 7 [(fhir_original_code) = "Coding"];
    CONTACT_DETAIL = 8 [(fhir_original_code) = "ContactDetail"];
    CONTACT_POINT = 9 [(fhir_original_code) = "ContactPoint"];
    CONTRIBUTOR = 10 [(fhir_original_code) = "Contributor"];
    COUNT = 11 [(fhir_original_code) = "Count"];
    DATA_REQUIREMENT = 12 [(fhir_original_code) = "DataRequirement"];
    DISTANCE = 13 [(fhir_original_code) = "Distance"];
    DOSAGE = 14 [(fhir_original_code) = "Dosage"];
    DURATION = 15 [(fhir_original_code) = "Duration"];
    ELEMENT = 16 [(fhir_original_code) = "Element"];
    ELEMENT_DEFINITION = 17 [(fhir_original_code) = "ElementDefinition"];
    EXTENSION = 18 [(fhir_original_code) = "Extension"];
    HUMAN_NAME = 19 [(fhir_original_code) = "HumanName"];
    IDENTIFIER = 20 [(fhir_original_code) = "Identifier"];
    META = 21 [(fhir_original_code) = "Meta"];
    MONEY = 22 [(fhir_original_code) = "Money"];
    NARRATIVE = 23 [(fhir_original_code) = "Narrative"];
    PARAMETER_DEFINITION = 24 [(fhir_original_code) = "ParameterDefinition"];
    PERIOD = 25 [(fhir_original_code) = "Period"];
    QUANTITY = 26 [(fhir_original_code) = "Quantity"];
    RANGE = 27 [(fhir_original_code) = "Range"];
    RATIO = 28 [(fhir_original_code) = "Ratio"];
    REFERENCE = 29 [(fhir_original_code) = "Reference"];
    RELATED_ARTIFACT = 30 [(fhir_original_code) = "RelatedArtifact"];
    SAMPLED_DATA = 31 [(fhir_original_code) = "SampledData"];
    SIGNATURE = 32 [(fhir_original_code) = "Signature"];
    SIMPLE_QUANTITY = 33 [(fhir_original_code) = "SimpleQuantity"];
    TIMING = 34 [(fhir_original_code) = "Timing"];
    TRIGGER_DEFINITION = 35 [(fhir_original_code) = "TriggerDefinition"];
    USAGE_CONTEXT = 36 [(fhir_original_code) = "UsageContext"];
    BASE64_BINARY = 37 [(fhir_original_code) = "base64Binary"];
    BOOLEAN = 38;
    CODE = 39;
    DATE = 40;
    DATE_TIME = 41 [(fhir_original_code) = "dateTime"];
    DECIMAL = 42;
    ID = 43;
    INSTANT = 44;
    INTEGER = 45;
    MARKDOWN = 46;
    OID = 47;
    POSITIVE_INT = 48 [(fhir_original_code) = "positiveInt"];
    STRING = 49;
    TIME = 50;
    UNSIGNED_INT = 51 [(fhir_original_code) = "unsignedInt"];
    URI = 52;
    UUID = 53;
    XHTML = 54;
    ACCOUNT = 55 [(fhir_original_code) = "Account"];
    ACTIVITY_DEFINITION = 56 [(fhir_original_code) = "ActivityDefinition"];
    ADVERSE_EVENT = 57 [(fhir_original_code) = "AdverseEvent"];
    ALLERGY_INTOLERANCE = 58 [(fhir_original_code) = "AllergyIntolerance"];
    APPOINTMENT = 59 [(fhir_original_code) = "Appointment"];
    APPOINTMENT_RESPONSE = 60 [(fhir_original_code) = "AppointmentResponse"];
    AUDIT_EVENT = 61 [(fhir_original_code) = "AuditEvent"];
    BASIC = 62 [(fhir_original_code) = "Basic"];
    BINARY = 63 [(fhir_original_code) = "Binary"];
    BODY_SITE = 64 [(fhir_original_code) = "BodySite"];
    BUNDLE = 65 [(fhir_original_code) = "Bundle"];
    CAPABILITY_STATEMENT = 66 [(fhir_original_code) = "CapabilityStatement"];
    CARE_PLAN = 67 [(fhir_original_code) = "CarePlan"];
    CARE_TEAM = 68 [(fhir_original_code) = "CareTeam"];
    CHARGE_ITEM = 69 [(fhir_original_code) = "ChargeItem"];
    CLAIM = 70 [(fhir_original_code) = "Claim"];
    CLAIM_RESPONSE = 71 [(fhir_original_code) = "ClaimResponse"];
    CLINICAL_IMPRESSION = 72 [(fhir_original_code) = "ClinicalImpression"];
    CODE_SYSTEM = 73 [(fhir_original_code) = "CodeSystem"];
    COMMUNICATION = 74 [(fhir_original_code) = "Communication"];
    COMMUNICATION_REQUEST = 75 [(fhir_original_code) = "CommunicationRequest"];
    COMPARTMENT_DEFINITION = 76
        [(fhir_original_code) = "CompartmentDefinition"];
    COMPOSITION = 77 [(fhir_original_code) = "Composition"];
    CONCEPT_MAP = 78 [(fhir_original_code) = "ConceptMap"];
    CONDITION = 79 [(fhir_original_code) = "Condition"];
    CONSENT = 80 [(fhir_original_code) = "Consent"];
    CONTRACT = 81 [(fhir_original_code) = "Contract"];
    COVERAGE = 82 [(fhir_original_code) = "Coverage"];
    DATA_ELEMENT = 83 [(fhir_original_code) = "DataElement"];
    DETECTED_ISSUE = 84 [(fhir_original_code) = "DetectedIssue"];
    DEVICE = 85 [(fhir_original_code) = "Device"];
    DEVICE_COMPONENT = 86 [(fhir_original_code) = "DeviceComponent"];
    DEVICE_METRIC = 87 [(fhir_original_code) = "DeviceMetric"];
    DEVICE_REQUEST = 88 [(fhir_original_code) = "DeviceRequest"];
    DEVICE_USE_STATEMENT = 89 [(fhir_original_code) = "DeviceUseStatement"];
    DIAGNOSTIC_REPORT = 90 [(fhir_original_code) = "DiagnosticReport"];
    DOCUMENT_MANIFEST = 91 [(fhir_original_code) = "DocumentManifest"];
    DOCUMENT_REFERENCE = 92 [(fhir_original_code) = "DocumentReference"];
    DOMAIN_RESOURCE = 93 [(fhir_original_code) = "DomainResource"];
    ELIGIBILITY_REQUEST = 94 [(fhir_original_code) = "EligibilityRequest"];
    ELIGIBILITY_RESPONSE = 95 [(fhir_original_code) = "EligibilityResponse"];
    ENCOUNTER = 96 [(fhir_original_code) = "Encounter"];
    ENDPOINT = 97 [(fhir_original_code) = "Endpoint"];
    ENROLLMENT_REQUEST = 98 [(fhir_original_code) = "EnrollmentRequest"];
    ENROLLMENT_RESPONSE = 99 [(fhir_original_code) = "EnrollmentResponse"];
    EPISODE_OF_CARE = 100 [(fhir_original_code) = "EpisodeOfCare"];
    EXPANSION_PROFILE = 101 [(fhir_original_code) = "ExpansionProfile"];
    EXPLANATION_OF_BENEFIT = 102
        [(fhir_original_code) = "ExplanationOfBenefit"];
    FAMILY_MEMBER_HISTORY = 103 [(fhir_original_code) = "FamilyMemberHistory"];
    FLAG = 104 [(fhir_original_code) = "Flag"];
    GOAL = 105 [(fhir_original_code) = "Goal"];
    GRAPH_DEFINITION = 106 [(fhir_original_code) = "GraphDefinition"];
    GROUP = 107 [(fhir_original_code) = "Group"];
    GUIDANCE_RESPONSE = 108 [(fhir_original_code) = "GuidanceResponse"];
    HEALTHCARE_SERVICE = 109 [(fhir_original_code) = "HealthcareService"];
    IMAGING_MANIFEST = 110 [(fhir_original_code) = "ImagingManifest"];
    IMAGING_STUDY = 111 [(fhir_original_code) = "ImagingStudy"];
    IMMUNIZATION = 112 [(fhir_original_code) = "Immunization"];
    IMMUNIZATION_RECOMMENDATION = 113
        [(fhir_original_code) = "ImmunizationRecommendation"];
    IMPLEMENTATION_GUIDE = 114 [(fhir_original_code) = "ImplementationGuide"];
    LIBRARY = 115 [(fhir_original_code) = "Library"];
    LINKAGE = 116 [(fhir_original_code) = "Linkage"];
    LIST = 117 [(fhir_original_code) = "List"];
    LOCATION = 118 [(fhir_original_code) = "Location"];
    MEASURE = 119 [(fhir_original_code) = "Measure"];
    MEASURE_REPORT = 120 [(fhir_original_code) = "MeasureReport"];
    MEDIA = 121 [(fhir_original_code) = "Media"];
    MEDICATION = 122 [(fhir_original_code) = "Medication"];
    MEDICATION_ADMINISTRATION = 123
        [(fhir_original_code) = "MedicationAdministration"];
    MEDICATION_DISPENSE = 124 [(fhir_original_code) = "MedicationDispense"];
    MEDICATION_REQUEST = 125 [(fhir_original_code) = "MedicationRequest"];
    MEDICATION_STATEMENT = 126 [(fhir_original_code) = "MedicationStatement"];
    MESSAGE_DEFINITION = 127 [(fhir_original_code) = "MessageDefinition"];
    MESSAGE_HEADER = 128 [(fhir_original_code) = "MessageHeader"];
    NAMING_SYSTEM = 129 [(fhir_original_code) = "NamingSystem"];
    NUTRITION_ORDER = 130 [(fhir_original_code) = "NutritionOrder"];
    OBSERVATION = 131 [(fhir_original_code) = "Observation"];
    OPERATIONDEFINITION = 132 [(fhir_original_code) = "OperationDefinition"];
    OPERATION_OUTCOME = 133 [(fhir_original_code) = "OperationOutcome"];
    ORGANIZATION = 134 [(fhir_original_code) = "Organization"];
    PARAMETERS = 135 [(fhir_original_code) = "Parameters"];
    PATIENT = 136 [(fhir_original_code) = "Patient"];
    PAYMENT_NOTICE = 137 [(fhir_original_code) = "PaymentNotice"];
    PAYMENT_RECONCILIATION = 138
        [(fhir_original_code) = "PaymentReconciliation"];
    PERSON = 139 [(fhir_original_code) = "Person"];
    PLAN_DEFINITION = 140 [(fhir_original_code) = "PlanDefinition"];
    PRACTITIONER = 141 [(fhir_original_code) = "Practitioner"];
    PRACTITIONER_ROLE = 142 [(fhir_original_code) = "PractitionerRole"];
    PROCEDURE = 143 [(fhir_original_code) = "Procedure"];
    PROCEDURE_REQUEST = 144 [(fhir_original_code) = "ProcedureRequest"];
    PROCESS_REQUEST = 145 [(fhir_original_code) = "ProcessRequest"];
    PROCESS_RESPONSE = 146 [(fhir_original_code) = "ProcessResponse"];
    PROVENANCE = 147 [(fhir_original_code) = "Provenance"];
    QUESTIONNAIRE = 148 [(fhir_original_code) = "Questionnaire"];
    QUESTIONNAIRE_RESPONSE = 149
        [(fhir_original_code) = "QuestionnaireResponse"];
    REFERRAL_REQUEST = 150 [(fhir_original_code) = "ReferralRequest"];
    RELATED_PERSON = 151 [(fhir_original_code) = "RelatedPerson"];
    REQUEST_GROUP = 152 [(fhir_original_code) = "RequestGroup"];
    RESEARCH_STUDY = 153 [(fhir_original_code) = "ResearchStudy"];
    RESEARCH_SUBJECT = 154 [(fhir_original_code) = "ResearchSubject"];
    RESOURCE = 155 [(fhir_original_code) = "Resource"];
    RISK_ASSESSMENT = 156 [(fhir_original_code) = "RiskAssessment"];
    SCHEDULE = 157 [(fhir_original_code) = "Schedule"];
    SEARCH_PARAMETER = 158 [(fhir_original_code) = "SearchParameter"];
    SEQUENCE = 159 [(fhir_original_code) = "Sequence"];
    SERVICE_DEFINITION = 160 [(fhir_original_code) = "ServiceDefinition"];
    SLOT = 161 [(fhir_original_code) = "Slot"];
    SPECIMEN = 162 [(fhir_original_code) = "Specimen"];
    STRUCTURE_DEFINITION = 163 [(fhir_original_code) = "StructureDefinition"];
    STRUCTURE_MAP = 164 [(fhir_original_code) = "StructureMap"];
    SUBSCRIPTION = 165 [(fhir_original_code) = "Subscription"];
    SUBSTANCE = 166 [(fhir_original_code) = "Substance"];
    SUPPLY_DELIVERY = 167 [(fhir_original_code) = "SupplyDelivery"];
    SUPPLY_REQUEST = 168 [(fhir_original_code) = "SupplyRequest"];
    TASK = 169 [(fhir_original_code) = "Task"];
    TEST_REPORT = 170 [(fhir_original_code) = "TestReport"];
    TEST_SCRIPT = 171 [(fhir_original_code) = "TestScript"];
    VALUE_SET = 172 [(fhir_original_code) = "ValueSet"];
    VISION_PRESCRIPTION = 173 [(fhir_original_code) = "VisionPrescription"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes identifying the lifecycle stage of a definition
// See http://hl7.org/fhir/ValueSet/definition-status
message DefinitionStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/definition-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    ACTIVE = 2;
    WITHDRAWN = 3;
    UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// High-level categorization of the definition, used for searching, sorting, and
// filtering See http://hl7.org/fhir/ValueSet/definition-topic
message DefinitionTopicCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/definition-topic";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TREATMENT = 1;
    EDUCATION = 2;
    ASSESSMENT = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the potential degree of impact of the identified issue on the
// patient. See http://hl7.org/fhir/ValueSet/detectedissue-severity
message DetectedIssueSeverityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/detectedissue-severity";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HIGH = 1;
    MODERATE = 2;
    LOW = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept indicating the current status of a the Device Usage
// See http://hl7.org/fhir/ValueSet/device-statement-status
message DeviceUseStatementStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/device-statement-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    COMPLETED = 2;
    ENTERED_IN_ERROR = 3;
    INTENDED = 4;
    STOPPED = 5;
    ON_HOLD = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The availability status of the device.
// See http://hl7.org/fhir/ValueSet/device-status
message FHIRDeviceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/device-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    ENTERED_IN_ERROR = 3;
    UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that can be used to express the role of
// a diagnosis on the Encounter or EpisodeOfCare record. See
// http://hl7.org/fhir/ValueSet/diagnosis-role
message DiagnosisRoleCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/diagnosis-role";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AD = 1 [(fhir_original_code) = "AD"];
    DD = 2 [(fhir_original_code) = "DD"];
    CC = 3 [(fhir_original_code) = "CC"];
    CM = 4 [(fhir_original_code) = "CM"];
    PRE_OP = 5;
    POST_OP = 6;
    BILLING = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of the diagnostic report as a whole.
// See http://hl7.org/fhir/ValueSet/diagnostic-report-status
message DiagnosticReportStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/diagnostic-report-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REGISTERED = 1;
    PARTIAL = 2;
    PRELIMINARY = 3;
    FINAL = 4;
    AMENDED = 5;
    CORRECTED = 6;
    APPENDED = 7;
    CANCELLED = 8;
    ENTERED_IN_ERROR = 9;
    UNKNOWN = 10;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Whether the media is a photo, video, or audio
// See http://hl7.org/fhir/ValueSet/digital-media-type
message DigitalMediaTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/digital-media-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PHOTO = 1;
    VIDEO = 2;
    AUDIO = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How an element value is interpreted when discrimination is evaluated
// See http://hl7.org/fhir/ValueSet/discriminator-type
message DiscriminatorTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/discriminator-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    VALUE = 1;
    EXISTS = 2;
    PATTERN = 3;
    TYPE = 4;
    PROFILE = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Whether the application produces or consumes documents.
// See http://hl7.org/fhir/ValueSet/document-mode
message DocumentModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/document-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PRODUCER = 1;
    CONSUMER = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of the document reference.
// See http://hl7.org/fhir/ValueSet/document-reference-status
message DocumentReferenceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/document-reference-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CURRENT = 1;
    SUPERSEDED = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of relationship between documents.
// See http://hl7.org/fhir/ValueSet/document-relationship-type
message DocumentRelationshipTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/document-relationship-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REPLACES = 1;
    TRANSFORMS = 2;
    SIGNS = 3;
    APPENDS = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that can be used to indicate from where
// the patient came in. See http://hl7.org/fhir/ValueSet/encounter-admit-source
message AdmitSourceCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/encounter-admit-source";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HOSP_TRANS = 1;
    EMD = 2;
    OUTP = 3;
    BORN = 4;
    GP = 5;
    MP = 6;
    NURSING = 7;
    PSYCH = 8;
    REHAB = 9;
    OTHER = 10;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that can be used to indicate dietary
// preferences or restrictions a patient may have. See
// http://hl7.org/fhir/ValueSet/encounter-diet
message DietCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/encounter-diet";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    VEGETARIAN = 1;
    DAIRY_FREE = 2;
    NUT_FREE = 3;
    GLUTEN_FREE = 4;
    VEGAN = 5;
    HALAL = 6;
    KOSHER = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that can be used to where the patient
// left the hospital. See
// http://hl7.org/fhir/ValueSet/encounter-discharge-disposition
message DischargeDispositionCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/encounter-discharge-disposition";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HOME = 1;
    OTHER_HCF = 2;
    HOSP = 3;
    LONG = 4;
    AADVICE = 5;
    EXP = 6;
    PSY = 7;
    REHAB = 8;
    SNF = 9;
    OTH = 10;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of the location.
// See http://hl7.org/fhir/ValueSet/encounter-location-status
message EncounterLocationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/encounter-location-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PLANNED = 1;
    ACTIVE = 2;
    RESERVED = 3;
    COMPLETED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that can be used to indicate the kinds
// of special arrangements in place for a patients visit. See
// http://hl7.org/fhir/ValueSet/encounter-special-arrangements
message SpecialArrangementsCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/encounter-special-arrangements";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    WHEEL = 1;
    ADD_BED = 2;
    INT = 3;
    ATT = 4;
    DOG = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Current state of the encounter
// See http://hl7.org/fhir/ValueSet/encounter-status
message EncounterStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/encounter-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PLANNED = 1;
    ARRIVED = 2;
    TRIAGED = 3;
    IN_PROGRESS = 4;
    ONLEAVE = 5;
    FINISHED = 6;
    CANCELLED = 7;
    ENTERED_IN_ERROR = 8;
    UNKNOWN = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate
// the type of encounter: a specific code indicating type of service provided.
// See http://hl7.org/fhir/ValueSet/encounter-type
message EncounterTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/encounter-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ADMS = 1 [(fhir_original_code) = "ADMS"];
    BD_BM_CLIN = 2 [(fhir_original_code) = "BD/BM-clin"];
    CCS60 = 3 [(fhir_original_code) = "CCS60"];
    OKI = 4 [(fhir_original_code) = "OKI"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of the endpoint
// See http://hl7.org/fhir/ValueSet/endpoint-status
message EndpointStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/endpoint-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    SUSPENDED = 2;
    ERROR = 3;
    OFF = 4;
    ENTERED_IN_ERROR = 5;
    TEST = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// EnteralFormulaAdditiveType: Codes for the type of modular component such as
// protein, carbohydrate or fiber to be provided in addition to or mixed with
// the base formula. This value set is provided as a suggestive example. See
// http://hl7.org/fhir/ValueSet/entformula-additive
message EnteralFormulaAdditiveTypeCodeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/entformula-additive";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LIPID = 1;
    PROTEIN = 2;
    CARBOHYDRATE = 3;
    FIBER = 4;
    WATER = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of the episode of care.
// See http://hl7.org/fhir/ValueSet/episode-of-care-status
message EpisodeOfCareStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/episode-of-care-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PLANNED = 1;
    WAITLIST = 2;
    ACTIVE = 3;
    ONHOLD = 4;
    FINISHED = 5;
    CANCELLED = 6;
    ENTERED_IN_ERROR = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to express the
// usage type of an EpisodeOfCare record. See
// http://hl7.org/fhir/ValueSet/episodeofcare-type
message EpisodeOfCareTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/episodeofcare-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HACC = 1;
    PAC = 2;
    DIAB = 3;
    DA = 4;
    CACP = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The mode of a message capability statement.
// See http://hl7.org/fhir/ValueSet/event-capability-mode
message EventCapabilityModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/event-capability-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SENDER = 1;
    RECEIVER = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes identifying the stage lifecycle stage of a event
// See http://hl7.org/fhir/ValueSet/event-status
message EventStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/event-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PREPARATION = 1;
    IN_PROGRESS = 2;
    SUSPENDED = 3;
    ABORTED = 4;
    COMPLETED = 5;
    ENTERED_IN_ERROR = 6;
    UNKNOWN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Real world event relating to the schedule.
// See http://hl7.org/fhir/ValueSet/event-timing
message EventTimingCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/event-timing";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MORN = 1 [(fhir_original_code) = "MORN"];
    AFT = 2 [(fhir_original_code) = "AFT"];
    EVE = 3 [(fhir_original_code) = "EVE"];
    NIGHT = 4 [(fhir_original_code) = "NIGHT"];
    PHS = 5 [(fhir_original_code) = "PHS"];
    HS = 6 [(fhir_original_code) = "HS"];
    WAKE = 7 [(fhir_original_code) = "WAKE"];
    C = 8 [(fhir_original_code) = "C"];
    CM = 9 [(fhir_original_code) = "CM"];
    CD = 10 [(fhir_original_code) = "CD"];
    CV = 11 [(fhir_original_code) = "CV"];
    AC = 12 [(fhir_original_code) = "AC"];
    ACM = 13 [(fhir_original_code) = "ACM"];
    ACD = 14 [(fhir_original_code) = "ACD"];
    ACV = 15 [(fhir_original_code) = "ACV"];
    PC = 16 [(fhir_original_code) = "PC"];
    PCM = 17 [(fhir_original_code) = "PCM"];
    PCD = 18 [(fhir_original_code) = "PCD"];
    PCV = 19 [(fhir_original_code) = "PCV"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of Claim payee Resource
// See http://hl7.org/fhir/ValueSet/ex-payee-resource-type
message ClaimPayeeResourceTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/ex-payee-resource-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ORGANIZATION = 1;
    PATIENT = 2;
    PRACTITIONER = 3;
    RELATEDPERSON = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code specifying the state of the resource instance.
// See http://hl7.org/fhir/ValueSet/explanationofbenefit-status
message ExplanationOfBenefitStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/explanationofbenefit-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    CANCELLED = 2;
    DRAFT = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How an extension context is interpreted.
// See http://hl7.org/fhir/ValueSet/extension-context
message ExtensionContextCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/extension-context";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    RESOURCE = 1;
    DATATYPE = 2;
    EXTENSION = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The kind of operation to perform as a part of a property based filter.
// See http://hl7.org/fhir/ValueSet/filter-operator
message FilterOperatorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/filter-operator";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    EQUALS = 1 [(fhir_original_code) = "="];
    IS_A = 2;
    DESCENDENT_OF = 3;
    IS_NOT_A = 4;
    REGEX = 5;
    IN = 6;
    NOT_IN = 7;
    GENERALIZES = 8;
    EXISTS = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Example list of general categories for flagged issues. (Not complete or
// necessarily appropriate.) See http://hl7.org/fhir/ValueSet/flag-category
message FlagCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/flag-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DIET = 1;
    DRUG = 2;
    LAB = 3;
    ADMIN = 4;
    CONTACT = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set is provided as an exemplar. The value set is driven by IHE
// Table B.8-4: Abnormal Flags, Alert Priority. See
// http://hl7.org/fhir/ValueSet/flag-priority
message FlagPriorityCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/flag-priority";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PN = 1 [(fhir_original_code) = "PN"];
    PL = 2 [(fhir_original_code) = "PL"];
    PM = 3 [(fhir_original_code) = "PM"];
    PH = 4 [(fhir_original_code) = "PH"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether this flag is active and needs to be displayed to a user, or
// whether it is no longer needed or entered in error. See
// http://hl7.org/fhir/ValueSet/flag-status
message FlagStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/flag-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set includes Status codes.
// See http://hl7.org/fhir/ValueSet/fm-status
message FinancialResourceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/fm-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    CANCELLED = 2;
    DRAFT = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes indicating whether the goal has been accepted by a stakeholder
// See http://hl7.org/fhir/ValueSet/goal-acceptance-status
message GoalAcceptanceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/goal-acceptance-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AGREE = 1;
    DISAGREE = 2;
    PENDING = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Example codes for grouping goals for filtering or presentation.
// See http://hl7.org/fhir/ValueSet/goal-category
message GoalCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/goal-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DIETARY = 1;
    SAFETY = 2;
    BEHAVIORAL = 3;
    NURSING = 4;
    PHYSIOTHERAPY = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the level of importance associated with reaching or sustaining a
// goal. See http://hl7.org/fhir/ValueSet/goal-priority
message GoalPriorityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/goal-priority";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HIGH_PRIORITY = 1;
    MEDIUM_PRIORITY = 2;
    LOW_PRIORITY = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Types of relationships between two goals
// See http://hl7.org/fhir/ValueSet/goal-relationship-type
message GoalRelationshipTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/goal-relationship-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PREDECESSOR = 1;
    SUCCESSOR = 2;
    REPLACEMENT = 3;
    MILESTONE = 4;
    OTHER = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether the goal has been met and is still being targeted
// See http://hl7.org/fhir/ValueSet/goal-status
message GoalStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/goal-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSED = 1;
    ACCEPTED = 2;
    PLANNED = 3;
    IN_PROGRESS = 4;
    ON_TARGET = 5;
    AHEAD_OF_TARGET = 6;
    BEHIND_TARGET = 7;
    SUSTAINING = 8;
    ACHIEVED = 9;
    ON_HOLD = 10;
    CANCELLED = 11;
    ENTERED_IN_ERROR = 12;
    REJECTED = 13;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Example codes indicating the reason for a current status.  Note that these
// are in no way complete and may not even be appropriate for some uses. See
// http://hl7.org/fhir/ValueSet/goal-status-reason
message GoalStatusReasonCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/goal-status-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SURGERY = 1;
    LIFE_EVENT = 2;
    REPLACED = 3;
    PATIENT_REQUEST = 4;
    TEMP_NOT_ATTAINABLE = 5;
    PERMANENT_NOT_ATTAINABLE = 6;
    FINANCIAL_BARRIER = 7;
    LACK_OF_TRANSPORTATION = 8;
    LACK_OF_SOCIAL_SUPPORT = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a compartment must be linked
// See http://hl7.org/fhir/ValueSet/graph-compartment-rule
message GraphCompartmentRuleCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/graph-compartment-rule";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    IDENTICAL = 1;
    MATCHING = 2;
    DIFFERENT = 3;
    CUSTOM = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Types of resources that are part of group
// See http://hl7.org/fhir/ValueSet/group-type
message GroupTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/group-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PERSON = 1;
    ANIMAL = 2;
    PRACTITIONER = 3;
    DEVICE = 4;
    MEDICATION = 5;
    SUBSTANCE = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of a guidance response
// See http://hl7.org/fhir/ValueSet/guidance-response-status
message GuidanceResponseStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/guidance-response-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SUCCESS = 1;
    DATA_REQUESTED = 2;
    DATA_REQUIRED = 3;
    IN_PROGRESS = 4;
    FAILURE = 5;
    ENTERED_IN_ERROR = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a dependency is represented when the guide is published.
// See http://hl7.org/fhir/ValueSet/guide-dependency-type
message GuideDependencyTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/guide-dependency-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REFERENCE = 1;
    INCLUSION = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The kind of an included page.
// See http://hl7.org/fhir/ValueSet/guide-page-kind
message GuidePageKindCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/guide-page-kind";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PAGE = 1;
    EXAMPLE = 2;
    LIST = 3;
    INCLUDE = 4;
    DIRECTORY = 5;
    DICTIONARY = 6;
    TOC = 7;
    RESOURCE = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes describing the reason why a family member history was not done.
// See http://hl7.org/fhir/ValueSet/history-not-done-reason
message FamilyHistoryNotDoneReasonCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/history-not-done-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SUBJECT_UNKNOWN = 1;
    WITHHELD = 2;
    UNABLE_TO_OBTAIN = 3;
    DEFERRED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that identifies the status of the family history record.
// See http://hl7.org/fhir/ValueSet/history-status
message FamilyHistoryStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/history-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PARTIAL = 1;
    COMPLETED = 2;
    ENTERED_IN_ERROR = 3;
    HEALTH_UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// An HL7 administrative unit that owns artifacts in the FHIR specification
// See http://hl7.org/fhir/ValueSet/hl7-work-group
message HL7WorkgroupCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/hl7-work-group";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CBCC = 1;
    CDS = 2;
    CQI = 3;
    CG = 4;
    DEV = 5;
    EHR = 6;
    FHIR = 7;
    FM = 8;
    HSI = 9;
    II = 10;
    INM = 11;
    ITS = 12;
    OO = 13;
    PA = 14;
    PC = 15;
    PHER = 16;
    PHX = 17;
    RCRIM = 18;
    SD = 19;
    SEC = 20;
    US = 21;
    VOCAB = 22;
    AID = 23;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The allowable request method or HTTP operation codes.
// See http://hl7.org/fhir/ValueSet/http-operations
message TestScriptRequestMethodCodeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/http-operations";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DELETE = 1;
    GET = 2;
    OPTIONS = 3;
    PATCH = 4;
    POST = 5;
    PUT = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// HTTP verbs (in the HTTP command line).
// See http://hl7.org/fhir/ValueSet/http-verb
message HTTPVerbCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/http-verb";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    GET = 1 [(fhir_original_code) = "GET"];
    POST = 2 [(fhir_original_code) = "POST"];
    PUT = 3 [(fhir_original_code) = "PUT"];
    DELETE = 4 [(fhir_original_code) = "DELETE"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identifies the purpose for this identifier, if known .
// See http://hl7.org/fhir/ValueSet/identifier-use
message IdentifierUseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/identifier-use";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    USUAL = 1;
    OFFICIAL = 2;
    TEMP = 3;
    SECONDARY = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The level of confidence that this link represents the same actual person,
// based on NIST Authentication Levels. See
// http://hl7.org/fhir/ValueSet/identity-assuranceLevel
message IdentityAssuranceLevelCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/identity-assuranceLevel";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LEVEL1 = 1;
    LEVEL2 = 2;
    LEVEL3 = 3;
    LEVEL4 = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The value set to instantiate this attribute should be drawn from a
// terminologically robust code system that consists of or contains concepts to
// support describing the source of the data when the report of the immunization
// event is not based on information from the person, entity or organization who
// administered the vaccine. This value set is provided as a suggestive example.
// See http://hl7.org/fhir/ValueSet/immunization-origin
message ImmunizationOriginCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/immunization-origin";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROVIDER = 1;
    RECORD = 2;
    RECALL = 3;
    SCHOOL = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The value set to instantiate this attribute should be drawn from a
// terminologically robust code system that consists of or contains concepts to
// support the definition of dates relevant to recommendations for future doses
// of vaccines. This value set is provided as a suggestive example. See
// http://hl7.org/fhir/ValueSet/immunization-recommendation-date-criterion
message ImmunizationRecommendationDateCriterionCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/immunization-recommendation-date-criterion";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DUE = 1;
    RECOMMENDED = 2;
    EARLIEST = 3;
    OVERDUE = 4;
    LATEST = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The value set to instantiate this attribute should be drawn from a
// terminologically robust code system that consists of or contains concepts to
// support describing the status of the patient towards perceived immunity
// against a vaccine preventable disease. This value set is provided as a
// suggestive example. See
// http://hl7.org/fhir/ValueSet/immunization-recommendation-status
message ImmunizationRecommendationStatusCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/immunization-recommendation-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DUE = 1;
    OVERDUE = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The value set to instantiate this attribute should be drawn from a
// terminologically robust code system that consists of or contains concepts to
// support describing the current status of the administered dose of vaccine.
// See http://hl7.org/fhir/ValueSet/immunization-status
message ImmunizationStatusCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/immunization-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    COMPLETED = 1;
    ENTERED_IN_ERROR = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A set codes that define the functional status of an implanted device.
// See http://hl7.org/fhir/ValueSet/implant-status
message ImplantStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/implant-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FUNCTIONAL = 1;
    NON_FUNCTIONAL = 2;
    DISABLED = 3;
    UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Availability of the resource
// See http://hl7.org/fhir/ValueSet/instance-availability
message InstanceAvailabilityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/instance-availability";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ONLINE = 1 [(fhir_original_code) = "ONLINE"];
    OFFLINE = 2 [(fhir_original_code) = "OFFLINE"];
    NEARLINE = 3 [(fhir_original_code) = "NEARLINE"];
    UNAVAILABLE = 4 [(fhir_original_code) = "UNAVAILABLE"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How the issue affects the success of the action.
// See http://hl7.org/fhir/ValueSet/issue-severity
message IssueSeverityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/issue-severity";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FATAL = 1;
    ERROR = 2;
    WARNING = 3;
    INFORMATION = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that describes the type of issue.
// See http://hl7.org/fhir/ValueSet/issue-type
message IssueTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/issue-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INVALID = 1;
    STRUCTURE = 2;
    REQUIRED = 3;
    VALUE = 4;
    INVARIANT = 5;
    SECURITY = 6;
    LOGIN = 7;
    UNKNOWN = 8;
    EXPIRED = 9;
    FORBIDDEN = 10;
    SUPPRESSED = 11;
    PROCESSING = 12;
    NOT_SUPPORTED = 13;
    DUPLICATE = 14;
    NOT_FOUND = 15;
    TOO_LONG = 16;
    CODE_INVALID = 17;
    EXTENSION = 18;
    TOO_COSTLY = 19;
    BUSINESS_RULE = 20;
    CONFLICT = 21;
    INCOMPLETE = 22;
    TRANSIENT = 23;
    LOCK_ERROR = 24;
    NO_STORE = 25;
    EXCEPTION = 26;
    TIMEOUT = 27;
    THROTTLED = 28;
    INFORMATIONAL = 29;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Distinguishes groups from questions and display text and indicates data type
// for questions See http://hl7.org/fhir/ValueSet/item-type
message QuestionnaireItemTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/item-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    GROUP = 1;
    DISPLAY = 2;
    QUESTION = 3;
    BOOLEAN = 4;
    DECIMAL = 5;
    INTEGER = 6;
    DATE = 7;
    DATE_TIME = 8 [(fhir_original_code) = "dateTime"];
    TIME = 9;
    STRING = 10;
    TEXT = 11;
    URL = 12;
    CHOICE = 13;
    OPEN_CHOICE = 14;
    ATTACHMENT = 15;
    REFERENCE = 16;
    QUANTITY = 17;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of knowledge asset this library contains
// See http://hl7.org/fhir/ValueSet/library-type
message LibraryTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/library-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LOGIC_LIBRARY = 1;
    MODEL_DEFINITION = 2;
    ASSET_COLLECTION = 3;
    MODULE_DEFINITION = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Used to distinguish different roles a resource can play within a set of
// linked resources See http://hl7.org/fhir/ValueSet/linkage-type
message LinkageTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/linkage-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SOURCE = 1;
    ALTERNATE = 2;
    HISTORICAL = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of link between this patient resource and another patient resource.
// See http://hl7.org/fhir/ValueSet/link-type
message LinkTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/link-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REPLACED_BY = 1;
    REPLACES = 2;
    REFER = 3;
    SEEALSO = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// General reasons for a list to be empty. Reasons are either related to a
// summary list (i.e. problem or medication list) or to a workflow related list
// (i.e. consultation list). See http://hl7.org/fhir/ValueSet/list-empty-reason
message ListEmptyReasonsCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/list-empty-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NILKNOWN = 1;
    NOTASKED = 2;
    WITHHELD = 3;
    UNAVAILABLE = 4;
    NOTSTARTED = 5;
    CLOSED = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Example use codes for the List resource - typical kinds of use.
// See http://hl7.org/fhir/ValueSet/list-example-codes
message ExampleUseCodesforListCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/list-example-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ALERTS = 1;
    ADVERSERXNS = 2;
    ALLERGIES = 3;
    MEDICATIONS = 4;
    PROBLEMS = 5;
    WORKLIST = 6;
    WAITING = 7;
    PROTOCOLS = 8;
    PLANS = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The processing mode that applies to this list
// See http://hl7.org/fhir/ValueSet/list-mode
message ListModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/list-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    WORKING = 1;
    SNAPSHOT = 2;
    CHANGES = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Base values for the order of the items in a list resource.
// See http://hl7.org/fhir/ValueSet/list-order
message ListOrderCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/list-order";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    USER = 1;
    SYSTEM = 2;
    EVENT_DATE = 3;
    ENTRY_DATE = 4;
    PRIORITY = 5;
    ALPHABETIC = 6;
    CATEGORY = 7;
    PATIENT = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The current state of the list
// See http://hl7.org/fhir/ValueSet/list-status
message ListStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/list-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CURRENT = 1;
    RETIRED = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether a resource instance represents a specific location or a
// class of locations. See http://hl7.org/fhir/ValueSet/location-mode
message LocationModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/location-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INSTANCE = 1;
    KIND = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate
// the physical form of the Location. See
// http://hl7.org/fhir/ValueSet/location-physical-type
message LocationTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/location-physical-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SI = 1;
    BU = 2;
    WI = 3;
    WA = 4;
    LVL = 5;
    CO = 6;
    RO = 7;
    BD = 8;
    VE = 9;
    HO = 10;
    CA = 11;
    RD = 12;
    AREA = 13;
    JDN = 14;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates whether the location is still in use.
// See http://hl7.org/fhir/ValueSet/location-status
message LocationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/location-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    SUSPENDED = 2;
    INACTIVE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How to interpret the context
// See http://hl7.org/fhir/ValueSet/map-context-type
message StructureMapContextTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/map-context-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TYPE = 1;
    VARIABLE = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// If this is the default rule set to apply for the source type, or this
// combination of types See http://hl7.org/fhir/ValueSet/map-group-type-mode
message StructureMapGroupTypeModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/map-group-type-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NONE = 1;
    TYPES = 2;
    TYPE_AND_TYPES = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Mode for this instance of data
// See http://hl7.org/fhir/ValueSet/map-input-mode
message StructureMapInputModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/map-input-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SOURCE = 1;
    TARGET = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How the referenced structure is used in this mapping
// See http://hl7.org/fhir/ValueSet/map-model-mode
message StructureMapModelModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/map-model-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SOURCE = 1;
    QUERIED = 2;
    TARGET = 3;
    PRODUCED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// If field is a list, how to manage the source
// See http://hl7.org/fhir/ValueSet/map-source-list-mode
message StructureMapSourceListModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/map-source-list-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FIRST = 1;
    NOT_FIRST = 2 [(fhir_original_code) = "not_first"];
    LAST = 3;
    NOT_LAST = 4 [(fhir_original_code) = "not_last"];
    ONLY_ONE = 5 [(fhir_original_code) = "only_one"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// If field is a list, how to manage the production
// See http://hl7.org/fhir/ValueSet/map-target-list-mode
message StructureMapTargetListModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/map-target-list-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FIRST = 1;
    SHARE = 2;
    LAST = 3;
    COLLATE = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How data is copied/created
// See http://hl7.org/fhir/ValueSet/map-transform
message StructureMapTransformCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/map-transform";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CREATE = 1;
    COPY = 2;
    TRUNCATE = 3;
    ESCAPE = 4;
    CAST = 5;
    APPEND = 6;
    TRANSLATE = 7;
    REFERENCE = 8;
    DATE_OP = 9 [(fhir_original_code) = "dateOp"];
    UUID = 10;
    POINTER = 11;
    EVALUATE = 12;
    CC = 13;
    C = 14;
    QTY = 15;
    ID = 16;
    CP = 17;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A Master Patient Index (MPI) assessment of whether a candidate patient record
// is a match or not. See http://hl7.org/fhir/ValueSet/match-grade
message MatchGradeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/match-grade";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CERTAIN = 1;
    PROBABLE = 2;
    POSSIBLE = 3;
    CERTAINLY_NOT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The intended usage for supplemental data elements in the measure
// See http://hl7.org/fhir/ValueSet/measure-data-usage
message MeasureDataUsageCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/measure-data-usage";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SUPPLEMENTAL_DATA = 1;
    RISK_ADJUSTMENT_FACTOR = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Different measurement principle supported by the device.
// See http://hl7.org/fhir/ValueSet/measurement-principle
message MeasmntPrincipleCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/measurement-principle";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OTHER = 1;
    CHEMICAL = 2;
    ELECTRICAL = 3;
    IMPEDANCE = 4;
    NUCLEAR = 5;
    OPTICAL = 6;
    THERMAL = 7;
    BIOLOGICAL = 8;
    MECHANICAL = 9;
    ACOUSTICAL = 10;
    MANUAL = 11;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of population
// See http://hl7.org/fhir/ValueSet/measure-population
message MeasurePopulationTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/measure-population";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INITIAL_POPULATION = 1;
    NUMERATOR = 2;
    NUMERATOR_EXCLUSION = 3;
    DENOMINATOR = 4;
    DENOMINATOR_EXCLUSION = 5;
    DENOMINATOR_EXCEPTION = 6;
    MEASURE_POPULATION = 7;
    MEASURE_POPULATION_EXCLUSION = 8;
    MEASURE_OBSERVATION = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of the measure report
// See http://hl7.org/fhir/ValueSet/measure-report-status
message MeasureReportStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/measure-report-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    COMPLETE = 1;
    PENDING = 2;
    ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of the measure report
// See http://hl7.org/fhir/ValueSet/measure-report-type
message MeasureReportTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/measure-report-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INDIVIDUAL = 1;
    PATIENT_LIST = 2;
    SUMMARY = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The scoring type of the measure
// See http://hl7.org/fhir/ValueSet/measure-scoring
message MeasureScoringCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/measure-scoring";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPORTION = 1;
    RATIO = 2;
    CONTINUOUS_VARIABLE = 3;
    COHORT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of measure (includes codes from 2.16.840.1.113883.1.11.20368)
// See http://hl7.org/fhir/ValueSet/measure-type
message MeasureTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/measure-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROCESS = 1;
    OUTCOME = 2;
    STRUCTURE = 3;
    PATIENT_REPORTED_OUTCOME = 4;
    COMPOSITE = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept describing where the medication administered is expected to
// occur See http://hl7.org/fhir/ValueSet/medication-admin-category
message MedicationAdministrationCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-admin-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INPATIENT = 1;
    OUTPATIENT = 2;
    COMMUNITY = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A set of codes indicating the current status of a MedicationAdministration.
// See http://hl7.org/fhir/ValueSet/medication-admin-status
message MedicationAdministrationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-admin-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    IN_PROGRESS = 1;
    ON_HOLD = 2;
    COMPLETED = 3;
    ENTERED_IN_ERROR = 4;
    STOPPED = 5;
    UNKNOWN = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code describing where the dispensed medication is expected to be consumed
// or administered See http://hl7.org/fhir/ValueSet/medication-dispense-category
message MedicationDispenseCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-dispense-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INPATIENT = 1;
    OUTPATIENT = 2;
    COMMUNITY = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept specifying the state of the dispense event.
// See http://hl7.org/fhir/ValueSet/medication-dispense-status
message MedicationDispenseStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-dispense-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PREPARATION = 1;
    IN_PROGRESS = 2;
    ON_HOLD = 3;
    COMPLETED = 4;
    ENTERED_IN_ERROR = 5;
    STOPPED = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept defining the kind of container a medication package is
// packaged in See http://hl7.org/fhir/ValueSet/medication-package-form
message MedicationContainerCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-package-form";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AMPOULE = 1;
    BOTTLE = 2;
    BOX = 3;
    CARTRIDGE = 4;
    CONTAINER = 5;
    TUBE = 6;
    UNITDOSE = 7;
    VIAL = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept identifying where the medication ordered is expected to be
// consumed or administered See
// http://hl7.org/fhir/ValueSet/medication-request-category
message MedicationRequestCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-request-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INPATIENT = 1;
    OUTPATIENT = 2;
    COMMUNITY = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The kind of medication order
// See http://hl7.org/fhir/ValueSet/medication-request-intent
message MedicationRequestIntentCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-request-intent";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSAL = 1;
    PLAN = 2;
    ORDER = 3;
    INSTANCE_ORDER = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identifies the level of importance to be assigned to actioning the request
// See http://hl7.org/fhir/ValueSet/medication-request-priority
message MedicationRequestPriorityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-request-priority";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ROUTINE = 1;
    URGENT = 2;
    STAT = 3;
    ASAP = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept specifying the state of the prescribing event. Describes the
// lifecycle of the prescription See
// http://hl7.org/fhir/ValueSet/medication-request-status
message MedicationRequestStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-request-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    ON_HOLD = 2;
    CANCELLED = 3;
    COMPLETED = 4;
    ENTERED_IN_ERROR = 5;
    STOPPED = 6;
    DRAFT = 7;
    UNKNOWN = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept identifying where the medication included in the
// medicationstatement is expected to be consumed or administered See
// http://hl7.org/fhir/ValueSet/medication-statement-category
message MedicationStatementCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-statement-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INPATIENT = 1;
    OUTPATIENT = 2;
    COMMUNITY = 3;
    PATIENTSPECIFIED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept indicating the current status of a MedicationStatement.
// See http://hl7.org/fhir/ValueSet/medication-statement-status
message MedicationStatementStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-statement-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    COMPLETED = 2;
    ENTERED_IN_ERROR = 3;
    INTENDED = 4;
    STOPPED = 5;
    ON_HOLD = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept identifying level of certainty if patient has taken or has
// not taken the medication See
// http://hl7.org/fhir/ValueSet/medication-statement-taken
message MedicationStatementTakenCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/medication-statement-taken";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    Y = 1;
    N = 2;
    UNK = 3;
    NA = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept defining if the medication is in active use
// See http://hl7.org/fhir/ValueSet/medication-status
message MedicationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/medication-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// One of the message events defined as part of FHIR.
// See http://hl7.org/fhir/ValueSet/message-events
message MessageEventCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/message-events";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CODE_SYSTEM_EXPAND = 1 [(fhir_original_code) = "CodeSystem-expand"];
    MEDICATION_ADMINISTRATION_COMPLETE = 2
        [(fhir_original_code) = "MedicationAdministration-Complete"];
    MEDICATION_ADMINISTRATION_NULLIFICATION = 3
        [(fhir_original_code) = "MedicationAdministration-Nullification"];
    MEDICATION_ADMINISTRATION_RECORDING = 4
        [(fhir_original_code) = "MedicationAdministration-Recording"];
    MEDICATION_ADMINISTRATION_UPDATE = 5
        [(fhir_original_code) = "MedicationAdministration-Update"];
    ADMIN_NOTIFY = 6;
    COMMUNICATION_REQUEST = 7;
    DIAGNOSTICREPORT_PROVIDE = 8;
    OBSERVATION_PROVIDE = 9;
    PATIENT_LINK = 10;
    PATIENT_UNLINK = 11;
    VALUESET_EXPAND = 12;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Example Message Reasons. These are the set of codes that might be used an
// updating an encounter using admin-update. See
// http://hl7.org/fhir/ValueSet/message-reason-encounter
message ExampleMessageReasonCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/message-reason-encounter";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ADMIT = 1;
    DISCHARGE = 2;
    ABSENT = 3;
    RETURN = 4;
    MOVED = 5;
    EDIT = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The impact of the content of a message.
// See http://hl7.org/fhir/ValueSet/message-significance-category
message MessageSignificanceCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/message-significance-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CONSEQUENCE = 1 [(fhir_original_code) = "Consequence"];
    CURRENCY = 2 [(fhir_original_code) = "Currency"];
    NOTIFICATION = 3 [(fhir_original_code) = "Notification"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The protocol used for message transport.
// See http://hl7.org/fhir/ValueSet/message-transport
message MessageTransportCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/message-transport";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HTTP = 1;
    FTP = 2;
    MLLP = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Describes the state of a metric calibration.
// See http://hl7.org/fhir/ValueSet/metric-calibration-state
message DeviceMetricCalibrationStateCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/metric-calibration-state";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NOT_CALIBRATED = 1;
    CALIBRATION_REQUIRED = 2;
    CALIBRATED = 3;
    UNSPECIFIED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Describes the type of a metric calibration.
// See http://hl7.org/fhir/ValueSet/metric-calibration-type
message DeviceMetricCalibrationTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/metric-calibration-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UNSPECIFIED = 1;
    OFFSET = 2;
    GAIN = 3;
    TWO_POINT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Describes the category of the metric.
// See http://hl7.org/fhir/ValueSet/metric-category
message DeviceMetricCategoryCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/metric-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MEASUREMENT = 1;
    SETTING = 2;
    CALCULATION = 3;
    UNSPECIFIED = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Describes the typical color of representation.
// See http://hl7.org/fhir/ValueSet/metric-color
message DeviceMetricColorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/metric-color";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    BLACK = 1;
    RED = 2;
    GREEN = 3;
    YELLOW = 4;
    BLUE = 5;
    MAGENTA = 6;
    CYAN = 7;
    WHITE = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Describes the operational status of the DeviceMetric.
// See http://hl7.org/fhir/ValueSet/metric-operational-status
message DeviceMetricOperationalStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/metric-operational-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ON = 1;
    OFF = 2;
    STANDBY = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that represents the preferred display order of the components of a
// human name See http://hl7.org/fhir/ValueSet/name-assembly-order
message HumanNameAssemblyOrderCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/name-assembly-order";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NL1 = 1 [(fhir_original_code) = "NL1"];
    NL2 = 2 [(fhir_original_code) = "NL2"];
    NL3 = 3 [(fhir_original_code) = "NL3"];
    NL4 = 4 [(fhir_original_code) = "NL4"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The use of a human name
// See http://hl7.org/fhir/ValueSet/name-use
message NameUseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/name-use";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    USUAL = 1;
    OFFICIAL = 2;
    TEMP = 3;
    NICKNAME = 4;
    ANONYMOUS = 5;
    OLD = 6;
    MAIDEN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identifies the style of unique identifier used to identify a namespace.
// See http://hl7.org/fhir/ValueSet/namingsystem-identifier-type
message NamingSystemIdentifierTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/namingsystem-identifier-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OID = 1;
    UUID = 2;
    URI = 3;
    OTHER = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identifies the purpose of the naming system.
// See http://hl7.org/fhir/ValueSet/namingsystem-type
message NamingSystemTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/namingsystem-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CODESYSTEM = 1;
    IDENTIFIER = 2;
    ROOT = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of a resource narrative
// See http://hl7.org/fhir/ValueSet/narrative-status
message NarrativeStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/narrative-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    GENERATED = 1;
    EXTENSIONS = 2;
    ADDITIONAL = 3;
    EMPTY = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of network access point of this agent in the audit event
// See http://hl7.org/fhir/ValueSet/network-type
message AuditEventAgentNetworkTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/network-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MACHINE_NAME = 1 [(fhir_original_code) = "1"];
    IP_ADDRESS = 2 [(fhir_original_code) = "2"];
    TELEPHONE_NUMBER = 3 [(fhir_original_code) = "3"];
    EMAIL_ADDRESS = 4 [(fhir_original_code) = "4"];
    URI = 5 [(fhir_original_code) = "5"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The presentation types of notes.
// See http://hl7.org/fhir/ValueSet/note-type
message NoteTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/note-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DISPLAY = 1;
    PRINT = 2;
    PRINTOPER = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes specifying the state of the request. Describes the lifecycle of the
// nutrition order. See http://hl7.org/fhir/ValueSet/nutrition-request-status
message NutritionOrderStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/nutrition-request-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSED = 1;
    DRAFT = 2;
    PLANNED = 3;
    REQUESTED = 4;
    ACTIVE = 5;
    ON_HOLD = 6;
    COMPLETED = 7;
    CANCELLED = 8;
    ENTERED_IN_ERROR = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Observation Category codes.
// See http://hl7.org/fhir/ValueSet/observation-category
message ObservationCategoryCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/observation-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SOCIAL_HISTORY = 1;
    VITAL_SIGNS = 2;
    IMAGING = 3;
    LABORATORY = 4;
    PROCEDURE = 5;
    SURVEY = 6;
    EXAM = 7;
    THERAPY = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes specifying how two observations are related.
// See http://hl7.org/fhir/ValueSet/observation-relationshiptypes
message ObservationRelationshipTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/observation-relationshiptypes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HAS_MEMBER = 1;
    DERIVED_FROM = 2;
    SEQUEL_TO = 3;
    REPLACES = 4;
    QUALIFIED_BY = 5;
    INTERFERED_BY = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The statistical operation parameter -\
// See http://hl7.org/fhir/ValueSet/observation-statistics
message StatisticsCodeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/observation-statistics";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AVERAGE = 1;
    MAXIMUM = 2;
    MINIMUM = 3;
    COUNT = 4;
    TOTALCOUNT = 5;
    MEDIAN = 6;
    STD_DEV = 7;
    SUM = 8;
    VARIANCE = 9;
    TWENTY_PERCENT = 10 [(fhir_original_code) = "20-percent"];
    EIGHTY_PERCENT = 11 [(fhir_original_code) = "80-percent"];
    FOUR_LOWER = 12 [(fhir_original_code) = "4-lower"];
    FOUR_UPPER = 13 [(fhir_original_code) = "4-upper"];
    FOUR_DEV = 14 [(fhir_original_code) = "4-dev"];
    FIVE_ONE = 15 [(fhir_original_code) = "5-1"];
    FIVE_TWO = 16 [(fhir_original_code) = "5-2"];
    FIVE_THREE = 17 [(fhir_original_code) = "5-3"];
    FIVE_FOUR = 18 [(fhir_original_code) = "5-4"];
    SKEW = 19;
    KURTOSIS = 20;
    REGRESSION = 21;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes providing the status of an observation.
// See http://hl7.org/fhir/ValueSet/observation-status
message ObservationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/observation-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REGISTERED = 1;
    PRELIMINARY = 2;
    FINAL = 3;
    AMENDED = 4;
    CORRECTED = 5;
    CANCELLED = 6;
    ENTERED_IN_ERROR = 7;
    UNKNOWN = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes representing the current status of the device - on, off, suspended,
// etc. See http://hl7.org/fhir/ValueSet/operational-status
message DeviceComponentOperationalStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/operational-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OFF = 1;
    ON = 2;
    NOT_READY = 3;
    STANDBY = 4;
    TRANSDUC_DISCON = 5;
    HW_DISCON = 6;
    ENTERED_IN_ERROR = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Whether an operation is a normal operation or a query.
// See http://hl7.org/fhir/ValueSet/operation-kind
message OperationKindCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/operation-kind";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OPERATION = 1;
    QUERY = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Operation Outcome codes used by FHIR test servers (see Implementation file
// translations.xml) See http://hl7.org/fhir/ValueSet/operation-outcome
message OperationOutcomeCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/operation-outcome";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MSG_AUTH_REQUIRED = 1 [(fhir_original_code) = "MSG_AUTH_REQUIRED"];
    MSG_BAD_FORMAT = 2 [(fhir_original_code) = "MSG_BAD_FORMAT"];
    MSG_BAD_SYNTAX = 3 [(fhir_original_code) = "MSG_BAD_SYNTAX"];
    MSG_CANT_PARSE_CONTENT = 4
        [(fhir_original_code) = "MSG_CANT_PARSE_CONTENT"];
    MSG_CANT_PARSE_ROOT = 5 [(fhir_original_code) = "MSG_CANT_PARSE_ROOT"];
    MSG_CREATED = 6 [(fhir_original_code) = "MSG_CREATED"];
    MSG_DATE_FORMAT = 7 [(fhir_original_code) = "MSG_DATE_FORMAT"];
    MSG_DELETED = 8 [(fhir_original_code) = "MSG_DELETED"];
    MSG_DELETED_DONE = 9 [(fhir_original_code) = "MSG_DELETED_DONE"];
    MSG_DELETED_ID = 10 [(fhir_original_code) = "MSG_DELETED_ID"];
    MSG_DUPLICATE_ID = 11 [(fhir_original_code) = "MSG_DUPLICATE_ID"];
    MSG_ERROR_PARSING = 12 [(fhir_original_code) = "MSG_ERROR_PARSING"];
    MSG_ID_INVALID = 13 [(fhir_original_code) = "MSG_ID_INVALID"];
    MSG_ID_TOO_LONG = 14 [(fhir_original_code) = "MSG_ID_TOO_LONG"];
    MSG_INVALID_ID = 15 [(fhir_original_code) = "MSG_INVALID_ID"];
    MSG_JSON_OBJECT = 16 [(fhir_original_code) = "MSG_JSON_OBJECT"];
    MSG_LOCAL_FAIL = 17 [(fhir_original_code) = "MSG_LOCAL_FAIL"];
    MSG_NO_MATCH = 18 [(fhir_original_code) = "MSG_NO_MATCH"];
    MSG_NO_EXIST = 19 [(fhir_original_code) = "MSG_NO_EXIST"];
    MSG_NO_MODULE = 20 [(fhir_original_code) = "MSG_NO_MODULE"];
    MSG_NO_SUMMARY = 21 [(fhir_original_code) = "MSG_NO_SUMMARY"];
    MSG_OP_NOT_ALLOWED = 22 [(fhir_original_code) = "MSG_OP_NOT_ALLOWED"];
    MSG_PARAM_CHAINED = 23 [(fhir_original_code) = "MSG_PARAM_CHAINED"];
    MSG_PARAM_NO_REPEAT = 24 [(fhir_original_code) = "MSG_PARAM_NO_REPEAT"];
    MSG_PARAM_UNKNOWN = 25 [(fhir_original_code) = "MSG_PARAM_UNKNOWN"];
    MSG_PARAM_INVALID = 26 [(fhir_original_code) = "MSG_PARAM_INVALID"];
    MSG_PARAM_MODIFIER_INVALID = 27
        [(fhir_original_code) = "MSG_PARAM_MODIFIER_INVALID"];
    MSG_RESOURCE_EXAMPLE_PROTECTED = 28
        [(fhir_original_code) = "MSG_RESOURCE_EXAMPLE_PROTECTED"];
    MSG_RESOURCE_ID_FAIL = 29 [(fhir_original_code) = "MSG_RESOURCE_ID_FAIL"];
    MSG_RESOURCE_NOT_ALLOWED = 30
        [(fhir_original_code) = "MSG_RESOURCE_NOT_ALLOWED"];
    MSG_RESOURCE_REQUIRED = 31 [(fhir_original_code) = "MSG_RESOURCE_REQUIRED"];
    MSG_RESOURCE_ID_MISMATCH = 32
        [(fhir_original_code) = "MSG_RESOURCE_ID_MISMATCH"];
    MSG_RESOURCE_ID_MISSING = 33
        [(fhir_original_code) = "MSG_RESOURCE_ID_MISSING"];
    MSG_RESOURCE_TYPE_MISMATCH = 34
        [(fhir_original_code) = "MSG_RESOURCE_TYPE_MISMATCH"];
    MSG_SORT_UNKNOWN = 35 [(fhir_original_code) = "MSG_SORT_UNKNOWN"];
    MSG_TRANSACTION_DUPLICATE_ID = 36
        [(fhir_original_code) = "MSG_TRANSACTION_DUPLICATE_ID"];
    MSG_TRANSACTION_MISSING_ID = 37
        [(fhir_original_code) = "MSG_TRANSACTION_MISSING_ID"];
    MSG_UNHANDLED_NODE_TYPE = 38
        [(fhir_original_code) = "MSG_UNHANDLED_NODE_TYPE"];
    MSG_UNKNOWN_CONTENT = 39 [(fhir_original_code) = "MSG_UNKNOWN_CONTENT"];
    MSG_UNKNOWN_OPERATION = 40 [(fhir_original_code) = "MSG_UNKNOWN_OPERATION"];
    MSG_UNKNOWN_TYPE = 41 [(fhir_original_code) = "MSG_UNKNOWN_TYPE"];
    MSG_UPDATED = 42 [(fhir_original_code) = "MSG_UPDATED"];
    MSG_VERSION_AWARE = 43 [(fhir_original_code) = "MSG_VERSION_AWARE"];
    MSG_VERSION_AWARE_CONFLICT = 44
        [(fhir_original_code) = "MSG_VERSION_AWARE_CONFLICT"];
    MSG_VERSION_AWARE_URL = 45 [(fhir_original_code) = "MSG_VERSION_AWARE_URL"];
    MSG_WRONG_NS = 46 [(fhir_original_code) = "MSG_WRONG_NS"];
    SEARCH_MULTIPLE = 47 [(fhir_original_code) = "SEARCH_MULTIPLE"];
    UPDATE_MULTIPLE_MATCHES = 48
        [(fhir_original_code) = "UPDATE_MULTIPLE_MATCHES"];
    DELETE_MULTIPLE_MATCHES = 49
        [(fhir_original_code) = "DELETE_MULTIPLE_MATCHES"];
    SEARCH_NONE = 50 [(fhir_original_code) = "SEARCH_NONE"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Whether an operation parameter is an input or an output parameter.
// See http://hl7.org/fhir/ValueSet/operation-parameter-use
message OperationParameterUseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/operation-parameter-use";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    IN = 1;
    OUT = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate a
// type of organization. See http://hl7.org/fhir/ValueSet/organization-type
message OrganizationTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/organization-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROV = 1;
    DEPT = 2;
    TEAM = 3;
    GOVT = 4;
    INS = 5;
    EDU = 6;
    RELI = 7;
    CRS = 8;
    CG = 9;
    BUS = 10;
    OTHER = 11;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes identifying groupings of parameters; e.g. Cardiovascular.
// See http://hl7.org/fhir/ValueSet/parameter-group
message DeviceComponentParameterGroupCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/parameter-group";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    HAEMODYNAMIC = 1;
    ECG = 2;
    RESPIRATORY = 3;
    VENTILATION = 4;
    NEUROLOGICAL = 5;
    DRUG_DELIVERY = 6;
    FLUID_CHEMISTRY = 7;
    BLOOD_CHEMISTRY = 8;
    MISCELLANEOUS = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Is the Participant required to attend the appointment.
// See http://hl7.org/fhir/ValueSet/participantrequired
message ParticipantRequiredCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/participantrequired";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REQUIRED = 1;
    OPTIONAL = 2;
    INFORMATION_ONLY = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The Participation status of an appointment.
// See http://hl7.org/fhir/ValueSet/participationstatus
message ParticipationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/participationstatus";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACCEPTED = 1;
    DECLINED = 2;
    TENTATIVE = 3;
    NEEDS_ACTION = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of PlanDefinition
// See http://hl7.org/fhir/ValueSet/plan-definition-type
message PlanDefinitionTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/plan-definition-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ORDER_SET = 1;
    PROTOCOL = 2;
    ECA_RULE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate
// the role of a Practitioner. See
// http://hl7.org/fhir/ValueSet/practitioner-role
message PractitionerRoleCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/practitioner-role";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DOCTOR = 1;
    NURSE = 2;
    PHARMACIST = 3;
    RESEARCHER = 4;
    TEACHER = 5;
    ICT = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This example value set defines a set of codes that can be used to indicate
// the specialty of a Practitioner. See
// http://hl7.org/fhir/ValueSet/practitioner-specialty
message PractitionerSpecialtyCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/practitioner-specialty";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CARDIO = 1;
    DENT = 2;
    DIETARY = 3;
    MIDW = 4;
    SYSARCH = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set is provided as an example. The value set to instantiate this
// attribute should be drawn from a robust terminology code system that consists
// of or contains concepts to support the procedure performance process. See
// http://hl7.org/fhir/ValueSet/procedure-progress-status-codes
message ProcedureProgressStatusCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/procedure-progress-status-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    IN_OPERATING_ROOM = 1;
    PREPARED = 2;
    ANESTHESIA_INDUCED = 3;
    OPEN_INCISION = 4;
    CLOSED_INCISION = 5;
    IN_RECOVERY_ROOM = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a property is represented when serialized.
// See http://hl7.org/fhir/ValueSet/property-representation
message PropertyRepresentationCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/property-representation";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    XML_ATTR = 1 [(fhir_original_code) = "xmlAttr"];
    XML_TEXT = 2 [(fhir_original_code) = "xmlText"];
    TYPE_ATTR = 3 [(fhir_original_code) = "typeAttr"];
    CDA_TEXT = 4 [(fhir_original_code) = "cdaText"];
    XHTML = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How an entity was used in an activity.
// See http://hl7.org/fhir/ValueSet/provenance-entity-role
message ProvenanceEntityRoleCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/provenance-entity-role";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DERIVATION = 1;
    REVISION = 2;
    QUOTATION = 3;
    SOURCE = 4;
    REMOVAL = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The lifecycle status of a Value Set or Concept Map.
// See http://hl7.org/fhir/ValueSet/publication-status
message PublicationStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/publication-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    ACTIVE = 2;
    RETIRED = 3;
    UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Type for quality report
// See http://hl7.org/fhir/ValueSet/quality-type
message QualityTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/quality-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INDEL = 1;
    SNP = 2;
    UNKNOWN = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How the Quantity should be understood and represented.
// See http://hl7.org/fhir/ValueSet/quantity-comparator
message QuantityComparatorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/quantity-comparator";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LT = 1 [(fhir_original_code) = "<"];
    LE = 2 [(fhir_original_code) = "<="];
    GE = 3 [(fhir_original_code) = ">="];
    GT = 4 [(fhir_original_code) = ">"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Flags an element as having unlimited repetitions
// See http://hl7.org/fhir/ValueSet/question-max-occurs
message MaxOccursCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/question-max-occurs";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MAX = 1 [(fhir_original_code) = "*"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Lifecycle status of the questionnaire response.
// See http://hl7.org/fhir/ValueSet/questionnaire-answers-status
message QuestionnaireResponseStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/questionnaire-answers-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    IN_PROGRESS = 1;
    COMPLETED = 2;
    AMENDED = 3;
    ENTERED_IN_ERROR = 4;
    STOPPED = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes defining the purpose of a Questionnaire item of type 'text'.
// See http://hl7.org/fhir/ValueSet/questionnaire-display-category
message QuestionnaireTextCategoriesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/questionnaire-display-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    INSTRUCTIONS = 1;
    SECURITY = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Starter set of user interface control/display mechanisms that might be used
// when rendering an item in a questionnaire. See
// http://hl7.org/fhir/ValueSet/questionnaire-item-control
message QuestionnaireItemUIControlCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/questionnaire-item-control";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LIST = 1;
    TABLE = 2;
    HEADER = 3;
    FOOTER = 4;
    INLINE = 5;
    PROMPT = 6;
    UNIT = 7;
    LOWER = 8;
    UPPER = 9;
    FLYOVER = 10;
    HELP = 11;
    AUTOCOMPLETE = 12;
    DROP_DOWN = 13;
    CHECK_BOX = 14;
    LOOKUP = 15;
    RADIO_BUTTON = 16;
    SLIDER = 17;
    SPINNER = 18;
    TEXT_BOX = 19;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Identifies the modes of usage of a questionnaire that should enable a
// particular questionnaire item See
// http://hl7.org/fhir/ValueSet/questionnaire-usage-mode
message QuestionnaireItemUsageModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/questionnaire-usage-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CAPTURE_DISPLAY = 1;
    CAPTURE = 2;
    DISPLAY = 3;
    DISPLAY_NON_EMPTY = 4;
    CAPTURE_DISPLAY_NON_EMPTY = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Statement about the degree of clinical certainty that a specific substance
// was the cause of the manifestation in a reaction event. See
// http://hl7.org/fhir/ValueSet/reaction-event-certainty
message AllergyIntoleranceCertaintyCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/reaction-event-certainty";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UNLIKELY = 1;
    LIKELY = 2;
    CONFIRMED = 3;
    UNKNOWN = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Clinical assessment of the severity of a reaction event as a whole,
// potentially considering multiple different manifestations. See
// http://hl7.org/fhir/ValueSet/reaction-event-severity
message AllergyIntoleranceSeverityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/reaction-event-severity";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MILD = 1;
    MODERATE = 2;
    SEVERE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set is provided as an example. The value set to instantiate this
// attribute should be drawn from a robust terminology code system that consists
// of or contains concepts to support the medication process. See
// http://hl7.org/fhir/ValueSet/reason-medication-given-codes
message ReasonMedicationGivenCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/reason-medication-given-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    A = 1;
    B = 2;
    C = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A set of flags that defines how references are supported.
// See http://hl7.org/fhir/ValueSet/reference-handling-policy
message ReferenceHandlingPolicyCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/reference-handling-policy";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    LITERAL = 1;
    LOGICAL = 2;
    RESOLVES = 3;
    ENFORCED = 4;
    LOCAL = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that can be used to indicate the
// meaning/use of a reference range for a particular target population. See
// http://hl7.org/fhir/ValueSet/referencerange-meaning
message ObservationReferenceRangeMeaningCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/referencerange-meaning";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NORMAL = 1;
    RECOMMENDED = 2;
    TREATMENT = 3;
    THERAPEUTIC = 4;
    PRE = 5;
    POST = 6;
    PRE_PUBERTY = 7;
    FOLLICULAR = 8;
    MIDCYCLE = 9;
    LUTEAL = 10;
    POSTMEOPAUSAL = 11;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Whether a reference needs to be version specific or version independent, or
// whether either can be used See
// http://hl7.org/fhir/ValueSet/reference-version-rules
message ReferenceVersionRulesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/reference-version-rules";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    EITHER = 1;
    INDEPENDENT = 2;
    SPECIFIC = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of relationship to the related artifact
// See http://hl7.org/fhir/ValueSet/related-artifact-type
message RelatedArtifactTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/related-artifact-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DOCUMENTATION = 1;
    JUSTIFICATION = 2;
    CITATION = 3;
    PREDECESSOR = 4;
    SUCCESSOR = 5;
    DERIVED_FROM = 6;
    DEPENDS_ON = 7;
    COMPOSED_OF = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The results of executing an action.
// See http://hl7.org/fhir/ValueSet/report-action-result-codes
message TestReportActionResultCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/report-action-result-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PASS = 1;
    SKIP = 2;
    FAIL = 3;
    WARNING = 4;
    ERROR = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of participant.
// See http://hl7.org/fhir/ValueSet/report-participant-type
message TestReportParticipantTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/report-participant-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TEST_ENGINE = 1;
    CLIENT = 2;
    SERVER = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The reported execution result.
// See http://hl7.org/fhir/ValueSet/report-result-codes
message TestReportResultCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/report-result-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PASS = 1;
    FAIL = 2;
    PENDING = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The current status of the test report.
// See http://hl7.org/fhir/ValueSet/report-status-codes
message TestReportStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/report-status-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    COMPLETED = 1;
    IN_PROGRESS = 2;
    WAITING = 3;
    STOPPED = 4;
    ENTERED_IN_ERROR = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Type for access of external URI
// See http://hl7.org/fhir/ValueSet/repository-type
message RepositoryTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/repository-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DIRECTLINK = 1;
    OPENAPI = 2;
    LOGIN = 3;
    OAUTH = 4;
    OTHER = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes indicating the degree of authority/intentionality associated with a
// request See http://hl7.org/fhir/ValueSet/request-intent
message RequestIntentCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/request-intent";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PROPOSAL = 1;
    PLAN = 2;
    ORDER = 3;
    ORIGINAL_ORDER = 4;
    REFLEX_ORDER = 5;
    FILLER_ORDER = 6;
    INSTANCE_ORDER = 7;
    OPTION = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The clinical priority of a diagnostic order.
// See http://hl7.org/fhir/ValueSet/request-priority
message RequestPriorityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/request-priority";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ROUTINE = 1;
    URGENT = 2;
    ASAP = 3;
    STAT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes identifying the stage lifecycle stage of a request
// See http://hl7.org/fhir/ValueSet/request-status
message RequestStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/request-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    ACTIVE = 2;
    SUSPENDED = 3;
    CANCELLED = 4;
    COMPLETED = 5;
    ENTERED_IN_ERROR = 6;
    UNKNOWN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes that convey the current status of the research study
// See http://hl7.org/fhir/ValueSet/research-study-status
message ResearchStudyStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/research-study-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    IN_PROGRESS = 2;
    SUSPENDED = 3;
    STOPPED = 4;
    COMPLETED = 5;
    ENTERED_IN_ERROR = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Indicates the progression of a study subject through a study
// See http://hl7.org/fhir/ValueSet/research-subject-status
message ResearchSubjectStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/research-subject-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CANDIDATE = 1;
    ENROLLED = 2;
    ACTIVE = 3;
    SUSPENDED = 4;
    WITHDRAWN = 5;
    COMPLETED = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How resource references can be aggregated.
// See http://hl7.org/fhir/ValueSet/resource-aggregation-mode
message AggregationModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/resource-aggregation-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CONTAINED = 1;
    REFERENCED = 2;
    BUNDLED = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How slices are interpreted when evaluating an instance.
// See http://hl7.org/fhir/ValueSet/resource-slicing-rules
message SlicingRulesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/resource-slicing-rules";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CLOSED = 1;
    OPEN = 2;
    OPEN_AT_END = 3 [(fhir_original_code) = "openAtEnd"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of payee Resource
// See http://hl7.org/fhir/ValueSet/resource-type-link
message PayeeResourceTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/resource-type-link";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ORGANIZATION = 1;
    PATIENT = 2;
    PRACTITIONER = 3;
    RELATEDPERSON = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// One of the resource types defined as part of FHIR.
// See http://hl7.org/fhir/ValueSet/resource-types
message ResourceTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/resource-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACCOUNT = 1 [(fhir_original_code) = "Account"];
    ACTIVITY_DEFINITION = 2 [(fhir_original_code) = "ActivityDefinition"];
    ADVERSE_EVENT = 3 [(fhir_original_code) = "AdverseEvent"];
    ALLERGY_INTOLERANCE = 4 [(fhir_original_code) = "AllergyIntolerance"];
    APPOINTMENT = 5 [(fhir_original_code) = "Appointment"];
    APPOINTMENT_RESPONSE = 6 [(fhir_original_code) = "AppointmentResponse"];
    AUDIT_EVENT = 7 [(fhir_original_code) = "AuditEvent"];
    BASIC = 8 [(fhir_original_code) = "Basic"];
    BINARY = 9 [(fhir_original_code) = "Binary"];
    BODY_SITE = 10 [(fhir_original_code) = "BodySite"];
    BUNDLE = 11 [(fhir_original_code) = "Bundle"];
    CAPABILITY_STATEMENT = 12 [(fhir_original_code) = "CapabilityStatement"];
    CARE_PLAN = 13 [(fhir_original_code) = "CarePlan"];
    CARE_TEAM = 14 [(fhir_original_code) = "CareTeam"];
    CHARGE_ITEM = 15 [(fhir_original_code) = "ChargeItem"];
    CLAIM = 16 [(fhir_original_code) = "Claim"];
    CLAIM_RESPONSE = 17 [(fhir_original_code) = "ClaimResponse"];
    CLINICAL_IMPRESSION = 18 [(fhir_original_code) = "ClinicalImpression"];
    CODE_SYSTEM = 19 [(fhir_original_code) = "CodeSystem"];
    COMMUNICATION = 20 [(fhir_original_code) = "Communication"];
    COMMUNICATION_REQUEST = 21 [(fhir_original_code) = "CommunicationRequest"];
    COMPARTMENT_DEFINITION = 22
        [(fhir_original_code) = "CompartmentDefinition"];
    COMPOSITION = 23 [(fhir_original_code) = "Composition"];
    CONCEPT_MAP = 24 [(fhir_original_code) = "ConceptMap"];
    CONDITION = 25 [(fhir_original_code) = "Condition"];
    CONSENT = 26 [(fhir_original_code) = "Consent"];
    CONTRACT = 27 [(fhir_original_code) = "Contract"];
    COVERAGE = 28 [(fhir_original_code) = "Coverage"];
    DATA_ELEMENT = 29 [(fhir_original_code) = "DataElement"];
    DETECTED_ISSUE = 30 [(fhir_original_code) = "DetectedIssue"];
    DEVICE = 31 [(fhir_original_code) = "Device"];
    DEVICE_COMPONENT = 32 [(fhir_original_code) = "DeviceComponent"];
    DEVICE_METRIC = 33 [(fhir_original_code) = "DeviceMetric"];
    DEVICE_REQUEST = 34 [(fhir_original_code) = "DeviceRequest"];
    DEVICE_USE_STATEMENT = 35 [(fhir_original_code) = "DeviceUseStatement"];
    DIAGNOSTIC_REPORT = 36 [(fhir_original_code) = "DiagnosticReport"];
    DOCUMENT_MANIFEST = 37 [(fhir_original_code) = "DocumentManifest"];
    DOCUMENT_REFERENCE = 38 [(fhir_original_code) = "DocumentReference"];
    DOMAIN_RESOURCE = 39 [(fhir_original_code) = "DomainResource"];
    ELIGIBILITY_REQUEST = 40 [(fhir_original_code) = "EligibilityRequest"];
    ELIGIBILITY_RESPONSE = 41 [(fhir_original_code) = "EligibilityResponse"];
    ENCOUNTER = 42 [(fhir_original_code) = "Encounter"];
    ENDPOINT = 43 [(fhir_original_code) = "Endpoint"];
    ENROLLMENT_REQUEST = 44 [(fhir_original_code) = "EnrollmentRequest"];
    ENROLLMENT_RESPONSE = 45 [(fhir_original_code) = "EnrollmentResponse"];
    EPISODE_OF_CARE = 46 [(fhir_original_code) = "EpisodeOfCare"];
    EXPANSION_PROFILE = 47 [(fhir_original_code) = "ExpansionProfile"];
    EXPLANATION_OF_BENEFIT = 48 [(fhir_original_code) = "ExplanationOfBenefit"];
    FAMILY_MEMBER_HISTORY = 49 [(fhir_original_code) = "FamilyMemberHistory"];
    FLAG = 50 [(fhir_original_code) = "Flag"];
    GOAL = 51 [(fhir_original_code) = "Goal"];
    GRAPH_DEFINITION = 52 [(fhir_original_code) = "GraphDefinition"];
    GROUP = 53 [(fhir_original_code) = "Group"];
    GUIDANCE_RESPONSE = 54 [(fhir_original_code) = "GuidanceResponse"];
    HEALTHCARE_SERVICE = 55 [(fhir_original_code) = "HealthcareService"];
    IMAGING_MANIFEST = 56 [(fhir_original_code) = "ImagingManifest"];
    IMAGING_STUDY = 57 [(fhir_original_code) = "ImagingStudy"];
    IMMUNIZATION = 58 [(fhir_original_code) = "Immunization"];
    IMMUNIZATION_RECOMMENDATION = 59
        [(fhir_original_code) = "ImmunizationRecommendation"];
    IMPLEMENTATION_GUIDE = 60 [(fhir_original_code) = "ImplementationGuide"];
    LIBRARY = 61 [(fhir_original_code) = "Library"];
    LINKAGE = 62 [(fhir_original_code) = "Linkage"];
    LIST = 63 [(fhir_original_code) = "List"];
    LOCATION = 64 [(fhir_original_code) = "Location"];
    MEASURE = 65 [(fhir_original_code) = "Measure"];
    MEASURE_REPORT = 66 [(fhir_original_code) = "MeasureReport"];
    MEDIA = 67 [(fhir_original_code) = "Media"];
    MEDICATION = 68 [(fhir_original_code) = "Medication"];
    MEDICATION_ADMINISTRATION = 69
        [(fhir_original_code) = "MedicationAdministration"];
    MEDICATION_DISPENSE = 70 [(fhir_original_code) = "MedicationDispense"];
    MEDICATION_REQUEST = 71 [(fhir_original_code) = "MedicationRequest"];
    MEDICATION_STATEMENT = 72 [(fhir_original_code) = "MedicationStatement"];
    MESSAGE_DEFINITION = 73 [(fhir_original_code) = "MessageDefinition"];
    MESSAGE_HEADER = 74 [(fhir_original_code) = "MessageHeader"];
    NAMING_SYSTEM = 75 [(fhir_original_code) = "NamingSystem"];
    NUTRITION_ORDER = 76 [(fhir_original_code) = "NutritionOrder"];
    OBSERVATION = 77 [(fhir_original_code) = "Observation"];
    OPERATION_DEFINITION = 78 [(fhir_original_code) = "OperationDefinition"];
    OPERATION_OUTCOME = 79 [(fhir_original_code) = "OperationOutcome"];
    ORGANIZATION = 80 [(fhir_original_code) = "Organization"];
    PARAMETERS = 81 [(fhir_original_code) = "Parameters"];
    PATIENT = 82 [(fhir_original_code) = "Patient"];
    PAYMENT_NOTICE = 83 [(fhir_original_code) = "PaymentNotice"];
    PAYMENT_RECONCILIATION = 84
        [(fhir_original_code) = "PaymentReconciliation"];
    PERSON = 85 [(fhir_original_code) = "Person"];
    PLAN_DEFINITION = 86 [(fhir_original_code) = "PlanDefinition"];
    PRACTITIONER = 87 [(fhir_original_code) = "Practitioner"];
    PRACTITIONER_ROLE = 88 [(fhir_original_code) = "PractitionerRole"];
    PROCEDURE = 89 [(fhir_original_code) = "Procedure"];
    PROCEDURE_REQUEST = 90 [(fhir_original_code) = "ProcedureRequest"];
    PROCESS_REQUEST = 91 [(fhir_original_code) = "ProcessRequest"];
    PROCESS_RESPONSE = 92 [(fhir_original_code) = "ProcessResponse"];
    PROVENANCE = 93 [(fhir_original_code) = "Provenance"];
    QUESTIONNAIRE = 94 [(fhir_original_code) = "Questionnaire"];
    QUESTIONNAIRE_RESPONSE = 95
        [(fhir_original_code) = "QuestionnaireResponse"];
    REFERRAL_REQUEST = 96 [(fhir_original_code) = "ReferralRequest"];
    RELATED_PERSON = 97 [(fhir_original_code) = "RelatedPerson"];
    REQUEST_GROUP = 98 [(fhir_original_code) = "RequestGroup"];
    RESEARCH_STUDY = 99 [(fhir_original_code) = "ResearchStudy"];
    RESEARCH_SUBJECT = 100 [(fhir_original_code) = "ResearchSubject"];
    RESOURCE = 101 [(fhir_original_code) = "Resource"];
    RISK_ASSESSMENT = 102 [(fhir_original_code) = "RiskAssessment"];
    SCHEDULE = 103 [(fhir_original_code) = "Schedule"];
    SEARCH_PARAMETER = 104 [(fhir_original_code) = "SearchParameter"];
    SEQUENCE = 105 [(fhir_original_code) = "Sequence"];
    SERVICE_DEFINITION = 106 [(fhir_original_code) = "ServiceDefinition"];
    SLOT = 107 [(fhir_original_code) = "Slot"];
    SPECIMEN = 108 [(fhir_original_code) = "Specimen"];
    STRUCTURE_DEFINITION = 109 [(fhir_original_code) = "StructureDefinition"];
    STRUCTURE_MAP = 110 [(fhir_original_code) = "StructureMap"];
    SUBSCRIPTION = 111 [(fhir_original_code) = "Subscription"];
    SUBSTANCE = 112 [(fhir_original_code) = "Substance"];
    SUPPLY_DELIVERY = 113 [(fhir_original_code) = "SupplyDelivery"];
    SUPPLY_REQUEST = 114 [(fhir_original_code) = "SupplyRequest"];
    TASK = 115 [(fhir_original_code) = "Task"];
    TEST_REPORT = 116 [(fhir_original_code) = "TestReport"];
    TEST_SCRIPT = 117 [(fhir_original_code) = "TestScript"];
    VALUE_SET = 118 [(fhir_original_code) = "ValueSet"];
    VISION_PRESCRIPTION = 119 [(fhir_original_code) = "VisionPrescription"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes indicating the type of validation to perform
// See http://hl7.org/fhir/ValueSet/resource-validation-mode
message ResourceValidationModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/resource-validation-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CREATE = 1;
    UPDATE = 2;
    DELETE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The kind of response to a message
// See http://hl7.org/fhir/ValueSet/response-code
message ResponseTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/response-code";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OK = 1;
    TRANSIENT_ERROR = 2;
    FATAL_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The mode of a RESTful capability statement.
// See http://hl7.org/fhir/ValueSet/restful-capability-mode
message RestfulCapabilityModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/restful-capability-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CLIENT = 1;
    SERVER = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Types of security services used with FHIR.
// See http://hl7.org/fhir/ValueSet/restful-security-service
message RestfulSecurityServiceCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/restful-security-service";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    OAUTH = 1 [(fhir_original_code) = "OAuth"];
    SMART_ON_FHIR = 2 [(fhir_original_code) = "SMART-on-FHIR"];
    NTLM = 3 [(fhir_original_code) = "NTLM"];
    BASIC = 4 [(fhir_original_code) = "Basic"];
    KERBEROS = 5 [(fhir_original_code) = "Kerberos"];
    CERTIFICATES = 6 [(fhir_original_code) = "Certificates"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes representing the likelihood of a particular outcome in a risk
// assessment. See http://hl7.org/fhir/ValueSet/risk-probability
message RiskProbabilityCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/risk-probability";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NEGLIGIBLE = 1;
    LOW = 2;
    MODERATE = 3;
    HIGH = 4;
    CERTAIN = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// What Search Comparator Codes are supported in search
// See http://hl7.org/fhir/ValueSet/search-comparator
message SearchComparatorCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/search-comparator";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    EQ = 1;
    NE = 2;
    GT = 3;
    LT = 4;
    GE = 5;
    LE = 6;
    SA = 7;
    EB = 8;
    AP = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Why an entry is in the result set - whether it's included as a match or
// because of an _include requirement. See
// http://hl7.org/fhir/ValueSet/search-entry-mode
message SearchEntryModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/search-entry-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MATCH = 1;
    INCLUDE = 2;
    OUTCOME = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A supported modifier for a search parameter.
// See http://hl7.org/fhir/ValueSet/search-modifier-code
message SearchModifierCodeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/search-modifier-code";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MISSING = 1;
    EXACT = 2;
    CONTAINS = 3;
    NOT = 4;
    TEXT = 5;
    IN = 6;
    NOT_IN = 7;
    BELOW = 8;
    ABOVE = 9;
    TYPE = 10;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Data types allowed to be used for search parameters.
// See http://hl7.org/fhir/ValueSet/search-param-type
message SearchParamTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/search-param-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NUMBER = 1;
    DATE = 2;
    STRING = 3;
    TOKEN = 4;
    REFERENCE = 5;
    COMPOSITE = 6;
    QUANTITY = 7;
    URI = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a search parameter relates to the set of elements returned by evaluating
// its xpath query. See http://hl7.org/fhir/ValueSet/search-xpath-usage
message XPathUsageTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/search-xpath-usage";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NORMAL = 1;
    PHONETIC = 2;
    NEARBY = 3;
    DISTANCE = 4;
    OTHER = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Type if a sequence -- DNA, RNA, or amino acid sequence
// See http://hl7.org/fhir/ValueSet/sequence-type
message SequenceTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/sequence-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AA = 1;
    DNA = 2;
    RNA = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The code(s) that detail the conditions under which the healthcare service is
// available/offered. See
// http://hl7.org/fhir/ValueSet/service-provision-conditions
message ServiceProvisionConditionsCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/service-provision-conditions";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FREE = 1;
    DISC = 2;
    COST = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The methods of referral can be used when referring to a specific
// HealthCareService resource. See
// http://hl7.org/fhir/ValueSet/service-referral-method
message ReferralMethodCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/service-referral-method";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FAX = 1;
    PHONE = 2;
    ELEC = 3;
    SEMAIL = 4;
    MAIL = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The free/busy status of the slot.
// See http://hl7.org/fhir/ValueSet/slotstatus
message SlotStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/slotstatus";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    BUSY = 1;
    FREE = 2;
    BUSY_UNAVAILABLE = 3;
    BUSY_TENTATIVE = 4;
    ENTERED_IN_ERROR = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A set of generally useful codes defined so they can be included in value
// sets. See http://hl7.org/fhir/ValueSet/special-values
message SpecialValuesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/special-values";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TRUE = 1;
    FALSE = 2;
    TRACE = 3;
    SUFFICIENT = 4;
    WITHDRAWN = 5;
    NIL_KNOWN = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes for device specification types such as serial number, part number,
// hardware revision, software revision, etc. See
// http://hl7.org/fhir/ValueSet/specification-type
message DeviceSpecificationSpecTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/specification-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UNSPECIFIED = 1;
    SERIAL_NUMBER = 2;
    PART_NUMBER = 3;
    HARDWARE_REVISION = 4;
    SOFTWARE_REVISION = 5;
    FIRMWARE_REVISION = 6;
    PROTOCOL_REVISION = 7;
    GMDN = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes providing the status/availability of a specimen.
// See http://hl7.org/fhir/ValueSet/specimen-status
message SpecimenStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/specimen-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    AVAILABLE = 1;
    UNAVAILABLE = 2;
    UNSATISFACTORY = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Defines the type of structure that a definition is describing.
// See http://hl7.org/fhir/ValueSet/structure-definition-kind
message StructureDefinitionKindCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/structure-definition-kind";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PRIMITIVE_TYPE = 1;
    COMPLEX_TYPE = 2;
    RESOURCE = 3;
    LOGICAL = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of method used to execute a subscription.
// See http://hl7.org/fhir/ValueSet/subscription-channel-type
message SubscriptionChannelTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/subscription-channel-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REST_HOOK = 1;
    WEBSOCKET = 2;
    EMAIL = 3;
    SMS = 4;
    MESSAGE = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The status of a subscription.
// See http://hl7.org/fhir/ValueSet/subscription-status
message SubscriptionStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/subscription-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REQUESTED = 1;
    ACTIVE = 2;
    ERROR = 3;
    OFF = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Tags to put on a resource after subscriptions have been sent.
// See http://hl7.org/fhir/ValueSet/subscription-tag
message SubscriptionTagCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/subscription-tag";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    QUEUED = 1;
    DELIVERED = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Substance category codes
// See http://hl7.org/fhir/ValueSet/substance-category
message SubstanceCategoryCodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/substance-category";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ALLERGEN = 1;
    BIOLOGICAL = 2;
    BODY = 3;
    CHEMICAL = 4;
    FOOD = 5;
    DRUG = 6;
    MATERIAL = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code to indicate if the substance is actively used
// See http://hl7.org/fhir/ValueSet/substance-status
message FHIRSubstanceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/substance-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ACTIVE = 1;
    INACTIVE = 2;
    ENTERED_IN_ERROR = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Status of the supply delivery.
// See http://hl7.org/fhir/ValueSet/supplydelivery-status
message SupplyDeliveryStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/supplydelivery-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    IN_PROGRESS = 1;
    COMPLETED = 2;
    ABANDONED = 3;
    ENTERED_IN_ERROR = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value sets refers to a specific supply item.
// See http://hl7.org/fhir/ValueSet/supplydelivery-type
message SupplyItemTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/supplydelivery-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    MEDICATION = 1;
    DEVICE = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value sets refers to a Category of supply.
// See http://hl7.org/fhir/ValueSet/supplyrequest-kind
message SupplyTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/supplyrequest-kind";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    CENTRAL = 1;
    NONSTOCK = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Why the supply item was requested
// See http://hl7.org/fhir/ValueSet/supplyrequest-reason
message SupplyRequestReasonCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/supplyrequest-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    PATIENT_CARE = 1;
    WARD_STOCK = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Status of the supply request
// See http://hl7.org/fhir/ValueSet/supplyrequest-status
message SupplyRequestStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/supplyrequest-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    ACTIVE = 2;
    SUSPENDED = 3;
    CANCELLED = 4;
    COMPLETED = 5;
    ENTERED_IN_ERROR = 6;
    UNKNOWN = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Operations supported by REST at the system level.
// http://hl7.org/fhir/ValueSet/system-restful-interaction
message SystemRestfulInteractionCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/system-restful-interaction";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    TRANSACTION = 1;
    BATCH = 2;
    SEARCH_SYSTEM = 3;
    HISTORY_SYSTEM = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How to manage the intersection between a fixed version in a value set, and a
// fixed version of the system in the expansion profile See
// http://hl7.org/fhir/ValueSet/system-version-processing-mode
message SystemVersionProcessingModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/system-version-processing-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DEFAULT = 1;
    CHECK = 2;
    OVERRIDE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type(s) of task performers allowed
// See http://hl7.org/fhir/ValueSet/task-performer-type
message TaskPerformerTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/task-performer-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    REQUESTER = 1;
    DISPATCHER = 2;
    SCHEDULER = 3;
    PERFORMER = 4;
    MONITOR = 5;
    MANAGER = 6;
    ACQUIRER = 7;
    REVIEWER = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The current status of the task.
// See http://hl7.org/fhir/ValueSet/task-status
message TaskStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/task-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    DRAFT = 1;
    REQUESTED = 2;
    RECEIVED = 3;
    ACCEPTED = 4;
    REJECTED = 5;
    READY = 6;
    CANCELLED = 7;
    IN_PROGRESS = 8;
    ON_HOLD = 9;
    FAILED = 10;
    COMPLETED = 11;
    ENTERED_IN_ERROR = 12;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that are used to indicate the supported
// operations of a testing engine or tool. See
// http://hl7.org/fhir/ValueSet/testscript-operation-codes
message TestScriptOperationCodeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/testscript-operation-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    READ = 1;
    VREAD = 2;
    UPDATE = 3;
    UPDATE_CREATE = 4 [(fhir_original_code) = "updateCreate"];
    DELETE = 5;
    DELETE_COND_SINGLE = 6 [(fhir_original_code) = "deleteCondSingle"];
    DELETE_COND_MULTIPLE = 7 [(fhir_original_code) = "deleteCondMultiple"];
    HISTORY = 8;
    CREATE = 9;
    SEARCH = 10;
    BATCH = 11;
    TRANSACTION = 12;
    CAPABILITIES = 13;
    APPLY = 14;
    CANCEL = 15;
    CLOSURE = 16;
    COMPOSE = 17;
    CONFORMS = 18;
    DATA_REQUIREMENTS = 19;
    DOCUMENT = 20;
    EVALUATE = 21;
    EVALUATE_MEASURE = 22;
    EVERYTHING = 23;
    EXPAND = 24;
    FAIL = 25;
    FIND = 26;
    FINISH = 27;
    IMPLEMENTS = 28;
    LOOKUP = 29;
    MATCH = 30;
    META = 31;
    META_ADD = 32;
    META_DELETE = 33;
    POPULATE = 34;
    POPULATEHTML = 35;
    POPULATELINK = 36;
    PROCESS_MESSAGE = 37;
    QUESTIONNAIRE = 38;
    RELEASE = 39;
    RESERVE = 40;
    RESUME = 41;
    SET_INPUT = 42;
    SET_OUTPUT = 43;
    START = 44;
    STATS = 45;
    STOP = 46;
    SUBSET = 47;
    SUBSUMES = 48;
    SUSPEND = 49;
    TRANSFORM = 50;
    TRANSLATE = 51;
    VALIDATE = 52;
    VALIDATE_CODE = 53;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that are used to indicate the profile
// type of a test system when acting as the destination within a TestScript. See
// http://hl7.org/fhir/ValueSet/testscript-profile-destination-types
message TestScriptProfileDestinationTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/testscript-profile-destination-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FHIR_SERVER = 1 [(fhir_original_code) = "FHIR-Server"];
    FHIR_SDC_FORMMANAGER = 2 [(fhir_original_code) = "FHIR-SDC-FormManager"];
    FHIR_SDC_FORMPROCESSOR = 3
        [(fhir_original_code) = "FHIR-SDC-FormProcessor"];
    FHIR_SDC_FORMRECEIVER = 4 [(fhir_original_code) = "FHIR-SDC-FormReceiver"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// This value set defines a set of codes that are used to indicate the profile
// type of a test system when acting as the origin within a TestScript. See
// http://hl7.org/fhir/ValueSet/testscript-profile-origin-types
message TestScriptProfileOriginTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/testscript-profile-origin-types";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    FHIR_CLIENT = 1 [(fhir_original_code) = "FHIR-Client"];
    FHIR_SDC_FORMFILLER = 2 [(fhir_original_code) = "FHIR-SDC-FormFiller"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that indicates how transactions are supported.
// See http://hl7.org/fhir/ValueSet/transaction-mode
message TransactionModeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/transaction-mode";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NOT_SUPPORTED = 1;
    BATCH = 2;
    TRANSACTION = 3;
    BOTH = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The type of trigger
// See http://hl7.org/fhir/ValueSet/trigger-type
message TriggerTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/trigger-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NAMED_EVENT = 1;
    PERIODIC = 2;
    DATA_ADDED = 3;
    DATA_MODIFIED = 4;
    DATA_REMOVED = 5;
    DATA_ACCESSED = 6;
    DATA_ACCESS_ENDED = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How a type relates to its baseDefinition.
// See http://hl7.org/fhir/ValueSet/type-derivation-rule
message TypeDerivationRuleCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/type-derivation-rule";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    SPECIALIZATION = 1;
    CONSTRAINT = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Operations supported by REST at the type or instance level.
// See http://hl7.org/fhir/ValueSet/type-restful-interaction
message TypeRestfulInteractionCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/type-restful-interaction";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    READ = 1;
    VREAD = 2;
    UPDATE = 3;
    PATCH = 4;
    DELETE = 5;
    HISTORY_INSTANCE = 6;
    HISTORY_TYPE = 7;
    CREATE = 8;
    SEARCH_TYPE = 9;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes to identify how UDI data was entered
// See http://hl7.org/fhir/ValueSet/udi-entry-type
message UDIEntryTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/udi-entry-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    BARCODE = 1;
    RFID = 2;
    MANUAL = 3;
    CARD = 4;
    SELF_REPORTED = 5;
    UNKNOWN = 6;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A unit of time (units from UCUM). See
// http://hl7.org/fhir/ValueSet/units-of-time
message UnitsOfTimeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/units-of-time";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    S = 1;
    MIN = 2;
    H = 3;
    D = 4;
    WK = 5;
    MO = 6;
    A = 7;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that indicates whether an application accepts unknown elements or
// extensions when reading resources. See
// http://hl7.org/fhir/ValueSet/unknown-content-code
message UnknownContentCodeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/unknown-content-code";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NO = 1;
    EXTENSIONS = 2;
    ELEMENTS = 3;
    BOTH = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A code that specifies a type of context being specified by a usage context
// See http://hl7.org/fhir/ValueSet/usage-context-type
message UsageContextTypeCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/usage-context-type";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    GENDER = 1;
    AGE = 2;
    FOCUS = 3;
    USER = 4;
    WORKFLOW = 5;
    TASK = 6;
    VENUE = 7;
    SPECIES = 8;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Set of codes used to value Act.Confidentiality and Role.Confidentiality
// attribute in accordance with the definition for concept domain
// "Confidentiality". See
// http://hl7.org/fhir/ValueSet/v3-ConfidentialityClassification
message ConfidentialityClassificationCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/v3-ConfidentialityClassification";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    U = 1 [(fhir_original_code) = "U"];
    L = 2 [(fhir_original_code) = "L"];
    M = 3 [(fhir_original_code) = "M"];
    N = 4 [(fhir_original_code) = "N"];
    R = 5 [(fhir_original_code) = "R"];
    V = 6 [(fhir_original_code) = "V"];
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The value set to instantiate this attribute should be drawn from a
// terminologically robust code system that consists of or contains concepts to
// support describing the validity of a dose relative to a particular
// recommended schedule. This value set is provided as a suggestive example. See
// http://hl7.org/fhir/ValueSet/vaccination-protocol-dose-status
message VaccinationProtocolDoseStatuscodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/vaccination-protocol-dose-status";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    COUNT = 1;
    NOCOUNT = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// The value set to instantiate this attribute should be drawn from a
// terminologically robust code system that consists of or contains concepts to
// support describing the reason why an administered dose has been assigned a
// particular status. Often, this reason describes why a dose is considered
// invalid. This value set is provided as a suggestive example. See
// http://hl7.org/fhir/ValueSet/vaccination-protocol-dose-status-reason
message VaccinationProtocolDoseStatusReasoncodesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) =
      "http://hl7.org/fhir/ValueSet/vaccination-protocol-dose-status-reason";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    ADVSTORAGE = 1;
    COLDCHBRK = 2;
    EXPLOT = 3;
    OUTSIDESCHED = 4;
    PRODRECALL = 5;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// Codes providing the status of the variant test result
// See http://hl7.org/fhir/ValueSet/variant-state
message sequenceStatusCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/variant-state";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    POSITIVE = 1;
    NEGATIVE = 2;
    ABSENT = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// How the system supports versioning for a resource.
// See http://hl7.org/fhir/ValueSet/versioning-policy
message ResourceVersionPolicyCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/versioning-policy";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    NO_VERSION = 1;
    VERSIONED = 2;
    VERSIONED_UPDATE = 3;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept listing the base codes.
// See http://hl7.org/fhir/ValueSet/vision-base-codes
message VisionBaseCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/vision-base-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    UP = 1;
    DOWN = 2;
    IN = 3;
    OUT = 4;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}

// A coded concept listing the eye codes.
// See http://hl7.org/fhir/ValueSet/vision-eye-codes
message VisionEyesCode {
  option (structure_definition_kind) = KIND_PRIMITIVE_TYPE;
  option (fhir_valueset_url) = "http://hl7.org/fhir/ValueSet/vision-eye-codes";

  // Primitive value for code
  enum Value {
    INVALID_UNINITIALIZED = 0;
    RIGHT = 1;
    LEFT = 2;
  }
  Value value = 1;

  // xml:id (or equivalent in JSON)
  String id = 2;

  // Additional Content defined by implementations
  repeated Extension extension = 3;
}
