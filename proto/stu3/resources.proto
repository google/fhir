//    Copyright 2018 Google Inc.
//
//    Licensed under the Apache License, Version 2.0 (the "License");
//    you may not use this file except in compliance with the License.
//    You may obtain a copy of the License at
//
//        https://www.apache.org/licenses/LICENSE-2.0
//
//    Unless required by applicable law or agreed to in writing, software
//    distributed under the License is distributed on an "AS IS" BASIS,
//    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//    See the License for the specific language governing permissions and
//    limitations under the License.

syntax = "proto3";

package google.fhir.stu3.proto;

import "proto/stu3/annotations.proto";
import "proto/stu3/codes.proto";
import "proto/stu3/datatypes.proto";
import "proto/stu3/metadatatypes.proto";

option java_multiple_files = true;
option java_package = "com.google.fhir.stu3.proto";

// Auto-generated from StructureDefinition for Account, last updated
// 2017-04-19T07:44:43.294+10:00. Tracks balance, charges, for patient or cost
// center. See https://www.hl7.org/fhir/account.html
message Account {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Account number
  repeated Identifier identifier = 9;

  // active | inactive | entered-in-error
  AccountStatusCode status = 10;

  // E.g. patient, expense, depreciation
  CodeableConcept type = 11;

  // Human-readable label
  String name = 12;

  // What is account tied to?
  Reference subject = 13;

  // Transaction window
  Period period = 14;

  // Time window that transactions may be posted to this account
  Period active = 15;

  // How much is in account?
  Money balance = 16;

  // The party(s) that are responsible for covering the payment of this account,
  // and what order should they be applied to the account
  message Coverage {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The party(s) that are responsible for covering the payment of this
    // account
    Reference coverage = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The priority of the coverage in the context of this account
    PositiveInt priority = 5;
  }
  repeated Coverage coverage = 17;

  // Who is responsible?
  Reference owner = 18;

  // Explanation of purpose/use
  String description = 19;

  // Responsible for the account
  message Guarantor {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Responsible entity
    Reference party = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Credit or other hold applied
    Boolean on_hold = 5;

    // Guarrantee account during
    Period period = 6;
  }
  repeated Guarantor guarantor = 20;
}

// Auto-generated from StructureDefinition for ActivityDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. The definition of a specific activity to be
// taken, independent of any particular patient or context. See
// https://www.hl7.org/fhir/activitydefinition.html
message ActivityDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this activity definition (globally unique)
  Uri url = 9;

  // Additional identifier for the activity definition
  repeated Identifier identifier = 10;

  // Business version of the activity definition
  String version = 11;

  // Name for this activity definition (computer friendly)
  String name = 12;

  // Name for this activity definition (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Natural language description of the activity definition
  Markdown description = 18;

  // Why this activity definition is defined
  Markdown purpose = 19;

  // Describes the clinical usage of the asset
  String usage = 20;

  // When the activity definition was approved by publisher
  Date approval_date = 21;

  // When the activity definition was last reviewed
  Date last_review_date = 22;

  // When the activity definition is expected to be used
  Period effective_period = 23;

  // Context the content is intended to support
  repeated UsageContext use_context = 24;

  // Intended jurisdiction for activity definition (if applicable)
  repeated CodeableConcept jurisdiction = 25;

  // E.g. Education, Treatment, Assessment, etc
  repeated CodeableConcept topic = 26;

  // A content contributor
  repeated Contributor contributor = 27;

  // Contact details for the publisher
  repeated ContactDetail contact = 28;

  // Use and/or publishing restrictions
  Markdown copyright = 29;

  // Additional documentation, citations, etc
  repeated RelatedArtifact related_artifact = 30;

  // Logic used by the asset
  repeated Reference library = 31;

  // Kind of resource
  ResourceTypeCode kind = 32;

  // Detail type of activity
  CodeableConcept code = 33;

  // When activity is to occur
  message TimingType {
    oneof timing {
      Timing timing_value = 1 [json_name = "timing"];
      DateTime date_time = 2;
      Period period = 3;
      Range range = 4;
    }
  }
  TimingType timing = 34 [(is_choice_type) = true];

  // Where it should happen
  Reference location = 35;

  // Who should participate in the action
  message Participant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // patient | practitioner | related-person
    ActionParticipantTypeCode type = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // E.g. Nurse, Surgeon, Parent, etc
    CodeableConcept role = 5;
  }
  repeated Participant participant = 36;

  // What's administered/supplied
  message Product {
    oneof product {
      Reference reference = 1;
      CodeableConcept codeable_concept = 2;
    }
  }
  Product product = 37 [(is_choice_type) = true];

  // How much is administered/consumed/supplied
  Quantity quantity = 38;

  // Detailed dosage instructions
  repeated Dosage dosage = 39;

  // What part of body to perform on
  repeated CodeableConcept body_site = 40;

  // Transform to apply the template
  Reference transform = 41;

  // Dynamic aspects of the definition
  message DynamicValue {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Natural language description of the dynamic value
    String description = 4;

    // The path to the element to be set dynamically
    String path = 5;

    // Language of the expression
    String language = 6;

    // An expression that provides the dynamic value for the customization
    String expression = 7;
  }
  repeated DynamicValue dynamic_value = 42;
}

// Auto-generated from StructureDefinition for AdverseEvent, last updated
// 2017-04-19T07:44:43.294+10:00. Medical care, research study or other
// healthcare event causing physical injury. See
// https://www.hl7.org/fhir/adverseevent.html
message AdverseEvent {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier for the event
  Identifier identifier = 9;

  // AE | PAE
  // An adverse event is an event that caused harm to a patient,  an adverse
  // reaction is a something that is a subject-specific event that is a result
  // of an exposure to a medication, food, device or environmental substance, a
  // potential adverse event is something that occurred and that could have
  // caused harm to a patient but did not
  AdverseEventCategoryCode category = 10;

  // actual | potential
  CodeableConcept type = 11;

  // Subject or group impacted by event
  Reference subject = 12;

  // When the event occurred
  DateTime date = 13;

  // Adverse Reaction Events linked to exposure to substance
  repeated Reference reaction = 14;

  // Location where adverse event occurred
  Reference location = 15;

  // Mild | Moderate | Severe
  CodeableConcept seriousness = 16;

  // resolved | recovering | ongoing | resolvedWithSequelae | fatal | unknown
  CodeableConcept outcome = 17;

  // Who recorded the adverse event
  Reference recorder = 18;

  // Who  was involved in the adverse event or the potential adverse event
  Reference event_participant = 19;

  // Description of the adverse event
  String description = 20;

  // The suspected agent causing the adverse event
  message SuspectEntity {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Refers to the specific entity that caused the adverse event
    Reference instance = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // causality1 | causality2
    AdverseEventCausalityCode causality = 5;

    // assess1 | assess2
    CodeableConcept causality_assessment = 6;

    // AdverseEvent.suspectEntity.causalityProductRelatedness
    String causality_product_relatedness = 7;

    // method1 | method2
    CodeableConcept causality_method = 8;

    // AdverseEvent.suspectEntity.causalityAuthor
    Reference causality_author = 9;

    // result1 | result2
    CodeableConcept causality_result = 10;
  }
  repeated SuspectEntity suspect_entity = 21;

  // AdverseEvent.subjectMedicalHistory
  repeated Reference subject_medical_history = 22;

  // AdverseEvent.referenceDocument
  repeated Reference reference_document = 23;

  // AdverseEvent.study
  repeated Reference study = 24;
}

// Auto-generated from StructureDefinition for AllergyIntolerance, last updated
// 2017-04-19T07:44:43.294+10:00. Allergy or Intolerance (generally: Risk of
// adverse reaction to a substance). See
// https://www.hl7.org/fhir/allergyintolerance.html
message AllergyIntolerance {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External ids for this item
  repeated Identifier identifier = 9;

  // active | inactive | resolved
  AllergyIntoleranceClinicalStatusCode clinical_status = 10;

  // unconfirmed | confirmed | refuted | entered-in-error
  AllergyIntoleranceVerificationStatusCode verification_status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // allergy | intolerance - Underlying mechanism (if known)
  AllergyIntoleranceTypeCode type = 12;

  // food | medication | environment | biologic
  repeated AllergyIntoleranceCategoryCode category = 13;

  // low | high | unable-to-assess
  AllergyIntoleranceCriticalityCode criticality = 14;

  // Code that identifies the allergy or intolerance
  CodeableConcept code = 15;

  // Who the sensitivity is for
  Reference patient = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // When allergy or intolerance was identified
  message Onset {
    oneof onset {
      DateTime date_time = 1;
      Age age = 2;
      Period period = 3;
      Range range = 4;
      String string_value = 5 [json_name = "string"];
    }
  }
  Onset onset = 17 [(is_choice_type) = true];

  // Date record was believed accurate
  DateTime asserted_date = 18;

  // Who recorded the sensitivity
  Reference recorder = 19;

  // Source of the information about the allergy
  Reference asserter = 20;

  // Date(/time) of last known occurrence of a reaction
  DateTime last_occurrence = 21;

  // Additional text not captured in other fields
  repeated Annotation note = 22;

  // Adverse Reaction Events linked to exposure to substance
  message Reaction {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Specific substance or pharmaceutical product considered to be responsible
    // for event
    CodeableConcept substance = 4;

    // Clinical symptoms/signs associated with the Event
    repeated CodeableConcept manifestation = 5
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Description of the event as a whole
    String description = 6;

    // Date(/time) when manifestations showed
    DateTime onset = 7;

    // mild | moderate | severe (of event as a whole)
    AllergyIntoleranceSeverityCode severity = 8;

    // How the subject was exposed to the substance
    CodeableConcept exposure_route = 9;

    // Text about event not captured in other fields
    repeated Annotation note = 10;
  }
  repeated Reaction reaction = 23;
}

// Auto-generated from StructureDefinition for Appointment, last updated
// 2017-04-19T07:44:43.294+10:00. A booking of a healthcare event among
// patient(s), practitioner(s), related person(s) and/or device(s) for a
// specific date/time. This may result in one or more Encounter(s). See
// https://www.hl7.org/fhir/appointment.html
message Appointment {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this item
  repeated Identifier identifier = 9;

  // proposed | pending | booked | arrived | fulfilled | cancelled | noshow |
  // entered-in-error
  AppointmentStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // A broad categorisation of the service that is to be performed during this
  // appointment
  CodeableConcept service_category = 11;

  // The specific service that is to be performed during this appointment
  repeated CodeableConcept service_type = 12;

  // The specialty of a practitioner that would be required to perform the
  // service requested in this appointment
  repeated CodeableConcept specialty = 13;

  // The style of appointment or patient that has been booked in the slot (not
  // service type)
  CodeableConcept appointment_type = 14;

  // Reason this appointment is scheduled
  repeated CodeableConcept reason = 15;

  // Reason the appointment is to takes place (resource)
  repeated Reference indication = 16;

  // Used to make informed decisions if needing to re-prioritize
  UnsignedInt priority = 17;

  // Shown on a subject line in a meeting request, or appointment list
  String description = 18;

  // Additional information to support the appointment
  repeated Reference supporting_information = 19;

  // When appointment is to take place
  Instant start = 20;

  // When appointment is to conclude
  Instant end = 21;

  // Can be less than start/end (e.g. estimate)
  PositiveInt minutes_duration = 22;

  // The slots that this appointment is filling
  repeated Reference slot = 23;

  // The date that this appointment was initially created
  DateTime created = 24;

  // Additional comments
  String comment = 25;

  // The ReferralRequest provided as information to allocate to the Encounter
  repeated Reference incoming_referral = 26;

  // Participants involved in appointment
  message Participant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Role of participant in the appointment
    repeated CodeableConcept type = 4;

    // Person, Location/HealthcareService or Device
    Reference actor = 5;

    // required | optional | information-only
    ParticipantRequiredCode required = 6;

    // accepted | declined | tentative | needs-action
    ParticipationStatusCode status = 7
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Participant participant = 27
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Potential date/time interval(s) requested to allocate the appointment
  // within
  repeated Period requested_period = 28;
}

// Auto-generated from StructureDefinition for AppointmentResponse, last updated
// 2017-04-19T07:44:43.294+10:00. A reply to an appointment request for a
// patient and/or practitioner(s), such as a confirmation or rejection. See
// https://www.hl7.org/fhir/appointmentresponse.html
message AppointmentResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this item
  repeated Identifier identifier = 9;

  // Appointment this response relates to
  Reference appointment = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Time from appointment, or requested new start time
  Instant start = 11;

  // Time from appointment, or requested new end time
  Instant end = 12;

  // Role of participant in the appointment
  repeated CodeableConcept participant_type = 13;

  // Person, Location/HealthcareService or Device
  Reference actor = 14;

  // accepted | declined | tentative | in-process | completed | needs-action |
  // entered-in-error
  ParticipationStatusCode participant_status = 15
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Additional comments
  String comment = 16;
}

// Auto-generated from StructureDefinition for AuditEvent, last updated
// 2017-04-19T07:44:43.294+10:00. Event record kept for security purposes. See
// https://www.hl7.org/fhir/auditevent.html
message AuditEvent {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Type/identifier of event
  Coding type = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // More specific type/id for the event
  repeated Coding subtype = 10;

  // Type of action performed during the event
  AuditEventActionCode action = 11;

  // Time when the event occurred on source
  Instant recorded = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Whether the event succeeded or failed
  AuditEventOutcomeCode outcome = 13;

  // Description of the event outcome
  String outcome_desc = 14;

  // The purposeOfUse of the event
  repeated CodeableConcept purpose_of_event = 15;

  // Actor involved in the event
  message Agent {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Agent role in the event
    repeated CodeableConcept role = 4;

    // Direct reference to resource
    Reference reference = 5;

    // Unique identifier for the user
    Identifier user_id = 6;

    // Alternative User id e.g. authentication
    String alt_id = 7;

    // Human-meaningful name for the agent
    String name = 8;

    // Whether user is initiator
    Boolean requestor = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Where
    Reference location = 10;

    // Policy that authorized event
    repeated Uri policy = 11;

    // Type of media
    Coding media = 12;

    // Logical network location for application activity
    message Network {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Identifier for the network access point of the user device
      String address = 4;

      // The type of network access point
      AuditEventAgentNetworkTypeCode type = 5;
    }
    Network network = 13;

    // Reason given for this user
    repeated CodeableConcept purpose_of_use = 14;
  }
  repeated Agent agent = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Audit Event Reporter
  message Source {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Logical source location within the enterprise
    String site = 4;

    // The identity of source detecting the event
    Identifier identifier = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The type of source where event originated
    repeated Coding type = 6;
  }
  Source source = 17 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Data or objects used
  message Entity {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Specific instance of object
    Identifier identifier = 4;

    // Specific instance of resource
    Reference reference = 5;

    // Type of entity involved
    Coding type = 6;

    // What role the entity played
    Coding role = 7;

    // Life-cycle stage for the entity
    Coding lifecycle = 8;

    // Security labels on the entity
    repeated Coding security_label = 9;

    // Descriptor for entity
    String name = 10;

    // Descriptive text
    String description = 11;

    // Query parameters
    Base64Binary query = 12;

    // Additional Information about the entity
    message Detail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Name of the property
      String type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Property value
      Base64Binary value = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Detail detail = 13;
  }
  repeated Entity entity = 18;
}

// Auto-generated from StructureDefinition for Basic, last updated
// 2017-04-19T07:44:43.294+10:00. Resource for non-supported content. See
// https://www.hl7.org/fhir/basic.html
message Basic {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // Kind of Resource
  CodeableConcept code = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Identifies the focus of this resource
  Reference subject = 11;

  // When created
  Date created = 12;

  // Who created
  Reference author = 13;
}

// Auto-generated from StructureDefinition for Binary, last updated
// 2017-04-19T07:44:43.294+10:00. Pure binary content defined by a format other
// than FHIR. See https://www.hl7.org/fhir/binary.html
message Binary {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // MimeType of the binary content
  MimeTypeCode content_type = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Access Control Management
  Reference security_context = 6;

  // The actual content
  Base64Binary content = 7 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for BodySite, last updated
// 2017-04-19T07:44:43.294+10:00. Specific and identified anatomical location.
// See https://www.hl7.org/fhir/bodysite.html
message BodySite {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Bodysite identifier
  repeated Identifier identifier = 9;

  // Whether this body site record is in active use
  Boolean active = 10;

  // Named anatomical location
  CodeableConcept code = 11;

  // Modification to location code
  repeated CodeableConcept qualifier = 12;

  // Anatomical location description
  String description = 13;

  // Attached images
  repeated Attachment image = 14;

  // Who this is about
  Reference patient = 15 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for Bundle, last updated
// 2017-04-19T07:44:43.294+10:00. Contains a collection of resources. See
// https://www.hl7.org/fhir/bundle.html
message Bundle {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Persistent identifier for the bundle
  Identifier identifier = 5;

  // document | message | transaction | transaction-response | batch |
  // batch-response | history | searchset | collection
  BundleTypeCode type = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

  // If search, the total number of matches
  UnsignedInt total = 7;

  // Links related to this Bundle
  message Link {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // See
    // http://www.iana.org/assignments/link-relations/link-relations.xhtml#link-relations-1
    String relation = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Reference details for the link
    Uri url = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Link link = 8;

  // Entry in the bundle - will have a resource, or information
  message Entry {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Links related to this entry
    repeated Link link = 4;

    // Absolute URL for resource (server address, or UUID/OID)
    Uri full_url = 5;

    // A resource in the bundle
    ContainedResource resource = 6;

    // Search related information
    message Search {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // match | include | outcome - why this is in the result set
      SearchEntryModeCode mode = 4;

      // Search ranking (between 0 and 1)
      Decimal score = 5;
    }
    Search search = 7;

    // Transaction Related Information
    message Request {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // GET | POST | PUT | DELETE
      HTTPVerbCode method = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // URL for HTTP equivalent of this entry
      Uri url = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

      // For managing cache currency
      String if_none_match = 6;

      // For managing update contention
      Instant if_modified_since = 7;

      // For managing update contention
      String if_match = 8;

      // For conditional creates
      String if_none_exist = 9;
    }
    Request request = 8;

    // Transaction Related Information
    message Response {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Status response code (text optional)
      String status = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // The location, if the operation returns a location
      Uri location = 5;

      // The etag for the resource (if relevant)
      String etag = 6;

      // Server's date time modified
      Instant last_modified = 7;

      // OperationOutcome with hints and warnings (for batch/transaction)
      ContainedResource outcome = 8;
    }
    Response response = 9;
  }
  repeated Entry entry = 9;

  // Digital Signature
  Signature signature = 10;
}

// Auto-generated from StructureDefinition for CapabilityStatement, last updated
// 2017-04-19T07:44:43.294+10:00. A statement of system capabilities. See
// https://www.hl7.org/fhir/capabilitystatement.html
message CapabilityStatement {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this capability statement (globally unique)
  Uri url = 9;

  // Business version of the capability statement
  String version = 10;

  // Name for this capability statement (computer friendly)
  String name = 11;

  // Name for this capability statement (human friendly)
  String title = 12;

  // draft | active | retired | unknown
  PublicationStatusCode status = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 14;

  // Date this was last changed
  DateTime date = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name of the publisher (organization or individual)
  String publisher = 16;

  // Contact details for the publisher
  repeated ContactDetail contact = 17;

  // Natural language description of the capability statement
  Markdown description = 18;

  // Context the content is intended to support
  repeated UsageContext use_context = 19;

  // Intended jurisdiction for capability statement (if applicable)
  repeated CodeableConcept jurisdiction = 20;

  // Why this capability statement is defined
  Markdown purpose = 21;

  // Use and/or publishing restrictions
  Markdown copyright = 22;

  // instance | capability | requirements
  CapabilityStatementKindCode kind = 23
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Canonical URL of another capability statement this implements
  repeated Uri instantiates = 24;

  // Software that is covered by this capability statement
  message Software {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // A name the software is known by
    String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Version covered by this statement
    String version = 5;

    // Date this version released
    DateTime release_date = 6;
  }
  Software software = 25;

  // If this describes a specific instance
  message Implementation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Describes this specific instance
    String description = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Base URL for the installation
    Uri url = 5;
  }
  Implementation implementation = 26;

  // FHIR Version the system uses
  Id fhir_version = 27 [(validation_requirement) = REQUIRED_BY_FHIR];

  // no | extensions | elements | both
  UnknownContentCodeCode accept_unknown = 28
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // formats supported (xml | json | ttl | mime type)
  repeated MimeTypeCode format = 29
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Patch formats supported
  repeated MimeTypeCode patch_format = 30;

  // Implementation guides supported
  repeated Uri implementation_guide = 31;

  // Profiles for use cases supported
  repeated Reference profile = 32;

  // If the endpoint is a RESTful one
  message Rest {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // client | server
    RestfulCapabilityModeCode mode = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // General description of implementation
    String documentation = 5;

    // Information about security of implementation
    message Security {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Adds CORS Headers (http://enable-cors.org/)
      Boolean cors = 4;

      // OAuth | SMART-on-FHIR | NTLM | Basic | Kerberos | Certificates
      repeated CodeableConcept service = 5;

      // General description of how security works
      String description = 6;

      // Certificates associated with security profiles
      message Certificate {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Mime type for certificates
        MimeTypeCode type = 4;

        // Actual certificate
        Base64Binary blob = 5;
      }
      repeated Certificate certificate = 7;
    }
    Security security = 6;

    // Resource served on the REST interface
    message Resource {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // A resource type that is supported
      ResourceTypeCode type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Base System profile for all uses of resource
      Reference profile = 5;

      // Additional information about the use of the resource type
      Markdown documentation = 6;

      // What operations are supported?
      message ResourceInteraction {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // read | vread | update | patch | delete | history-instance |
        // history-type | create | search-type
        TypeRestfulInteractionCode code = 4
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // Anything special about operation behavior
        String documentation = 5;
      }
      repeated ResourceInteraction interaction = 7
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // no-version | versioned | versioned-update
      ResourceVersionPolicyCode versioning = 8;

      // Whether vRead can return past versions
      Boolean read_history = 9;

      // If update can commit to a new identity
      Boolean update_create = 10;

      // If allows/uses conditional create
      Boolean conditional_create = 11;

      // not-supported | modified-since | not-match | full-support
      ConditionalReadStatusCode conditional_read = 12;

      // If allows/uses conditional update
      Boolean conditional_update = 13;

      // not-supported | single | multiple - how conditional delete is supported
      ConditionalDeleteStatusCode conditional_delete = 14;

      // literal | logical | resolves | enforced | local
      repeated ReferenceHandlingPolicyCode reference_policy = 15;

      // _include values supported by the server
      repeated String search_include = 16;

      // _revinclude values supported by the server
      repeated String search_rev_include = 17;

      // Search parameters supported by implementation
      message SearchParam {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Name of search parameter
        String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Source of definition for parameter
        Uri definition = 5;

        // number | date | string | token | reference | composite | quantity |
        // uri
        SearchParamTypeCode type = 6
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // Server-specific usage
        String documentation = 7;
      }
      repeated SearchParam search_param = 18;
    }
    repeated Resource resource = 7;

    // What operations are supported?
    message SystemInteraction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // transaction | batch | search-system | history-system
      SystemRestfulInteractionCode code = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Anything special about operation behavior
      String documentation = 5;
    }
    repeated SystemInteraction interaction = 8;

    // Search parameters for searching all resources
    repeated Resource.SearchParam search_param = 9;

    // Definition of an operation or a custom query
    message Operation {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Name by which the operation/query is invoked
      String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // The defined operation/query
      Reference definition = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Operation operation = 10;

    // Compartments served/used by system
    repeated Uri compartment = 11;
  }
  repeated Rest rest = 33;

  // If messaging is supported
  message Messaging {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Where messages should be sent
    message Endpoint {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // http | ftp | mllp +
      Coding protocol = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Network address or identifier of the end-point
      Uri address = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Endpoint endpoint = 4;

    // Reliable Message Cache Length (min)
    UnsignedInt reliable_cache = 5;

    // Messaging interface behavior details
    String documentation = 6;

    // Messages supported by this system
    message SupportedMessage {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // sender | receiver
      EventCapabilityModeCode mode = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Message supported by this system
      Reference definition = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated SupportedMessage supported_message = 7;

    // Declare support for this event
    message Event {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Event type
      Coding code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Consequence | Currency | Notification
      MessageSignificanceCategoryCode category = 5;

      // sender | receiver
      EventCapabilityModeCode mode = 6
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Resource that's focus of message
      ResourceTypeCode focus = 7 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Profile that describes the request
      Reference request = 8 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Profile that describes the response
      Reference response = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Endpoint-specific event documentation
      String documentation = 10;
    }
    repeated Event event = 8;
  }
  repeated Messaging messaging = 34;

  // Document definition
  message Document {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // producer | consumer
    DocumentModeCode mode = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Description of document support
    String documentation = 5;

    // Constraint on a resource used in the document
    Reference profile = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Document document = 35;
}

// Auto-generated from StructureDefinition for CarePlan, last updated
// 2017-04-19T07:44:43.294+10:00. Healthcare plan for patient or group. See
// https://www.hl7.org/fhir/careplan.html
message CarePlan {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this plan
  repeated Identifier identifier = 9;

  // Protocol or definition
  repeated Reference definition = 10;

  // Fulfills care plan
  repeated Reference based_on = 11;

  // CarePlan replaced by this CarePlan
  repeated Reference replaces = 12;

  // Part of referenced CarePlan
  repeated Reference part_of = 13;

  // draft | active | suspended | completed | entered-in-error | cancelled |
  // unknown
  CarePlanStatusCode status = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // proposal | plan | order | option
  CarePlanIntentCode intent = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Type of plan
  repeated CodeableConcept category = 16;

  // Human-friendly name for the CarePlan
  String title = 17;

  // Summary of nature of plan
  String description = 18;

  // Who care plan is for
  Reference subject = 19 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Created in context of
  Reference context = 20;

  // Time period plan covers
  Period period = 21;

  // Who is responsible for contents of the plan
  repeated Reference author = 22;

  // Who's involved in plan?
  repeated Reference care_team = 23;

  // Health issues this plan addresses
  repeated Reference addresses = 24;

  // Information considered as part of plan
  repeated Reference supporting_info = 25;

  // Desired outcome of plan
  repeated Reference goal = 26;

  // Action to occur as part of plan
  message Activity {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Results of the activity
    repeated CodeableConcept outcome_codeable_concept = 4;

    // Appointment, Encounter, Procedure, etc.
    repeated Reference outcome_reference = 5;

    // Comments about the activity status/progress
    repeated Annotation progress = 6;

    // Activity details defined in specific resource
    Reference reference = 7;

    // In-line definition of activity
    message Detail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // diet | drug | encounter | observation | procedure | supply | other
      CodeableConcept category = 4;

      // Protocol or definition
      Reference definition = 5;

      // Detail type of activity
      CodeableConcept code = 6;

      // Why activity should be done or why activity was prohibited
      repeated CodeableConcept reason_code = 7;

      // Condition triggering need for activity
      repeated Reference reason_reference = 8;

      // Goals this activity relates to
      repeated Reference goal = 9;

      // not-started | scheduled | in-progress | on-hold | completed | cancelled
      // | unknown
      CarePlanActivityStatusCode status = 10
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Reason for current status
      String status_reason = 11;

      // Do NOT do
      Boolean prohibited = 12;

      // When activity is to occur
      message Scheduled {
        oneof scheduled {
          Timing timing = 1;
          Period period = 2;
          String string_value = 3 [json_name = "string"];
        }
      }
      Scheduled scheduled = 13 [(is_choice_type) = true];

      // Where it should happen
      Reference location = 14;

      // Who will be responsible?
      repeated Reference performer = 15;

      // What is to be administered/supplied
      message Product {
        oneof product {
          CodeableConcept codeable_concept = 1;
          Reference reference = 2;
        }
      }
      Product product = 16 [(is_choice_type) = true];

      // How to consume/day?
      Quantity daily_amount = 17;

      // How much to administer/supply/consume
      Quantity quantity = 18;

      // Extra info describing activity to perform
      String description = 19;
    }
    Detail detail = 8;
  }
  repeated Activity activity = 27;

  // Comments about the plan
  repeated Annotation note = 28;
}

// Auto-generated from StructureDefinition for CareTeam, last updated
// 2017-04-19T07:44:43.294+10:00. Planned participants in the coordination and
// delivery of care for a patient or group. See
// https://www.hl7.org/fhir/careteam.html
message CareTeam {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this team
  repeated Identifier identifier = 9;

  // proposed | active | suspended | inactive | entered-in-error
  CareTeamStatusCode status = 10;

  // Type of team
  repeated CodeableConcept category = 11;

  // Name of the team, such as crisis assessment team
  String name = 12;

  // Who care team is for
  Reference subject = 13;

  // Encounter or episode associated with CareTeam
  Reference context = 14;

  // Time period team covers
  Period period = 15;

  // Members of the team
  message Participant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of involvement
    CodeableConcept role = 4;

    // Who is involved
    Reference member = 5;

    // Organization of the practitioner
    Reference on_behalf_of = 6;

    // Time period of participant
    Period period = 7;
  }
  repeated Participant participant = 16;

  // Why the care team exists
  repeated CodeableConcept reason_code = 17;

  // Why the care team exists
  repeated Reference reason_reference = 18;

  // Organization responsible for the care team
  repeated Reference managing_organization = 19;

  // Comments made about the CareTeam
  repeated Annotation note = 20;
}

// Auto-generated from StructureDefinition for ChargeItem, last updated
// 2017-04-19T07:44:43.294+10:00. Item containing charge code(s) associated with
// the provision of healthcare provider products. See
// https://www.hl7.org/fhir/chargeitem.html
message ChargeItem {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier for item
  Identifier identifier = 9;

  // Defining information about the code of this charge item
  repeated Uri definition = 10;

  // planned | billable | not-billable | aborted | billed | entered-in-error |
  // unknown
  ChargeItemStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Part of referenced ChargeItem
  repeated Reference part_of = 12;

  // A code that identifies the charge, like a billing code
  CodeableConcept code = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Individual service was done for/to
  Reference subject = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter / Episode associated with event
  Reference context = 15;

  // When the charged service was applied
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
      Timing timing = 3;
    }
  }
  Occurrence occurrence = 16 [(is_choice_type) = true];

  // Who performed charged service
  message Participant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // What type of performance was done
    CodeableConcept role = 4;

    // Individual who was performing
    Reference actor = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Participant participant = 17;

  // Organization providing the charged sevice
  Reference performing_organization = 18;

  // Organization requesting the charged service
  Reference requesting_organization = 19;

  // Quantity of which the charge item has been serviced
  Quantity quantity = 20;

  // Anatomical location, if relevant
  repeated CodeableConcept bodysite = 21;

  // Factor overriding the associated rules
  Decimal factor_override = 22;

  // Price overriding the associated rules
  Money price_override = 23;

  // Reason for overriding the list price/factor
  String override_reason = 24;

  // Individual who was entering
  Reference enterer = 25;

  // Date the charge item was entered
  DateTime entered_date = 26;

  // Why was the charged  service rendered?
  repeated CodeableConcept reason = 27;

  // Which rendered service is being charged?
  repeated Reference service = 28;

  // Account to place this charge
  repeated Reference account = 29;

  // Comments made about the ChargeItem
  repeated Annotation note = 30;

  // Further information supporting the this charge
  repeated Reference supporting_information = 31;
}

// Auto-generated from StructureDefinition for Claim, last updated
// 2017-04-19T07:44:43.294+10:00. Claim, Pre-determination or Pre-authorization.
// See https://www.hl7.org/fhir/claim.html
message Claim {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Claim number
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Type or discipline
  CodeableConcept type = 11;

  // Finer grained claim type information
  repeated CodeableConcept sub_type = 12;

  // complete | proposed | exploratory | other
  UseCode use = 13;

  // The subject of the Products and Services
  Reference patient = 14;

  // Period for charge submission
  Period billable_period = 15;

  // Creation date
  DateTime created = 16;

  // Author
  Reference enterer = 17;

  // Target
  Reference insurer = 18;

  // Responsible provider
  Reference provider = 19;

  // Responsible organization
  Reference organization = 20;

  // Desired processing priority
  CodeableConcept priority = 21;

  // Funds requested to be reserved
  CodeableConcept funds_reserve = 22;

  // Related Claims which may be revelant to processing this claimn
  message RelatedClaim {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reference to the related claim
    Reference claim = 4;

    // How the reference claim is related
    CodeableConcept relationship = 5;

    // Related file or case reference
    Identifier reference = 6;
  }
  repeated RelatedClaim related = 23;

  // Prescription authorizing services or products
  Reference prescription = 24;

  // Original prescription if superceded by fulfiller
  Reference original_prescription = 25;

  // Party to be paid any benefits payable
  message Payee {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of party: Subscriber, Provider, other
    CodeableConcept type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // organization | patient | practitioner | relatedperson
    Coding resource_type = 5;

    // Party to receive the payable
    Reference party = 6;
  }
  Payee payee = 26;

  // Treatment Referral
  Reference referral = 27;

  // Servicing Facility
  Reference facility = 28;

  // Members of the care team
  message CareTeam {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Number to covey order of careTeam
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Provider individual or organization
    Reference provider = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Billing provider
    Boolean responsible = 6;

    // Role on the team
    CodeableConcept role = 7;

    // Type, classification or Specialization
    CodeableConcept qualification = 8;
  }
  repeated CareTeam care_team = 29;

  // Exceptions, special considerations, the condition, situation, prior or
  // concurrent issues
  message SpecialCondition {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Information instance identifier
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // General class of information
    CodeableConcept category = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Type of information
    CodeableConcept code = 6;

    // When it occurred
    message TimingType {
      oneof timing {
        Date date = 1;
        Period period = 2;
      }
    }
    TimingType timing = 7 [(is_choice_type) = true];

    // Additional Data or supporting information
    message Value {
      oneof value {
        String string_value = 1 [json_name = "string"];
        Quantity quantity = 2;
        Attachment attachment = 3;
        Reference reference = 4;
      }
    }
    Value value = 8 [(is_choice_type) = true];

    // Reason associated with the information
    CodeableConcept reason = 9;
  }
  repeated SpecialCondition information = 30;

  // List of Diagnosis
  message Diagnosis {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Number to covey order of diagnosis
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Patient's diagnosis
    message DiagnosisType {
      oneof diagnosis {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    DiagnosisType diagnosis = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Timing or nature of the diagnosis
    repeated CodeableConcept type = 6;

    // Package billing code
    CodeableConcept package_code = 7;
  }
  repeated Diagnosis diagnosis = 31;

  // Procedures performed
  message Procedure {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Procedure sequence for reference
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // When the procedure was performed
    DateTime date = 5;

    // Patient's list of procedures performed
    message ProcedureType {
      oneof procedure {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    ProcedureType procedure = 6
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Procedure procedure = 32;

  // Insurance or medical plan
  message Insurance {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instance identifier
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Is the focal Coverage
    Boolean focal = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Insurance information
    Reference coverage = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Business agreement
    String business_arrangement = 7;

    // Pre-Authorization/Determination Reference
    repeated String pre_auth_ref = 8;

    // Adjudication results
    Reference claim_response = 9;
  }
  repeated Insurance insurance = 33;

  // Details about an accident
  message Accident {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // When the accident occurred
    // see information codes
    // see information codes
    Date date = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The nature of the accident
    CodeableConcept type = 5;

    // Accident Place
    message Location {
      oneof location {
        Address address = 1;
        Reference reference = 2;
      }
    }
    Location location = 6 [(is_choice_type) = true];
  }
  Accident accident = 34;

  // Period unable to work
  Period employment_impacted = 35;

  // Period in hospital
  Period hospitalization = 36;

  // Goods and Services
  message Item {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instance
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Applicable careTeam members
    repeated PositiveInt care_team_link_id = 5;

    // Applicable diagnoses
    repeated PositiveInt diagnosis_link_id = 6;

    // Applicable procedures
    repeated PositiveInt procedure_link_id = 7;

    // Applicable exception and supporting information
    repeated PositiveInt information_link_id = 8;

    // Revenue or cost center code
    CodeableConcept revenue = 9;

    // Type of service or product
    CodeableConcept category = 10;

    // Billing Code
    CodeableConcept service = 11;

    // Service/Product billing modifiers
    repeated CodeableConcept modifier = 12;

    // Program specific reason for item inclusion
    repeated CodeableConcept program_code = 13;

    // Date or dates of Service
    message Serviced {
      oneof serviced {
        Date date = 1;
        Period period = 2;
      }
    }
    Serviced serviced = 14 [(is_choice_type) = true];

    // Place of service
    message Location {
      oneof location {
        CodeableConcept codeable_concept = 1;
        Address address = 2;
        Reference reference = 3;
      }
    }
    Location location = 15 [(is_choice_type) = true];

    // Count of Products or Services
    Quantity quantity = 16;

    // Fee, charge or cost per point
    Money unit_price = 17;

    // Price scaling factor
    Decimal factor = 18;

    // Total item cost
    Money net = 19;

    // Unique Device Identifier
    repeated Reference udi = 20;

    // Service Location
    CodeableConcept body_site = 21;

    // Service Sub-location
    repeated CodeableConcept sub_site = 22;

    // Encounters related to this billed item
    repeated Reference encounter = 23;

    // Additional items
    message Detail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Service instance
      PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Revenue or cost center code
      CodeableConcept revenue = 5;

      // Type of service or product
      CodeableConcept category = 6;

      // Billing Code
      CodeableConcept service = 7;

      // Service/Product billing modifiers
      repeated CodeableConcept modifier = 8;

      // Program specific reason for item inclusion
      repeated CodeableConcept program_code = 9;

      // Count of Products or Services
      Quantity quantity = 10;

      // Fee, charge or cost per point
      Money unit_price = 11;

      // Price scaling factor
      Decimal factor = 12;

      // Total additional item cost
      Money net = 13;

      // Unique Device Identifier
      repeated Reference udi = 14;

      // Additional items
      message SubDetail {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Service instance
        PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Revenue or cost center code
        CodeableConcept revenue = 5;

        // Type of service or product
        CodeableConcept category = 6;

        // Billing Code
        CodeableConcept service = 7;

        // Service/Product billing modifiers
        repeated CodeableConcept modifier = 8;

        // Program specific reason for item inclusion
        repeated CodeableConcept program_code = 9;

        // Count of Products or Services
        Quantity quantity = 10;

        // Fee, charge or cost per point
        Money unit_price = 11;

        // Price scaling factor
        Decimal factor = 12;

        // Net additional item cost
        Money net = 13;

        // Unique Device Identifier
        repeated Reference udi = 14;
      }
      repeated SubDetail sub_detail = 15;
    }
    repeated Detail detail = 24;
  }
  repeated Item item = 37;

  // Total claim cost
  Money total = 38;
}

// Auto-generated from StructureDefinition for ClaimResponse, last updated
// 2017-04-19T07:44:43.294+10:00. Remittance resource. See
// https://www.hl7.org/fhir/claimresponse.html
message ClaimResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Response  number
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // The subject of the Products and Services
  Reference patient = 11;

  // Creation date
  DateTime created = 12;

  // Insurance issuing organization
  Reference insurer = 13;

  // Responsible practitioner
  Reference request_provider = 14;

  // Responsible organization
  Reference request_organization = 15;

  // Id of resource triggering adjudication
  Reference request = 16;

  // complete | error | partial
  CodeableConcept outcome = 17;

  // Disposition Message
  String disposition = 18;

  // Party to be paid any benefits payable
  CodeableConcept payee_type = 19;

  // Line items
  message Item {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instance
    PositiveInt sequence_link_id = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // List of note numbers which apply
    repeated PositiveInt note_number = 5;

    // Adjudication details
    message Adjudication {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Adjudication category such as co-pay, eligible, benefit, etc.
      CodeableConcept category = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Explanation of Adjudication outcome
      CodeableConcept reason = 5;

      // Monetary amount
      Money amount = 6;

      // Non-monetary value
      Decimal value = 7;
    }
    repeated Adjudication adjudication = 6;

    // Detail line items
    message ItemDetail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Service instance
      PositiveInt sequence_link_id = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // List of note numbers which apply
      repeated PositiveInt note_number = 5;

      // Detail level adjudication details
      repeated Adjudication adjudication = 6;

      // Subdetail line items
      message SubDetail {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Service instance
        PositiveInt sequence_link_id = 4
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // List of note numbers which apply
        repeated PositiveInt note_number = 5;

        // Subdetail level adjudication details
        repeated Adjudication adjudication = 6;
      }
      repeated SubDetail sub_detail = 7;
    }
    repeated ItemDetail detail = 7;
  }
  repeated Item item = 20;

  // Insurer added line items
  message AddedItem {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instances
    repeated PositiveInt sequence_link_id = 4;

    // Revenue or cost center code
    CodeableConcept revenue = 5;

    // Type of service or product
    CodeableConcept category = 6;

    // Group, Service or Product
    CodeableConcept service = 7;

    // Service/Product billing modifiers
    repeated CodeableConcept modifier = 8;

    // Professional fee or Product charge
    Money fee = 9;

    // List of note numbers which apply
    repeated PositiveInt note_number = 10;

    // Added items adjudication
    repeated Item.Adjudication adjudication = 11;

    // Added items details
    message AddedItemsDetail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Revenue or cost center code
      CodeableConcept revenue = 4;

      // Type of service or product
      CodeableConcept category = 5;

      // Service or Product
      CodeableConcept service = 6;

      // Service/Product billing modifiers
      repeated CodeableConcept modifier = 7;

      // Professional fee or Product charge
      Money fee = 8;

      // List of note numbers which apply
      repeated PositiveInt note_number = 9;

      // Added items detail adjudication
      repeated Item.Adjudication adjudication = 10;
    }
    repeated AddedItemsDetail detail = 12;
  }
  repeated AddedItem add_item = 21;

  // Processing errors
  message Error {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Item sequence number
    PositiveInt sequence_link_id = 4;

    // Detail sequence number
    PositiveInt detail_sequence_link_id = 5;

    // Subdetail sequence number
    PositiveInt subdetail_sequence_link_id = 6;

    // Error code detailing processing issues
    CodeableConcept code = 7 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Error error = 22;

  // Total Cost of service from the Claim
  Money total_cost = 23;

  // Unallocated deductible
  Money unalloc_deductable = 24;

  // Total benefit payable for the Claim
  Money total_benefit = 25;

  // Payment details, if paid
  message Payment {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Partial or Complete
    CodeableConcept type = 4;

    // Payment adjustment for non-Claim issues
    Money adjustment = 5;

    // Explanation for the non-claim adjustment
    CodeableConcept adjustment_reason = 6;

    // Expected data of Payment
    Date date = 7;

    // Payable amount after adjustment
    Money amount = 8;

    // Identifier of the payment instrument
    Identifier identifier = 9;
  }
  Payment payment = 26;

  // Funds reserved status
  Coding reserved = 27;

  // Printed Form Identifier
  CodeableConcept form = 28;

  // Processing notes
  message Note {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Sequence Number for this note
    PositiveInt number = 4;

    // display | print | printoper
    CodeableConcept type = 5;

    // Note explanatory text
    String text = 6;

    // Language if different from the resource
    CodeableConcept language = 7;
  }
  repeated Note process_note = 29;

  // Request for additional information
  repeated Reference communication_request = 30;

  // Insurance or medical plan
  message Insurance {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instance identifier
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Is the focal Coverage
    Boolean focal = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Insurance information
    Reference coverage = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Business agreement
    String business_arrangement = 7;

    // Pre-Authorization/Determination Reference
    repeated String pre_auth_ref = 8;

    // Adjudication results
    Reference claim_response = 9;
  }
  repeated Insurance insurance = 31;
}

// Auto-generated from StructureDefinition for ClinicalImpression, last updated
// 2017-04-19T07:44:43.294+10:00. A clinical assessment performed when planning
// treatments and management strategies for a patient. See
// https://www.hl7.org/fhir/clinicalimpression.html
message ClinicalImpression {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // draft | completed | entered-in-error
  ClinicalImpressionStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Kind of assessment performed
  CodeableConcept code = 11;

  // Why/how the assessment was performed
  String description = 12;

  // Patient or group assessed
  Reference subject = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter or Episode created from
  Reference context = 14;

  // Time of assessment
  message Effective {
    oneof effective {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Effective effective = 15 [(is_choice_type) = true];

  // When the assessment was documented
  DateTime date = 16;

  // The clinician performing the assessment
  Reference assessor = 17;

  // Reference to last assessment
  Reference previous = 18;

  // Relevant impressions of patient state
  repeated Reference problem = 19;

  // One or more sets of investigations (signs, symptions, etc.)
  message Investigation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // A name/code for the set
    CodeableConcept code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Record of a specific investigation
    repeated Reference item = 5;
  }
  repeated Investigation investigation = 20;

  // Clinical Protocol followed
  repeated Uri protocol = 21;

  // Summary of the assessment
  String summary = 22;

  // Possible or likely findings and diagnoses
  message Finding {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // What was found
    message Item {
      oneof item {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    Item item = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Which investigations support finding
    String basis = 5;
  }
  repeated Finding finding = 23;

  // Estimate of likely outcome
  repeated CodeableConcept prognosis_codeable_concept = 24;

  // RiskAssessment expressing likely outcome
  repeated Reference prognosis_reference = 25;

  // Action taken as part of assessment procedure
  repeated Reference action = 26;

  // Comments made about the ClinicalImpression
  repeated Annotation note = 27;
}

// Auto-generated from StructureDefinition for CodeSystem, last updated
// 2017-04-19T07:44:43.294+10:00. A set of codes drawn from one or more code
// systems. See https://www.hl7.org/fhir/codesystem.html
message CodeSystem {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this code system (globally unique) (Coding.system)
  Uri url = 9;

  // Additional identifier for the code system
  Identifier identifier = 10;

  // Business version of the code system (Coding.version)
  String version = 11;

  // Name for this code system (computer friendly)
  String name = 12;

  // Name for this code system (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the code system
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for code system (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Why this code system is defined
  Markdown purpose = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // If code comparison is case sensitive
  Boolean case_sensitive = 24;

  // Canonical URL for value set with entire code system
  Uri value_set = 25;

  // grouped-by | is-a | part-of | classified-with
  CodeSystemHierarchyMeaningCode hierarchy_meaning = 26;

  // If code system defines a post-composition grammar
  Boolean compositional = 27;

  // If definitions are not stable
  Boolean version_needed = 28;

  // not-present | example | fragment | complete
  CodeSystemContentModeCode content = 29
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Total concepts in the code system
  UnsignedInt count = 30;

  // Filter that can be used in a value set
  message Filter {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Code that identifies the filter
    Code code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // How or why the filter is used
    String description = 5;

    // Operators that can be used with filter
    repeated FilterOperatorCode operator = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // What to use for the value
    String value = 7 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Filter filter = 31;

  // Additional information supplied about each concept
  message Property {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Identifies the property on the concepts, and when referred to in
    // operations
    Code code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Formal identifier for the property
    Uri uri = 5;

    // Why the property is defined, and/or what it conveys
    String description = 6;

    // code | Coding | string | integer | boolean | dateTime
    PropertyTypeCode type = 7 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Property property = 32;

  // Concepts in the code system
  message ConceptDefinition {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Code that identifies concept
    Code code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Text to display to the user
    String display = 5;

    // Formal definition
    String definition = 6;

    // Additional representations for the concept
    message Designation {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Human language of the designation
      LanguageCode language = 4;

      // Details how this designation would be used
      Coding use = 5;

      // The text value for this designation
      String value = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Designation designation = 7;

    // Property value for the concept
    message ConceptProperty {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Reference to CodeSystem.property.code
      Code code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Value of the property for this concept
      message Value {
        oneof value {
          option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
          Code code = 1;
          Coding coding = 2;
          String string_value = 3 [json_name = "string"];
          Integer integer = 4;
          Boolean boolean = 5;
          DateTime date_time = 6;
        }
      }
      Value value = 5 [
        (is_choice_type) = true,
        (validation_requirement) = REQUIRED_BY_FHIR
      ];
    }
    repeated ConceptProperty property = 8;

    // Child Concepts (is-a/contains/categorizes)
    repeated ConceptDefinition concept = 9;
  }
  repeated ConceptDefinition concept = 33;
}

// Auto-generated from StructureDefinition for Communication, last updated
// 2017-04-19T07:44:43.294+10:00. A record of information transmitted from a
// sender to a receiver. See https://www.hl7.org/fhir/communication.html
message Communication {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique identifier
  repeated Identifier identifier = 9;

  // Instantiates protocol or definition
  repeated Reference definition = 10;

  // Request fulfilled by this communication
  repeated Reference based_on = 11;

  // Part of this action
  repeated Reference part_of = 12;

  // preparation | in-progress | suspended | aborted | completed |
  // entered-in-error
  EventStatusCode status = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Communication did not occur
  Boolean not_done = 14;

  // Why communication did not occur
  CodeableConcept not_done_reason = 15;

  // Message category
  repeated CodeableConcept category = 16;

  // A channel of communication
  repeated CodeableConcept medium = 17;

  // Focus of message
  Reference subject = 18;

  // Message recipient
  repeated Reference recipient = 19;

  // Focal resources
  repeated Reference topic = 20;

  // Encounter or episode leading to message
  Reference context = 21;

  // When sent
  DateTime sent = 22;

  // When received
  DateTime received = 23;

  // Message sender
  Reference sender = 24;

  // Indication for message
  repeated CodeableConcept reason_code = 25;

  // Why was communication done?
  repeated Reference reason_reference = 26;

  // Message payload
  message Payload {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Message part content
    message Content {
      oneof content {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        String string_value = 1 [json_name = "string"];
        Attachment attachment = 2;
        Reference reference = 3;
      }
    }
    Content content = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Payload payload = 27;

  // Comments made about the communication
  repeated Annotation note = 28;
}

// Auto-generated from StructureDefinition for CommunicationRequest, last
// updated 2017-04-19T07:44:43.294+10:00. A request for information to be sent
// to a receiver. See https://www.hl7.org/fhir/communicationrequest.html
message CommunicationRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique identifier
  repeated Identifier identifier = 9;

  // Fulfills plan or proposal
  repeated Reference based_on = 10;

  // Request(s) replaced by this request
  repeated Reference replaces = 11;

  // Composite request this is part of
  Identifier group_identifier = 12;

  // draft | active | suspended | cancelled | completed | entered-in-error |
  // unknown
  RequestStatusCode status = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Message category
  repeated CodeableConcept category = 14;

  // Message urgency
  RequestPriorityCode priority = 15;

  // A channel of communication
  repeated CodeableConcept medium = 16;

  // Focus of message
  Reference subject = 17;

  // Message recipient
  repeated Reference recipient = 18;

  // Focal resources
  repeated Reference topic = 19;

  // Encounter or episode leading to message
  Reference context = 20;

  // Message payload
  message Payload {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Message part content
    message Content {
      oneof content {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        String string_value = 1 [json_name = "string"];
        Attachment attachment = 2;
        Reference reference = 3;
      }
    }
    Content content = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Payload payload = 21;

  // When scheduled
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Occurrence occurrence = 22 [(is_choice_type) = true];

  // When request transitioned to being actionable
  DateTime authored_on = 23;

  // Message sender
  Reference sender = 24;

  // Who/what is requesting service
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual making the request
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization agent is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 25;

  // Why is communication needed?
  repeated CodeableConcept reason_code = 26;

  // Why is communication needed?
  repeated Reference reason_reference = 27;

  // Comments made about communication request
  repeated Annotation note = 28;
}

// Auto-generated from StructureDefinition for CompartmentDefinition, last
// updated 2017-04-19T07:44:43.294+10:00. Compartment Definition for a resource.
// See https://www.hl7.org/fhir/compartmentdefinition.html
message CompartmentDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this compartment definition (globally unique)
  Uri url = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name for this compartment definition (computer friendly)
  String name = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name for this compartment definition (human friendly)
  String title = 11;

  // draft | active | retired | unknown
  PublicationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 13;

  // Date this was last changed
  DateTime date = 14;

  // Name of the publisher (organization or individual)
  String publisher = 15;

  // Contact details for the publisher
  repeated ContactDetail contact = 16;

  // Natural language description of the compartment definition
  Markdown description = 17;

  // Why this compartment definition is defined
  Markdown purpose = 18;

  // Context the content is intended to support
  repeated UsageContext use_context = 19;

  // Intended jurisdiction for compartment definition (if applicable)
  repeated CodeableConcept jurisdiction = 20;

  // Patient | Encounter | RelatedPerson | Practitioner | Device
  CompartmentTypeCode code = 21 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Whether the search syntax is supported
  Boolean search = 22 [(validation_requirement) = REQUIRED_BY_FHIR];

  // How a resource is related to the compartment
  message Resource {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name of resource type
    ResourceTypeCode code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Search Parameter Name, or chained parameters
    repeated String param = 5;

    // Additional documentation about the resource and compartment
    String documentation = 6;
  }
  repeated Resource resource = 23;
}

// Auto-generated from StructureDefinition for Composition, last updated
// 2017-04-19T07:44:43.294+10:00. A set of resources composed into a single
// coherent clinical statement with clinical attestation. See
// https://www.hl7.org/fhir/composition.html
message Composition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical identifier of composition (version-independent)
  Identifier identifier = 9;

  // preliminary | final | amended | entered-in-error
  CompositionStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Kind of composition (LOINC if possible)
  CodeableConcept type = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Categorization of Composition
  CodeableConcept class = 12;

  // Who and/or what the composition is about
  Reference subject = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Context of the Composition
  Reference encounter = 14;

  // Composition editing time
  DateTime date = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who and/or what authored the composition
  repeated Reference author = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Human Readable name/title
  String title = 17 [(validation_requirement) = REQUIRED_BY_FHIR];

  // As defined by affinity domain
  ConfidentialityClassificationCode confidentiality = 18;

  // Attests to accuracy of composition
  message Attester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // personal | professional | legal | official
    repeated CompositionAttestationModeCode mode = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // When the composition was attested
    DateTime time = 5;

    // Who attested the composition
    Reference party = 6;
  }
  repeated Attester attester = 19;

  // Organization which maintains the composition
  Reference custodian = 20;

  // Relationships to other compositions/documents
  message RelatesTo {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // replaces | transforms | signs | appends
    DocumentRelationshipTypeCode code = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Target of the relationship
    message Target {
      oneof target {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Identifier identifier = 1;
        Reference reference = 2;
      }
    }
    Target target = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated RelatesTo relates_to = 21;

  // The clinical service(s) being documented
  message Event {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Code(s) that apply to the event being documented
    repeated CodeableConcept code = 4;

    // The period covered by the documentation
    Period period = 5;

    // The event(s) being documented
    repeated Reference detail = 6;
  }
  repeated Event event = 22;

  // Composition is broken into sections
  message Section {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Label for section (e.g. for ToC)
    String title = 4;

    // Classification of section (recommended)
    CodeableConcept code = 5;

    // Text summary of the section, for human interpretation
    Narrative text = 6;

    // working | snapshot | changes
    ListModeCode mode = 7;

    // Order of section entries
    CodeableConcept ordered_by = 8;

    // A reference to data that supports this section
    repeated Reference entry = 9;

    // Why the section is empty
    CodeableConcept empty_reason = 10;

    // Nested Section
    repeated Section section = 11;
  }
  repeated Section section = 23;
}

// Auto-generated from StructureDefinition for ConceptMap, last updated
// 2017-04-19T07:44:43.294+10:00. A map from one set of concepts to one or more
// other concepts. See https://www.hl7.org/fhir/conceptmap.html
message ConceptMap {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this concept map (globally unique)
  Uri url = 9;

  // Additional identifier for the concept map
  Identifier identifier = 10;

  // Business version of the concept map
  String version = 11;

  // Name for this concept map (computer friendly)
  String name = 12;

  // Name for this concept map (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the concept map
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for concept map (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Why this concept map is defined
  Markdown purpose = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // Identifies the source of the concepts which are being mapped
  message Source {
    oneof source {
      Uri uri = 1;
      Reference reference = 2;
    }
  }
  Source source = 24 [(is_choice_type) = true];

  // Provides context to the mappings
  message Target {
    oneof target {
      Uri uri = 1;
      Reference reference = 2;
    }
  }
  Target target = 25 [(is_choice_type) = true];

  // Same source and target systems
  message Group {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Code System (if value set crosses code systems)
    Uri source = 4;

    // Specific version of the  code system
    String source_version = 5;

    // System of the target (if necessary)
    Uri target = 6;

    // Specific version of the  code system
    String target_version = 7;

    // Mappings for a concept from the source set
    message SourceElement {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Identifies element being mapped
      Code code = 4;

      // Display for the code
      String display = 5;

      // Concept in target system for element
      message TargetElement {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Code that identifies the target element
        Code code = 4;

        // Display for the code
        String display = 5;

        // relatedto | equivalent | equal | wider | subsumes | narrower |
        // specializes | inexact | unmatched | disjoint
        ConceptMapEquivalenceCode equivalence = 6;

        // Description of status/issues in mapping
        String comment = 7;

        // Other elements required for this mapping (from context)
        message OtherElement {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // Reference to property mapping depends on
          Uri property = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

          // Code System (if necessary)
          Uri system = 5;

          // Value of the referenced element
          String code = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

          // Display for the code
          String display = 7;
        }
        repeated OtherElement depends_on = 8;

        // Other concepts that this mapping also produces
        repeated OtherElement product = 9;
      }
      repeated TargetElement target = 6;
    }
    repeated SourceElement element = 8
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // When no match in the mappings
    message Unmapped {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // provided | fixed | other-map
      ConceptMapGroupUnmappedModeCode mode = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Fixed code when mode = fixed
      Code code = 5;

      // Display for the code
      String display = 6;

      // Canonical URL for other concept map
      Uri url = 7;
    }
    Unmapped unmapped = 9;
  }
  repeated Group group = 26;
}

// Auto-generated from StructureDefinition for Condition, last updated
// 2017-04-19T07:44:43.294+10:00. Detailed information about conditions,
// problems or diagnoses. See https://www.hl7.org/fhir/condition.html
message Condition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this condition
  repeated Identifier identifier = 9;

  // active | recurrence | inactive | remission | resolved
  ConditionClinicalStatusCodesCode clinical_status = 10;

  // provisional | differential | confirmed | refuted | entered-in-error |
  // unknown
  ConditionVerificationStatusCode verification_status = 11;

  // problem-list-item | encounter-diagnosis
  repeated CodeableConcept category = 12;

  // Subjective severity of condition
  CodeableConcept severity = 13;

  // Identification of the condition, problem or diagnosis
  CodeableConcept code = 14;

  // Anatomical location, if relevant
  repeated CodeableConcept body_site = 15;

  // Who has the condition?
  Reference subject = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter or episode when condition first asserted
  Reference context = 17;

  // Estimated or actual date,  date-time, or age
  message Onset {
    oneof onset {
      DateTime date_time = 1;
      Age age = 2;
      Period period = 3;
      Range range = 4;
      String string_value = 5 [json_name = "string"];
    }
  }
  Onset onset = 18 [(is_choice_type) = true];

  // If/when in resolution/remission
  message Abatement {
    oneof abatement {
      DateTime date_time = 1;
      Age age = 2;
      Boolean boolean = 3;
      Period period = 4;
      Range range = 5;
      String string_value = 6 [json_name = "string"];
    }
  }
  Abatement abatement = 19 [(is_choice_type) = true];

  // Date record was believed accurate
  DateTime asserted_date = 20;

  // Person who asserts this condition
  Reference asserter = 21;

  // Stage/grade, usually assessed formally
  message Stage {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Simple summary (disease specific)
    CodeableConcept summary = 4;

    // Formal record of assessment
    repeated Reference assessment = 5;
  }
  Stage stage = 22;

  // Supporting evidence
  message Evidence {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Manifestation/symptom
    repeated CodeableConcept code = 4;

    // Supporting information found elsewhere
    repeated Reference detail = 5;
  }
  repeated Evidence evidence = 23;

  // Additional information about the Condition
  repeated Annotation note = 24;
}

// Auto-generated from StructureDefinition for Consent, last updated
// 2017-04-19T07:44:43.294+10:00. A healthcare consumer's policy choices to
// permits or denies recipients or roles to perform actions for specific
// purposes and periods of time. See https://www.hl7.org/fhir/consent.html
message Consent {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifier for this record (external references)
  Identifier identifier = 9;

  // draft | proposed | active | rejected | inactive | entered-in-error
  ConsentStateCode status = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Classification of the consent statement - for indexing/retrieval
  repeated CodeableConcept category = 11;

  // Who the consent applies to
  Reference patient = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Period that this consent applies
  Period period = 13;

  // When this Consent was created or indexed
  DateTime date_time = 14;

  // Who is agreeing to the policy and exceptions
  repeated Reference consenting_party = 15;

  // Who|what controlled by this consent (or group, by role)
  message Actor {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // How the actor is involved
    CodeableConcept role = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Resource for the actor (or group, by role)
    Reference reference = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Actor actor = 16;

  // Actions controlled by this consent
  repeated CodeableConcept action = 17;

  // Custodian of the consent
  repeated Reference organization = 18;

  // Source from which this consent is taken
  message Source {
    oneof source {
      Attachment attachment = 1;
      Identifier identifier = 2;
      Reference reference = 3;
    }
  }
  Source source = 19 [(is_choice_type) = true];

  // Policies covered by this consent
  message Policy {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Enforcement source for policy
    Uri authority = 4;

    // Specific policy covered by this consent
    Uri uri = 5;
  }
  repeated Policy policy = 20;

  // Policy that this consents to
  Uri policy_rule = 21;

  // Security Labels that define affected resources
  repeated Coding security_label = 22;

  // Context of activities for which the agreement is made
  repeated Coding purpose = 23;

  // Timeframe for data controlled by this consent
  Period data_period = 24;

  // Data controlled by this consent
  message Data {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // instance | related | dependents | authoredby
    ConsentDataMeaningCode meaning = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // The actual data reference
    Reference reference = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Data data = 25;

  // Additional rule -  addition or removal of permissions
  message Except {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // deny | permit
    ConsentExceptTypeCode type = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Timeframe for this exception
    Period period = 5;

    // Who|what controlled by this exception (or group, by role)
    message ExceptActor {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // How the actor is involved
      CodeableConcept role = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Resource for the actor (or group, by role)
      Reference reference = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated ExceptActor actor = 6;

    // Actions controlled by this exception
    repeated CodeableConcept action = 7;

    // Security Labels that define affected resources
    repeated Coding security_label = 8;

    // Context of activities covered by this exception
    repeated Coding purpose = 9;

    // e.g. Resource Type, Profile, or CDA etc
    repeated Coding class = 10;

    // e.g. LOINC or SNOMED CT code, etc in the content
    repeated Coding code = 11;

    // Timeframe for data controlled by this exception
    Period data_period = 12;

    // Data controlled by this exception
    message ExceptData {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // instance | related | dependents | authoredby
      ConsentDataMeaningCode meaning = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // The actual data reference
      Reference reference = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated ExceptData data = 13;
  }
  repeated Except except = 26;
}

// Auto-generated from StructureDefinition for Contract, last updated
// 2017-04-19T07:44:43.294+10:00. Legal Agreement. See
// https://www.hl7.org/fhir/contract.html
message Contract {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Contract number
  Identifier identifier = 9;

  // amended | appended | cancelled | disputed | entered-in-error | executable |
  // executed | negotiable | offered | policy | rejected | renewed | revoked |
  // resolved | terminated
  ContractResourceStatusCode status = 10;

  // When this Contract was issued
  DateTime issued = 11;

  // Effective time
  Period applies = 12;

  // Contract Target Entity
  repeated Reference subject = 13;

  // Context of the Contract
  repeated Reference topic = 14;

  // Authority under which this Contract has standing
  repeated Reference authority = 15;

  // Domain in which this Contract applies
  repeated Reference domain = 16;

  // Type or form
  CodeableConcept type = 17;

  // Subtype within the context of type
  repeated CodeableConcept sub_type = 18;

  // Action stipulated by this Contract
  repeated CodeableConcept action = 19;

  // Rationale for the stiplulated action
  repeated CodeableConcept action_reason = 20;

  // Decision by Grantor
  CodeableConcept decision_type = 21;

  // Content derived from the basal information
  CodeableConcept content_derivative = 22;

  // Security Labels that define affected resources
  repeated Coding security_label = 23;

  // Entity being ascribed responsibility
  message Agent {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Contract Agent Type
    Reference actor = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Role type of the agent
    repeated CodeableConcept role = 5;
  }
  repeated Agent agent = 24;

  // Contract Signatory
  message Signatory {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Contract Signatory Role
    Coding type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Contract Signatory Party
    Reference party = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Contract Documentation Signature
    repeated Signature signature = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Signatory signer = 25;

  // Contract Valued Item List
  message ValuedItem {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Contract Valued Item Type
    message Entity {
      oneof entity {
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    Entity entity = 4 [(is_choice_type) = true];

    // Contract Valued Item Number
    Identifier identifier = 5;

    // Contract Valued Item Effective Tiem
    DateTime effective_time = 6;

    // Count of Contract Valued Items
    Quantity quantity = 7;

    // Contract Valued Item fee, charge, or cost
    Money unit_price = 8;

    // Contract Valued Item Price Scaling Factor
    Decimal factor = 9;

    // Contract Valued Item Difficulty Scaling Factor
    Decimal points = 10;

    // Total Contract Valued Item Value
    Money net = 11;
  }
  repeated ValuedItem valued_item = 26;

  // Contract Term List
  message Term {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Contract Term Number
    Identifier identifier = 4;

    // Contract Term Issue Date Time
    DateTime issued = 5;

    // Contract Term Effective Time
    Period applies = 6;

    // Contract Term Type or Form
    CodeableConcept type = 7;

    // Contract Term Type specific classification
    CodeableConcept sub_type = 8;

    // Context of the Contract term
    repeated Reference topic = 9;

    // Contract Term Activity
    repeated CodeableConcept action = 10;

    // Purpose for the Contract Term Action
    repeated CodeableConcept action_reason = 11;

    // Security Labels that define affected terms
    repeated Coding security_label = 12;

    // Contract Term Agent List
    message TermAgent {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Contract Term Agent Subject
      Reference actor = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Type of the Contract Term Agent
      repeated CodeableConcept role = 5;
    }
    repeated TermAgent agent = 13;

    // Human readable Contract term text
    String text = 14;

    // Contract Term Valued Item List
    message TermValuedItem {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Contract Term Valued Item Type
      message Entity {
        oneof entity {
          CodeableConcept codeable_concept = 1;
          Reference reference = 2;
        }
      }
      Entity entity = 4 [(is_choice_type) = true];

      // Contract Term Valued Item Number
      Identifier identifier = 5;

      // Contract Term Valued Item Effective Tiem
      DateTime effective_time = 6;

      // Contract Term Valued Item Count
      Quantity quantity = 7;

      // Contract Term Valued Item fee, charge, or cost
      Money unit_price = 8;

      // Contract Term Valued Item Price Scaling Factor
      Decimal factor = 9;

      // Contract Term Valued Item Difficulty Scaling Factor
      Decimal points = 10;

      // Total Contract Term Valued Item Value
      Money net = 11;
    }
    repeated TermValuedItem valued_item = 15;

    // Nested Contract Term Group
    repeated Term group = 16;
  }
  repeated Term term = 27;

  // Binding Contract
  message Binding {
    oneof binding {
      Attachment attachment = 1;
      Reference reference = 2;
    }
  }
  Binding binding = 28 [(is_choice_type) = true];

  // Contract Friendly Language
  message FriendlyLanguage {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Easily comprehended representation of this Contract
    message Content {
      oneof content {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Attachment attachment = 1;
        Reference reference = 2;
      }
    }
    Content content = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated FriendlyLanguage friendly = 29;

  // Contract Legal Language
  message LegalLanguage {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Contract Legal Text
    message Content {
      oneof content {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Attachment attachment = 1;
        Reference reference = 2;
      }
    }
    Content content = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated LegalLanguage legal = 30;

  // Computable Contract Language
  message ComputableLanguage {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Computable Contract Rules
    message Content {
      oneof content {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Attachment attachment = 1;
        Reference reference = 2;
      }
    }
    Content content = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated ComputableLanguage rule = 31;
}

// Auto-generated from StructureDefinition for Coverage, last updated
// 2017-04-19T07:44:43.294+10:00. Insurance or medical plan or a payment
// agreement. See https://www.hl7.org/fhir/coverage.html
message Coverage {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // The primary coverage ID
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Type of coverage such as medical or accident
  CodeableConcept type = 11;

  // Owner of the policy
  Reference policy_holder = 12;

  // Subscriber to the policy
  Reference subscriber = 13;

  // ID assigned to the Subscriber
  String subscriber_id = 14;

  // Plan Beneficiary
  Reference beneficiary = 15;

  // Beneficiary relationship to the Subscriber
  CodeableConcept relationship = 16;

  // Coverage start and end dates
  Period period = 17;

  // Identifier for the plan or agreement issuer
  repeated Reference payor = 18;

  // Additional coverage classifications
  message Group {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // An identifier for the group
    String group = 4;

    // Display text for an identifier for the group
    String group_display = 5;

    // An identifier for the subsection of the group
    String sub_group = 6;

    // Display text for the subsection of the group
    String sub_group_display = 7;

    // An identifier for the plan
    String plan = 8;

    // Display text for the plan
    String plan_display = 9;

    // An identifier for the subsection of the plan
    String sub_plan = 10;

    // Display text for the subsection of the plan
    String sub_plan_display = 11;

    // An identifier for the class
    String class = 12;

    // Display text for the class
    String class_display = 13;

    // An identifier for the subsection of the class
    String sub_class = 14;

    // Display text for the subsection of the subclass
    String sub_class_display = 15;
  }
  Group grouping = 19;

  // Dependent number
  String dependent = 20;

  // The plan instance or sequence counter
  String sequence = 21;

  // Relative order of the coverage
  PositiveInt order = 22;

  // Insurer network
  String network = 23;

  // Contract details
  repeated Reference contract = 24;
}

// Auto-generated from StructureDefinition for DataElement, last updated
// 2017-04-19T07:44:43.294+10:00. Resource data element. See
// https://www.hl7.org/fhir/dataelement.html
message DataElement {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this data element (globally unique)
  Uri url = 9;

  // Additional identifier for the data element
  repeated Identifier identifier = 10;

  // Business version of the data element
  String version = 11;

  // draft | active | retired | unknown
  PublicationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 13;

  // Date this was last changed
  DateTime date = 14;

  // Name of the publisher (organization or individual)
  String publisher = 15;

  // Name for this data element (computer friendly)
  String name = 16;

  // Name for this data element (human friendly)
  String title = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Context the content is intended to support
  repeated UsageContext use_context = 19;

  // Intended jurisdiction for data element (if applicable)
  repeated CodeableConcept jurisdiction = 20;

  // Use and/or publishing restrictions
  Markdown copyright = 21;

  // comparable | fully-specified | equivalent | convertable | scaleable |
  // flexible
  DataElementStringencyCode stringency = 22;

  // External specification mapped to
  message Mapping {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Internal id when this mapping is used
    Id identity = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Identifies what this mapping refers to
    Uri uri = 5;

    // Names what this mapping refers to
    String name = 6;

    // Versions, issues, scope limitations, etc.
    String comment = 7;
  }
  repeated Mapping mapping = 23;

  // Definition of element
  repeated ElementDefinition element = 24
      [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for DetectedIssue, last updated
// 2017-04-19T07:44:43.294+10:00. Clinical issue with action. See
// https://www.hl7.org/fhir/detectedissue.html
message DetectedIssue {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique id for the detected issue
  Identifier identifier = 9;

  // registered | preliminary | final | amended +
  ObservationStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Issue Category, e.g. drug-drug, duplicate therapy, etc.
  CodeableConcept category = 11;

  // high | moderate | low
  DetectedIssueSeverityCode severity = 12;

  // Associated patient
  Reference patient = 13;

  // When identified
  DateTime date = 14;

  // The provider or device that identified the issue
  Reference author = 15;

  // Problem resource
  repeated Reference implicated = 16;

  // Description and context
  String detail = 17;

  // Authority for issue
  Uri reference = 18;

  // Step taken to address
  message Mitigation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // What mitigation?
    CodeableConcept action = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Date committed
    DateTime date = 5;

    // Who is committing?
    Reference author = 6;
  }
  repeated Mitigation mitigation = 19;
}

// Auto-generated from StructureDefinition for Device, last updated
// 2017-04-19T07:44:43.294+10:00. Item used in healthcare. See
// https://www.hl7.org/fhir/device.html
message Device {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Instance identifier
  repeated Identifier identifier = 9;

  // Unique Device Identifier (UDI) Barcode string
  message Udi {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Mandatory fixed portion of UDI
    String device_identifier = 4;

    // Device Name as appears on UDI label
    String name = 5;

    // Regional UDI authority
    Uri jurisdiction = 6;

    // UDI Human Readable Barcode String
    String carrier_h_r_f = 7;

    // UDI Machine Readable Barcode String
    Base64Binary carrier_a_i_d_c = 8;

    // UDI Issuing Organization
    Uri issuer = 9;

    // barcode | rfid | manual +
    UDIEntryTypeCode entry_type = 10;
  }
  Udi udi = 10;

  // active | inactive | entered-in-error | unknown
  FHIRDeviceStatusCode status = 11;

  // What kind of device this is
  CodeableConcept type = 12;

  // Lot number of manufacture
  String lot_number = 13;

  // Name of device manufacturer
  String manufacturer = 14;

  // Date when the device was made
  DateTime manufacture_date = 15;

  // Date and time of expiry of this device (if applicable)
  DateTime expiration_date = 16;

  // Model id assigned by the manufacturer
  String model = 17;

  // Version number (i.e. software)
  String version = 18;

  // Patient to whom Device is affixed
  Reference patient = 19;

  // Organization responsible for device
  Reference owner = 20;

  // Details for human/organization for support
  repeated ContactPoint contact = 21;

  // Where the resource is found
  Reference location = 22;

  // Network address to contact device
  Uri url = 23;

  // Device notes and comments
  repeated Annotation note = 24;

  // Safety Characteristics of Device
  repeated CodeableConcept safety = 25;
}

// Auto-generated from StructureDefinition for DeviceComponent, last updated
// 2017-04-19T07:44:43.294+10:00. An instance of a medical-related component of
// a medical device. See https://www.hl7.org/fhir/devicecomponent.html
message DeviceComponent {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Instance id assigned by the software stack
  Identifier identifier = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // What kind of component it is
  CodeableConcept type = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Recent system change timestamp
  Instant last_system_change = 11;

  // Top-level device resource link
  Reference source = 12;

  // Parent resource link
  Reference parent = 13;

  // Current operational status of the component, for example On, Off or Standby
  repeated CodeableConcept operational_status = 14;

  // Current supported parameter group
  CodeableConcept parameter_group = 15;

  // other | chemical | electrical | impedance | nuclear | optical | thermal |
  // biological | mechanical | acoustical | manual+
  MeasmntPrincipleCode measurement_principle = 16;

  // Specification details such as Component Revisions, or Serial Numbers
  message ProductionSpecification {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type or kind of production specification, for example serial number or
    // software revision
    CodeableConcept spec_type = 4;

    // Internal component unique identification
    Identifier component_id = 5;

    // A printable string defining the component
    String production_spec = 6;
  }
  repeated ProductionSpecification production_specification = 17;

  // Language code for the human-readable text strings produced by the device
  CodeableConcept language_code = 18;
}

// Auto-generated from StructureDefinition for DeviceMetric, last updated
// 2017-04-19T07:44:43.294+10:00. Measurement, calculation or setting capability
// of a medical device. See https://www.hl7.org/fhir/devicemetric.html
message DeviceMetric {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique identifier of this DeviceMetric
  Identifier identifier = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Identity of metric, for example Heart Rate or PEEP Setting
  CodeableConcept type = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Unit of Measure for the Metric
  CodeableConcept unit = 11;

  // Describes the link to the source Device
  Reference source = 12;

  // Describes the link to the parent DeviceComponent
  Reference parent = 13;

  // on | off | standby | entered-in-error
  DeviceMetricOperationalStatusCode operational_status = 14;

  // black | red | green | yellow | blue | magenta | cyan | white
  DeviceMetricColorCode color = 15;

  // measurement | setting | calculation | unspecified
  DeviceMetricCategoryCode category = 16
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Describes the measurement repetition time
  Timing measurement_period = 17;

  // Describes the calibrations that have been performed or that are required to
  // be performed
  message Calibration {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // unspecified | offset | gain | two-point
    DeviceMetricCalibrationTypeCode type = 4;

    // not-calibrated | calibration-required | calibrated | unspecified
    DeviceMetricCalibrationStateCode state = 5;

    // Describes the time last calibration has been performed
    Instant time = 6;
  }
  repeated Calibration calibration = 18;
}

// Auto-generated from StructureDefinition for DeviceRequest, last updated
// 2017-04-19T07:44:43.294+10:00. Medical device request. See
// https://www.hl7.org/fhir/devicerequest.html
message DeviceRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Request identifier
  repeated Identifier identifier = 9;

  // Protocol or definition
  repeated Reference definition = 10;

  // What request fulfills
  repeated Reference based_on = 11;

  // What request replaces
  repeated Reference prior_request = 12;

  // Identifier of composite request
  Identifier group_identifier = 13;

  // draft | active | suspended | completed | entered-in-error | cancelled
  RequestStatusCode status = 14;

  // proposal | plan | original-order | encoded | reflex-order
  CodeableConcept intent = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Indicates how quickly the {{title}} should be addressed with respect to
  // other requests
  RequestPriorityCode priority = 16;

  // Device requested
  message Code {
    oneof code {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      Reference reference = 1;
      CodeableConcept codeable_concept = 2;
    }
  }
  Code code = 17
      [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

  // Focus of request
  Reference subject = 18 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter or Episode motivating request
  Reference context = 19;

  // Desired time or schedule for use
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
      Timing timing = 3;
    }
  }
  Occurrence occurrence = 20 [(is_choice_type) = true];

  // When recorded
  DateTime authored_on = 21;

  // Who/what is requesting diagnostics
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual making the request
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization agent is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 22;

  // Fille role
  CodeableConcept performer_type = 23;

  // Requested Filler
  Reference performer = 24;

  // Coded Reason for request
  repeated CodeableConcept reason_code = 25;

  // Linked Reason for request
  repeated Reference reason_reference = 26;

  // Additional clinical information
  repeated Reference supporting_info = 27;

  // Notes or comments
  repeated Annotation note = 28;

  // Request provenance
  repeated Reference relevant_history = 29;
}

// Auto-generated from StructureDefinition for DeviceUseStatement, last updated
// 2017-04-19T07:44:43.294+10:00. Record of use of a device. See
// https://www.hl7.org/fhir/deviceusestatement.html
message DeviceUseStatement {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifier for this record
  repeated Identifier identifier = 9;

  // active | completed | entered-in-error +
  DeviceUseStatementStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Patient using device
  Reference subject = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Period device was used
  Period when_used = 12;

  // How often  the device was used
  message TimingType {
    oneof timing {
      Timing timing_value = 1 [json_name = "timing"];
      Period period = 2;
      DateTime date_time = 3;
    }
  }
  TimingType timing = 13 [(is_choice_type) = true];

  // When statement was recorded
  DateTime recorded_on = 14;

  // Who made the statement
  Reference source = 15;

  // Reference to device used
  Reference device = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Why device was used
  repeated CodeableConcept indication = 17;

  // Target body site
  CodeableConcept body_site = 18;

  // Addition details (comments, instructions)
  repeated Annotation note = 19;
}

// Auto-generated from StructureDefinition for DiagnosticReport, last updated
// 2017-04-19T07:44:43.294+10:00. A Diagnostic report - a combination of request
// information, atomic results, images, interpretation, as well as formatted
// reports. See https://www.hl7.org/fhir/diagnosticreport.html
message DiagnosticReport {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier for report
  repeated Identifier identifier = 9;

  // What was requested
  repeated Reference based_on = 10;

  // registered | partial | preliminary | final +
  DiagnosticReportStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Service category
  CodeableConcept category = 12;

  // Name/Code for this diagnostic report
  CodeableConcept code = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The subject of the report - usually, but not always, the patient
  Reference subject = 14;

  // Health care event when test ordered
  Reference context = 15;

  // Clinically relevant time/time-period for report
  message Effective {
    oneof effective {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Effective effective = 16 [(is_choice_type) = true];

  // DateTime this version was released
  Instant issued = 17;

  // Participants in producing the report
  message Performer {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of performer
    CodeableConcept role = 4;

    // Practitioner or Organization  participant
    Reference actor = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Performer performer = 18;

  // Specimens this report is based on
  repeated Reference specimen = 19;

  // Observations - simple, or complex nested groups
  repeated Reference result = 20;

  // Reference to full details of imaging associated with the diagnostic report
  repeated Reference imaging_study = 21;

  // Key images associated with this report
  message Image {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Comment about the image (e.g. explanation)
    String comment = 4;

    // Reference to the image source
    Reference link = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Image image = 22;

  // Clinical Interpretation of test results
  String conclusion = 23;

  // Codes for the conclusion
  repeated CodeableConcept coded_diagnosis = 24;

  // Entire report as issued
  repeated Attachment presented_form = 25;
}

// Auto-generated from StructureDefinition for DocumentManifest, last updated
// 2017-04-19T07:44:43.294+10:00. A list that defines a set of documents. See
// https://www.hl7.org/fhir/documentmanifest.html
message DocumentManifest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique Identifier for the set of documents
  Identifier master_identifier = 9;

  // Other identifiers for the manifest
  repeated Identifier identifier = 10;

  // current | superseded | entered-in-error
  DocumentReferenceStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Kind of document set
  CodeableConcept type = 12;

  // The subject of the set of documents
  Reference subject = 13;

  // When this document manifest created
  DateTime created = 14;

  // Who and/or what authored the manifest
  repeated Reference author = 15;

  // Intended to get notified about this set of documents
  repeated Reference recipient = 16;

  // The source system/application/software
  Uri source = 17;

  // Human-readable description (title)
  String description = 18;

  // The items included
  message Content {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Contents of this set of documents
    message P {
      oneof p {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Attachment attachment = 1;
        Reference reference = 2;
      }
    }
    P p = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Content content = 19 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Related things
  message Related {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Identifiers of things that are related
    Identifier identifier = 4;

    // Related Resource
    Reference ref = 5;
  }
  repeated Related related = 20;
}

// Auto-generated from StructureDefinition for DocumentReference, last updated
// 2017-04-19T07:44:43.294+10:00. A reference to a document. See
// https://www.hl7.org/fhir/documentreference.html
message DocumentReference {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Master Version Specific Identifier
  Identifier master_identifier = 9;

  // Other identifiers for the document
  repeated Identifier identifier = 10;

  // current | superseded | entered-in-error
  DocumentReferenceStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // preliminary | final | appended | amended | entered-in-error
  CompositionStatusCode doc_status = 12;

  // Kind of document (LOINC if possible)
  CodeableConcept type = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Categorization of document
  CodeableConcept class = 14;

  // Who/what is the subject of the document
  Reference subject = 15;

  // Document creation time
  DateTime created = 16;

  // When this document reference was created
  Instant indexed = 17 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who and/or what authored the document
  repeated Reference author = 18;

  // Who/what authenticated the document
  Reference authenticator = 19;

  // Organization which maintains the document
  Reference custodian = 20;

  // Relationships to other documents
  message RelatesTo {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // replaces | transforms | signs | appends
    DocumentRelationshipTypeCode code = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Target of the relationship
    Reference target = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated RelatesTo relates_to = 21;

  // Human-readable description (title)
  String description = 22;

  // Document security-tags
  repeated CodeableConcept security_label = 23;

  // Document referenced
  message Content {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Where to access the document
    Attachment attachment = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Format/content rules for the document
    Coding format = 5;
  }
  repeated Content content = 24 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Clinical context of document
  message Context {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Context of the document  content
    Reference encounter = 4;

    // Main clinical acts documented
    repeated CodeableConcept event = 5;

    // Time of service that is being documented
    Period period = 6;

    // Kind of facility where patient was seen
    CodeableConcept facility_type = 7;

    // Additional details about where the content was created (e.g. clinical
    // specialty)
    CodeableConcept practice_setting = 8;

    // Patient demographics from source
    Reference source_patient_info = 9;

    // Related identifiers or resources
    message Related {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Identifier of related objects or events
      Identifier identifier = 4;

      // Related Resource
      Reference ref = 5;
    }
    repeated Related related = 10;
  }
  Context context = 25;
}

// Auto-generated from StructureDefinition for EligibilityRequest, last updated
// 2017-04-19T07:44:43.294+10:00. Determine insurance validity and scope of
// coverage. See https://www.hl7.org/fhir/eligibilityrequest.html
message EligibilityRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Desired processing priority
  CodeableConcept priority = 11;

  // The subject of the Products and Services
  Reference patient = 12;

  // Estimated date or dates of Service
  message Serviced {
    oneof serviced {
      Date date = 1;
      Period period = 2;
    }
  }
  Serviced serviced = 13 [(is_choice_type) = true];

  // Creation date
  DateTime created = 14;

  // Author
  Reference enterer = 15;

  // Responsible practitioner
  Reference provider = 16;

  // Responsible organization
  Reference organization = 17;

  // Target
  Reference insurer = 18;

  // Servicing Facility
  Reference facility = 19;

  // Insurance or medical plan
  Reference coverage = 20;

  // Business agreement
  String business_arrangement = 21;

  // Type of services covered
  CodeableConcept benefit_category = 22;

  // Detailed services covered within the type
  CodeableConcept benefit_sub_category = 23;
}

// Auto-generated from StructureDefinition for EligibilityResponse, last updated
// 2017-04-19T07:44:43.294+10:00. EligibilityResponse resource. See
// https://www.hl7.org/fhir/eligibilityresponse.html
message EligibilityResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Creation date
  DateTime created = 11;

  // Responsible practitioner
  Reference request_provider = 12;

  // Responsible organization
  Reference request_organization = 13;

  // Eligibility reference
  Reference request = 14;

  // complete | error | partial
  CodeableConcept outcome = 15;

  // Disposition Message
  String disposition = 16;

  // Insurer issuing the coverage
  Reference insurer = 17;

  // Coverage inforce indicator
  Boolean inforce = 18;

  // Details by insurance coverage
  message Insurance {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Updated Coverage details
    Reference coverage = 4;

    // Contract details
    Reference contract = 5;

    // Benefits by Category
    message Benefits {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Type of services covered
      CodeableConcept category = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Detailed services covered within the type
      CodeableConcept sub_category = 5;

      // Excluded from the plan
      Boolean excluded = 6;

      // Short name for the benefit
      String name = 7;

      // Description of the benefit or services covered
      String description = 8;

      // In or out of network
      CodeableConcept network = 9;

      // Individual or family
      CodeableConcept unit = 10;

      // Annual or lifetime
      CodeableConcept term = 11;

      // Benefit Summary
      message Benefit {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Deductable, visits, benefit amount
        CodeableConcept type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Benefits allowed
        message Allowed {
          oneof allowed {
            UnsignedInt unsigned_int = 1;
            String string_value = 2 [json_name = "string"];
            Money money = 3;
          }
        }
        Allowed allowed = 5 [(is_choice_type) = true];

        // Benefits used
        message Used {
          oneof used {
            UnsignedInt unsigned_int = 1;
            Money money = 2;
          }
        }
        Used used = 6 [(is_choice_type) = true];
      }
      repeated Benefit financial = 12;
    }
    repeated Benefits benefit_balance = 6;
  }
  repeated Insurance insurance = 19;

  // Printed Form Identifier
  CodeableConcept form = 20;

  // Processing errors
  message Errors {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Error code detailing processing issues
    CodeableConcept code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Errors error = 21;
}

// Auto-generated from StructureDefinition for Encounter, last updated
// 2017-04-19T07:44:43.294+10:00. An interaction during which services are
// provided to the patient. See https://www.hl7.org/fhir/encounter.html
message Encounter {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifier(s) by which this encounter is known
  repeated Identifier identifier = 9;

  // planned | arrived | triaged | in-progress | onleave | finished | cancelled
  // +
  EncounterStatusCode status = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // List of past encounter statuses
  message StatusHistory {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // planned | arrived | triaged | in-progress | onleave | finished |
    // cancelled +
    EncounterStatusCode status = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // The time that the episode was in the specified status
    Period period = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated StatusHistory status_history = 11;

  // inpatient | outpatient | ambulatory | emergency +
  Coding class = 12;

  // List of past encounter classes
  message ClassHistory {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // inpatient | outpatient | ambulatory | emergency +
    Coding class = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The time that the episode was in the specified class
    Period period = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated ClassHistory class_history = 13;

  // Specific type of encounter
  repeated CodeableConcept type = 14;

  // Indicates the urgency of the encounter
  CodeableConcept priority = 15;

  // The patient ro group present at the encounter
  Reference subject = 16;

  // Episode(s) of care that this encounter should be recorded against
  repeated Reference episode_of_care = 17;

  // The ReferralRequest that initiated this encounter
  repeated Reference incoming_referral = 18;

  // List of participants involved in the encounter
  message Participant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Role of participant in encounter
    repeated CodeableConcept type = 4;

    // Period of time during the encounter that the participant participated
    Period period = 5;

    // Persons involved in the encounter other than the patient
    Reference individual = 6;
  }
  repeated Participant participant = 19;

  // The appointment that scheduled this encounter
  Reference appointment = 20;

  // The start and end time of the encounter
  Period period = 21;

  // Quantity of time the encounter lasted (less time absent)
  Duration length = 22;

  // Reason the encounter takes place (code)
  repeated CodeableConcept reason = 23;

  // The list of diagnosis relevant to this encounter
  message Diagnosis {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reason the encounter takes place (resource)
    Reference condition = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Role that this diagnosis has within the encounter (e.g. admission,
    // billing, discharge …)
    CodeableConcept role = 5;

    // Ranking of the diagnosis (for each role type)
    PositiveInt rank = 6;
  }
  repeated Diagnosis diagnosis = 24;

  // The set of accounts that may be used for billing for this Encounter
  repeated Reference account = 25;

  // Details about the admission to a healthcare service
  message Hospitalization {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Pre-admission identifier
    Identifier pre_admission_identifier = 4;

    // The location from which the patient came before admission
    Reference origin = 5;

    // From where patient was admitted (physician referral, transfer)
    CodeableConcept admit_source = 6;

    // The type of hospital re-admission that has occurred (if any). If the
    // value is absent, then this is not identified as a readmission
    CodeableConcept re_admission = 7;

    // Diet preferences reported by the patient
    repeated CodeableConcept diet_preference = 8;

    // Special courtesies (VIP, board member)
    repeated CodeableConcept special_courtesy = 9;

    // Wheelchair, translator, stretcher, etc.
    repeated CodeableConcept special_arrangement = 10;

    // Location to which the patient is discharged
    Reference destination = 11;

    // Category or kind of location after discharge
    CodeableConcept discharge_disposition = 12;
  }
  Hospitalization hospitalization = 26;

  // List of locations where the patient has been
  message Location {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Location the encounter takes place
    Reference location = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // planned | active | reserved | completed
    EncounterLocationStatusCode status = 5;

    // Time period during which the patient was present at the location
    Period period = 6;
  }
  repeated Location location = 27;

  // The custodian organization of this Encounter record
  Reference service_provider = 28;

  // Another Encounter this encounter is part of
  Reference part_of = 29;
}

// Auto-generated from StructureDefinition for Endpoint, last updated
// 2017-04-19T07:44:43.294+10:00. The technical details of an endpoint that can
// be used for electronic services. See https://www.hl7.org/fhir/endpoint.html
message Endpoint {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifies this endpoint across multiple systems
  repeated Identifier identifier = 9;

  // active | suspended | error | off | entered-in-error | test
  EndpointStatusCode status = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Protocol/Profile/Standard to be used with this endpoint connection
  Coding connection_type = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // A name that this endpoint can be identified by
  String name = 12;

  // Organization that manages this endpoint (may not be the organization that
  // exposes the endpoint)
  Reference managing_organization = 13;

  // Contact details for source (e.g. troubleshooting)
  repeated ContactPoint contact = 14;

  // Interval the endpoint is expected to be operational
  Period period = 15;

  // The type of content that may be used at this endpoint (e.g. XDS Discharge
  // summaries)
  repeated CodeableConcept payload_type = 16
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Mimetype to send. If not specified, the content could be anything
  // (including no payload, if the connectionType defined this)
  repeated MimeTypeCode payload_mime_type = 17;

  // The technical base address for connecting to this endpoint
  Uri address = 18 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Usage depends on the channel type
  repeated String header = 19;
}

// Auto-generated from StructureDefinition for EnrollmentRequest, last updated
// 2017-04-19T07:44:43.294+10:00. Enrollment request. See
// https://www.hl7.org/fhir/enrollmentrequest.html
message EnrollmentRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Creation date
  DateTime created = 11;

  // Target
  Reference insurer = 12;

  // Responsible practitioner
  Reference provider = 13;

  // Responsible organization
  Reference organization = 14;

  // The subject of the Products and Services
  Reference subject = 15;

  // Insurance information
  Reference coverage = 16;
}

// Auto-generated from StructureDefinition for EnrollmentResponse, last updated
// 2017-04-19T07:44:43.294+10:00. EnrollmentResponse resource. See
// https://www.hl7.org/fhir/enrollmentresponse.html
message EnrollmentResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Claim reference
  Reference request = 11;

  // complete | error | partial
  CodeableConcept outcome = 12;

  // Disposition Message
  String disposition = 13;

  // Creation date
  DateTime created = 14;

  // Insurer
  Reference organization = 15;

  // Responsible practitioner
  Reference request_provider = 16;

  // Responsible organization
  Reference request_organization = 17;
}

// Auto-generated from StructureDefinition for EpisodeOfCare, last updated
// 2017-04-19T07:44:43.294+10:00. An association of a Patient with an
// Organization and  Healthcare Provider(s) for a period of time that the
// Organization assumes some level of responsibility. See
// https://www.hl7.org/fhir/episodeofcare.html
message EpisodeOfCare {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier(s) relevant for this EpisodeOfCare
  repeated Identifier identifier = 9;

  // planned | waitlist | active | onhold | finished | cancelled |
  // entered-in-error
  EpisodeOfCareStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Past list of status codes (the current status may be included to cover the
  // start date of the status)
  message StatusHistory {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // planned | waitlist | active | onhold | finished | cancelled |
    // entered-in-error
    EpisodeOfCareStatusCode status = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Duration the EpisodeOfCare was in the specified status
    Period period = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated StatusHistory status_history = 11;

  // Type/class  - e.g. specialist referral, disease management
  repeated CodeableConcept type = 12;

  // The list of diagnosis relevant to this episode of care
  message Diagnosis {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Conditions/problems/diagnoses this episode of care is for
    Reference condition = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Role that this diagnosis has within the episode of care (e.g. admission,
    // billing, discharge …)
    CodeableConcept role = 5;

    // Ranking of the diagnosis (for each role type)
    PositiveInt rank = 6;
  }
  repeated Diagnosis diagnosis = 13;

  // The patient who is the focus of this episode of care
  Reference patient = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Organization that assumes care
  Reference managing_organization = 15;

  // Interval during responsibility is assumed
  Period period = 16;

  // Originating Referral Request(s)
  repeated Reference referral_request = 17;

  // Care manager/care co-ordinator for the patient
  Reference care_manager = 18;

  // Other practitioners facilitating this episode of care
  repeated Reference team = 19;

  // The set of accounts that may be used for billing for this EpisodeOfCare
  repeated Reference account = 20;
}

// Auto-generated from StructureDefinition for ExpansionProfile, last updated
// 2017-04-19T07:44:43.294+10:00. Defines behaviour and contraints on the
// ValueSet Expansion operation. See
// https://www.hl7.org/fhir/expansionprofile.html
message ExpansionProfile {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this expansion profile (globally unique)
  Uri url = 9;

  // Additional identifier for the expansion profile
  Identifier identifier = 10;

  // Business version of the expansion profile
  String version = 11;

  // Name for this expansion profile (computer friendly)
  String name = 12;

  // draft | active | retired | unknown
  PublicationStatusCode status = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 14;

  // Date this was last changed
  DateTime date = 15;

  // Name of the publisher (organization or individual)
  String publisher = 16;

  // Contact details for the publisher
  repeated ContactDetail contact = 17;

  // Natural language description of the expansion profile
  Markdown description = 18;

  // Context the content is intended to support
  repeated UsageContext use_context = 19;

  // Intended jurisdiction for expansion profile (if applicable)
  repeated CodeableConcept jurisdiction = 20;

  // Fix use of a code system to a particular version
  message FixedVersion {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // System to have its version fixed
    Uri system = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Specific version of the code system referred to
    String version = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // default | check | override
    SystemVersionProcessingModeCode mode = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated FixedVersion fixed_version = 21;

  // Systems/Versions to be exclude
  message ExcludedSystem {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The specific code system to be excluded
    Uri system = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Specific version of the code system referred to
    String version = 5;
  }
  ExcludedSystem excluded_system = 22;

  // Whether the expansion should include concept designations
  Boolean include_designations = 23;

  // When the expansion profile imposes designation contraints
  message Designation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Designations to be included
    message DesignationInclude {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The designation to be included
      message DesignationIncludeDesignation {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Human language of the designation to be included
        LanguageCode language = 4;

        // What kind of Designation to include
        Coding use = 5;
      }
      repeated DesignationIncludeDesignation designation = 4;
    }
    DesignationInclude include = 4;

    // Designations to be excluded
    message DesignationExclude {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The designation to be excluded
      message DesignationExcludeDesignation {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Human language of the designation to be excluded
        LanguageCode language = 4;

        // What kind of Designation to exclude
        Coding use = 5;
      }
      repeated DesignationExcludeDesignation designation = 4;
    }
    DesignationExclude exclude = 5;
  }
  Designation designation = 24;

  // Include or exclude the value set definition in the expansion
  Boolean include_definition = 25;

  // Include or exclude inactive concepts in the expansion
  Boolean active_only = 26;

  // Nested codes in the expansion or not
  Boolean exclude_nested = 27;

  // Include or exclude codes which cannot be rendered in user interfaces in the
  // value set expansion
  Boolean exclude_not_for_u_i = 28;

  // Include or exclude codes which are post coordinated expressions in the
  // value set expansion
  Boolean exclude_post_coordinated = 29;

  // Specify the language for the display element of codes in the value set
  // expansion
  LanguageCode display_language = 30;

  // Controls behaviour of the value set expand operation when value sets are
  // too large to be completely expanded
  Boolean limited_expansion = 31;
}

// Auto-generated from StructureDefinition for ExplanationOfBenefit, last
// updated 2017-04-19T07:44:43.294+10:00. Explanation of Benefit resource. See
// https://www.hl7.org/fhir/explanationofbenefit.html
message ExplanationOfBenefit {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  ExplanationOfBenefitStatusCode status = 10;

  // Type or discipline
  CodeableConcept type = 11;

  // Finer grained claim type information
  repeated CodeableConcept sub_type = 12;

  // The subject of the Products and Services
  Reference patient = 13;

  // Period for charge submission
  Period billable_period = 14;

  // Creation date
  DateTime created = 15;

  // Author
  Reference enterer = 16;

  // Insurer responsible for the EOB
  Reference insurer = 17;

  // Responsible provider for the claim
  Reference provider = 18;

  // Responsible organization for the claim
  Reference organization = 19;

  // Treatment Referral
  Reference referral = 20;

  // Servicing Facility
  Reference facility = 21;

  // Claim reference
  Reference claim = 22;

  // Claim response reference
  Reference claim_response = 23;

  // complete | error | partial
  CodeableConcept outcome = 24;

  // Disposition Message
  String disposition = 25;

  // Related Claims which may be revelant to processing this claim
  message RelatedClaim {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reference to the related claim
    Reference claim = 4;

    // How the reference claim is related
    CodeableConcept relationship = 5;

    // Related file or case reference
    Identifier reference = 6;
  }
  repeated RelatedClaim related = 26;

  // Prescription authorizing services or products
  Reference prescription = 27;

  // Original prescription if superceded by fulfiller
  Reference original_prescription = 28;

  // Party to be paid any benefits payable
  message Payee {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of party: Subscriber, Provider, other
    CodeableConcept type = 4;

    // organization | patient | practitioner | relatedperson
    CodeableConcept resource_type = 5;

    // Party to receive the payable
    Reference party = 6;
  }
  Payee payee = 29;

  // Exceptions, special considerations, the condition, situation, prior or
  // concurrent issues
  message SupportingInformation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Information instance identifier
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // General class of information
    CodeableConcept category = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Type of information
    CodeableConcept code = 6;

    // When it occurred
    message TimingType {
      oneof timing {
        Date date = 1;
        Period period = 2;
      }
    }
    TimingType timing = 7 [(is_choice_type) = true];

    // Additional Data or supporting information
    message Value {
      oneof value {
        String string_value = 1 [json_name = "string"];
        Quantity quantity = 2;
        Attachment attachment = 3;
        Reference reference = 4;
      }
    }
    Value value = 8 [(is_choice_type) = true];

    // Reason associated with the information
    Coding reason = 9;
  }
  repeated SupportingInformation information = 30;

  // Care Team members
  message CareTeam {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Number to covey order of careteam
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Member of the Care Team
    Reference provider = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Billing practitioner
    Boolean responsible = 6;

    // Role on the team
    CodeableConcept role = 7;

    // Type, classification or Specialization
    CodeableConcept qualification = 8;
  }
  repeated CareTeam care_team = 31;

  // List of Diagnosis
  message Diagnosis {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Number to covey order of diagnosis
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Patient's diagnosis
    message DiagnosisType {
      oneof diagnosis {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    DiagnosisType diagnosis = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Timing or nature of the diagnosis
    repeated CodeableConcept type = 6;

    // Package billing code
    CodeableConcept package_code = 7;
  }
  repeated Diagnosis diagnosis = 32;

  // Procedures performed
  message Procedure {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Procedure sequence for reference
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // When the procedure was performed
    DateTime date = 5;

    // Patient's list of procedures performed
    message ProcedureType {
      oneof procedure {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    ProcedureType procedure = 6
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Procedure procedure = 33;

  // Precedence (primary, secondary, etc.)
  PositiveInt precedence = 34;

  // Insurance or medical plan
  message Insurance {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Insurance information
    Reference coverage = 4;

    // Pre-Authorization/Determination Reference
    repeated String pre_auth_ref = 5;
  }
  Insurance insurance = 35;

  // Details of an accident
  message Accident {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // When the accident occurred
    Date date = 4;

    // The nature of the accident
    CodeableConcept type = 5;

    // Accident Place
    message Location {
      oneof location {
        Address address = 1;
        Reference reference = 2;
      }
    }
    Location location = 6 [(is_choice_type) = true];
  }
  Accident accident = 36;

  // Period unable to work
  Period employment_impacted = 37;

  // Period in hospital
  Period hospitalization = 38;

  // Goods and Services
  message Item {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instance
    PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Applicable careteam members
    repeated PositiveInt care_team_link_id = 5;

    // Applicable diagnoses
    repeated PositiveInt diagnosis_link_id = 6;

    // Applicable procedures
    repeated PositiveInt procedure_link_id = 7;

    // Applicable exception and supporting information
    repeated PositiveInt information_link_id = 8;

    // Revenue or cost center code
    CodeableConcept revenue = 9;

    // Type of service or product
    CodeableConcept category = 10;

    // Billing Code
    CodeableConcept service = 11;

    // Service/Product billing modifiers
    repeated CodeableConcept modifier = 12;

    // Program specific reason for item inclusion
    repeated CodeableConcept program_code = 13;

    // Date or dates of Service
    message Serviced {
      oneof serviced {
        Date date = 1;
        Period period = 2;
      }
    }
    Serviced serviced = 14 [(is_choice_type) = true];

    // Place of service
    message Location {
      oneof location {
        CodeableConcept codeable_concept = 1;
        Address address = 2;
        Reference reference = 3;
      }
    }
    Location location = 15 [(is_choice_type) = true];

    // Count of Products or Services
    Quantity quantity = 16;

    // Fee, charge or cost per point
    Money unit_price = 17;

    // Price scaling factor
    Decimal factor = 18;

    // Total item cost
    Money net = 19;

    // Unique Device Identifier
    repeated Reference udi = 20;

    // Service Location
    CodeableConcept body_site = 21;

    // Service Sub-location
    repeated CodeableConcept sub_site = 22;

    // Encounters related to this billed item
    repeated Reference encounter = 23;

    // List of note numbers which apply
    repeated PositiveInt note_number = 24;

    // Adjudication details
    message Adjudication {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Adjudication category such as co-pay, eligible, benefit, etc.
      CodeableConcept category = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Explanation of Adjudication outcome
      CodeableConcept reason = 5;

      // Monetary amount
      Money amount = 6;

      // Non-monitory value
      Decimal value = 7;
    }
    repeated Adjudication adjudication = 25;

    // Additional items
    message Detail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Service instance
      PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Group or type of product or service
      CodeableConcept type = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Revenue or cost center code
      CodeableConcept revenue = 6;

      // Type of service or product
      CodeableConcept category = 7;

      // Billing Code
      CodeableConcept service = 8;

      // Service/Product billing modifiers
      repeated CodeableConcept modifier = 9;

      // Program specific reason for item inclusion
      repeated CodeableConcept program_code = 10;

      // Count of Products or Services
      Quantity quantity = 11;

      // Fee, charge or cost per point
      Money unit_price = 12;

      // Price scaling factor
      Decimal factor = 13;

      // Total additional item cost
      Money net = 14;

      // Unique Device Identifier
      repeated Reference udi = 15;

      // List of note numbers which apply
      repeated PositiveInt note_number = 16;

      // Detail level adjudication details
      repeated Adjudication adjudication = 17;

      // Additional items
      message SubDetail {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Service instance
        PositiveInt sequence = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Type of product or service
        CodeableConcept type = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Revenue or cost center code
        CodeableConcept revenue = 6;

        // Type of service or product
        CodeableConcept category = 7;

        // Billing Code
        CodeableConcept service = 8;

        // Service/Product billing modifiers
        repeated CodeableConcept modifier = 9;

        // Program specific reason for item inclusion
        repeated CodeableConcept program_code = 10;

        // Count of Products or Services
        Quantity quantity = 11;

        // Fee, charge or cost per point
        Money unit_price = 12;

        // Price scaling factor
        Decimal factor = 13;

        // Net additional item cost
        Money net = 14;

        // Unique Device Identifier
        repeated Reference udi = 15;

        // List of note numbers which apply
        repeated PositiveInt note_number = 16;

        // Language if different from the resource
        repeated Adjudication adjudication = 17;
      }
      repeated SubDetail sub_detail = 18;
    }
    repeated Detail detail = 26;
  }
  repeated Item item = 39;

  // Insurer added line items
  message AddedItem {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instances
    repeated PositiveInt sequence_link_id = 4;

    // Revenue or cost center code
    CodeableConcept revenue = 5;

    // Type of service or product
    CodeableConcept category = 6;

    // Billing Code
    CodeableConcept service = 7;

    // Service/Product billing modifiers
    repeated CodeableConcept modifier = 8;

    // Professional fee or Product charge
    Money fee = 9;

    // List of note numbers which apply
    repeated PositiveInt note_number = 10;

    // Added items adjudication
    repeated Item.Adjudication adjudication = 11;

    // Added items details
    message AddedItemsDetail {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Revenue or cost center code
      CodeableConcept revenue = 4;

      // Type of service or product
      CodeableConcept category = 5;

      // Billing Code
      CodeableConcept service = 6;

      // Service/Product billing modifiers
      repeated CodeableConcept modifier = 7;

      // Professional fee or Product charge
      Money fee = 8;

      // List of note numbers which apply
      repeated PositiveInt note_number = 9;

      // Added items detail adjudication
      repeated Item.Adjudication adjudication = 10;
    }
    repeated AddedItemsDetail detail = 12;
  }
  repeated AddedItem add_item = 40;

  // Total Cost of service from the Claim
  Money total_cost = 41;

  // Unallocated deductable
  Money unalloc_deductable = 42;

  // Total benefit payable for the Claim
  Money total_benefit = 43;

  // Payment (if paid)
  message Payment {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Partial or Complete
    CodeableConcept type = 4;

    // Payment adjustment for non-Claim issues
    Money adjustment = 5;

    // Explanation for the non-claim adjustment
    CodeableConcept adjustment_reason = 6;

    // Expected date of Payment
    Date date = 7;

    // Payable amount after adjustment
    Money amount = 8;

    // Identifier of the payment instrument
    Identifier identifier = 9;
  }
  Payment payment = 44;

  // Printed Form Identifier
  CodeableConcept form = 45;

  // Processing notes
  message Note {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Sequence number for this note
    PositiveInt number = 4;

    // display | print | printoper
    CodeableConcept type = 5;

    // Note explanitory text
    String text = 6;

    // Language if different from the resource
    CodeableConcept language = 7;
  }
  repeated Note process_note = 46;

  // Balance by Benefit Category
  message BenefitBalance {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of services covered
    CodeableConcept category = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Detailed services covered within the type
    CodeableConcept sub_category = 5;

    // Excluded from the plan
    Boolean excluded = 6;

    // Short name for the benefit
    String name = 7;

    // Description of the benefit or services covered
    String description = 8;

    // In or out of network
    CodeableConcept network = 9;

    // Individual or family
    CodeableConcept unit = 10;

    // Annual or lifetime
    CodeableConcept term = 11;

    // Benefit Summary
    message Benefit {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Deductable, visits, benefit amount
      CodeableConcept type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Benefits allowed
      message Allowed {
        oneof allowed {
          UnsignedInt unsigned_int = 1;
          String string_value = 2 [json_name = "string"];
          Money money = 3;
        }
      }
      Allowed allowed = 5 [(is_choice_type) = true];

      // Benefits used
      message Used {
        oneof used {
          UnsignedInt unsigned_int = 1;
          Money money = 2;
        }
      }
      Used used = 6 [(is_choice_type) = true];
    }
    repeated Benefit financial = 12;
  }
  repeated BenefitBalance benefit_balance = 47;
}

// Auto-generated from StructureDefinition for FamilyMemberHistory, last updated
// 2017-04-19T07:44:43.294+10:00. Information about patient's relatives,
// relevant for patient. See https://www.hl7.org/fhir/familymemberhistory.html
message FamilyMemberHistory {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Id(s) for this record
  repeated Identifier identifier = 9;

  // Instantiates protocol or definition
  repeated Reference definition = 10;

  // partial | completed | entered-in-error | health-unknown
  FamilyHistoryStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // The taking of a family member's history did not occur
  Boolean not_done = 12;

  // subject-unknown | withheld | unable-to-obtain | deferred
  CodeableConcept not_done_reason = 13;

  // Patient history is about
  Reference patient = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // When history was captured/updated
  DateTime date = 15;

  // The family member described
  String name = 16;

  // Relationship to the subject
  CodeableConcept relationship = 17
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // male | female | other | unknown
  AdministrativeGenderCode gender = 18;

  // (approximate) date of birth
  message Born {
    oneof born {
      Period period = 1;
      Date date = 2;
      String string_value = 3 [json_name = "string"];
    }
  }
  Born born = 19 [(is_choice_type) = true];

  // (approximate) age
  message AgeType {
    oneof age {
      Age age_value = 1 [json_name = "age"];
      Range range = 2;
      String string_value = 3 [json_name = "string"];
    }
  }
  AgeType age = 20 [(is_choice_type) = true];

  // Age is estimated?
  Boolean estimated_age = 21;

  // Dead? How old/when?
  message Deceased {
    oneof deceased {
      Boolean boolean = 1;
      Age age = 2;
      Range range = 3;
      Date date = 4;
      String string_value = 5 [json_name = "string"];
    }
  }
  Deceased deceased = 22 [(is_choice_type) = true];

  // Why was family member history performed?
  repeated CodeableConcept reason_code = 23;

  // Why was family member history performed?
  repeated Reference reason_reference = 24;

  // General note about related person
  repeated Annotation note = 25;

  // Condition that the related person had
  message Condition {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Condition suffered by relation
    CodeableConcept code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // deceased | permanent disability | etc.
    CodeableConcept outcome = 5;

    // When condition first manifested
    message Onset {
      oneof onset {
        Age age = 1;
        Range range = 2;
        Period period = 3;
        String string_value = 4 [json_name = "string"];
      }
    }
    Onset onset = 6 [(is_choice_type) = true];

    // Extra information about condition
    repeated Annotation note = 7;
  }
  repeated Condition condition = 26;
}

// Auto-generated from StructureDefinition for Flag, last updated
// 2017-04-19T07:44:43.294+10:00. Key information to flag to healthcare
// providers. See https://www.hl7.org/fhir/flag.html
message Flag {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // active | inactive | entered-in-error
  FlagStatusCode status = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Clinical, administrative, etc.
  CodeableConcept category = 11;

  // Coded or textual message to display to user
  CodeableConcept code = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who/What is flag about?
  Reference subject = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Time period when flag is active
  Period period = 14;

  // Alert relevant during encounter
  Reference encounter = 15;

  // Flag creator
  Reference author = 16;
}

// Auto-generated from StructureDefinition for Goal, last updated
// 2017-04-19T07:44:43.294+10:00. Describes the intended objective(s) for a
// patient, group or organization. See https://www.hl7.org/fhir/goal.html
message Goal {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this goal
  repeated Identifier identifier = 9;

  // proposed | accepted | planned | in-progress | on-target | ahead-of-target |
  // behind-target | sustaining | achieved | on-hold | cancelled |
  // entered-in-error | rejected
  GoalStatusCode status = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // E.g. Treatment, dietary, behavioral, etc.
  repeated CodeableConcept category = 11;

  // high-priority | medium-priority | low-priority
  CodeableConcept priority = 12;

  // Code or text describing goal
  CodeableConcept description = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who this goal is intended for
  Reference subject = 14;

  // When goal pursuit begins
  message Start {
    oneof start {
      Date date = 1;
      CodeableConcept codeable_concept = 2;
    }
  }
  Start start = 15 [(is_choice_type) = true];

  // Target outcome for the goal
  message Target {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The parameter whose value is being tracked
    CodeableConcept measure = 4;

    // The target value to be achieved
    message Detail {
      oneof detail {
        Quantity quantity = 1;
        Range range = 2;
        CodeableConcept codeable_concept = 3;
      }
    }
    Detail detail = 5 [(is_choice_type) = true];

    // Reach goal on or before
    message Due {
      oneof due {
        Date date = 1;
        Duration duration = 2;
      }
    }
    Due due = 6 [(is_choice_type) = true];
  }
  Target target = 16;

  // When goal status took effect
  Date status_date = 17;

  // Reason for current status
  String status_reason = 18;

  // Who's responsible for creating Goal?
  Reference expressed_by = 19;

  // Issues addressed by this goal
  repeated Reference addresses = 20;

  // Comments about the goal
  repeated Annotation note = 21;

  // What result was achieved regarding the goal?
  repeated CodeableConcept outcome_code = 22;

  // Observation that resulted from goal
  repeated Reference outcome_reference = 23;
}

// Auto-generated from StructureDefinition for GraphDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. Definition of an graph of resources. See
// https://www.hl7.org/fhir/graphdefinition.html
message GraphDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this graph definition (globally unique)
  Uri url = 9;

  // Business version of the graph definition
  String version = 10;

  // Name for this graph definition (computer friendly)
  String name = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // draft | active | retired | unknown
  PublicationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 13;

  // Date this was last changed
  DateTime date = 14;

  // Name of the publisher (organization or individual)
  String publisher = 15;

  // Contact details for the publisher
  repeated ContactDetail contact = 16;

  // Natural language description of the graph definition
  Markdown description = 17;

  // Context the content is intended to support
  repeated UsageContext use_context = 18;

  // Intended jurisdiction for graph definition (if applicable)
  repeated CodeableConcept jurisdiction = 19;

  // Why this graph definition is defined
  Markdown purpose = 20;

  // Type of resource at which the graph starts
  ResourceTypeCode start = 21 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Profile on base resource
  Uri profile = 22;

  // Links this graph makes rules about
  message Link {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Path in the resource that contains the link
    String path = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Which slice (if profiled)
    String slice_name = 5;

    // Minimum occurrences for this link
    Integer min = 6;

    // Maximum occurrences for this link
    String max = 7;

    // Why this link is specified
    String description = 8;

    // Potential target for the link
    message Target {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Type of resource this link refers to
      ResourceTypeCode type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Profile for the target resource
      Uri profile = 5;

      // Compartment Consistency Rules
      message Compartment {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Identifies the compartment
        CompartmentTypeCode code = 4
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // identical | matching | different | custom
        GraphCompartmentRuleCode rule = 5
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // Custom rule, as a FHIRPath expression
        String expression = 6;

        // Documentation for FHIRPath expression
        String description = 7;
      }
      repeated Compartment compartment = 6;

      // Additional links from target resource
      repeated Link link = 7;
    }
    repeated Target target = 9 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Link link = 23;
}

// Auto-generated from StructureDefinition for Group, last updated
// 2017-04-19T07:44:43.294+10:00. Group of multiple entities. See
// https://www.hl7.org/fhir/group.html
message Group {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique id
  repeated Identifier identifier = 9;

  // Whether this group's record is in active use
  Boolean active = 10;

  // person | animal | practitioner | device | medication | substance
  GroupTypeCode type = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Descriptive or actual
  Boolean actual = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Kind of Group members
  CodeableConcept code = 13;

  // Label for Group
  String name = 14;

  // Number of members
  UnsignedInt quantity = 15;

  // Trait of group members
  message Characteristic {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Kind of characteristic
    CodeableConcept code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Value held by characteristic
    message Value {
      oneof value {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Boolean boolean = 2;
        Quantity quantity = 3;
        Range range = 4;
      }
    }
    Value value = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Group includes or excludes
    Boolean exclude = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Period over which characteristic is tested
    Period period = 7;
  }
  repeated Characteristic characteristic = 16;

  // Who or what is in group
  message Member {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reference to the group member
    Reference entity = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Period member belonged to the group
    Period period = 5;

    // If member is no longer in group
    Boolean inactive = 6;
  }
  repeated Member member = 17;
}

// Auto-generated from StructureDefinition for GuidanceResponse, last updated
// 2017-04-19T07:44:43.294+10:00. The formal response to a guidance request. See
// https://www.hl7.org/fhir/guidanceresponse.html
message GuidanceResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // The id of the request associated with this response, if any
  Id request_id = 9;

  // Business identifier
  Identifier identifier = 10;

  // A reference to a knowledge module
  Reference module = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // success | data-requested | data-required | in-progress | failure |
  // entered-in-error
  GuidanceResponseStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Patient the request was performed for
  Reference subject = 13;

  // Encounter or Episode during which the response was returned
  Reference context = 14;

  // When the guidance response was processed
  DateTime occurrence_date_time = 15;

  // Device returning the guidance
  Reference performer = 16;

  // Reason for the response
  message Reason {
    oneof reason {
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Reason reason = 17 [(is_choice_type) = true];

  // Additional notes about the response
  repeated Annotation note = 18;

  // Messages resulting from the evaluation of the artifact or artifacts
  repeated Reference evaluation_message = 19;

  // The output parameters of the evaluation, if any
  Reference output_parameters = 20;

  // Proposed actions, if any
  Reference result = 21;

  // Additional required data
  repeated DataRequirement data_requirement = 22;
}

// Auto-generated from StructureDefinition for HealthcareService, last updated
// 2017-04-19T07:44:43.294+10:00. The details of a healthcare service available
// at a location. See https://www.hl7.org/fhir/healthcareservice.html
message HealthcareService {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifiers for this item
  repeated Identifier identifier = 9;

  // Whether this healthcareservice is in active use
  Boolean active = 10;

  // Organization that provides this service
  Reference provided_by = 11;

  // Broad category of service being performed or delivered
  CodeableConcept category = 12;

  // Type of service that may be delivered or performed
  repeated CodeableConcept type = 13;

  // Specialties handled by the HealthcareService
  repeated CodeableConcept specialty = 14;

  // Location(s) where service may be provided
  repeated Reference location = 15;

  // Description of service as presented to a consumer while searching
  String name = 16;

  // Additional description and/or any specific issues not covered elsewhere
  String comment = 17;

  // Extra details about the service that can't be placed in the other fields
  String extra_details = 18;

  // Facilitates quick identification of the service
  Attachment photo = 19;

  // Contacts related to the healthcare service
  repeated ContactPoint telecom = 20;

  // Location(s) service is inteded for/available to
  repeated Reference coverage_area = 21;

  // Conditions under which service is available/offered
  repeated CodeableConcept service_provision_code = 22;

  // Specific eligibility requirements required to use the service
  CodeableConcept eligibility = 23;

  // Describes the eligibility conditions for the service
  String eligibility_note = 24;

  // Program Names that categorize the service
  repeated String program_name = 25;

  // Collection of characteristics (attributes)
  repeated CodeableConcept characteristic = 26;

  // Ways that the service accepts referrals
  repeated CodeableConcept referral_method = 27;

  // If an appointment is required for access to this service
  Boolean appointment_required = 28;

  // Times the Service Site is available
  message AvailableTime {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // mon | tue | wed | thu | fri | sat | sun
    repeated DaysOfWeekCode days_of_week = 4;

    // Always available? e.g. 24 hour service
    Boolean all_day = 5;

    // Opening time of day (ignored if allDay = true)
    Time available_start_time = 6;

    // Closing time of day (ignored if allDay = true)
    Time available_end_time = 7;
  }
  repeated AvailableTime available_time = 29;

  // Not available during this time due to provided reason
  message NotAvailable {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reason presented to the user explaining why time not available
    String description = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Service not availablefrom this date
    Period during = 5;
  }
  repeated NotAvailable not_available = 30;

  // Description of availability exceptions
  String availability_exceptions = 31;

  // Technical endpoints providing access to services operated for the location
  repeated Reference endpoint = 32;
}

// Auto-generated from StructureDefinition for ImagingManifest, last updated
// 2017-04-19T07:44:43.294+10:00. Key Object Selection. See
// https://www.hl7.org/fhir/imagingmanifest.html
message ImagingManifest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // SOP Instance UID
  Identifier identifier = 9;

  // Patient of the selected objects
  Reference patient = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Time when the selection of instances was made
  DateTime authoring_time = 11;

  // Author (human or machine)
  Reference author = 12;

  // Description text
  String description = 13;

  // Study identity of the selected instances
  message Study {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Study instance UID
    Oid uid = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Reference to ImagingStudy
    Reference imaging_study = 5;

    // Study access service endpoint
    repeated Reference endpoint = 6;

    // Series identity of the selected instances
    message Series {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Series instance UID
      Oid uid = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Series access endpoint
      repeated Reference endpoint = 5;

      // The selected instance
      message Instance {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // SOP class UID of instance
        Oid sop_class = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Selected instance UID
        Oid uid = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
      }
      repeated Instance instance = 6
          [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Series series = 7 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Study study = 14 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for ImagingStudy, last updated
// 2017-04-19T07:44:43.294+10:00. A set of images produced in single study (one
// or more series of references images). See
// https://www.hl7.org/fhir/imagingstudy.html
message ImagingStudy {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Formal DICOM identifier for the study
  Oid uid = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Related workflow identifier ("Accession Number")
  Identifier accession = 10;

  // Other identifiers for the study
  repeated Identifier identifier = 11;

  // ONLINE | OFFLINE | NEARLINE | UNAVAILABLE
  InstanceAvailabilityCode availability = 12;

  // All series modality if actual acquisition modalities
  repeated Coding modality_list = 13;

  // Who the images are of
  Reference patient = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Originating context
  Reference context = 15;

  // When the study was started
  DateTime started = 16;

  // Request fulfilled
  repeated Reference based_on = 17;

  // Referring physician
  Reference referrer = 18;

  // Who interpreted images
  repeated Reference interpreter = 19;

  // Study access endpoint
  repeated Reference endpoint = 20;

  // Number of Study Related Series
  UnsignedInt number_of_series = 21;

  // Number of Study Related Instances
  UnsignedInt number_of_instances = 22;

  // The performed Procedure reference
  repeated Reference procedure_reference = 23;

  // The performed procedure code
  repeated CodeableConcept procedure_code = 24;

  // Why the study was requested
  CodeableConcept reason = 25;

  // Institution-generated description
  String description = 26;

  // Each study has one or more series of instances
  message Series {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Formal DICOM identifier for this series
    Oid uid = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Numeric identifier of this series
    UnsignedInt number = 5;

    // The modality of the instances in the series
    Coding modality = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // A short human readable summary of the series
    String description = 7;

    // Number of Series Related Instances
    UnsignedInt number_of_instances = 8;

    // ONLINE | OFFLINE | NEARLINE | UNAVAILABLE
    InstanceAvailabilityCode availability = 9;

    // Series access endpoint
    repeated Reference endpoint = 10;

    // Body part examined
    Coding body_site = 11;

    // Body part laterality
    Coding laterality = 12;

    // When the series started
    DateTime started = 13;

    // Who performed the series
    repeated Reference performer = 14;

    // A single SOP instance from the series
    message Instance {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Formal DICOM identifier for this instance
      Oid uid = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // The number of this instance in the series
      UnsignedInt number = 5;

      // DICOM class type
      Oid sop_class = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Description of instance
      String title = 7;
    }
    repeated Instance instance = 15;
  }
  repeated Series series = 27;
}

// Auto-generated from StructureDefinition for Immunization, last updated
// 2017-04-19T07:44:43.294+10:00. Immunization event information. See
// https://www.hl7.org/fhir/immunization.html
message Immunization {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // completed | entered-in-error
  ImmunizationStatusCodesCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Flag for whether immunization was given
  Boolean not_given = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Vaccine product administered
  CodeableConcept vaccine_code = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who was immunized
  Reference patient = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter administered as part of
  Reference encounter = 14;

  // Vaccination administration date
  DateTime date = 15;

  // Indicates context the data was recorded in
  Boolean primary_source = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Indicates the source of a secondarily reported record
  CodeableConcept report_origin = 17;

  // Where vaccination occurred
  Reference location = 18;

  // Vaccine manufacturer
  Reference manufacturer = 19;

  // Vaccine lot number
  String lot_number = 20;

  // Vaccine expiration date
  Date expiration_date = 21;

  // Body site vaccine  was administered
  CodeableConcept site = 22;

  // How vaccine entered body
  CodeableConcept route = 23;

  // Amount of vaccine administered
  Quantity dose_quantity = 24;

  // Who performed event
  message Practitioner {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // What type of performance was done
    CodeableConcept role = 4;

    // Individual who was performing
    Reference actor = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Practitioner practitioner = 25;

  // Vaccination notes
  repeated Annotation note = 26;

  // Administration/non-administration reasons
  message Explanation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Why immunization occurred
    repeated CodeableConcept reason = 4;

    // Why immunization did not occur
    repeated CodeableConcept reason_not_given = 5;
  }
  Explanation explanation = 27;

  // Details of a reaction that follows immunization
  message Reaction {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // When reaction started
    DateTime date = 4;

    // Additional information on reaction
    Reference detail = 5;

    // Indicates self-reported reaction
    Boolean reported = 6;
  }
  repeated Reaction reaction = 28;

  // What protocol was followed
  message VaccinationProtocol {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Dose number within series
    PositiveInt dose_sequence = 4;

    // Details of vaccine protocol
    String description = 5;

    // Who is responsible for protocol
    Reference authority = 6;

    // Name of vaccine series
    String series = 7;

    // Recommended number of doses for immunity
    PositiveInt series_doses = 8;

    // Disease immunized against
    repeated CodeableConcept target_disease = 9
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Indicates if dose counts towards immunity
    CodeableConcept dose_status = 10
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Why dose does (not) count
    CodeableConcept dose_status_reason = 11;
  }
  repeated VaccinationProtocol vaccination_protocol = 29;
}

// Auto-generated from StructureDefinition for ImmunizationRecommendation, last
// updated 2017-04-19T07:44:43.294+10:00. Guidance or advice relating to an
// immunization. See https://www.hl7.org/fhir/immunizationrecommendation.html
message ImmunizationRecommendation {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // Who this profile is for
  Reference patient = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Vaccine administration recommendations
  message Recommendation {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Date recommendation created
    DateTime date = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Vaccine recommendation applies to
    CodeableConcept vaccine_code = 5;

    // Disease to be immunized against
    CodeableConcept target_disease = 6;

    // Recommended dose number
    PositiveInt dose_number = 7;

    // Vaccine administration status
    CodeableConcept forecast_status = 8
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Dates governing proposed immunization
    message DateCriterion {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Type of date
      CodeableConcept code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Recommended date
      DateTime value = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated DateCriterion date_criterion = 9;

    // Protocol used by recommendation
    message Protocol {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Dose number within sequence
      PositiveInt dose_sequence = 4;

      // Protocol details
      String description = 5;

      // Who is responsible for protocol
      Reference authority = 6;

      // Name of vaccination series
      String series = 7;
    }
    Protocol protocol = 10;

    // Past immunizations supporting recommendation
    repeated Reference supporting_immunization = 11;

    // Patient observations supporting recommendation
    repeated Reference supporting_patient_information = 12;
  }
  repeated Recommendation recommendation = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for ImplementationGuide, last updated
// 2017-04-19T07:44:43.294+10:00. A set of rules about how FHIR is used. See
// https://www.hl7.org/fhir/implementationguide.html
message ImplementationGuide {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this implementation guide (globally unique)
  Uri url = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Business version of the implementation guide
  String version = 10;

  // Name for this implementation guide (computer friendly)
  String name = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // draft | active | retired | unknown
  PublicationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 13;

  // Date this was last changed
  DateTime date = 14;

  // Name of the publisher (organization or individual)
  String publisher = 15;

  // Contact details for the publisher
  repeated ContactDetail contact = 16;

  // Natural language description of the implementation guide
  Markdown description = 17;

  // Context the content is intended to support
  repeated UsageContext use_context = 18;

  // Intended jurisdiction for implementation guide (if applicable)
  repeated CodeableConcept jurisdiction = 19;

  // Use and/or publishing restrictions
  Markdown copyright = 20;

  // FHIR Version this Implementation Guide targets
  Id fhir_version = 21;

  // Another Implementation guide this depends on
  message Dependency {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // reference | inclusion
    GuideDependencyTypeCode type = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Where to find dependency
    Uri uri = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Dependency dependency = 22;

  // Group of resources as used in .page.package
  message Package {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name used .page.package
    String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Human readable text describing the package
    String description = 5;

    // Resource in the implementation guide
    message Resource {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // If not an example, has its normal meaning
      Boolean example = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Human Name for the resource
      String name = 5;

      // Reason why included in guide
      String description = 6;

      // Short code to identify the resource
      String acronym = 7;

      // Location of the resource
      message Source {
        oneof source {
          option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
          Uri uri = 1;
          Reference reference = 2;
        }
      }
      Source source = 8 [
        (is_choice_type) = true,
        (validation_requirement) = REQUIRED_BY_FHIR
      ];

      // Resource this is an example of (if applicable)
      Reference example_for = 9;
    }
    repeated Resource resource = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Package package_value = 23 [json_name = "package"];

  // Profiles that apply globally
  message Global {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type this profiles applies to
    ResourceTypeCode type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Profile that all resources must conform to
    Reference profile = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Global global = 24;

  // Image, css, script, etc.
  repeated Uri binary = 25;

  // Page/Section in the Guide
  message Page {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Where to find that page
    Uri source = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Short title shown for navigational assistance
    String title = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // page | example | list | include | directory | dictionary | toc | resource
    GuidePageKindCode kind = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Kind of resource to include in the list
    repeated ResourceTypeCode type = 7;

    // Name of package to include
    repeated String package_value = 8 [json_name = "package"];

    // Format of the page (e.g. html, markdown, etc.)
    MimeTypeCode format = 9;

    // Nested Pages / Sections
    repeated Page page = 10;
  }
  Page page = 26;
}

// Auto-generated from StructureDefinition for Library, last updated
// 2017-04-19T07:44:43.294+10:00. Represents a library of quality improvement
// components. See https://www.hl7.org/fhir/library.html
message Library {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this library (globally unique)
  Uri url = 9;

  // Additional identifier for the library
  repeated Identifier identifier = 10;

  // Business version of the library
  String version = 11;

  // Name for this library (computer friendly)
  String name = 12;

  // Name for this library (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // logic-library | model-definition | asset-collection | module-definition
  CodeableConcept type = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Date this was last changed
  DateTime date = 17;

  // Name of the publisher (organization or individual)
  String publisher = 18;

  // Natural language description of the library
  Markdown description = 19;

  // Why this library is defined
  Markdown purpose = 20;

  // Describes the clinical usage of the library
  String usage = 21;

  // When the library was approved by publisher
  Date approval_date = 22;

  // When the library was last reviewed
  Date last_review_date = 23;

  // When the library is expected to be used
  Period effective_period = 24;

  // Context the content is intended to support
  repeated UsageContext use_context = 25;

  // Intended jurisdiction for library (if applicable)
  repeated CodeableConcept jurisdiction = 26;

  // E.g. Education, Treatment, Assessment, etc
  repeated CodeableConcept topic = 27;

  // A content contributor
  repeated Contributor contributor = 28;

  // Contact details for the publisher
  repeated ContactDetail contact = 29;

  // Use and/or publishing restrictions
  Markdown copyright = 30;

  // Additional documentation, citations, etc.
  repeated RelatedArtifact related_artifact = 31;

  // Parameters defined by the library
  repeated ParameterDefinition parameter = 32;

  // What data is referenced by this library
  repeated DataRequirement data_requirement = 33;

  // Contents of the library, either embedded or referenced
  repeated Attachment content = 34;
}

// Auto-generated from StructureDefinition for Linkage, last updated
// 2017-04-19T07:44:43.294+10:00. Links records for 'same' item. See
// https://www.hl7.org/fhir/linkage.html
message Linkage {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Whether this linkage assertion is active or not
  Boolean active = 9;

  // Who is responsible for linkages
  Reference author = 10;

  // Item to be linked
  message Item {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // source | alternate | historical
    LinkageTypeCode type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Resource being linked
    Reference resource = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Item item = 11 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for List, last updated
// 2017-04-19T07:44:43.294+10:00. Information summarized from a list of other
// resources. See https://www.hl7.org/fhir/list.html
message List {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // current | retired | entered-in-error
  ListStatusCode status = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

  // working | snapshot | changes
  ListModeCode mode = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Descriptive name for the list
  String title = 12;

  // What the purpose of this list is
  CodeableConcept code = 13;

  // If all resources have the same subject
  Reference subject = 14;

  // Context in which list created
  Reference encounter = 15;

  // When the list was prepared
  DateTime date = 16;

  // Who and/or what defined the list contents (aka Author)
  Reference source = 17;

  // What order the list has
  CodeableConcept ordered_by = 18;

  // Comments about the list
  repeated Annotation note = 19;

  // Entries in the list
  message Entry {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Status/Workflow information about this item
    CodeableConcept flag = 4;

    // If this item is actually marked as deleted
    Boolean deleted = 5;

    // When item added to list
    DateTime date = 6;

    // Actual entry
    Reference item = 7 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Entry entry = 20;

  // Why list is empty
  CodeableConcept empty_reason = 21;
}

// Auto-generated from StructureDefinition for Location, last updated
// 2017-04-19T07:44:43.294+10:00. Details and position information for a
// physical place. See https://www.hl7.org/fhir/location.html
message Location {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique code or number identifying the location to its users
  repeated Identifier identifier = 9;

  // active | suspended | inactive
  LocationStatusCode status = 10;

  // The Operational status of the location (typically only for a bed/room)
  Coding operational_status = 11;

  // Name of the location as used by humans
  String name = 12;

  // A list of alternate names that the location is known as, or was known as in
  // the past
  repeated String alias = 13;

  // Additional details about the location that could be displayed as further
  // information to identify the location beyond its name
  String description = 14;

  // instance | kind
  LocationModeCode mode = 15;

  // Type of function performed
  CodeableConcept type = 16;

  // Contact details of the location
  repeated ContactPoint telecom = 17;

  // Physical location
  Address address = 18;

  // Physical form of the location
  CodeableConcept physical_type = 19;

  // The absolute geographic location
  message Position {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Longitude with WGS84 datum
    Decimal longitude = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Latitude with WGS84 datum
    Decimal latitude = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Altitude with WGS84 datum
    Decimal altitude = 6;
  }
  Position position = 20;

  // Organization responsible for provisioning and upkeep
  Reference managing_organization = 21;

  // Another Location this one is physically part of
  Reference part_of = 22;

  // Technical endpoints providing access to services operated for the location
  repeated Reference endpoint = 23;
}

// Auto-generated from StructureDefinition for Measure, last updated
// 2017-04-19T07:44:43.294+10:00. A quality measure definition. See
// https://www.hl7.org/fhir/measure.html
message Measure {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this measure (globally unique)
  Uri url = 9;

  // Additional identifier for the measure
  repeated Identifier identifier = 10;

  // Business version of the measure
  String version = 11;

  // Name for this measure (computer friendly)
  String name = 12;

  // Name for this measure (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Natural language description of the measure
  Markdown description = 18;

  // Why this measure is defined
  Markdown purpose = 19;

  // Describes the clinical usage of the measure
  String usage = 20;

  // When the measure was approved by publisher
  Date approval_date = 21;

  // When the measure was last reviewed
  Date last_review_date = 22;

  // When the measure is expected to be used
  Period effective_period = 23;

  // Context the content is intended to support
  repeated UsageContext use_context = 24;

  // Intended jurisdiction for measure (if applicable)
  repeated CodeableConcept jurisdiction = 25;

  // E.g. Education, Treatment, Assessment, etc
  repeated CodeableConcept topic = 26;

  // A content contributor
  repeated Contributor contributor = 27;

  // Contact details for the publisher
  repeated ContactDetail contact = 28;

  // Use and/or publishing restrictions
  Markdown copyright = 29;

  // Additional documentation, citations, etc
  repeated RelatedArtifact related_artifact = 30;

  // Logic used by the measure
  repeated Reference library = 31;

  // Disclaimer for use of the measure or its referenced content
  Markdown disclaimer = 32;

  // proportion | ratio | continuous-variable | cohort
  CodeableConcept scoring = 33;

  // opportunity | all-or-nothing | linear | weighted
  CodeableConcept composite_scoring = 34;

  // process | outcome | structure | patient-reported-outcome | composite
  repeated CodeableConcept type = 35;

  // How is risk adjustment applied for this measure
  String risk_adjustment = 36;

  // How is rate aggregation performed for this measure
  String rate_aggregation = 37;

  // Why does this measure exist
  Markdown rationale = 38;

  // Summary of clinical guidelines
  Markdown clinical_recommendation_statement = 39;

  // Improvement notation for the measure, e.g. higher score indicates better
  // quality
  String improvement_notation = 40;

  // Defined terms used in the measure documentation
  repeated Markdown definition = 41;

  // Additional guidance for implementers
  Markdown guidance = 42;

  // The measure set, e.g. Preventive Care and Screening
  String set = 43;

  // Population criteria group
  message Group {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Unique identifier
    Identifier identifier = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Short name
    String name = 5;

    // Summary description
    String description = 6;

    // Population criteria
    message Population {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Unique identifier
      Identifier identifier = 4;

      // initial-population | numerator | numerator-exclusion | denominator |
      // denominator-exclusion | denominator-exception | measure-population |
      // measure-population-exclusion | measure-observation
      CodeableConcept code = 5;

      // Short name
      String name = 6;

      // The human readable description of this population criteria
      String description = 7;

      // The name of a valid referenced CQL expression (may be namespaced) that
      // defines this population criteria
      String criteria = 8 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Population population = 7;

    // Stratifier criteria for the measure
    message Stratifier {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The identifier for the stratifier used to coordinate the reported data
      // back to this stratifier
      Identifier identifier = 4;

      // How the measure should be stratified
      String criteria = 5;

      // Path to the stratifier
      String path = 6;
    }
    repeated Stratifier stratifier = 8;
  }
  repeated Group group = 44;

  // What other data should be reported with the measure
  message SupplementalData {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Identifier, unique within the measure
    Identifier identifier = 4;

    // supplemental-data | risk-adjustment-factor
    repeated CodeableConcept usage = 5;

    // Expression describing additional data to be reported
    String criteria = 6;

    // Path to the supplemental data element
    String path = 7;
  }
  repeated SupplementalData supplemental_data = 45;
}

// Auto-generated from StructureDefinition for MeasureReport, last updated
// 2017-04-19T07:44:43.294+10:00. Results of a measure evaluation. See
// https://www.hl7.org/fhir/measurereport.html
message MeasureReport {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Additional identifier for the Report
  Identifier identifier = 9;

  // complete | pending | error
  MeasureReportStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // individual | patient-list | summary
  MeasureReportTypeCode type = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // What measure was evaluated
  Reference measure = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // What patient the report is for
  Reference patient = 13;

  // When the report was generated
  DateTime date = 14;

  // Who is reporting the data
  Reference reporting_organization = 15;

  // What period the report covers
  Period period = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Measure results for each group
  message Group {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // What group of the measure
    Identifier identifier = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The populations in the group
    message Population {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Population identifier as defined in the measure
      Identifier identifier = 4;

      // initial-population | numerator | numerator-exclusion | denominator |
      // denominator-exclusion | denominator-exception | measure-population |
      // measure-population-exclusion | measure-score
      CodeableConcept code = 5;

      // Size of the population
      Integer count = 6;

      // For patient-list reports, the patients in this population
      Reference patients = 7;
    }
    repeated Population population = 5;

    // What score this group achieved
    Decimal measure_score = 6;

    // Stratification results
    message Stratifier {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // What stratifier of the group
      Identifier identifier = 4;

      // Stratum results, one for each unique value in the stratifier
      message StratifierGroup {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // The stratum value, e.g. male
        String value = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Population results in this stratum
        message StratifierGroupPopulation {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // Population identifier as defined in the measure
          Identifier identifier = 4;

          // initial-population | numerator | numerator-exclusion | denominator
          // | denominator-exclusion | denominator-exception |
          // measure-population | measure-population-exclusion | measure-score
          CodeableConcept code = 5;

          // Size of the population
          Integer count = 6;

          // For patient-list reports, the patients in this population
          Reference patients = 7;
        }
        repeated StratifierGroupPopulation population = 5;

        // What score this stratum achieved
        Decimal measure_score = 6;
      }
      repeated StratifierGroup stratum = 5;
    }
    repeated Stratifier stratifier = 7;
  }
  repeated Group group = 17;

  // What data was evaluated to produce the measure score
  Reference evaluated_resources = 18;
}

// Auto-generated from StructureDefinition for Media, last updated
// 2017-04-19T07:44:43.294+10:00. A photo, video, or audio recording acquired or
// used in healthcare. The actual content may be inline or provided by direct
// reference. See https://www.hl7.org/fhir/media.html
message Media {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifier(s) for the image
  repeated Identifier identifier = 9;

  // Procedure that caused this media to be created
  repeated Reference based_on = 10;

  // photo | video | audio
  DigitalMediaTypeCode type = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The type of acquisition equipment/process
  CodeableConcept subtype = 12;

  // Imaging view, e.g. Lateral or Antero-posterior
  CodeableConcept view = 13;

  // Who/What this Media is a record of
  Reference subject = 14;

  // Encounter / Episode associated with media
  Reference context = 15;

  // When Media was collected
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Occurrence occurrence = 16 [(is_choice_type) = true];

  // The person who generated the image
  Reference operator = 17;

  // Why was event performed?
  repeated CodeableConcept reason_code = 18;

  // Body part in media
  CodeableConcept body_site = 19;

  // Observing Device
  Reference device = 20;

  // Height of the image in pixels (photo/video)
  PositiveInt height = 21;

  // Width of the image in pixels (photo/video)
  PositiveInt width = 22;

  // Number of frames if > 1 (photo)
  PositiveInt frames = 23;

  // Length in seconds (audio / video)
  UnsignedInt duration = 24;

  // Actual Media - reference or data
  Attachment content = 25 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Comments made about the media
  repeated Annotation note = 26;
}

// Auto-generated from StructureDefinition for Medication, last updated
// 2017-04-19T07:44:43.294+10:00. Definition of a Medication. See
// https://www.hl7.org/fhir/medication.html
message Medication {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Codes that identify this medication
  CodeableConcept code = 9;

  // active | inactive | entered-in-error
  MedicationStatusCode status = 10;

  // True if a brand
  Boolean is_brand = 11;

  // True if medication does not require a prescription
  Boolean is_over_the_counter = 12;

  // Manufacturer of the item
  Reference manufacturer = 13;

  // powder | tablets | capsule +
  CodeableConcept form = 14;

  // Active or inactive ingredient
  message Ingredient {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The product contained
    message Item {
      oneof item {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    Item item = 4
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Active ingredient indicator
    Boolean is_active = 5;

    // Quantity of ingredient present
    Ratio amount = 6;
  }
  repeated Ingredient ingredient = 15;

  // Details about packaged medications
  message Package {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // E.g. box, vial, blister-pack
    CodeableConcept container = 4;

    // What is  in the package
    message Content {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The item in the package
      message Item {
        oneof item {
          option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
          CodeableConcept codeable_concept = 1;
          Reference reference = 2;
        }
      }
      Item item = 4 [
        (is_choice_type) = true,
        (validation_requirement) = REQUIRED_BY_FHIR
      ];

      // Quantity present in the package
      Quantity amount = 5;
    }
    repeated Content content = 5;

    // Identifies a single production run
    message Batch {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Identifier assigned to batch
      String lot_number = 4;

      // When batch will expire
      DateTime expiration_date = 5;
    }
    repeated Batch batch = 6;
  }
  Package package_value = 16 [json_name = "package"];

  // Picture of the medication
  repeated Attachment image = 17;
}

// Auto-generated from StructureDefinition for MedicationAdministration, last
// updated 2017-04-19T07:44:43.294+10:00. Administration of medication to a
// patient. See https://www.hl7.org/fhir/medicationadministration.html
message MedicationAdministration {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifier
  repeated Identifier identifier = 9;

  // Instantiates protocol or definition
  repeated Reference definition = 10;

  // Part of referenced event
  repeated Reference part_of = 11;

  // in-progress | on-hold | completed | entered-in-error | stopped | unknown
  MedicationAdministrationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Type of medication usage
  CodeableConcept category = 13;

  // What was administered
  message Medication {
    oneof medication {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Medication medication = 14
      [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

  // Who received medication
  Reference subject = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter or Episode of Care administered as part of
  Reference context = 16;

  // Additional information to support administration
  repeated Reference supporting_information = 17;

  // Start and end time of administration
  message Effective {
    oneof effective {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Effective effective = 18
      [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

  // Who administered substance
  message Performer {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual who was performing
    Reference actor = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization organization was acting for
    Reference on_behalf_of = 5;
  }
  repeated Performer performer = 19;

  // True if medication not administered
  Boolean not_given = 20;

  // Reason administration not performed
  repeated CodeableConcept reason_not_given = 21;

  // Reason administration performed
  repeated CodeableConcept reason_code = 22;

  // Condition or Observation that supports why the medication was administered
  repeated Reference reason_reference = 23;

  // Request administration performed against
  Reference prescription = 24;

  // Device used to administer
  repeated Reference device = 25;

  // Information about the administration
  repeated Annotation note = 26;

  // Details of how medication was taken
  message Dosage {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Free text dosage instructions e.g. SIG
    String text = 4;

    // Body site administered to
    CodeableConcept site = 5;

    // Path of substance into body
    CodeableConcept route = 6;

    // How drug was administered
    CodeableConcept method = 7;

    // Amount of medication per dose
    Quantity dose = 8;

    // Dose quantity per unit of time
    message Rate {
      oneof rate {
        Ratio ratio = 1;
        Quantity quantity = 2;
      }
    }
    Rate rate = 9 [(is_choice_type) = true];
  }
  Dosage dosage = 27;

  // A list of events of interest in the lifecycle
  repeated Reference event_history = 28;
}

// Auto-generated from StructureDefinition for MedicationDispense, last updated
// 2017-04-19T07:44:43.294+10:00. Dispensing a medication to a named patient.
// See https://www.hl7.org/fhir/medicationdispense.html
message MedicationDispense {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifier
  repeated Identifier identifier = 9;

  // Event that dispense is part of
  repeated Reference part_of = 10;

  // preparation | in-progress | on-hold | completed | entered-in-error |
  // stopped
  MedicationDispenseStatusCode status = 11;

  // Type of medication dispense
  CodeableConcept category = 12;

  // What medication was supplied
  message Medication {
    oneof medication {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Medication medication = 13
      [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

  // Who the dispense is for
  Reference subject = 14;

  // Encounter / Episode associated with event
  Reference context = 15;

  // Information that supports the dispensing of the medication
  repeated Reference supporting_information = 16;

  // Who performed event
  message Performer {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual who was performing
    Reference actor = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization organization was acting for
    Reference on_behalf_of = 5;
  }
  repeated Performer performer = 17;

  // Medication order that authorizes the dispense
  repeated Reference authorizing_prescription = 18;

  // Trial fill, partial fill, emergency fill, etc.
  CodeableConcept type = 19;

  // Amount dispensed
  Quantity quantity = 20;

  // Amount of medication expressed as a timing amount
  Quantity days_supply = 21;

  // When product was packaged and reviewed
  DateTime when_prepared = 22;

  // When product was given out
  DateTime when_handed_over = 23;

  // Where the medication was sent
  Reference destination = 24;

  // Who collected the medication
  repeated Reference receiver = 25;

  // Information about the dispense
  repeated Annotation note = 26;

  // How the medication is to be used by the patient or administered by the
  // caregiver
  repeated Dosage dosage_instruction = 27;

  // Whether a substitution was performed on the dispense
  message Substitution {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Whether a substitution was or was not performed on the dispense
    Boolean was_substituted = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Code signifying whether a different drug was dispensed from what was
    // prescribed
    CodeableConcept type = 5;

    // Why was substitution made
    repeated CodeableConcept reason = 6;

    // Who is responsible for the substitution
    repeated Reference responsible_party = 7;
  }
  Substitution substitution = 28;

  // Clinical issue with action
  repeated Reference detected_issue = 29;

  // Whether the dispense was or was not performed
  Boolean not_done = 30;

  // Why a dispense was not performed
  message NotDoneReason {
    oneof not_done_reason {
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  NotDoneReason not_done_reason = 31 [(is_choice_type) = true];

  // A list of releveant lifecycle events
  repeated Reference event_history = 32;
}

// Auto-generated from StructureDefinition for MedicationRequest, last updated
// 2017-04-19T07:44:43.294+10:00. Ordering of medication for patient or group.
// See https://www.hl7.org/fhir/medicationrequest.html
message MedicationRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External ids for this request
  repeated Identifier identifier = 9;

  // Protocol or definition
  repeated Reference definition = 10;

  // What request fulfills
  repeated Reference based_on = 11;

  // Composite request this is part of
  Identifier group_identifier = 12;

  // active | on-hold | cancelled | completed | entered-in-error | stopped |
  // draft | unknown
  MedicationRequestStatusCode status = 13;

  // proposal | plan | order | instance-order
  MedicationRequestIntentCode intent = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Type of medication usage
  CodeableConcept category = 15;

  // routine | urgent | stat | asap
  MedicationRequestPriorityCode priority = 16;

  // Medication to be taken
  message Medication {
    oneof medication {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Medication medication = 17
      [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

  // Who or group medication request is for
  Reference subject = 18 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Created during encounter/admission/stay
  Reference context = 19;

  // Information to support ordering of the medication
  repeated Reference supporting_information = 20;

  // When request was initially authored
  DateTime authored_on = 21;

  // Who/What requested the Request
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Who ordered the initial medication(s)
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization agent is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 22;

  // Person who entered the request
  Reference recorder = 23;

  // Reason or indication for writing the prescription
  repeated CodeableConcept reason_code = 24;

  // Condition or Observation that supports why the prescription is being
  // written
  repeated Reference reason_reference = 25;

  // Information about the prescription
  repeated Annotation note = 26;

  // How the medication should be taken
  repeated Dosage dosage_instruction = 27;

  // Medication supply authorization
  message DispenseRequest {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Time period supply is authorized for
    Period validity_period = 4;

    // Number of refills authorized
    PositiveInt number_of_repeats_allowed = 5;

    // Amount of medication to supply per dispense
    Quantity quantity = 6;

    // Number of days supply per dispense
    Duration expected_supply_duration = 7;

    // Intended dispenser
    Reference performer = 8;
  }
  DispenseRequest dispense_request = 28;

  // Any restrictions on medication substitution
  message Substitution {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Whether substitution is allowed or not
    Boolean allowed = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Why should (not) substitution be made
    CodeableConcept reason = 5;
  }
  Substitution substitution = 29;

  // An order/prescription that is being replaced
  Reference prior_prescription = 30;

  // Clinical Issue with action
  repeated Reference detected_issue = 31;

  // A list of events of interest in the lifecycle
  repeated Reference event_history = 32;
}

// Auto-generated from StructureDefinition for MedicationStatement, last updated
// 2017-04-19T07:44:43.294+10:00. Record of medication being taken by a patient.
// See https://www.hl7.org/fhir/medicationstatement.html
message MedicationStatement {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifier
  repeated Identifier identifier = 9;

  // Fulfils plan, proposal or order
  repeated Reference based_on = 10;

  // Part of referenced event
  repeated Reference part_of = 11;

  // Encounter / Episode associated with MedicationStatement
  Reference context = 12;

  // active | completed | entered-in-error | intended | stopped | on-hold
  MedicationStatementStatusCode status = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Type of medication usage
  CodeableConcept category = 14;

  // What medication was taken
  message Medication {
    oneof medication {
      option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Medication medication = 15
      [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

  // The date/time or interval when the medication was taken
  message Effective {
    oneof effective {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Effective effective = 16 [(is_choice_type) = true];

  // When the statement was asserted?
  DateTime date_asserted = 17;

  // Person or organization that provided the information about the taking of
  // this medication
  Reference information_source = 18;

  // Who is/was taking  the medication
  Reference subject = 19 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Additional supporting information
  repeated Reference derived_from = 20;

  // y | n | unk | na
  MedicationStatementTakenCode taken = 21
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // True if asserting medication was not given
  repeated CodeableConcept reason_not_taken = 22;

  // Reason for why the medication is being/was taken
  repeated CodeableConcept reason_code = 23;

  // Condition or observation that supports why the medication is being/was
  // taken
  repeated Reference reason_reference = 24;

  // Further information about the statement
  repeated Annotation note = 25;

  // Details of how medication is/was taken or should be taken
  repeated Dosage dosage = 26;
}

// Auto-generated from StructureDefinition for MessageDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. A resource that defines a type of message that
// can be exchanged between systems. See
// https://www.hl7.org/fhir/messagedefinition.html
message MessageDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this message definition (globally unique)
  Uri url = 9;

  // Additional identifier for the message definition
  Identifier identifier = 10;

  // Business version of the message definition
  String version = 11;

  // Name for this message definition (computer friendly)
  String name = 12;

  // Name for this message definition (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the message definition
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for message definition (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Why this message definition is defined
  Markdown purpose = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // Definition this one is based on
  Reference base = 24;

  // Protocol/workflow this is part of
  repeated Reference parent = 25;

  // Takes the place of
  repeated Reference replaces = 26;

  // Event type
  Coding event = 27 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Consequence | Currency | Notification
  MessageSignificanceCategoryCode category = 28;

  // Resource(s) that are the subject of the event
  message Focus {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of resource
    ResourceTypeCode code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Profile that must be adhered to by focus
    Reference profile = 5;

    // Minimum number of focuses of this type
    UnsignedInt min = 6;

    // Maximum number of focuses of this type
    String max = 7;
  }
  repeated Focus focus = 29;

  // Is a response required?
  Boolean response_required = 30;

  // Responses to this message
  message AllowedResponse {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reference to allowed message definition response
    Reference message = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // When should this response be used
    Markdown situation = 5;
  }
  repeated AllowedResponse allowed_response = 31;
}

// Auto-generated from StructureDefinition for MessageHeader, last updated
// 2017-04-19T07:44:43.294+10:00. A resource that describes a message that is
// exchanged between systems. See https://www.hl7.org/fhir/messageheader.html
message MessageHeader {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Code for the event this message represents
  Coding event = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Message destination application(s)
  message MessageDestination {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name of system
    String name = 4;

    // Particular delivery destination within the destination
    Reference target = 5;

    // Actual destination address or id
    Uri endpoint = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated MessageDestination destination = 10;

  // Intended "real-world" recipient for the data
  Reference receiver = 11;

  // Real world sender of the message
  Reference sender = 12;

  // Time that the message was sent
  Instant timestamp = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The source of the data entry
  Reference enterer = 14;

  // The source of the decision
  Reference author = 15;

  // Message source application
  message MessageSource {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name of system
    String name = 4;

    // Name of software running the system
    String software = 5;

    // Version of software running
    String version = 6;

    // Human contact for problems
    ContactPoint contact = 7;

    // Actual message source address or id
    Uri endpoint = 8 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  MessageSource source = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Final responsibility for event
  Reference responsible = 17;

  // Cause of event
  CodeableConcept reason = 18;

  // If this is a reply to prior message
  message Response {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Id of original message
    Id identifier = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // ok | transient-error | fatal-error
    ResponseTypeCode code = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Specific list of hints/warnings/errors
    Reference details = 6;
  }
  Response response = 19;

  // The actual content of the message
  repeated Reference focus = 20;
}

// Auto-generated from StructureDefinition for NamingSystem, last updated
// 2017-04-19T07:44:43.294+10:00. System of unique identification. See
// https://www.hl7.org/fhir/namingsystem.html
message NamingSystem {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Name for this naming system (computer friendly)
  String name = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // draft | active | retired | unknown
  PublicationStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // codesystem | identifier | root
  NamingSystemTypeCode kind = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Date this was last changed
  DateTime date = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name of the publisher (organization or individual)
  String publisher = 13;

  // Contact details for the publisher
  repeated ContactDetail contact = 14;

  // Who maintains system namespace?
  String responsible = 15;

  // e.g. driver,  provider,  patient, bank etc.
  CodeableConcept type = 16;

  // Natural language description of the naming system
  Markdown description = 17;

  // Context the content is intended to support
  repeated UsageContext use_context = 18;

  // Intended jurisdiction for naming system (if applicable)
  repeated CodeableConcept jurisdiction = 19;

  // How/where is it used
  String usage = 20;

  // Unique identifiers used for system
  message UniqueId {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // oid | uuid | uri | other
    NamingSystemIdentifierTypeCode type = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // The unique identifier
    String value = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Is this the id that should be used for this type
    Boolean preferred = 6;

    // Notes about identifier usage
    String comment = 7;

    // When is identifier valid?
    Period period = 8;
  }
  repeated UniqueId unique_id = 21
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Use this instead
  Reference replaced_by = 22;
}

// Auto-generated from StructureDefinition for NutritionOrder, last updated
// 2017-04-19T07:44:43.294+10:00. Diet, formula or nutritional supplement
// request. See https://www.hl7.org/fhir/nutritionorder.html
message NutritionOrder {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifiers assigned to this order
  repeated Identifier identifier = 9;

  // proposed | draft | planned | requested | active | on-hold | completed |
  // cancelled | entered-in-error
  NutritionOrderStatusCode status = 10;

  // The person who requires the diet, formula or nutritional supplement
  Reference patient = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The encounter associated with this nutrition order
  Reference encounter = 12;

  // Date and time the nutrition order was requested
  DateTime date_time = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who ordered the diet, formula or nutritional supplement
  Reference orderer = 14;

  // List of the patient's food and nutrition-related allergies and intolerances
  repeated Reference allergy_intolerance = 15;

  // Order-specific modifier about the type of food that should be given
  repeated CodeableConcept food_preference_modifier = 16;

  // Order-specific modifier about the type of food that should not be given
  repeated CodeableConcept exclude_food_modifier = 17;

  // Oral diet components
  message OralDiet {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of oral diet or diet restrictions that describe what can be consumed
    // orally
    repeated CodeableConcept type = 4;

    // Scheduled frequency of diet
    repeated Timing schedule = 5;

    // Required  nutrient modifications
    message Nutrient {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Type of nutrient that is being modified
      CodeableConcept modifier = 4;

      // Quantity of the specified nutrient
      Quantity amount = 5;
    }
    repeated Nutrient nutrient = 6;

    // Required  texture modifications
    message Texture {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Code to indicate how to alter the texture of the foods, e.g. pureed
      CodeableConcept modifier = 4;

      // Concepts that are used to identify an entity that is ingested for
      // nutritional purposes
      CodeableConcept food_type = 5;
    }
    repeated Texture texture = 7;

    // The required consistency of fluids and liquids provided to the patient
    repeated CodeableConcept fluid_consistency_type = 8;

    // Instructions or additional information about the oral diet
    String instruction = 9;
  }
  OralDiet oral_diet = 18;

  // Supplement components
  message Supplement {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of supplement product requested
    CodeableConcept type = 4;

    // Product or brand name of the nutritional supplement
    String product_name = 5;

    // Scheduled frequency of supplement
    repeated Timing schedule = 6;

    // Amount of the nutritional supplement
    Quantity quantity = 7;

    // Instructions or additional information about the oral supplement
    String instruction = 8;
  }
  repeated Supplement supplement = 19;

  // Enteral formula components
  message EnteralFormula {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of enteral or infant formula
    CodeableConcept base_formula_type = 4;

    // Product or brand name of the enteral or infant formula
    String base_formula_product_name = 5;

    // Type of modular component to add to the feeding
    CodeableConcept additive_type = 6;

    // Product or brand name of the modular additive
    String additive_product_name = 7;

    // Amount of energy per specified volume that is required
    Quantity caloric_density = 8;

    // How the formula should enter the patient's gastrointestinal tract
    CodeableConcept routeof_administration = 9;

    // Formula feeding instruction as structured data
    message Administration {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Scheduled frequency of enteral feeding
      Timing schedule = 4;

      // The volume of formula to provide
      Quantity quantity = 5;

      // Speed with which the formula is provided per period of time
      message Rate {
        oneof rate {
          Quantity quantity = 1;
          Ratio ratio = 2;
        }
      }
      Rate rate = 6 [(is_choice_type) = true];
    }
    repeated Administration administration = 10;

    // Upper limit on formula volume per unit of time
    Quantity max_volume_to_deliver = 11;

    // Formula feeding instructions expressed as text
    String administration_instruction = 12;
  }
  EnteralFormula enteral_formula = 20;
}

// Auto-generated from StructureDefinition for Observation, last updated
// 2017-04-19T07:44:43.294+10:00. Measurements and simple assertions. See
// https://www.hl7.org/fhir/observation.html
message Observation {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier for observation
  repeated Identifier identifier = 9;

  // Fulfills plan, proposal or order
  repeated Reference based_on = 10;

  // registered | preliminary | final | amended +
  ObservationStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Classification of  type of observation
  repeated CodeableConcept category = 12;

  // Type of observation (code / type)
  CodeableConcept code = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who and/or what this is about
  Reference subject = 14;

  // Healthcare event during which this observation is made
  Reference context = 15;

  // Clinically relevant time/time-period for observation
  message Effective {
    oneof effective {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Effective effective = 16 [(is_choice_type) = true];

  // Date/Time this was made available
  Instant issued = 17;

  // Who is responsible for the observation
  repeated Reference performer = 18;

  // Actual result
  message Value {
    oneof value {
      Quantity quantity = 1;
      CodeableConcept codeable_concept = 2;
      String string_value = 3 [json_name = "string"];
      Boolean boolean = 4;
      Range range = 5;
      Ratio ratio = 6;
      SampledData sampled_data = 7;
      Attachment attachment = 8;
      Time time = 9;
      DateTime date_time = 10;
      Period period = 11;
    }
  }
  Value value = 19 [(is_choice_type) = true];

  // Why the result is missing
  CodeableConcept data_absent_reason = 20;

  // High, low, normal, etc.
  CodeableConcept interpretation = 21;

  // Comments about result
  String comment = 22;

  // Observed body part
  CodeableConcept body_site = 23;

  // How it was done
  CodeableConcept method = 24;

  // Specimen used for this observation
  Reference specimen = 25;

  // (Measurement) Device
  Reference device = 26;

  // Provides guide for interpretation
  message ReferenceRange {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Low Range, if relevant
    Quantity low = 4;

    // High Range, if relevant
    Quantity high = 5;

    // Reference range qualifier
    CodeableConcept type = 6;

    // Reference range population
    repeated CodeableConcept applies_to = 7;

    // Applicable age range, if relevant
    Range age = 8;

    // Text based reference range in an observation
    String text = 9;
  }
  repeated ReferenceRange reference_range = 27;

  // Resource related to this observation
  message Related {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // has-member | derived-from | sequel-to | replaces | qualified-by |
    // interfered-by
    ObservationRelationshipTypeCode type = 4;

    // Resource that is related to this one
    Reference target = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Related related = 28;

  // Component results
  message Component {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type of component observation (code / type)
    CodeableConcept code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Actual component result
    message Value {
      oneof value {
        Quantity quantity = 1;
        CodeableConcept codeable_concept = 2;
        String string_value = 3 [json_name = "string"];
        Range range = 4;
        Ratio ratio = 5;
        SampledData sampled_data = 6;
        Attachment attachment = 7;
        Time time = 8;
        DateTime date_time = 9;
        Period period = 10;
      }
    }
    Value value = 5 [(is_choice_type) = true];

    // Why the component result is missing
    CodeableConcept data_absent_reason = 6;

    // High, low, normal, etc.
    CodeableConcept interpretation = 7;

    // Provides guide for interpretation of component result
    repeated ReferenceRange reference_range = 8;
  }
  repeated Component component = 29;
}

// Auto-generated from StructureDefinition for OperationDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. Definition of an operation or a named query.
// See https://www.hl7.org/fhir/operationdefinition.html
message OperationDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this operation definition (globally unique)
  Uri url = 9;

  // Business version of the operation definition
  String version = 10;

  // Name for this operation definition (computer friendly)
  String name = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // draft | active | retired | unknown
  PublicationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // operation | query
  OperationKindCode kind = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 14;

  // Date this was last changed
  DateTime date = 15;

  // Name of the publisher (organization or individual)
  String publisher = 16;

  // Contact details for the publisher
  repeated ContactDetail contact = 17;

  // Natural language description of the operation definition
  Markdown description = 18;

  // Context the content is intended to support
  repeated UsageContext use_context = 19;

  // Intended jurisdiction for operation definition (if applicable)
  repeated CodeableConcept jurisdiction = 20;

  // Why this operation definition is defined
  Markdown purpose = 21;

  // Whether content is unchanged by the operation
  Boolean idempotent = 22;

  // Name used to invoke the operation
  Code code = 23 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Additional information about use
  String comment = 24;

  // Marks this as a profile of the base
  Reference base = 25;

  // Types this operation applies to
  repeated ResourceTypeCode resource = 26;

  // Invoke at the system level?
  Boolean system = 27 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Invole at the type level?
  Boolean type = 28 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Invoke on an instance?
  Boolean instance = 29 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Parameters for the operation/query
  message Parameter {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name in Parameters.parameter.name or in URL
    Code name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // in | out
    OperationParameterUseCode use = 5
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Minimum Cardinality
    Integer min = 6 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Maximum Cardinality (a number or *)
    String max = 7 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Description of meaning/use
    String documentation = 8;

    // What type this parameter has
    FHIRAllTypesCode type = 9;

    // number | date | string | token | reference | composite | quantity | uri
    SearchParamTypeCode search_type = 10;

    // Profile on the type
    Reference profile = 11;

    // ValueSet details if this is coded
    message Binding {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // required | extensible | preferred | example
      BindingStrengthCode strength = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Source of value set
      message ValueSet {
        oneof value_set {
          option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
          Uri uri = 1;
          Reference reference = 2;
        }
      }
      ValueSet value_set = 5 [
        (is_choice_type) = true,
        (validation_requirement) = REQUIRED_BY_FHIR
      ];
    }
    Binding binding = 12;

    // Parts of a nested Parameter
    repeated Parameter part = 13;
  }
  repeated Parameter parameter = 30;

  // Define overloaded variants for when  generating code
  message Overload {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name of parameter to include in overload
    repeated String parameter_name = 4;

    // Comments to go on overload
    String comment = 5;
  }
  repeated Overload overload = 31;
}

// Auto-generated from StructureDefinition for OperationOutcome, last updated
// 2017-04-19T07:44:43.294+10:00. Information about the success/failure of an
// action. See https://www.hl7.org/fhir/operationoutcome.html
message OperationOutcome {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // A single issue associated with the action
  message Issue {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // fatal | error | warning | information
    IssueSeverityCode severity = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Error or warning code
    IssueTypeCode code = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Additional details about the error
    CodeableConcept details = 6;

    // Additional diagnostic information about the issue
    String diagnostics = 7;

    // Path of element(s) related to issue
    repeated String location = 8;

    // FHIRPath of element(s) related to issue
    repeated String expression = 9;
  }
  repeated Issue issue = 9 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for Organization, last updated
// 2017-04-19T07:44:43.294+10:00. A grouping of people or organizations with a
// common purpose. See https://www.hl7.org/fhir/organization.html
message Organization {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifies this organization  across multiple systems
  repeated Identifier identifier = 9;

  // Whether the organization's record is still in active use
  Boolean active = 10;

  // Kind of organization
  repeated CodeableConcept type = 11;

  // Name used for the organization
  String name = 12;

  // A list of alternate names that the organization is known as, or was known
  // as in the past
  repeated String alias = 13;

  // A contact detail for the organization
  repeated ContactPoint telecom = 14;

  // An address for the organization
  repeated Address address = 15;

  // The organization of which this organization forms a part
  Reference part_of = 16;

  // Contact for the organization for a certain purpose
  message Contact {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The type of contact
    CodeableConcept purpose = 4;

    // A name associated with the contact
    HumanName name = 5;

    // Contact details (telephone, email, etc.)  for a contact
    repeated ContactPoint telecom = 6;

    // Visiting or postal addresses for the contact
    Address address = 7;
  }
  repeated Contact contact = 17;

  // Technical endpoints providing access to services operated for the
  // organization
  repeated Reference endpoint = 18;
}

// Auto-generated from StructureDefinition for Parameters, last updated
// 2017-04-19T07:44:43.294+10:00. Operation Request or Response. See
// https://www.hl7.org/fhir/parameters.html
message Parameters {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Operation Parameter
  message Parameter {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Name from the definition
    String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // If parameter is a data type
    message Value {
      oneof value {
        Base64Binary base64_binary = 1;
        Boolean boolean = 2;
        Code code = 3;
        Date date = 4;
        DateTime date_time = 5;
        Decimal decimal = 6;
        Id id = 7;
        Instant instant = 8;
        Integer integer = 9;
        Markdown markdown = 10;
        Oid oid = 11;
        PositiveInt positive_int = 12;
        String string_value = 13 [json_name = "string"];
        Time time = 14;
        UnsignedInt unsigned_int = 15;
        Uri uri = 16;
        Address address = 17;
        Age age = 18;
        Annotation annotation = 19;
        Attachment attachment = 20;
        CodeableConcept codeable_concept = 21;
        Coding coding = 22;
        ContactPoint contact_point = 23;
        Count count = 24;
        Distance distance = 25;
        Duration duration = 26;
        HumanName human_name = 27;
        Identifier identifier = 28;
        Money money = 29;
        Period period = 30;
        Quantity quantity = 31;
        Range range = 32;
        Ratio ratio = 33;
        Reference reference = 34;
        SampledData sampled_data = 35;
        Signature signature = 36;
        Timing timing = 37;
        Meta meta = 38;
      }
    }
    Value value = 5 [(is_choice_type) = true];

    // If parameter is a whole resource
    ContainedResource resource = 6;

    // Named part of a multi-part parameter
    repeated Parameter part = 7;
  }
  repeated Parameter parameter = 5;
}

// Auto-generated from StructureDefinition for Patient, last updated
// 2017-04-19T07:44:43.294+10:00. Information about an individual or animal
// receiving health care services. See https://www.hl7.org/fhir/patient.html
message Patient {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // An identifier for this patient
  repeated Identifier identifier = 9;

  // Whether this patient's record is in active use
  Boolean active = 10;

  // A name associated with the patient
  repeated HumanName name = 11;

  // A contact detail for the individual
  repeated ContactPoint telecom = 12;

  // male | female | other | unknown
  AdministrativeGenderCode gender = 13;

  // The date of birth for the individual
  Date birth_date = 14;

  // Indicates if the individual is deceased or not
  message Deceased {
    oneof deceased {
      Boolean boolean = 1;
      DateTime date_time = 2;
    }
  }
  Deceased deceased = 15 [(is_choice_type) = true];

  // Addresses for the individual
  repeated Address address = 16;

  // Marital (civil) status of a patient
  CodeableConcept marital_status = 17;

  // Whether patient is part of a multiple birth
  message MultipleBirth {
    oneof multiple_birth {
      Boolean boolean = 1;
      Integer integer = 2;
    }
  }
  MultipleBirth multiple_birth = 18 [(is_choice_type) = true];

  // Image of the patient
  repeated Attachment photo = 19;

  // A contact party (e.g. guardian, partner, friend) for the patient
  message Contact {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The kind of relationship
    repeated CodeableConcept relationship = 4;

    // A name associated with the contact person
    HumanName name = 5;

    // A contact detail for the person
    repeated ContactPoint telecom = 6;

    // Address for the contact person
    Address address = 7;

    // male | female | other | unknown
    AdministrativeGenderCode gender = 8;

    // Organization that is associated with the contact
    Reference organization = 9;

    // The period during which this contact person or organization is valid to
    // be contacted relating to this patient
    Period period = 10;
  }
  repeated Contact contact = 20;

  // This patient is known to be an animal (non-human)
  message Animal {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // E.g. Dog, Cow
    CodeableConcept species = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // E.g. Poodle, Angus
    CodeableConcept breed = 5;

    // E.g. Neutered, Intact
    CodeableConcept gender_status = 6;
  }
  Animal animal = 21;

  // A list of Languages which may be used to communicate with the patient about
  // his or her health
  message Communication {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The language which can be used to communicate with the patient about his
    // or her health
    CodeableConcept language = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Language preference indicator
    Boolean preferred = 5;
  }
  repeated Communication communication = 22;

  // Patient's nominated primary care provider
  repeated Reference general_practitioner = 23;

  // Organization that is the custodian of the patient record
  Reference managing_organization = 24;

  // Link to another patient resource that concerns the same actual person
  message Link {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The other patient or related person resource that the link refers to
    Reference other = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // replaced-by | replaces | refer | seealso - type of link
    LinkTypeCode type = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Link link = 25;
}

// Auto-generated from StructureDefinition for PaymentNotice, last updated
// 2017-04-19T07:44:43.294+10:00. PaymentNotice request. See
// https://www.hl7.org/fhir/paymentnotice.html
message PaymentNotice {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Request reference
  Reference request = 11;

  // Response reference
  Reference response = 12;

  // Payment or clearing date
  Date status_date = 13;

  // Creation date
  DateTime created = 14;

  // Insurer or Regulatory body
  Reference target = 15;

  // Responsible practitioner
  Reference provider = 16;

  // Responsible organization
  Reference organization = 17;

  // Whether payment has been sent or cleared
  CodeableConcept payment_status = 18;
}

// Auto-generated from StructureDefinition for PaymentReconciliation, last
// updated 2017-04-19T07:44:43.294+10:00. PaymentReconciliation resource. See
// https://www.hl7.org/fhir/paymentreconciliation.html
message PaymentReconciliation {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Period covered
  Period period = 11;

  // Creation date
  DateTime created = 12;

  // Insurer
  Reference organization = 13;

  // Claim reference
  Reference request = 14;

  // complete | error | partial
  CodeableConcept outcome = 15;

  // Disposition Message
  String disposition = 16;

  // Responsible practitioner
  Reference request_provider = 17;

  // Responsible organization
  Reference request_organization = 18;

  // List of settlements
  message Details {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Type code
    CodeableConcept type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Claim
    Reference request = 5;

    // Claim Response
    Reference response = 6;

    // Organization which submitted the claim
    Reference submitter = 7;

    // Organization which is receiving the payment
    Reference payee = 8;

    // Invoice date
    Date date = 9;

    // Amount being paid
    Money amount = 10;
  }
  repeated Details detail = 19;

  // Printed Form Identifier
  CodeableConcept form = 20;

  // Total amount of Payment
  Money total = 21;

  // Processing comments
  message Notes {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // display | print | printoper
    CodeableConcept type = 4;

    // Comment on the processing
    String text = 5;
  }
  repeated Notes process_note = 22;
}

// Auto-generated from StructureDefinition for Person, last updated
// 2017-04-19T07:44:43.294+10:00. A generic person record. See
// https://www.hl7.org/fhir/person.html
message Person {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // A human identifier for this person
  repeated Identifier identifier = 9;

  // A name associated with the person
  repeated HumanName name = 10;

  // A contact detail for the person
  repeated ContactPoint telecom = 11;

  // male | female | other | unknown
  AdministrativeGenderCode gender = 12;

  // The date on which the person was born
  Date birth_date = 13;

  // One or more addresses for the person
  repeated Address address = 14;

  // Image of the person
  Attachment photo = 15;

  // The organization that is the custodian of the person record
  Reference managing_organization = 16;

  // This person's record is in active use
  Boolean active = 17;

  // Link to a resource that concerns the same actual person
  message Link {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The resource to which this actual person is associated
    Reference target = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // level1 | level2 | level3 | level4
    IdentityAssuranceLevelCode assurance = 5;
  }
  repeated Link link = 18;
}

// Auto-generated from StructureDefinition for PlanDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. The definition of a plan for a series of
// actions, independent of any specific patient or context. See
// https://www.hl7.org/fhir/plandefinition.html
message PlanDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this plan definition (globally unique)
  Uri url = 9;

  // Additional identifier for the plan definition
  repeated Identifier identifier = 10;

  // Business version of the plan definition
  String version = 11;

  // Name for this plan definition (computer friendly)
  String name = 12;

  // Name for this plan definition (human friendly)
  String title = 13;

  // order-set | protocol | eca-rule
  CodeableConcept type = 14;

  // draft | active | retired | unknown
  PublicationStatusCode status = 15
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 16;

  // Date this was last changed
  DateTime date = 17;

  // Name of the publisher (organization or individual)
  String publisher = 18;

  // Natural language description of the plan definition
  Markdown description = 19;

  // Why this plan definition is defined
  Markdown purpose = 20;

  // Describes the clinical usage of the asset
  String usage = 21;

  // When the plan definition was approved by publisher
  Date approval_date = 22;

  // When the plan definition was last reviewed
  Date last_review_date = 23;

  // When the plan definition is expected to be used
  Period effective_period = 24;

  // Context the content is intended to support
  repeated UsageContext use_context = 25;

  // Intended jurisdiction for plan definition (if applicable)
  repeated CodeableConcept jurisdiction = 26;

  // E.g. Education, Treatment, Assessment, etc
  repeated CodeableConcept topic = 27;

  // A content contributor
  repeated Contributor contributor = 28;

  // Contact details for the publisher
  repeated ContactDetail contact = 29;

  // Use and/or publishing restrictions
  Markdown copyright = 30;

  // Related artifacts for the asset
  repeated RelatedArtifact related_artifact = 31;

  // Logic used by the plan definition
  repeated Reference library = 32;

  // What the plan is trying to accomplish
  message Goal {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // E.g. Treatment, dietary, behavioral, etc
    CodeableConcept category = 4;

    // Code or text describing the goal
    CodeableConcept description = 5
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // high-priority | medium-priority | low-priority
    CodeableConcept priority = 6;

    // When goal pursuit begins
    CodeableConcept start = 7;

    // What does the goal address
    repeated CodeableConcept addresses = 8;

    // Supporting documentation for the goal
    repeated RelatedArtifact documentation = 9;

    // Target outcome for the goal
    message Target {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The parameter whose value is to be tracked
      CodeableConcept measure = 4;

      // The target value to be achieved
      message Detail {
        oneof detail {
          Quantity quantity = 1;
          Range range = 2;
          CodeableConcept codeable_concept = 3;
        }
      }
      Detail detail = 5 [(is_choice_type) = true];

      // Reach goal within
      Duration due = 6;
    }
    repeated Target target = 10;
  }
  repeated Goal goal = 33;

  // Action defined by the plan
  message Action {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // User-visible label for the action (e.g. 1. or A.)
    String label = 4;

    // User-visible title
    String title = 5;

    // Short description of the action
    String description = 6;

    // Static text equivalent of the action, used if the dynamic aspects cannot
    // be interpreted by the receiving system
    String text_equivalent = 7;

    // Code representing the meaning of the action or sub-actions
    repeated CodeableConcept code = 8;

    // Why the action should be performed
    repeated CodeableConcept reason = 9;

    // Supporting documentation for the intended performer of the action
    repeated RelatedArtifact documentation = 10;

    // What goals this action supports
    repeated Id goal_id = 11;

    // When the action should be triggered
    repeated TriggerDefinition trigger_definition = 12;

    // Whether or not the action is applicable
    message Condition {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // applicability | start | stop
      ActionConditionKindCode kind = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Natural language description of the condition
      String description = 5;

      // Language of the expression
      String language = 6;

      // Boolean-valued expression
      String expression = 7;
    }
    repeated Condition condition = 13;

    // Input data requirements
    repeated DataRequirement input = 14;

    // Output data definition
    repeated DataRequirement output = 15;

    // Relationship to another action
    message RelatedAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // What action is this related to
      Id action_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // before-start | before | before-end | concurrent-with-start | concurrent
      // | concurrent-with-end | after-start | after | after-end
      ActionRelationshipTypeCode relationship = 5
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Time offset for the relationship
      message Offset {
        oneof offset {
          Duration duration = 1;
          Range range = 2;
        }
      }
      Offset offset = 6 [(is_choice_type) = true];
    }
    repeated RelatedAction related_action = 16;

    // When the action should take place
    message TimingType {
      oneof timing {
        DateTime date_time = 1;
        Period period = 2;
        Duration duration = 3;
        Range range = 4;
        Timing timing_value = 5 [json_name = "timing"];
      }
    }
    TimingType timing = 17 [(is_choice_type) = true];

    // Who should participate in the action
    message Participant {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // patient | practitioner | related-person
      ActionParticipantTypeCode type = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // E.g. Nurse, Surgeon, Parent, etc
      CodeableConcept role = 5;
    }
    repeated Participant participant = 18;

    // create | update | remove | fire-event
    Coding type = 19;

    // visual-group | logical-group | sentence-group
    ActionGroupingBehaviorCode grouping_behavior = 20;

    // any | all | all-or-none | exactly-one | at-most-one | one-or-more
    ActionSelectionBehaviorCode selection_behavior = 21;

    // must | could | must-unless-documented
    ActionRequiredBehaviorCode required_behavior = 22;

    // yes | no
    ActionPrecheckBehaviorCode precheck_behavior = 23;

    // single | multiple
    ActionCardinalityBehaviorCode cardinality_behavior = 24;

    // Description of the activity to be performed
    Reference definition = 25;

    // Transform to apply the template
    Reference transform = 26;

    // Dynamic aspects of the definition
    message DynamicValue {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Natural language description of the dynamic value
      String description = 4;

      // The path to the element to be set dynamically
      String path = 5;

      // Language of the expression
      String language = 6;

      // An expression that provides the dynamic value for the customization
      String expression = 7;
    }
    repeated DynamicValue dynamic_value = 27;

    // A sub-action
    repeated Action action = 28;
  }
  repeated Action action = 34;
}

// Auto-generated from StructureDefinition for Practitioner, last updated
// 2017-04-19T07:44:43.294+10:00. A person with a  formal responsibility in the
// provisioning of healthcare or related services. See
// https://www.hl7.org/fhir/practitioner.html
message Practitioner {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // A identifier for the person as this agent
  repeated Identifier identifier = 9;

  // Whether this practitioner's record is in active use
  Boolean active = 10;

  // The name(s) associated with the practitioner
  repeated HumanName name = 11;

  // A contact detail for the practitioner (that apply to all roles)
  repeated ContactPoint telecom = 12;

  // Address(es) of the practitioner that are not role specific (typically home
  // address)
  repeated Address address = 13;

  // male | female | other | unknown
  AdministrativeGenderCode gender = 14;

  // The date  on which the practitioner was born
  Date birth_date = 15;

  // Image of the person
  repeated Attachment photo = 16;

  // Qualifications obtained by training and certification
  message Qualification {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // An identifier for this qualification for the practitioner
    repeated Identifier identifier = 4;

    // Coded representation of the qualification
    CodeableConcept code = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Period during which the qualification is valid
    Period period = 6;

    // Organization that regulates and issues the qualification
    Reference issuer = 7;
  }
  repeated Qualification qualification = 17;

  // A language the practitioner is able to use in patient communication
  repeated CodeableConcept communication = 18;
}

// Auto-generated from StructureDefinition for PractitionerRole, last updated
// 2017-04-19T07:44:43.294+10:00. Roles/organizations the practitioner is
// associated with. See https://www.hl7.org/fhir/practitionerrole.html
message PractitionerRole {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifiers that are specific to a role/location
  repeated Identifier identifier = 9;

  // Whether this practitioner's record is in active use
  Boolean active = 10;

  // The period during which the practitioner is authorized to perform in these
  // role(s)
  Period period = 11;

  // Practitioner that is able to provide the defined services for the
  // organation
  Reference practitioner = 12;

  // Organization where the roles are available
  Reference organization = 13;

  // Roles which this practitioner may perform
  repeated CodeableConcept code = 14;

  // Specific specialty of the practitioner
  repeated CodeableConcept specialty = 15;

  // The location(s) at which this practitioner provides care
  repeated Reference location = 16;

  // The list of healthcare services that this worker provides for this role's
  // Organization/Location(s)
  repeated Reference healthcare_service = 17;

  // Contact details that are specific to the role/location/service
  repeated ContactPoint telecom = 18;

  // Times the Service Site is available
  message AvailableTime {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // mon | tue | wed | thu | fri | sat | sun
    repeated DaysOfWeekCode days_of_week = 4;

    // Always available? e.g. 24 hour service
    Boolean all_day = 5;

    // Opening time of day (ignored if allDay = true)
    Time available_start_time = 6;

    // Closing time of day (ignored if allDay = true)
    Time available_end_time = 7;
  }
  repeated AvailableTime available_time = 19;

  // Not available during this time due to provided reason
  message NotAvailable {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Reason presented to the user explaining why time not available
    String description = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Service not availablefrom this date
    Period during = 5;
  }
  repeated NotAvailable not_available = 20;

  // Description of availability exceptions
  String availability_exceptions = 21;

  // Technical endpoints providing access to services operated for the
  // practitioner with this role
  repeated Reference endpoint = 22;
}

// Auto-generated from StructureDefinition for Procedure, last updated
// 2017-04-19T07:44:43.294+10:00. An action that is being or was performed on a
// patient. See https://www.hl7.org/fhir/procedure.html
message Procedure {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Identifiers for this procedure
  repeated Identifier identifier = 9;

  // Instantiates protocol or definition
  repeated Reference definition = 10;

  // A request for this procedure
  repeated Reference based_on = 11;

  // Part of referenced event
  repeated Reference part_of = 12;

  // preparation | in-progress | suspended | aborted | completed |
  // entered-in-error | unknown
  EventStatusCode status = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // True if procedure was not performed as scheduled
  Boolean not_done = 14;

  // Reason procedure was not performed
  CodeableConcept not_done_reason = 15;

  // Classification of the procedure
  CodeableConcept category = 16;

  // Identification of the procedure
  CodeableConcept code = 17;

  // Who the procedure was performed on
  Reference subject = 18 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter or episode associated with the procedure
  Reference context = 19;

  // Date/Period the procedure was performed
  message Performed {
    oneof performed {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Performed performed = 20 [(is_choice_type) = true];

  // The people who performed the procedure
  message Performer {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The role the actor was in
    CodeableConcept role = 4;

    // The reference to the practitioner
    Reference actor = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization the device or practitioner was acting for
    Reference on_behalf_of = 6;
  }
  repeated Performer performer = 21;

  // Where the procedure happened
  Reference location = 22;

  // Coded reason procedure performed
  repeated CodeableConcept reason_code = 23;

  // Condition that is the reason the procedure performed
  repeated Reference reason_reference = 24;

  // Target body sites
  repeated CodeableConcept body_site = 25;

  // The result of procedure
  CodeableConcept outcome = 26;

  // Any report resulting from the procedure
  repeated Reference report = 27;

  // Complication following the procedure
  repeated CodeableConcept complication = 28;

  // A condition that is a result of the procedure
  repeated Reference complication_detail = 29;

  // Instructions for follow up
  repeated CodeableConcept follow_up = 30;

  // Additional information about the procedure
  repeated Annotation note = 31;

  // Device changed in procedure
  message FocalDevice {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Kind of change to device
    CodeableConcept action = 4;

    // Device that was changed
    Reference manipulated = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated FocalDevice focal_device = 32;

  // Items used during procedure
  repeated Reference used_reference = 33;

  // Coded items used during the procedure
  repeated CodeableConcept used_code = 34;
}

// Auto-generated from StructureDefinition for ProcedureRequest, last updated
// 2017-04-19T07:44:43.294+10:00. A request for a procedure or diagnostic to be
// performed. See https://www.hl7.org/fhir/procedurerequest.html
message ProcedureRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Identifiers assigned to this order
  repeated Identifier identifier = 9;

  // Protocol or definition
  repeated Reference definition = 10;

  // What request fulfills
  repeated Reference based_on = 11;

  // What request replaces
  repeated Reference replaces = 12;

  // Composite Request ID
  Identifier requisition = 13;

  // draft | active | suspended | completed | entered-in-error | cancelled
  RequestStatusCode status = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // proposal | plan | order +
  RequestIntentCode intent = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // routine | urgent | asap | stat
  RequestPriorityCode priority = 16;

  // True if procedure should not be performed
  Boolean do_not_perform = 17;

  // Classification of procedure
  repeated CodeableConcept category = 18;

  // What is being requested/ordered
  CodeableConcept code = 19 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Individual the service is ordered for
  Reference subject = 20 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Encounter or Episode during which request was created
  Reference context = 21;

  // When procedure should occur
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
      Timing timing = 3;
    }
  }
  Occurrence occurrence = 22 [(is_choice_type) = true];

  // Preconditions for procedure or diagnostic
  message AsNeeded {
    oneof as_needed {
      Boolean boolean = 1;
      CodeableConcept codeable_concept = 2;
    }
  }
  AsNeeded as_needed = 23 [(is_choice_type) = true];

  // Date request signed
  DateTime authored_on = 24;

  // Who/what is requesting procedure or diagnostic
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual making the request
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization agent is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 25;

  // Performer role
  CodeableConcept performer_type = 26;

  // Requested perfomer
  Reference performer = 27;

  // Explanation/Justification for test
  repeated CodeableConcept reason_code = 28;

  // Explanation/Justification for test
  repeated Reference reason_reference = 29;

  // Additional clinical information
  repeated Reference supporting_info = 30;

  // Procedure Samples
  repeated Reference specimen = 31;

  // Location on Body
  repeated CodeableConcept body_site = 32;

  // Comments
  repeated Annotation note = 33;

  // Request provenance
  repeated Reference relevant_history = 34;
}

// Auto-generated from StructureDefinition for ProcessRequest, last updated
// 2017-04-19T07:44:43.294+10:00. Request to perform some action on or in
// regards to an existing resource. See
// https://www.hl7.org/fhir/processrequest.html
message ProcessRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // cancel | poll | reprocess | status
  ActionListCode action = 11;

  // Party which is the target of the request
  Reference target = 12;

  // Creation date
  DateTime created = 13;

  // Responsible practitioner
  Reference provider = 14;

  // Responsible organization
  Reference organization = 15;

  // Reference to the Request resource
  Reference request = 16;

  // Reference to the Response resource
  Reference response = 17;

  // Remove history
  Boolean nullify = 18;

  // Reference number/string
  String reference = 19;

  // Items to re-adjudicate
  message Items {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Service instance
    Integer sequence_link_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Items item = 20;

  // Resource type(s) to include
  repeated String include = 21;

  // Resource type(s) to exclude
  repeated String exclude = 22;

  // Selection period
  Period period = 23;
}

// Auto-generated from StructureDefinition for ProcessResponse, last updated
// 2017-04-19T07:44:43.294+10:00. ProcessResponse resource. See
// https://www.hl7.org/fhir/processresponse.html
message ProcessResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Creation date
  DateTime created = 11;

  // Authoring Organization
  Reference organization = 12;

  // Request reference
  Reference request = 13;

  // Processing outcome
  CodeableConcept outcome = 14;

  // Disposition Message
  String disposition = 15;

  // Responsible Practitioner
  Reference request_provider = 16;

  // Responsible organization
  Reference request_organization = 17;

  // Printed Form Identifier
  CodeableConcept form = 18;

  // Processing comments or additional requirements
  message ProcessNote {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // display | print | printoper
    CodeableConcept type = 4;

    // Comment on the processing
    String text = 5;
  }
  repeated ProcessNote process_note = 19;

  // Error code
  repeated CodeableConcept error = 20;

  // Request for additional information
  repeated Reference communication_request = 21;
}

// Auto-generated from StructureDefinition for Provenance, last updated
// 2017-04-19T07:44:43.294+10:00. Who, What, When for a set of resources. See
// https://www.hl7.org/fhir/provenance.html
message Provenance {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Target Reference(s) (usually version specific)
  repeated Reference target = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // When the activity occurred
  Period period = 10;

  // When the activity was recorded / updated
  Instant recorded = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Policy or plan the activity was defined by
  repeated Uri policy = 12;

  // Where the activity occurred, if relevant
  Reference location = 13;

  // Reason the activity is occurring
  repeated Coding reason = 14;

  // Activity that occurred
  Coding activity = 15;

  // Actor involved
  message Agent {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // What the agents role was
    repeated CodeableConcept role = 4;

    // Who participated
    message Who {
      oneof who {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Uri uri = 1;
        Reference reference = 2;
      }
    }
    Who who = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Who the agent is representing
    message OnBehalfOf {
      oneof on_behalf_of {
        Uri uri = 1;
        Reference reference = 2;
      }
    }
    OnBehalfOf on_behalf_of = 6 [(is_choice_type) = true];

    // Type of relationship between agents
    CodeableConcept related_agent_type = 7;
  }
  repeated Agent agent = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // An entity used in this activity
  message Entity {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // derivation | revision | quotation | source | removal
    ProvenanceEntityRoleCode role = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Identity of entity
    message What {
      oneof what {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Uri uri = 1;
        Reference reference = 2;
        Identifier identifier = 3;
      }
    }
    What what = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];

    // Entity is attributed to this agent
    repeated Agent agent = 6;
  }
  repeated Entity entity = 17;

  // Signature on target
  repeated Signature signature = 18;
}

// Auto-generated from StructureDefinition for Questionnaire, last updated
// 2017-04-19T07:44:43.294+10:00. A structured set of questions. See
// https://www.hl7.org/fhir/questionnaire.html
message Questionnaire {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this questionnaire (globally unique)
  Uri url = 9;

  // Additional identifier for the questionnaire
  repeated Identifier identifier = 10;

  // Business version of the questionnaire
  String version = 11;

  // Name for this questionnaire (computer friendly)
  String name = 12;

  // Name for this questionnaire (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Natural language description of the questionnaire
  Markdown description = 18;

  // Why this questionnaire is defined
  Markdown purpose = 19;

  // When the questionnaire was approved by publisher
  Date approval_date = 20;

  // When the questionnaire was last reviewed
  Date last_review_date = 21;

  // When the questionnaire is expected to be used
  Period effective_period = 22;

  // Context the content is intended to support
  repeated UsageContext use_context = 23;

  // Intended jurisdiction for questionnaire (if applicable)
  repeated CodeableConcept jurisdiction = 24;

  // Contact details for the publisher
  repeated ContactDetail contact = 25;

  // Use and/or publishing restrictions
  Markdown copyright = 26;

  // Concept that represents the overall questionnaire
  repeated Coding code = 27;

  // Resource that can be subject of QuestionnaireResponse
  repeated ResourceTypeCode subject_type = 28;

  // Questions and sections within the Questionnaire
  message Item {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Unique id for item in questionnaire
    String link_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // ElementDefinition - details for the item
    Uri definition = 5;

    // Corresponding concept for this item in a terminology
    repeated Coding code = 6;

    // E.g. "1(a)", "2.5.3"
    String prefix = 7;

    // Primary text for the item
    String text = 8;

    // group | display | boolean | decimal | integer | date | dateTime +
    QuestionnaireItemTypeCode type = 9
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Only allow data when
    message EnableWhen {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Question that determines whether item is enabled
      String question = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Enable when answered or not
      Boolean has_answer_value = 5 [json_name = "hasAnswer"];

      // Value question must have
      message Answer {
        oneof answer {
          Boolean boolean = 1;
          Decimal decimal = 2;
          Integer integer = 3;
          Date date = 4;
          DateTime date_time = 5;
          Time time = 6;
          String string_value = 7 [json_name = "string"];
          Uri uri = 8;
          Attachment attachment = 9;
          Coding coding = 10;
          Quantity quantity = 11;
          Reference reference = 12;
        }
      }
      Answer answer = 6 [(is_choice_type) = true];
    }
    repeated EnableWhen enable_when = 10;

    // Whether the item must be included in data results
    Boolean required = 11;

    // Whether the item may repeat
    Boolean repeats = 12;

    // Don't allow human editing
    Boolean read_only = 13;

    // No more than this many characters
    Integer max_length = 14;

    // Valueset containing permitted answers
    Reference options = 15;

    // Permitted answer
    message Option {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Answer value
      message Value {
        oneof value {
          option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
          Integer integer = 1;
          Date date = 2;
          Time time = 3;
          String string_value = 4 [json_name = "string"];
          Coding coding = 5;
        }
      }
      Value value = 4 [
        (is_choice_type) = true,
        (validation_requirement) = REQUIRED_BY_FHIR
      ];
    }
    repeated Option option = 16;

    // Default value when item is first rendered
    message Initial {
      oneof initial {
        Boolean boolean = 1;
        Decimal decimal = 2;
        Integer integer = 3;
        Date date = 4;
        DateTime date_time = 5;
        Time time = 6;
        String string_value = 7 [json_name = "string"];
        Uri uri = 8;
        Attachment attachment = 9;
        Coding coding = 10;
        Quantity quantity = 11;
        Reference reference = 12;
      }
    }
    Initial initial = 17 [(is_choice_type) = true];

    // Nested questionnaire items
    repeated Item item = 18;
  }
  repeated Item item = 29;
}

// Auto-generated from StructureDefinition for QuestionnaireResponse, last
// updated 2017-04-19T07:44:43.294+10:00. A structured set of questions and
// their answers. See https://www.hl7.org/fhir/questionnaireresponse.html
message QuestionnaireResponse {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique id for this set of answers
  Identifier identifier = 9;

  // Request fulfilled by this QuestionnaireResponse
  repeated Reference based_on = 10;

  // Part of this action
  repeated Reference parent = 11;

  // Form being answered
  Reference questionnaire = 12;

  // in-progress | completed | amended | entered-in-error | stopped
  QuestionnaireResponseStatusCode status = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // The subject of the questions
  Reference subject = 14;

  // Encounter or Episode during which questionnaire was completed
  Reference context = 15;

  // Date the answers were gathered
  DateTime authored = 16;

  // Person who received and recorded the answers
  Reference author = 17;

  // The person who answered the questions
  Reference source = 18;

  // Groups and questions
  message Item {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Pointer to specific item from Questionnaire
    String link_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // ElementDefinition - details for the item
    Uri definition = 5;

    // Name for group or question text
    String text = 6;

    // The subject this group's answers are about
    Reference subject = 7;

    // The response(s) to the question
    message Answer {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Single-valued answer to the question
      message Value {
        oneof value {
          Boolean boolean = 1;
          Decimal decimal = 2;
          Integer integer = 3;
          Date date = 4;
          DateTime date_time = 5;
          Time time = 6;
          String string_value = 7 [json_name = "string"];
          Uri uri = 8;
          Attachment attachment = 9;
          Coding coding = 10;
          Quantity quantity = 11;
          Reference reference = 12;
        }
      }
      Value value = 4 [(is_choice_type) = true];

      // Nested groups and questions
      repeated Item item = 5;
    }
    repeated Answer answer = 8;

    // Nested questionnaire response items
    repeated Item item = 9;
  }
  repeated Item item = 19;
}

// Auto-generated from StructureDefinition for ReferralRequest, last updated
// 2017-04-19T07:44:43.294+10:00. A request for referral or transfer of care.
// See https://www.hl7.org/fhir/referralrequest.html
message ReferralRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // Instantiates protocol or definition
  repeated Reference definition = 10;

  // Request fulfilled by this request
  repeated Reference based_on = 11;

  // Request(s) replaced by this request
  repeated Reference replaces = 12;

  // Composite request this is part of
  Identifier group_identifier = 13;

  // draft | active | suspended | cancelled | completed | entered-in-error |
  // unknown
  RequestStatusCode status = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // proposal | plan | order
  RequestIntentCode intent = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Referral/Transition of care request type
  CodeableConcept type = 16;

  // Urgency of referral / transfer of care request
  RequestPriorityCode priority = 17;

  // Actions requested as part of the referral
  repeated CodeableConcept service_requested = 18;

  // Patient referred to care or transfer
  Reference subject = 19 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Originating encounter
  Reference context = 20;

  // When the service(s) requested in the referral should occur
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Occurrence occurrence = 21 [(is_choice_type) = true];

  // Date of creation/activation
  DateTime authored_on = 22;

  // Who/what is requesting service
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual making the request
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization agent is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 23;

  // The clinical specialty (discipline) that the referral is requested for
  CodeableConcept specialty = 24;

  // Receiver of referral / transfer of care request
  repeated Reference recipient = 25;

  // Reason for referral / transfer of care request
  repeated CodeableConcept reason_code = 26;

  // Why is service needed?
  repeated Reference reason_reference = 27;

  // A textual description of the referral
  String description = 28;

  // Additonal information to support referral or transfer of care request
  repeated Reference supporting_info = 29;

  // Comments made about referral request
  repeated Annotation note = 30;

  // Key events in history of request
  repeated Reference relevant_history = 31;
}

// Auto-generated from StructureDefinition for RelatedPerson, last updated
// 2017-04-19T07:44:43.294+10:00. An person that is related to a patient, but
// who is not a direct target of care. See
// https://www.hl7.org/fhir/relatedperson.html
message RelatedPerson {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // A human identifier for this person
  repeated Identifier identifier = 9;

  // Whether this related person's record is in active use
  Boolean active = 10;

  // The patient this person is related to
  Reference patient = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The nature of the relationship
  CodeableConcept relationship = 12;

  // A name associated with the person
  repeated HumanName name = 13;

  // A contact detail for the person
  repeated ContactPoint telecom = 14;

  // male | female | other | unknown
  AdministrativeGenderCode gender = 15;

  // The date on which the related person was born
  Date birth_date = 16;

  // Address where the related person can be contacted or visited
  repeated Address address = 17;

  // Image of the person
  repeated Attachment photo = 18;

  // Period of time that this relationship is considered valid
  Period period = 19;
}

// Auto-generated from StructureDefinition for RequestGroup, last updated
// 2017-04-19T07:44:43.294+10:00. A group of related requests. See
// https://www.hl7.org/fhir/requestgroup.html
message RequestGroup {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // Instantiates protocol or definition
  repeated Reference definition = 10;

  // Fulfills plan, proposal, or order
  repeated Reference based_on = 11;

  // Request(s) replaced by this request
  repeated Reference replaces = 12;

  // Composite request this is part of
  Identifier group_identifier = 13;

  // draft | active | suspended | cancelled | completed | entered-in-error |
  // unknown
  RequestStatusCode status = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // proposal | plan | order
  RequestIntentCode intent = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // routine | urgent | asap | stat
  RequestPriorityCode priority = 16;

  // Who the request group is about
  Reference subject = 17;

  // Encounter or Episode for the request group
  Reference context = 18;

  // When the request group was authored
  DateTime authored_on = 19;

  // Device or practitioner that authored the request group
  Reference author = 20;

  // Reason for the request group
  message Reason {
    oneof reason {
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Reason reason = 21 [(is_choice_type) = true];

  // Additional notes about the response
  repeated Annotation note = 22;

  // Proposed actions, if any
  message Action {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // User-visible label for the action (e.g. 1. or A.)
    String label = 4;

    // User-visible title
    String title = 5;

    // Short description of the action
    String description = 6;

    // Static text equivalent of the action, used if the dynamic aspects cannot
    // be interpreted by the receiving system
    String text_equivalent = 7;

    // Code representing the meaning of the action or sub-actions
    repeated CodeableConcept code = 8;

    // Supporting documentation for the intended performer of the action
    repeated RelatedArtifact documentation = 9;

    // Whether or not the action is applicable
    message Condition {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // applicability | start | stop
      ActionConditionKindCode kind = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Natural language description of the condition
      String description = 5;

      // Language of the expression
      String language = 6;

      // Boolean-valued expression
      String expression = 7;
    }
    repeated Condition condition = 10;

    // Relationship to another action
    message RelatedAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // What action this is related to
      Id action_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // before-start | before | before-end | concurrent-with-start | concurrent
      // | concurrent-with-end | after-start | after | after-end
      ActionRelationshipTypeCode relationship = 5
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Time offset for the relationship
      message Offset {
        oneof offset {
          Duration duration = 1;
          Range range = 2;
        }
      }
      Offset offset = 6 [(is_choice_type) = true];
    }
    repeated RelatedAction related_action = 11;

    // When the action should take place
    message TimingType {
      oneof timing {
        DateTime date_time = 1;
        Period period = 2;
        Duration duration = 3;
        Range range = 4;
        Timing timing_value = 5 [json_name = "timing"];
      }
    }
    TimingType timing = 12 [(is_choice_type) = true];

    // Who should perform the action
    repeated Reference participant = 13;

    // create | update | remove | fire-event
    Coding type = 14;

    // visual-group | logical-group | sentence-group
    ActionGroupingBehaviorCode grouping_behavior = 15;

    // any | all | all-or-none | exactly-one | at-most-one | one-or-more
    ActionSelectionBehaviorCode selection_behavior = 16;

    // must | could | must-unless-documented
    ActionRequiredBehaviorCode required_behavior = 17;

    // yes | no
    ActionPrecheckBehaviorCode precheck_behavior = 18;

    // single | multiple
    ActionCardinalityBehaviorCode cardinality_behavior = 19;

    // The target of the action
    Reference resource = 20;

    // Sub action
    repeated Action action = 21;
  }
  repeated Action action = 23;
}

// Auto-generated from StructureDefinition for ResearchStudy, last updated
// 2017-04-19T07:44:43.294+10:00. Investigation to increase healthcare-related
// patient-independent knowledge. See
// https://www.hl7.org/fhir/researchstudy.html
message ResearchStudy {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier for study
  repeated Identifier identifier = 9;

  // Name for this study
  String title = 10;

  // Steps followed in executing study
  repeated Reference protocol = 11;

  // Part of larger study
  repeated Reference part_of = 12;

  // draft | in-progress | suspended | stopped | completed | entered-in-error
  ResearchStudyStatusCode status = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Classifications for the study
  repeated CodeableConcept category = 14;

  // Drugs, devices, conditions, etc. under study
  repeated CodeableConcept focus = 15;

  // Contact details for the study
  repeated ContactDetail contact = 16;

  // References and dependencies
  repeated RelatedArtifact related_artifact = 17;

  // Used to search for the study
  repeated CodeableConcept keyword = 18;

  // Geographic region(s) for study
  repeated CodeableConcept jurisdiction = 19;

  // What this is study doing
  Markdown description = 20;

  // Inclusion & exclusion criteria
  repeated Reference enrollment = 21;

  // When the study began and ended
  Period period = 22;

  // Organization responsible for the study
  Reference sponsor = 23;

  // The individual responsible for the study
  Reference principal_investigator = 24;

  // Location involved in study execution
  repeated Reference site = 25;

  // Reason for terminating study early
  CodeableConcept reason_stopped = 26;

  // Comments made about the event
  repeated Annotation note = 27;

  // Defined path through the study for a subject
  message Arm {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Label for study arm
    String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Categorization of study arm
    CodeableConcept code = 5;

    // Short explanation of study path
    String description = 6;
  }
  repeated Arm arm = 28;
}

// Auto-generated from StructureDefinition for ResearchSubject, last updated
// 2017-04-19T07:44:43.294+10:00. Investigation to increase healthcare-related
// patient-independent knowledge. See
// https://www.hl7.org/fhir/researchsubject.html
message ResearchSubject {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business Identifier for research subject
  Identifier identifier = 9;

  // candidate | enrolled | active | suspended | withdrawn | completed
  ResearchSubjectStatusCode status = 10
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Start and end of participation
  Period period = 11;

  // Study subject is part of
  Reference study = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who is part of study
  Reference individual = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // What path should be followed
  String assigned_arm = 14;

  // What path was followed
  String actual_arm = 15;

  // Agreement to participate in study
  Reference consent = 16;
}

// Auto-generated from StructureDefinition for RiskAssessment, last updated
// 2017-04-19T07:44:43.294+10:00. Potential outcomes for a subject with
// likelihood. See https://www.hl7.org/fhir/riskassessment.html
message RiskAssessment {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique identifier for the assessment
  Identifier identifier = 9;

  // Request fulfilled by this assessment
  Reference based_on = 10;

  // Part of this occurrence
  Reference parent = 11;

  // registered | preliminary | final | amended +
  ObservationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Evaluation mechanism
  CodeableConcept method = 13;

  // Type of assessment
  CodeableConcept code = 14;

  // Who/what does assessment apply to?
  Reference subject = 15;

  // Where was assessment performed?
  Reference context = 16;

  // When was assessment made?
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
    }
  }
  Occurrence occurrence = 17 [(is_choice_type) = true];

  // Condition assessed
  Reference condition = 18;

  // Who did assessment?
  Reference performer = 19;

  // Why the assessment was necessary?
  message Reason {
    oneof reason {
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Reason reason = 20 [(is_choice_type) = true];

  // Information used in assessment
  repeated Reference basis = 21;

  // Outcome predicted
  message Prediction {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Possible outcome for the subject
    CodeableConcept outcome = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Likelihood of specified outcome
    message Probability {
      oneof probability {
        Decimal decimal = 1;
        Range range = 2;
      }
    }
    Probability probability = 5 [(is_choice_type) = true];

    // Likelihood of specified outcome as a qualitative value
    CodeableConcept qualitative_risk = 6;

    // Relative likelihood
    Decimal relative_risk = 7;

    // Timeframe or age range
    message When {
      oneof when {
        Period period = 1;
        Range range = 2;
      }
    }
    When when = 8 [(is_choice_type) = true];

    // Explanation of prediction
    String rationale = 9;
  }
  repeated Prediction prediction = 22;

  // How to reduce risk
  String mitigation = 23;

  // Comments on the risk assessment
  String comment = 24;
}

// Auto-generated from StructureDefinition for Schedule, last updated
// 2017-04-19T07:44:43.294+10:00. A container for slots of time that may be
// available for booking appointments. See
// https://www.hl7.org/fhir/schedule.html
message Schedule {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this item
  repeated Identifier identifier = 9;

  // Whether this schedule is in active use
  Boolean active = 10;

  // A broad categorisation of the service that is to be performed during this
  // appointment
  CodeableConcept service_category = 11;

  // The specific service that is to be performed during this appointment
  repeated CodeableConcept service_type = 12;

  // The specialty of a practitioner that would be required to perform the
  // service requested in this appointment
  repeated CodeableConcept specialty = 13;

  // The resource this Schedule resource is providing availability information
  // for. These are expected to usually be one of HealthcareService, Location,
  // Practitioner, PractitionerRole, Device, Patient or RelatedPerson
  repeated Reference actor = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The period of time that the slots that are attached to this Schedule
  // resource cover (even if none exist). These  cover the amount of time that
  // an organization's planning horizon; the interval for which they are
  // currently accepting appointments. This does not define a "template" for
  // planning outside these dates
  Period planning_horizon = 15;

  // Comments on the availability to describe any extended information. Such as
  // custom constraints on the slots that may be associated
  String comment = 16;
}

// Auto-generated from StructureDefinition for SearchParameter, last updated
// 2017-04-19T07:44:43.294+10:00. Search Parameter for a resource. See
// https://www.hl7.org/fhir/searchparameter.html
message SearchParameter {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this search parameter (globally unique)
  Uri url = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Business version of the search parameter
  String version = 10;

  // Name for this search parameter (computer friendly)
  String name = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // draft | active | retired | unknown
  PublicationStatusCode status = 12
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 13;

  // Date this was last changed
  DateTime date = 14;

  // Name of the publisher (organization or individual)
  String publisher = 15;

  // Contact details for the publisher
  repeated ContactDetail contact = 16;

  // Context the content is intended to support
  repeated UsageContext use_context = 17;

  // Intended jurisdiction for search parameter (if applicable)
  repeated CodeableConcept jurisdiction = 18;

  // Why this search parameter is defined
  Markdown purpose = 19;

  // Code used in URL
  Code code = 20 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The resource type(s) this search parameter applies to
  repeated ResourceTypeCode base = 21
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // number | date | string | token | reference | composite | quantity | uri
  SearchParamTypeCode type = 22 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Original Definition for the search parameter
  Uri derived_from = 23;

  // Natural language description of the search parameter
  Markdown description = 24 [(validation_requirement) = REQUIRED_BY_FHIR];

  // FHIRPath expression that extracts the values
  String expression = 25;

  // XPath that extracts the values
  String xpath = 26;

  // normal | phonetic | nearby | distance | other
  XPathUsageTypeCode xpath_usage = 27;

  // Types of resource (if a resource reference)
  repeated ResourceTypeCode target = 28;

  // eq | ne | gt | lt | ge | le | sa | eb | ap
  repeated SearchComparatorCode comparator = 29;

  // missing | exact | contains | not | text | in | not-in | below | above |
  // type
  repeated SearchModifierCodeCode modifier = 30;

  // Chained names supported
  repeated String chain = 31;

  // For Composite resources to define the parts
  message Component {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Defines how the part works
    Reference definition = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Subexpression relative to main expression
    String expression = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Component component = 32;
}

// Auto-generated from StructureDefinition for Sequence, last updated
// 2017-04-19T07:44:43.294+10:00. Information about a biological sequence. See
// https://www.hl7.org/fhir/sequence.html
message Sequence {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique ID for this particular sequence. This is a FHIR-defined id
  repeated Identifier identifier = 9;

  // aa | dna | rna
  SequenceTypeCode type = 10;

  // Base number of coordinate system (0 for 0-based numbering or coordinates,
  // inclusive start, exclusive end, 1 for 1-based numbering, inclusive start,
  // inclusive end)
  Integer coordinate_system = 11 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Who and/or what this is about
  Reference patient = 12;

  // Specimen used for sequencing
  Reference specimen = 13;

  // The method for sequencing
  Reference device = 14;

  // Who should be responsible for test result
  Reference performer = 15;

  // The number of copies of the seqeunce of interest.  (RNASeq)
  Quantity quantity = 16;

  // A sequence used as reference
  message ReferenceSeq {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Chromosome containing genetic finding
    CodeableConcept chromosome = 4;

    // The Genome Build used for reference, following GRCh build versions e.g.
    // 'GRCh 37'
    String genome_build = 5;

    // Reference identifier
    CodeableConcept reference_seq_id = 6;

    // A Pointer to another Sequence entity as reference sequence
    Reference reference_seq_pointer = 7;

    // A string to represent reference sequence
    String reference_seq_string = 8;

    // Directionality of DNA ( +1/-1)
    Integer strand = 9;

    // Start position of the window on the  reference sequence
    Integer window_start = 10 [(validation_requirement) = REQUIRED_BY_FHIR];

    // End position of the window on the reference sequence
    Integer window_end = 11 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  ReferenceSeq reference_seq = 17;

  // Variant in sequence
  message Variant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Start position of the variant on the  reference sequence
    Integer start = 4;

    // End position of the variant on the reference sequence
    Integer end = 5;

    // Allele that was observed
    String observed_allele = 6;

    // Allele in the reference sequence
    String reference_allele = 7;

    // Extended CIGAR string for aligning the sequence with reference bases
    String cigar = 8;

    // Pointer to observed variant information
    Reference variant_pointer = 9;
  }
  repeated Variant variant = 18;

  // Sequence that was observed
  String observed_seq = 19;

  // An set of value as quality of sequence
  message Quality {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // indel | snp | unknown
    QualityTypeCode type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Standard sequence for comparison
    CodeableConcept standard_sequence = 5;

    // Start position of the sequence
    Integer start = 6;

    // End position of the sequence
    Integer end = 7;

    // Quality score for the comparison
    Quantity score = 8;

    // Method to get quality
    CodeableConcept method = 9;

    // True positives from the perspective of the truth data
    Decimal truth_t_p = 10;

    // True positives from the perspective of the query data
    Decimal query_t_p = 11;

    // False negatives
    Decimal truth_f_n = 12;

    // False positives
    Decimal query_f_p = 13;

    // False positives where the non-REF alleles in the Truth and Query Call
    // Sets match
    Decimal gt_f_p = 14;

    // Precision of comparison
    Decimal precision = 15;

    // Recall of comparison
    Decimal recall = 16;

    // F-score
    Decimal f_score = 17;
  }
  repeated Quality quality = 20;

  // Average number of reads representing a given nucleotide in the
  // reconstructed sequence
  Integer read_coverage = 21;

  // External repository which contains detailed report related with observedSeq
  // in this resource
  message Repository {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // directlink | openapi | login | oauth | other
    RepositoryTypeCode type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // URI of the repository
    Uri url = 5;

    // Repository's name
    String name = 6;

    // Id of the dataset that used to call for dataset in repository
    String dataset_id = 7;

    // Id of the variantset that used to call for variantset in repository
    String variantset_id = 8;

    // Id of the read
    String readset_id = 9;
  }
  repeated Repository repository = 22;

  // Pointer to next atomic sequence
  repeated Reference pointer = 23;
}

// Auto-generated from StructureDefinition for ServiceDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. A description of decision support service
// functionality. See https://www.hl7.org/fhir/servicedefinition.html
message ServiceDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this service definition (globally unique)
  Uri url = 9;

  // Additional identifier for the service definition
  repeated Identifier identifier = 10;

  // Business version of the service definition
  String version = 11;

  // Name for this service definition (computer friendly)
  String name = 12;

  // Name for this service definition (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Natural language description of the service definition
  Markdown description = 18;

  // Why this service definition is defined
  Markdown purpose = 19;

  // Describes the clinical usage of the module
  String usage = 20;

  // When the service definition was approved by publisher
  Date approval_date = 21;

  // When the service definition was last reviewed
  Date last_review_date = 22;

  // When the service definition is expected to be used
  Period effective_period = 23;

  // Context the content is intended to support
  repeated UsageContext use_context = 24;

  // Intended jurisdiction for service definition (if applicable)
  repeated CodeableConcept jurisdiction = 25;

  // E.g. Education, Treatment, Assessment, etc
  repeated CodeableConcept topic = 26;

  // A content contributor
  repeated Contributor contributor = 27;

  // Contact details for the publisher
  repeated ContactDetail contact = 28;

  // Use and/or publishing restrictions
  Markdown copyright = 29;

  // Additional documentation, citations, etc
  repeated RelatedArtifact related_artifact = 30;

  // "when" the module should be invoked
  repeated TriggerDefinition trigger = 31;

  // What data is used by the module
  repeated DataRequirement data_requirement = 32;

  // Operation to invoke
  Reference operation_definition = 33;
}

// Auto-generated from StructureDefinition for Slot, last updated
// 2017-04-19T07:44:43.294+10:00. A slot of time on a schedule that may be
// available for booking appointments. See https://www.hl7.org/fhir/slot.html
message Slot {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Ids for this item
  repeated Identifier identifier = 9;

  // A broad categorisation of the service that is to be performed during this
  // appointment
  CodeableConcept service_category = 10;

  // The type of appointments that can be booked into this slot (ideally this
  // would be an identifiable service - which is at a location, rather than the
  // location itself). If provided then this overrides the value provided on the
  // availability resource
  repeated CodeableConcept service_type = 11;

  // The specialty of a practitioner that would be required to perform the
  // service requested in this appointment
  repeated CodeableConcept specialty = 12;

  // The style of appointment or patient that may be booked in the slot (not
  // service type)
  CodeableConcept appointment_type = 13;

  // The schedule resource that this slot defines an interval of status
  // information
  Reference schedule = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // busy | free | busy-unavailable | busy-tentative | entered-in-error
  SlotStatusCode status = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Date/Time that the slot is to begin
  Instant start = 16 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Date/Time that the slot is to conclude
  Instant end = 17 [(validation_requirement) = REQUIRED_BY_FHIR];

  // This slot has already been overbooked, appointments are unlikely to be
  // accepted for this time
  Boolean overbooked = 18;

  // Comments on the slot to describe any extended information. Such as custom
  // constraints on the slot
  String comment = 19;
}

// Auto-generated from StructureDefinition for Specimen, last updated
// 2017-04-19T07:44:43.294+10:00. Sample for analysis. See
// https://www.hl7.org/fhir/specimen.html
message Specimen {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External Identifier
  repeated Identifier identifier = 9;

  // Identifier assigned by the lab
  Identifier accession_identifier = 10;

  // available | unavailable | unsatisfactory | entered-in-error
  SpecimenStatusCode status = 11;

  // Kind of material that forms the specimen
  CodeableConcept type = 12;

  // Where the specimen came from. This may be from the patient(s) or from the
  // environment or a device
  Reference subject = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // The time when specimen was received for processing
  DateTime received_time = 14;

  // Specimen from which this specimen originated
  repeated Reference parent = 15;

  // Why the specimen was collected
  repeated Reference request = 16;

  // Collection details
  message Collection {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Who collected the specimen
    Reference collector = 4;

    // Collection time
    message Collected {
      oneof collected {
        DateTime date_time = 1;
        Period period = 2;
      }
    }
    Collected collected = 5 [(is_choice_type) = true];

    // The quantity of specimen collected
    Quantity quantity = 6;

    // Technique used to perform collection
    CodeableConcept method = 7;

    // Anatomical collection site
    CodeableConcept body_site = 8;
  }
  Collection collection = 17;

  // Processing and processing step details
  message Processing {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Textual description of procedure
    String description = 4;

    // Indicates the treatment step  applied to the specimen
    CodeableConcept procedure = 5;

    // Material used in the processing step
    repeated Reference additive = 6;

    // Date and time of specimen processing
    message Time {
      oneof time {
        DateTime date_time = 1;
        Period period = 2;
      }
    }
    Time time = 7 [(is_choice_type) = true];
  }
  repeated Processing processing = 18;

  // Direct container of specimen (tube/slide, etc.)
  message Container {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Id for the container
    repeated Identifier identifier = 4;

    // Textual description of the container
    String description = 5;

    // Kind of container directly associated with specimen
    CodeableConcept type = 6;

    // Container volume or size
    Quantity capacity = 7;

    // Quantity of specimen within container
    Quantity specimen_quantity = 8;

    // Additive associated with container
    message Additive {
      oneof additive {
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    Additive additive = 9 [(is_choice_type) = true];
  }
  repeated Container container = 19;

  // Comments
  repeated Annotation note = 20;
}

// Auto-generated from StructureDefinition for StructureDefinition, last updated
// 2017-04-19T07:44:43.294+10:00. Structural Definition. See
// https://www.hl7.org/fhir/structuredefinition.html
message StructureDefinition {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this structure definition (globally unique)
  Uri url = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Additional identifier for the structure definition
  repeated Identifier identifier = 10;

  // Business version of the structure definition
  String version = 11;

  // Name for this structure definition (computer friendly)
  String name = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name for this structure definition (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the structure definition
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for structure definition (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Why this structure definition is defined
  Markdown purpose = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // Assist with indexing and finding
  repeated Coding keyword = 24;

  // FHIR Version this StructureDefinition targets
  Id fhir_version = 25;

  // External specification that the content is mapped to
  message Mapping {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Internal id when this mapping is used
    Id identity = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Identifies what this mapping refers to
    Uri uri = 5;

    // Names what this mapping refers to
    String name = 6;

    // Versions, Issues, Scope limitations etc.
    String comment = 7;
  }
  repeated Mapping mapping = 26;

  // primitive-type | complex-type | resource | logical
  StructureDefinitionKindCode kind = 27
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Whether the structure is abstract
  Boolean abstract = 28 [(validation_requirement) = REQUIRED_BY_FHIR];

  // resource | datatype | extension
  ExtensionContextCode context_type = 29;

  // Where the extension can be used in instances
  repeated String context = 30;

  // FHIRPath invariants - when the extension can be used
  repeated String context_invariant = 31;

  // Type defined or constrained by this structure
  FHIRDefinedTypeExtCode type = 32
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Definition that this type is constrained/specialized from
  Uri base_definition = 33;

  // specialization | constraint - How relates to base definition
  TypeDerivationRuleCode derivation = 34;

  // Snapshot view of the structure
  message Snapshot {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Definition of elements in the resource (if no StructureDefinition)
    repeated ElementDefinition element = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Snapshot snapshot = 35;

  // Differential view of the structure
  message Differential {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Definition of elements in the resource (if no StructureDefinition)
    repeated ElementDefinition element = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Differential differential = 36;
}

// Auto-generated from StructureDefinition for StructureMap, last updated
// 2017-04-19T07:44:43.294+10:00. A Map of relationships between 2 structures
// that can be used to transform data. See
// https://www.hl7.org/fhir/structuremap.html
message StructureMap {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this structure map (globally unique)
  Uri url = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Additional identifier for the structure map
  repeated Identifier identifier = 10;

  // Business version of the structure map
  String version = 11;

  // Name for this structure map (computer friendly)
  String name = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name for this structure map (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the structure map
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for structure map (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Why this structure map is defined
  Markdown purpose = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // Structure Definition used by this map
  message Structure {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Canonical URL for structure definition
    Uri url = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // source | queried | target | produced
    StructureMapModelModeCode mode = 5
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Name for type in this map
    String alias = 6;

    // Documentation on use of structure
    String documentation = 7;
  }
  repeated Structure structure = 24;

  // Other maps used by this map (canonical URLs)
  repeated Uri import = 25;

  // Named sections for reader convenience
  message Group {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Human-readable label
    Id name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Another group that this group adds rules to
    Id extends = 5;

    // none | types | type-and-types
    StructureMapGroupTypeModeCode type_mode = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Additional description/explaination for group
    String documentation = 7;

    // Named instance provided when invoking the map
    message Input {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Name for this instance of data
      Id name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Type for this instance of data
      String type = 5;

      // source | target
      StructureMapInputModeCode mode = 6
          [(validation_requirement) = REQUIRED_BY_FHIR];

      // Documentation for this instance of data
      String documentation = 7;
    }
    repeated Input input = 8 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Transform Rule from source to target
    message Rule {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Name of the rule for internal references
      Id name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Source inputs to the mapping
      message Source {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Type or variable this rule applies to
        Id context = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Specified minimum cardinality
        Integer min = 5;

        // Specified maximum cardinality (number or *)
        String max = 6;

        // Rule only applies if source has this type
        String type = 7;

        // Default value if no value exists
        message DefaultValue {
          oneof default_value {
            Base64Binary base64_binary = 1;
            Boolean boolean = 2;
            Code code = 3;
            Date date = 4;
            DateTime date_time = 5;
            Decimal decimal = 6;
            Id id = 7;
            Instant instant = 8;
            Integer integer = 9;
            Markdown markdown = 10;
            Oid oid = 11;
            PositiveInt positive_int = 12;
            String string_value = 13 [json_name = "string"];
            Time time = 14;
            UnsignedInt unsigned_int = 15;
            Uri uri = 16;
            Address address = 17;
            Age age = 18;
            Annotation annotation = 19;
            Attachment attachment = 20;
            CodeableConcept codeable_concept = 21;
            Coding coding = 22;
            ContactPoint contact_point = 23;
            Count count = 24;
            Distance distance = 25;
            Duration duration = 26;
            HumanName human_name = 27;
            Identifier identifier = 28;
            Money money = 29;
            Period period = 30;
            Quantity quantity = 31;
            Range range = 32;
            Ratio ratio = 33;
            Reference reference = 34;
            SampledData sampled_data = 35;
            Signature signature = 36;
            Timing timing = 37;
            Meta meta = 38;
          }
        }
        DefaultValue default_value = 8 [(is_choice_type) = true];

        // Optional field for this source
        String element = 9;

        // first | not_first | last | not_last | only_one
        StructureMapSourceListModeCode list_mode = 10;

        // Named context for field, if a field is specified
        Id variable = 11;

        // FHIRPath expression  - must be true or the rule does not apply
        String condition = 12;

        // FHIRPath expression  - must be true or the mapping engine throws an
        // error instead of completing
        String check = 13;
      }
      repeated Source source = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Content to create because of this mapping rule
      message Target {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Type or variable this rule applies to
        Id context = 4;

        // type | variable
        StructureMapContextTypeCode context_type = 5;

        // Field to create in the context
        String element = 6;

        // Named context for field, if desired, and a field is specified
        Id variable = 7;

        // first | share | last | collate
        repeated StructureMapTargetListModeCode list_mode = 8;

        // Internal rule reference for shared list items
        Id list_rule_id = 9;

        // create | copy +
        StructureMapTransformCode transform = 10;

        // Parameters to the transform
        message Parameter {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // Parameter value - variable or literal
          message Value {
            oneof value {
              option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
              Id id = 1;
              String string_value = 2 [json_name = "string"];
              Boolean boolean = 3;
              Integer integer = 4;
              Decimal decimal = 5;
            }
          }
          Value value = 4 [
            (is_choice_type) = true,
            (validation_requirement) = REQUIRED_BY_FHIR
          ];
        }
        repeated Parameter parameter = 11;
      }
      repeated Target target = 6;

      // Rules contained in this rule
      repeated Rule rule = 7;

      // Which other rules to apply in the context of this rule
      message Dependent {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Name of a rule or group to apply
        Id name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Variable to pass to the rule or group
        repeated String variable = 5
            [(validation_requirement) = REQUIRED_BY_FHIR];
      }
      repeated Dependent dependent = 8;

      // Documentation for this instance of data
      String documentation = 9;
    }
    repeated Rule rule = 9 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Group group = 26 [(validation_requirement) = REQUIRED_BY_FHIR];
}

// Auto-generated from StructureDefinition for Subscription, last updated
// 2017-04-19T07:44:43.294+10:00. A server push subscription criteria. See
// https://www.hl7.org/fhir/subscription.html
message Subscription {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // requested | active | error | off
  SubscriptionStatusCode status = 9
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Contact details for source (e.g. troubleshooting)
  repeated ContactPoint contact = 10;

  // When to automatically delete the subscription
  Instant end = 11;

  // Description of why this subscription was created
  String reason = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Rule for server push criteria
  String criteria = 13 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Latest error note
  String error = 14;

  // The channel on which to report matches to the criteria
  message Channel {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // rest-hook | websocket | email | sms | message
    SubscriptionChannelTypeCode type = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Where the channel points to
    Uri endpoint = 5;

    // Mimetype to send, or omit for no payload
    String payload = 6;

    // Usage depends on the channel type
    repeated String header = 7;
  }
  Channel channel = 15 [(validation_requirement) = REQUIRED_BY_FHIR];

  // A tag to add to matching resources
  repeated Coding tag = 16;
}

// Auto-generated from StructureDefinition for Substance, last updated
// 2017-04-19T07:44:43.294+10:00. A homogeneous material with a definite
// composition. See https://www.hl7.org/fhir/substance.html
message Substance {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique identifier
  repeated Identifier identifier = 9;

  // active | inactive | entered-in-error
  FHIRSubstanceStatusCode status = 10;

  // What class/type of substance this is
  repeated CodeableConcept category = 11;

  // What substance this is
  CodeableConcept code = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Textual description of the substance, comments
  String description = 13;

  // If this describes a specific package/container of the substance
  message Instance {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Identifier of the package/container
    Identifier identifier = 4;

    // When no longer valid to use
    DateTime expiry = 5;

    // Amount of substance in the package
    Quantity quantity = 6;
  }
  repeated Instance instance = 14;

  // Composition information about the substance
  message Ingredient {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Optional amount (concentration)
    Ratio quantity = 4;

    // A component of the substance
    message SubstanceType {
      oneof substance {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    SubstanceType substance = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Ingredient ingredient = 15;
}

// Auto-generated from StructureDefinition for SupplyDelivery, last updated
// 2017-04-19T07:44:43.294+10:00. Delivery of bulk Supplies. See
// https://www.hl7.org/fhir/supplydelivery.html
message SupplyDelivery {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifier
  Identifier identifier = 9;

  // Fulfills plan, proposal or order
  repeated Reference based_on = 10;

  // Part of referenced event
  repeated Reference part_of = 11;

  // in-progress | completed | abandoned | entered-in-error
  SupplyDeliveryStatusCode status = 12;

  // Patient for whom the item is supplied
  Reference patient = 13;

  // Category of dispense event
  CodeableConcept type = 14;

  // The item that is delivered or supplied
  message SuppliedItem {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Amount dispensed
    Quantity quantity = 4;

    // Medication, Substance, or Device supplied
    message Item {
      oneof item {
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    Item item = 5 [(is_choice_type) = true];
  }
  SuppliedItem supplied_item = 15;

  // When event occurred
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
      Timing timing = 3;
    }
  }
  Occurrence occurrence = 16 [(is_choice_type) = true];

  // Dispenser
  Reference supplier = 17;

  // Where the Supply was sent
  Reference destination = 18;

  // Who collected the Supply
  repeated Reference receiver = 19;
}

// Auto-generated from StructureDefinition for SupplyRequest, last updated
// 2017-04-19T07:44:43.294+10:00. Request for a medication, substance or device.
// See https://www.hl7.org/fhir/supplyrequest.html
message SupplyRequest {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Unique identifier
  Identifier identifier = 9;

  // draft | active | suspended +
  SupplyRequestStatusCode status = 10;

  // The kind of supply (central, non-stock, etc.)
  CodeableConcept category = 11;

  // routine | urgent | asap | stat
  RequestPriorityCode priority = 12;

  // The item being requested
  message OrderedItem {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The requested amount of the item indicated
    Quantity quantity = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Medication, Substance, or Device requested to be supplied
    message Item {
      oneof item {
        CodeableConcept codeable_concept = 1;
        Reference reference = 2;
      }
    }
    Item item = 5 [(is_choice_type) = true];
  }
  OrderedItem ordered_item = 13;

  // When the request should be fulfilled
  message Occurrence {
    oneof occurrence {
      DateTime date_time = 1;
      Period period = 2;
      Timing timing = 3;
    }
  }
  Occurrence occurrence = 14 [(is_choice_type) = true];

  // When the request was made
  DateTime authored_on = 15;

  // Who/what is requesting service
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual making the request
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization agent is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 16;

  // Who is intended to fulfill the request
  repeated Reference supplier = 17;

  // Why the supply item was requested
  message Reason {
    oneof reason {
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Reason reason = 18 [(is_choice_type) = true];

  // The origin of the supply
  Reference deliver_from = 19;

  // The destination of the supply
  Reference deliver_to = 20;
}

// Auto-generated from StructureDefinition for Task, last updated
// 2017-04-19T07:44:43.294+10:00. A task to be performed. See
// https://www.hl7.org/fhir/task.html
message Task {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Task Instance Identifier
  repeated Identifier identifier = 9;

  // Formal definition of task
  message Definition {
    oneof definition {
      Uri uri = 1;
      Reference reference = 2;
    }
  }
  Definition definition = 10 [(is_choice_type) = true];

  // Request fulfilled by this task
  repeated Reference based_on = 11;

  // Requisition or grouper id
  Identifier group_identifier = 12;

  // Composite task
  repeated Reference part_of = 13;

  // draft | requested | received | accepted | +
  TaskStatusCode status = 14 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Reason for current status
  CodeableConcept status_reason = 15;

  // E.g. "Specimen collected", "IV prepped"
  CodeableConcept business_status = 16;

  // proposal | plan | order +
  RequestIntentCode intent = 17 [(validation_requirement) = REQUIRED_BY_FHIR];

  // normal | urgent | asap | stat
  RequestPriorityCode priority = 18;

  // Task Type
  CodeableConcept code = 19;

  // Human-readable explanation of task
  String description = 20;

  // What task is acting on
  Reference focus = 21;

  // Beneficiary of the Task
  Reference for_value = 22 [json_name = "for"];

  // Healthcare event during which this task originated
  Reference context = 23;

  // Start and end time of execution
  Period execution_period = 24;

  // Task Creation Date
  DateTime authored_on = 25;

  // Task Last Modified Date
  DateTime last_modified = 26;

  // Who is asking for task to be done
  message Requester {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Individual asking for task
    Reference agent = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Organization individual is acting for
    Reference on_behalf_of = 5;
  }
  Requester requester = 27;

  // requester | dispatcher | scheduler | performer | monitor | manager |
  // acquirer | reviewer
  repeated CodeableConcept performer_type = 28;

  // Responsible individual
  Reference owner = 29;

  // Why task is needed
  CodeableConcept reason = 30;

  // Comments made about the task
  repeated Annotation note = 31;

  // Key events in history of the Task
  repeated Reference relevant_history = 32;

  // Constraints on fulfillment tasks
  message Restriction {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // How many times to repeat
    PositiveInt repetitions = 4;

    // When fulfillment sought
    Period period = 5;

    // For whom is fulfillment sought?
    repeated Reference recipient = 6;
  }
  Restriction restriction = 33;

  // Information used to perform task
  message Parameter {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Label for the input
    CodeableConcept type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Content to use in performing the task
    message Value {
      oneof value {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Base64Binary base64_binary = 1;
        Boolean boolean = 2;
        Code code = 3;
        Date date = 4;
        DateTime date_time = 5;
        Decimal decimal = 6;
        Id id = 7;
        Instant instant = 8;
        Integer integer = 9;
        Markdown markdown = 10;
        Oid oid = 11;
        PositiveInt positive_int = 12;
        String string_value = 13 [json_name = "string"];
        Time time = 14;
        UnsignedInt unsigned_int = 15;
        Uri uri = 16;
        Address address = 17;
        Age age = 18;
        Annotation annotation = 19;
        Attachment attachment = 20;
        CodeableConcept codeable_concept = 21;
        Coding coding = 22;
        ContactPoint contact_point = 23;
        Count count = 24;
        Distance distance = 25;
        Duration duration = 26;
        HumanName human_name = 27;
        Identifier identifier = 28;
        Money money = 29;
        Period period = 30;
        Quantity quantity = 31;
        Range range = 32;
        Ratio ratio = 33;
        Reference reference = 34;
        SampledData sampled_data = 35;
        Signature signature = 36;
        Timing timing = 37;
        Meta meta = 38;
      }
    }
    Value value = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Parameter input = 34;

  // Information produced as part of task
  message Output {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Label for output
    CodeableConcept type = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Result of output
    message Value {
      oneof value {
        option (oneof_validation_requirement) = REQUIRED_BY_FHIR;
        Base64Binary base64_binary = 1;
        Boolean boolean = 2;
        Code code = 3;
        Date date = 4;
        DateTime date_time = 5;
        Decimal decimal = 6;
        Id id = 7;
        Instant instant = 8;
        Integer integer = 9;
        Markdown markdown = 10;
        Oid oid = 11;
        PositiveInt positive_int = 12;
        String string_value = 13 [json_name = "string"];
        Time time = 14;
        UnsignedInt unsigned_int = 15;
        Uri uri = 16;
        Address address = 17;
        Age age = 18;
        Annotation annotation = 19;
        Attachment attachment = 20;
        CodeableConcept codeable_concept = 21;
        Coding coding = 22;
        ContactPoint contact_point = 23;
        Count count = 24;
        Distance distance = 25;
        Duration duration = 26;
        HumanName human_name = 27;
        Identifier identifier = 28;
        Money money = 29;
        Period period = 30;
        Quantity quantity = 31;
        Range range = 32;
        Ratio ratio = 33;
        Reference reference = 34;
        SampledData sampled_data = 35;
        Signature signature = 36;
        Timing timing = 37;
        Meta meta = 38;
      }
    }
    Value value = 5
        [(is_choice_type) = true, (validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Output output = 35;
}

// Auto-generated from StructureDefinition for TestReport, last updated
// 2017-04-19T07:44:43.294+10:00. Describes the results of a TestScript
// execution. See https://www.hl7.org/fhir/testreport.html
message TestReport {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // External identifier
  Identifier identifier = 9;

  // Informal name of the executed TestScript
  String name = 10;

  // completed | in-progress | waiting | stopped | entered-in-error
  TestReportStatusCode status = 11
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // Reference to the  version-specific TestScript that was executed to produce
  // this TestReport
  Reference test_script = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // pass | fail | pending
  TestReportResultCode result = 13
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // The final score (percentage of tests passed) resulting from the execution
  // of the TestScript
  Decimal score = 14;

  // Name of the tester producing this report (Organization or individual)
  String tester = 15;

  // When the TestScript was executed and this TestReport was generated
  DateTime issued = 16;

  // A participant in the test execution, either the execution engine, a client,
  // or a server
  message Participant {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // test-engine | client | server
    TestReportParticipantTypeCode type = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // The uri of the participant. An absolute URL is preferred
    Uri uri = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The display name of the participant
    String display = 6;
  }
  repeated Participant participant = 17;

  // The results of the series of required setup operations before the tests
  // were executed
  message Setup {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // A setup operation or assert that was executed
    message SetupAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The operation to perform
      message Operation {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // pass | skip | fail | warning | error
        TestReportActionResultCode result = 4
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // A message associated with the result
        Markdown message = 5;

        // A link to further details on the result
        Uri detail = 6;
      }
      Operation operation = 4;

      // The assertion to perform
      message Assert {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // pass | skip | fail | warning | error
        TestReportActionResultCode result = 4
            [(validation_requirement) = REQUIRED_BY_FHIR];

        // A message associated with the result
        Markdown message = 5;

        // A link to further details on the result
        String detail = 6;
      }
      Assert assert_value = 5 [json_name = "assert"];
    }
    repeated SetupAction action = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Setup setup = 18;

  // A test executed from the test script
  message Test {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Tracking/logging name of this test
    String name = 4;

    // Tracking/reporting short description of the test
    String description = 5;

    // A test operation or assert that was performed
    message TestAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The operation performed
      Setup.SetupAction.Operation operation = 4;

      // The assertion performed
      Setup.SetupAction.Assert assert_value = 5 [json_name = "assert"];
    }
    repeated TestAction action = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Test test = 19;

  // The results of running the series of required clean up steps
  message Teardown {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // One or more teardown operations performed
    message TeardownAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The teardown operation performed
      Setup.SetupAction.Operation operation = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated TeardownAction action = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Teardown teardown = 20;
}

// Auto-generated from StructureDefinition for TestScript, last updated
// 2017-04-19T07:44:43.294+10:00. Describes a set of tests. See
// https://www.hl7.org/fhir/testscript.html
message TestScript {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this test script (globally unique)
  Uri url = 9 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Additional identifier for the test script
  Identifier identifier = 10;

  // Business version of the test script
  String version = 11;

  // Name for this test script (computer friendly)
  String name = 12 [(validation_requirement) = REQUIRED_BY_FHIR];

  // Name for this test script (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the test script
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for test script (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Why this test script is defined
  Markdown purpose = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // An abstract server representing a client or sender in a message exchange
  message Origin {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The index of the abstract origin server starting at 1
    Integer index = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // FHIR-Client | FHIR-SDC-FormFiller
    Coding profile = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Origin origin = 24;

  // An abstract server representing a destination or receiver in a message
  // exchange
  message Destination {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // The index of the abstract destination server starting at 1
    Integer index = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // FHIR-Server | FHIR-SDC-FormManager | FHIR-SDC-FormReceiver |
    // FHIR-SDC-FormProcessor
    Coding profile = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Destination destination = 25;

  // Required capability that is assumed to function correctly on the FHIR
  // server being tested
  message Metadata {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Links to the FHIR specification
    message Link {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // URL to the specification
      Uri url = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Short description
      String description = 5;
    }
    repeated Link link = 4;

    // Capabilities  that are assumed to function correctly on the FHIR server
    // being tested
    message Capability {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Are the capabilities required?
      Boolean required = 4;

      // Are the capabilities validated?
      Boolean validated = 5;

      // The expected capabilities of the server
      String description = 6;

      // Which origin server these requirements apply to
      repeated Integer origin = 7;

      // Which server these requirements apply to
      Integer destination = 8;

      // Links to the FHIR specification
      repeated Uri link = 9;

      // Required Capability Statement
      Reference capabilities = 10 [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated Capability capability = 5
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Metadata metadata = 26;

  // Fixture in the test script - by reference (uri)
  message Fixture {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Whether or not to implicitly create the fixture during setup
    Boolean autocreate = 4;

    // Whether or not to implicitly delete the fixture during teardown
    Boolean autodelete = 5;

    // Reference of the resource
    Reference resource = 6;
  }
  repeated Fixture fixture = 27;

  // Reference of the validation profile
  repeated Reference profile = 28;

  // Placeholder for evaluated elements
  message Variable {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Descriptive name for this variable
    String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Default, hard-coded, or user-defined value for this variable
    String default_value = 5;

    // Natural language description of the variable
    String description = 6;

    // The fluentpath expression against the fixture body
    String expression = 7;

    // HTTP header field name for source
    String header_field = 8;

    // Hint help text for default value to enter
    String hint = 9;

    // XPath or JSONPath against the fixture body
    String path = 10;

    // Fixture Id of source expression or headerField within this variable
    Id source_id = 11;
  }
  repeated Variable variable = 29;

  // Assert rule used within the test script
  message Rule {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Assert rule resource reference
    Reference resource = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Rule parameter template
    message RuleParam {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Parameter name matching external assert rule parameter
      String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Parameter value defined either explicitly or dynamically
      String value = 5;
    }
    repeated RuleParam param = 5;
  }
  repeated Rule rule = 30;

  // Assert ruleset used within the test script
  message Ruleset {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Assert ruleset resource reference
    Reference resource = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // The referenced rule within the ruleset
    message RulesetRule {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Id of referenced rule within the ruleset
      Id rule_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Ruleset rule parameter template
      message RulesetRuleParam {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Parameter name matching external assert ruleset rule parameter
        String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Parameter value defined either explicitly or dynamically
        String value = 5;
      }
      repeated RulesetRuleParam param = 5;
    }
    repeated RulesetRule rule = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Ruleset ruleset = 31;

  // A series of required setup operations before tests are executed
  message Setup {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // A setup operation or assert to perform
    message SetupAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The setup operation to perform
      message Operation {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // The operation code type that will be executed
        Coding type = 4;

        // Resource type
        FHIRDefinedTypeCode resource = 5;

        // Tracking/logging operation label
        String label = 6;

        // Tracking/reporting operation description
        String description = 7;

        // xml | json | ttl | none
        ContentTypeCode accept = 8;

        // xml | json | ttl | none
        ContentTypeCode content_type = 9;

        // Server responding to the request
        Integer destination = 10;

        // Whether or not to send the request url in encoded format
        Boolean encode_request_url = 11;

        // Server initiating the request
        Integer origin = 12;

        // Explicitly defined path parameters
        String params = 13;

        // Each operation can have one or more header elements
        message RequestHeader {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // HTTP header field name
          String field = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

          // HTTP headerfield value
          String value = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
        }
        repeated RequestHeader request_header = 14;

        // Fixture Id of mapped request
        Id request_id = 15;

        // Fixture Id of mapped response
        Id response_id = 16;

        // Fixture Id of body for PUT and POST requests
        Id source_id = 17;

        // Id of fixture used for extracting the [id],  [type], and [vid] for
        // GET requests
        Id target_id = 18;

        // Request URL
        String url = 19;
      }
      Operation operation = 4;

      // The assertion to perform
      message Assert {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Tracking/logging assertion label
        String label = 4;

        // Tracking/reporting assertion description
        String description = 5;

        // response | request
        AssertionDirectionTypeCode direction = 6;

        // Id of the source fixture to be evaluated
        String compare_to_source_id = 7;

        // The fluentpath expression to evaluate against the source fixture
        String compare_to_source_expression = 8;

        // XPath or JSONPath expression to evaluate against the source fixture
        String compare_to_source_path = 9;

        // xml | json | ttl | none
        ContentTypeCode content_type = 10;

        // The fluentpath expression to be evaluated
        String expression = 11;

        // HTTP header field name
        String header_field = 12;

        // Fixture Id of minimum content resource
        String minimum_id = 13;

        // Perform validation on navigation links?
        Boolean navigation_links = 14;

        // equals | notEquals | in | notIn | greaterThan | lessThan | empty |
        // notEmpty | contains | notContains | eval
        AssertionOperatorTypeCode operator = 15;

        // XPath or JSONPath expression
        String path = 16;

        // delete | get | options | patch | post | put
        TestScriptRequestMethodCodeCode request_method = 17;

        // Request URL comparison value
        String request_u_r_l = 18;

        // Resource type
        FHIRDefinedTypeCode resource = 19;

        // okay | created | noContent | notModified | bad | forbidden | notFound
        // | methodNotAllowed | conflict | gone | preconditionFailed |
        // unprocessable
        AssertionResponseTypesCode response = 20;

        // HTTP response code to test
        String response_code = 21;

        // The reference to a TestScript.rule
        message ActionAssertRule {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // Id of the TestScript.rule
          Id rule_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

          // Rule parameter template
          message ActionAssertRuleParam {
            // xml:id (or equivalent in JSON)
            String id = 1;

            // Additional Content defined by implementations
            repeated Extension extension = 2;

            // Extensions that cannot be ignored
            repeated Extension modifier_extension = 3;

            // Parameter name matching external assert rule parameter
            String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

            // Parameter value defined either explicitly or dynamically
            String value = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
          }
          repeated ActionAssertRuleParam param = 5;
        }
        ActionAssertRule rule = 22;

        // The reference to a TestScript.ruleset
        message ActionAssertRuleset {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // Id of the TestScript.ruleset
          Id ruleset_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

          // The referenced rule within the ruleset
          message ActionAssertRulesetRule {
            // xml:id (or equivalent in JSON)
            String id = 1;

            // Additional Content defined by implementations
            repeated Extension extension = 2;

            // Extensions that cannot be ignored
            repeated Extension modifier_extension = 3;

            // Id of referenced rule within the ruleset
            Id rule_id = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

            // Rule parameter template
            message Param {
              // xml:id (or equivalent in JSON)
              String id = 1;

              // Additional Content defined by implementations
              repeated Extension extension = 2;

              // Extensions that cannot be ignored
              repeated Extension modifier_extension = 3;

              // Parameter name matching external assert ruleset rule parameter
              String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

              // Parameter value defined either explicitly or dynamically
              String value = 5 [(validation_requirement) = REQUIRED_BY_FHIR];
            }
            repeated Param param = 5;
          }
          repeated ActionAssertRulesetRule rule = 5;
        }
        ActionAssertRuleset ruleset = 23;

        // Fixture Id of source expression or headerField
        Id source_id = 24;

        // Profile Id of validation profile reference
        Id validate_profile_id = 25;

        // The value to compare to
        String value = 26;

        // Will this assert produce a warning only on error?
        Boolean warning_only = 27;
      }
      Assert assert_value = 5 [json_name = "assert"];
    }
    repeated SetupAction action = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Setup setup = 32;

  // A test in this script
  message Test {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Tracking/logging name of this test
    String name = 4;

    // Tracking/reporting short description of the test
    String description = 5;

    // A test operation or assert to perform
    message TestAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The setup operation to perform
      Setup.SetupAction.Operation operation = 4;

      // The setup assertion to perform
      Setup.SetupAction.Assert assert_value = 5 [json_name = "assert"];
    }
    repeated TestAction action = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  repeated Test test = 33;

  // A series of required clean up steps
  message Teardown {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // One or more teardown operations to perform
    message TeardownAction {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The teardown operation to perform
      Setup.SetupAction.Operation operation = 4
          [(validation_requirement) = REQUIRED_BY_FHIR];
    }
    repeated TeardownAction action = 4
        [(validation_requirement) = REQUIRED_BY_FHIR];
  }
  Teardown teardown = 34;
}

// Auto-generated from StructureDefinition for ValueSet, last updated
// 2017-04-19T07:44:43.294+10:00. A set of codes drawn from one or more code
// systems. See https://www.hl7.org/fhir/valueset.html
message ValueSet {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Logical URI to reference this value set (globally unique)
  Uri url = 9;

  // Additional identifier for the value set
  repeated Identifier identifier = 10;

  // Business version of the value set
  String version = 11;

  // Name for this value set (computer friendly)
  String name = 12;

  // Name for this value set (human friendly)
  String title = 13;

  // draft | active | retired | unknown
  PublicationStatusCode status = 14
      [(validation_requirement) = REQUIRED_BY_FHIR];

  // For testing purposes, not real usage
  Boolean experimental = 15;

  // Date this was last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the value set
  Markdown description = 19;

  // Context the content is intended to support
  repeated UsageContext use_context = 20;

  // Intended jurisdiction for value set (if applicable)
  repeated CodeableConcept jurisdiction = 21;

  // Indicates whether or not any change to the content logical definition may
  // occur
  Boolean immutable = 22;

  // Why this value set is defined
  Markdown purpose = 23;

  // Use and/or publishing restrictions
  Markdown copyright = 24;

  // Whether this is intended to be used with an extensible binding
  Boolean extensible = 25;

  // Definition of the content of the value set (CLD)
  message Compose {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Fixed date for version-less references (transitive)
    Date locked_date = 4;

    // Whether inactive codes are in the value set
    Boolean inactive = 5;

    // Include one or more codes from a code system or other value set(s)
    message ConceptSet {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // The system the codes come from
      Uri system = 4;

      // Specific version of the code system referred to
      String version = 5;

      // A concept defined in the system
      message ConceptReference {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // Code or expression from system
        Code code = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Text to display for this code for this value set in this valueset
        String display = 5;

        // Additional representations for this concept
        message Designation {
          // xml:id (or equivalent in JSON)
          String id = 1;

          // Additional Content defined by implementations
          repeated Extension extension = 2;

          // Extensions that cannot be ignored
          repeated Extension modifier_extension = 3;

          // Human language of the designation
          LanguageCode language = 4;

          // Details how this designation would be used
          Coding use = 5;

          // The text value for this designation
          String value = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
        }
        repeated Designation designation = 6;
      }
      repeated ConceptReference concept = 6;

      // Select codes/concepts by their properties (including relationships)
      message Filter {
        // xml:id (or equivalent in JSON)
        String id = 1;

        // Additional Content defined by implementations
        repeated Extension extension = 2;

        // Extensions that cannot be ignored
        repeated Extension modifier_extension = 3;

        // A property defined by the code system
        Code property = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

        // = | is-a | descendent-of | is-not-a | regex | in | not-in |
        // generalizes | exists
        FilterOperatorCode op = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

        // Code from the system, or regex criteria, or boolean value for exists
        Code value = 6 [(validation_requirement) = REQUIRED_BY_FHIR];
      }
      repeated Filter filter = 7;

      // Select only contents included in this value set
      repeated Uri value_set = 8;
    }
    repeated ConceptSet include = 6
        [(validation_requirement) = REQUIRED_BY_FHIR];

    // Explicitly exclude codes from a code system or other value sets
    repeated ConceptSet exclude = 7;
  }
  Compose compose = 26;

  // Used when the value set is "expanded"
  message Expansion {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Uniquely identifies this expansion
    Uri identifier = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Time ValueSet expansion happened
    DateTime timestamp = 5 [(validation_requirement) = REQUIRED_BY_FHIR];

    // Total number of codes in the expansion
    Integer total = 6;

    // Offset at which this resource starts
    Integer offset = 7;

    // Parameter that controlled the expansion process
    message Parameter {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // Name as assigned by the server
      String name = 4 [(validation_requirement) = REQUIRED_BY_FHIR];

      // Value of the named parameter
      message Value {
        oneof value {
          String string_value = 1 [json_name = "string"];
          Boolean boolean = 2;
          Integer integer = 3;
          Decimal decimal = 4;
          Uri uri = 5;
          Code code = 6;
        }
      }
      Value value = 5 [(is_choice_type) = true];
    }
    repeated Parameter parameter = 8;

    // Codes in the value set
    message Contains {
      // xml:id (or equivalent in JSON)
      String id = 1;

      // Additional Content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored
      repeated Extension modifier_extension = 3;

      // System value for the code
      Uri system = 4;

      // If user cannot select this entry
      Boolean abstract = 5;

      // If concept is inactive in the code system
      Boolean inactive = 6;

      // Version in which this code/display is defined
      String version = 7;

      // Code - if blank, this is not a selectable code
      Code code = 8;

      // User display for the concept
      String display = 9;

      // Additional representations for this item
      repeated Compose.ConceptSet.ConceptReference.Designation designation = 10;

      // Codes contained under this entry
      repeated Contains contains = 11;
    }
    repeated Contains contains = 9;
  }
  Expansion expansion = 27;
}

// Auto-generated from StructureDefinition for VisionPrescription, last updated
// 2017-04-19T07:44:43.294+10:00. Prescription for vision correction products
// for a patient. See https://www.hl7.org/fhir/visionprescription.html
message VisionPrescription {
  option (structure_definition_kind) = KIND_RESOURCE;

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  LanguageCode language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated ContainedResource contained = 6;

  // Additional Content defined by implementations
  repeated Extension extension = 7;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 8;

  // Business identifier
  repeated Identifier identifier = 9;

  // active | cancelled | draft | entered-in-error
  FinancialResourceStatusCode status = 10;

  // Who prescription is for
  Reference patient = 11;

  // Created during encounter / admission / stay
  Reference encounter = 12;

  // When prescription was authorized
  DateTime date_written = 13;

  // Who authorizes the vision product
  Reference prescriber = 14;

  // Reason or indication for writing the prescription
  message Reason {
    oneof reason {
      CodeableConcept codeable_concept = 1;
      Reference reference = 2;
    }
  }
  Reason reason = 15 [(is_choice_type) = true];

  // Vision supply authorization
  message Dispense {
    // xml:id (or equivalent in JSON)
    String id = 1;

    // Additional Content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored
    repeated Extension modifier_extension = 3;

    // Product to be supplied
    CodeableConcept product = 4;

    // right | left
    VisionEyesCode eye = 5;

    // Lens sphere
    Decimal sphere = 6;

    // Lens cylinder
    Decimal cylinder = 7;

    // Lens axis
    Integer axis = 8;

    // Lens prism
    Decimal prism = 9;

    // up | down | in | out
    VisionBaseCode base = 10;

    // Lens add
    Decimal add = 11;

    // Contact lens power
    Decimal power = 12;

    // Contact lens back curvature
    Decimal back_curve = 13;

    // Contact lens diameter
    Decimal diameter = 14;

    // Lens wear duration
    Quantity duration = 15;

    // Color required
    String color = 16;

    // Brand required
    String brand = 17;

    // Notes for coatings
    repeated Annotation note = 18;
  }
  repeated Dispense dispense = 16;
}

message ContainedResource {
  oneof oneof_resource {
    Account account = 1;
    ActivityDefinition activity_definition = 2;
    AdverseEvent adverse_event = 3;
    AllergyIntolerance allergy_intolerance = 4;
    Appointment appointment = 5;
    AppointmentResponse appointment_response = 6;
    AuditEvent audit_event = 7;
    Basic basic = 8;
    Binary binary = 9;
    BodySite body_site = 10;
    Bundle bundle = 11;
    CapabilityStatement capability_statement = 12;
    CarePlan care_plan = 13;
    CareTeam care_team = 14;
    ChargeItem charge_item = 15;
    Claim claim = 16;
    ClaimResponse claim_response = 17;
    ClinicalImpression clinical_impression = 18;
    CodeSystem code_system = 19;
    Communication communication = 20;
    CommunicationRequest communication_request = 21;
    CompartmentDefinition compartment_definition = 22;
    Composition composition = 23;
    ConceptMap concept_map = 24;
    Condition condition = 25;
    Consent consent = 26;
    Contract contract = 27;
    Coverage coverage = 28;
    DataElement data_element = 29;
    DetectedIssue detected_issue = 30;
    Device device = 31;
    DeviceComponent device_component = 32;
    DeviceMetric device_metric = 33;
    DeviceRequest device_request = 34;
    DeviceUseStatement device_use_statement = 35;
    DiagnosticReport diagnostic_report = 36;
    DocumentManifest document_manifest = 37;
    DocumentReference document_reference = 38;
    EligibilityRequest eligibility_request = 39;
    EligibilityResponse eligibility_response = 40;
    Encounter encounter = 41;
    Endpoint endpoint = 42;
    EnrollmentRequest enrollment_request = 43;
    EnrollmentResponse enrollment_response = 44;
    EpisodeOfCare episode_ofcare = 45;
    ExpansionProfile expansion_profile = 46;
    ExplanationOfBenefit explanation_of_benefit = 47;
    FamilyMemberHistory family_member_history = 48;
    Flag flag = 49;
    Goal goal = 50;
    GraphDefinition graph_definition = 51;
    Group group = 52;
    GuidanceResponse guidance_response = 53;
    HealthcareService healthcare_service = 54;
    ImagingManifest imaging_manifest = 55;
    ImagingStudy imaging_study = 56;
    Immunization immunization = 57;
    ImmunizationRecommendation immunization_recommendation = 58;
    ImplementationGuide implementation_guide = 59;
    Library library = 60;
    Linkage linkage = 61;
    List list = 62;
    Location location = 63;
    Measure measure = 64;
    MeasureReport measure_report = 65;
    Media media = 66;
    Medication medication = 67;
    MedicationAdministration medication_administration = 68;
    MedicationDispense medication_dispense = 69;
    MedicationRequest medication_request = 70;
    MedicationStatement medication_statement = 71;
    MessageDefinition message_definition = 72;
    MessageHeader message_header = 73;
    NamingSystem naming_system = 74;
    NutritionOrder nutrition_order = 75;
    Observation observation = 76;
    OperationDefinition operation_definition = 77;
    OperationOutcome operation_outcome = 78;
    Organization organization = 79;
    Parameters parameters = 80;
    Patient patient = 81;
    PaymentNotice payment_notice = 82;
    PaymentReconciliation payment_reconciliation = 83;
    Person person = 84;
    PlanDefinition plan_definition = 85;
    Practitioner practitioner = 86;
    PractitionerRole practitioner_role = 87;
    Procedure procedure = 88;
    ProcedureRequest procedure_request = 89;
    ProcessRequest process_request = 90;
    ProcessResponse process_response = 91;
    Provenance provenance = 92;
    Questionnaire questionnaire = 93;
    QuestionnaireResponse questionnaire_response = 94;
    ReferralRequest referral_request = 95;
    RelatedPerson related_person = 96;
    RequestGroup request_group = 97;
    ResearchStudy research_study = 98;
    ResearchSubject research_subject = 99;
    RiskAssessment risk_assessment = 100;
    Schedule schedule = 101;
    SearchParameter search_parameter = 102;
    Sequence sequence = 103;
    ServiceDefinition service_definition = 104;
    Slot slot = 105;
    Specimen specimen = 106;
    StructureDefinition structure_definition = 107;
    StructureMap structure_map = 108;
    Subscription subscription = 109;
    Substance substance = 110;
    SupplyDelivery supply_delivery = 111;
    SupplyRequest supply_request = 112;
    Task task = 113;
    TestReport test_report = 114;
    TestScript test_script = 115;
    ValueSet value_set = 116;
    VisionPrescription vision_prescription = 117;
  }
}
