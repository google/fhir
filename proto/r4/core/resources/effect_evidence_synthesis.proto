//    Copyright 2019 Google Inc.
//
//    Licensed under the Apache License, Version 2.0 (the "License");
//    you may not use this file except in compliance with the License.
//    You may obtain a copy of the License at
//
//        https://www.apache.org/licenses/LICENSE-2.0
//
//    Unless required by applicable law or agreed to in writing, software
//    distributed under the License is distributed on an "AS IS" BASIS,
//    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//    See the License for the specific language governing permissions and
//    limitations under the License.

syntax = "proto3";

package google.fhir.r4.core;

import "google/protobuf/any.proto";
import "proto/annotations.proto";
import "proto/r4/core/codes.proto";
import "proto/r4/core/datatypes.proto";

option java_multiple_files = true;
option java_package = "com.google.fhir.r4.core";
option (.google.fhir.proto.fhir_version) = R4;

// Auto-generated from StructureDefinition for EffectEvidenceSynthesis, last
// updated 2018-12-27T22:37:54.724+11:00. A quantified estimate of effect based
// on a body of evidence. See
// http://hl7.org/fhir/StructureDefinition/EffectEvidenceSynthesis
message EffectEvidenceSynthesis {
  option (.google.fhir.proto.structure_definition_kind) = KIND_RESOURCE;
  option (.google.fhir.proto.fhir_structure_definition_url) =
      "http://hl7.org/fhir/StructureDefinition/EffectEvidenceSynthesis";
  option (.google.fhir.proto.fhir_path_message_constraint) =
      "name.matches('[A-Z]([A-Za-z0-9_]){0,254}')";

  // Logical id of this artifact
  Id id = 1;

  // Metadata about the resource
  Meta meta = 2;

  // A set of rules under which this content was created
  Uri implicit_rules = 3;

  // Language of the resource content
  Code language = 4;

  // Text summary of the resource, for human interpretation
  Narrative text = 5;

  // Contained, inline Resources
  repeated protobuf.Any contained = 6;

  // Field 7 reserved for strongly-typed ContainedResource for id:
  // EffectEvidenceSynthesis.contained
  reserved 7;

  // Additional content defined by implementations
  repeated Extension extension = 8;

  // Extensions that cannot be ignored
  repeated Extension modifier_extension = 9;

  // Canonical identifier for this effect evidence synthesis, represented as a
  // URI (globally unique)
  Uri url = 10;

  // Additional identifier for the effect evidence synthesis
  repeated Identifier identifier = 11;

  // Business version of the effect evidence synthesis
  String version = 12;

  // Name for this effect evidence synthesis (computer friendly)
  String name = 13;

  // Name for this effect evidence synthesis (human friendly)
  String title = 14;

  // draft | active | retired | unknown
  message StatusCode {
    option (.google.fhir.proto.structure_definition_kind) = KIND_PRIMITIVE_TYPE;
    option (.google.fhir.proto.fhir_valueset_url) =
        "http://hl7.org/fhir/ValueSet/publication-status";
    option (.google.fhir.proto.fhir_profile_base) =
        "http://hl7.org/fhir/StructureDefinition/code";

    PublicationStatusCode.Value value = 1;

    String id = 2;

    repeated Extension extension = 3;
  }
  StatusCode status = 15
      [(.google.fhir.proto.validation_requirement) = REQUIRED_BY_FHIR];

  // Date last changed
  DateTime date = 16;

  // Name of the publisher (organization or individual)
  String publisher = 17;

  // Contact details for the publisher
  repeated ContactDetail contact = 18;

  // Natural language description of the effect evidence synthesis
  Markdown description = 19;

  // Used for footnotes or explanatory notes
  repeated Annotation note = 20;

  // The context that the content is intended to support
  repeated UsageContext use_context = 21;

  // Intended jurisdiction for effect evidence synthesis (if applicable)
  repeated CodeableConcept jurisdiction = 22;

  // Use and/or publishing restrictions
  Markdown copyright = 23;

  // When the effect evidence synthesis was approved by publisher
  Date approval_date = 24;

  // When the effect evidence synthesis was last reviewed
  Date last_review_date = 25;

  // When the effect evidence synthesis is expected to be used
  Period effective_period = 26;

  // The category of the EffectEvidenceSynthesis, such as Education, Treatment,
  // Assessment, etc.
  repeated CodeableConcept topic = 27;

  // Who authored the content
  repeated ContactDetail author = 28;

  // Who edited the content
  repeated ContactDetail editor = 29;

  // Who reviewed the content
  repeated ContactDetail reviewer = 30;

  // Who endorsed the content
  repeated ContactDetail endorser = 31;

  // Additional documentation, citations, etc.
  repeated RelatedArtifact related_artifact = 32;

  // Type of synthesis
  CodeableConcept synthesis_type = 33;

  // Type of study
  CodeableConcept study_type = 34;

  // What population?
  Reference population = 35 [
    (.google.fhir.proto.validation_requirement) = REQUIRED_BY_FHIR,
    (.google.fhir.proto.valid_reference_type) = "EvidenceVariable"
  ];

  // What exposure?
  Reference exposure = 36 [
    (.google.fhir.proto.validation_requirement) = REQUIRED_BY_FHIR,
    (.google.fhir.proto.valid_reference_type) = "EvidenceVariable"
  ];

  // What comparison exposure?
  Reference exposure_alternative = 37 [
    (.google.fhir.proto.validation_requirement) = REQUIRED_BY_FHIR,
    (.google.fhir.proto.valid_reference_type) = "EvidenceVariable"
  ];

  // What outcome?
  Reference outcome = 38 [
    (.google.fhir.proto.validation_requirement) = REQUIRED_BY_FHIR,
    (.google.fhir.proto.valid_reference_type) = "EvidenceVariable"
  ];

  // What sample size was involved?
  message SampleSize {
    // Unique id for inter-element referencing
    String id = 1;

    // Additional content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored even if unrecognized
    repeated Extension modifier_extension = 3;

    // Description of sample size
    String description = 4;

    // How many studies?
    Integer number_of_studies = 5;

    // How many participants?
    Integer number_of_participants = 6;
  }
  SampleSize sample_size = 39;

  // What was the result per exposure?
  message ResultsByExposure {
    // Unique id for inter-element referencing
    String id = 1;

    // Additional content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored even if unrecognized
    repeated Extension modifier_extension = 3;

    // Description of results by exposure
    String description = 4;

    // exposure | exposure-alternative
    message ExposureStateCode {
      option (.google.fhir.proto.structure_definition_kind) =
          KIND_PRIMITIVE_TYPE;
      option (.google.fhir.proto.fhir_valueset_url) =
          "http://hl7.org/fhir/ValueSet/exposure-state";
      option (.google.fhir.proto.fhir_profile_base) =
          "http://hl7.org/fhir/StructureDefinition/code";

      .google.fhir.r4.core.ExposureStateCode.Value value = 1;

      String id = 2;

      repeated Extension extension = 3;
    }
    ExposureStateCode exposure_state = 5;

    // Variant exposure states
    CodeableConcept variant_state = 6;

    // Risk evidence synthesis
    Reference risk_evidence_synthesis = 7 [
      (.google.fhir.proto.validation_requirement) = REQUIRED_BY_FHIR,
      (.google.fhir.proto.valid_reference_type) = "RiskEvidenceSynthesis"
    ];
  }
  repeated ResultsByExposure results_by_exposure = 40;

  // What was the estimated effect
  message EffectEstimate {
    // Unique id for inter-element referencing
    String id = 1;

    // Additional content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored even if unrecognized
    repeated Extension modifier_extension = 3;

    // Description of effect estimate
    String description = 4;

    // Type of efffect estimate
    CodeableConcept type = 5;

    // Variant exposure states
    CodeableConcept variant_state = 6;

    // Point estimate
    Decimal value = 7;

    // What unit is the outcome described in?
    CodeableConcept unit_of_measure = 8;

    // How precise the estimate is
    message PrecisionEstimate {
      // Unique id for inter-element referencing
      String id = 1;

      // Additional content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored even if unrecognized
      repeated Extension modifier_extension = 3;

      // Type of precision estimate
      CodeableConcept type = 4;

      // Level of confidence interval
      Decimal level = 5;

      // Lower bound
      Decimal from = 6;

      // Upper bound
      Decimal to = 7;
    }
    repeated PrecisionEstimate precision_estimate = 9;
  }
  repeated EffectEstimate effect_estimate = 41;

  // How certain is the effect
  message Certainty {
    // Unique id for inter-element referencing
    String id = 1;

    // Additional content defined by implementations
    repeated Extension extension = 2;

    // Extensions that cannot be ignored even if unrecognized
    repeated Extension modifier_extension = 3;

    // Certainty rating
    repeated CodeableConcept rating = 4;

    // Used for footnotes or explanatory notes
    repeated Annotation note = 5;

    // A component that contributes to the overall certainty
    message CertaintySubcomponent {
      // Unique id for inter-element referencing
      String id = 1;

      // Additional content defined by implementations
      repeated Extension extension = 2;

      // Extensions that cannot be ignored even if unrecognized
      repeated Extension modifier_extension = 3;

      // Type of subcomponent of certainty rating
      CodeableConcept type = 4;

      // Subcomponent certainty rating
      repeated CodeableConcept rating = 5;

      // Used for footnotes or explanatory notes
      repeated Annotation note = 6;
    }
    repeated CertaintySubcomponent certainty_subcomponent = 6;
  }
  repeated Certainty certainty = 42;
}
